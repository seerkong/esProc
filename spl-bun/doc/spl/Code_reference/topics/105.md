# 2.6.14 修改 值 的 指定 字段

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.field("SALARY",[8000,9000])
=A1.field(9,[8000,9000])
```

## 提取内容
2.6.14 修改 值 的 指定 字段
|
=demo.query("select
* 从 EMPLOYEE")
|
=A1.field("SALARY",[8000,9000])
|
修改 第一 两个 值 的 SALARY
字段
|
=A1.field(9,[8000,9000])
|
修改 第一 两个 值 的 9 th
字段
