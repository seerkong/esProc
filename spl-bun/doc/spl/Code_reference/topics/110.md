# 2.7.4 将表序列存储为文本文件

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=file("D:/employee.txt")
>A2.export(A1)
>A2.export@t(A1)
```

## 提取内容
2.7.4
将表序列存储为文本文件
|
=demo.query("select
EID,NAME,STATE, GENDER, BIRTHDAY,HIREDATE,DEPT, SALARY from EMPLOYEE")
|
=file("D:/employee.txt")
|
>A2.export(A1)
|
>A2.export@t(A1)
|
Set 字段 names as title
