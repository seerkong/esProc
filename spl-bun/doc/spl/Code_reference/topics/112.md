# 2.8.1 取回 data 从 数据库 as 表 序列 通过 SQL

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
```

## 提取内容
2.8.1
取回 data 从 数据库 as 表 序列 通过 SQL
|
=demo.query("select
* 从 EMPLOYEE")
|
=demo.query("select
* 从 EMPLOYEE 其中 EID=?",1)
|
