# 2.8.2 返回 单个 值 result 的 SQL computation

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query@1("select
count(*) from EMPLOYEE")
=demo.query@1("select
count(*) from EMPLOYEE where SALARY>?",10000)
```

## 提取内容
2.8.2
返回 单个 值 result 的
SQL computation
|
=demo.query@1("select
count(*) from EMPLOYEE")
|
=demo.query@1("select
count(*) from EMPLOYEE where SALARY>?",10000)
|
