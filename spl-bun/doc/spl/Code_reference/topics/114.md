# 2.8.3 使用 数据库 存储的 procedure 到 返回 one 或 多个 表 序列

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
= db .proc("{call
proc1(?,?)}",:101: "o":a,:101:"o":b)
=A1(1)
=A1(2)
=a
```

## 提取内容
2.8.3
使用 数据库 存储的 procedure
到 返回 one 或 多个 表 序列
|
= db .proc("{call
proc1(?,?)}",:101: "o":a,:101:"o":b)
|
执行 存储的 procedure 和 返回 2
表 序列 s
|
=A1(1)
|
第一 表 序列
|
=A1(2)
|
第二 表 序列
|
=a
|
使用 variable name 到 访问 第一 表 序列
