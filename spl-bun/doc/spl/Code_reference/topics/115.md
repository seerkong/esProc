# 2.8.4 运行 SQL 语句 在 数据库 到 修改 data

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
>demo.execute("update
=create(wineID,wineName,wineType,wineProduction,wineStock).record([100,"Amaretto","Liqueur","France",2130,200,"Tia
>demo.execute(A2,"update
>demo.execute([1,3,5],"delete
>demo.execute(A2,"insert
```

## 提取内容
2.8.4
运行 SQL 语句 在 数据库
到 修改 data
|
>demo.execute("update
SCORES set SCORE=? 其中 STUDENTID=10",90)
|
Update
|
=create(wineID,wineName,wineType,wineProduction,wineStock).record([100,"Amaretto","Liqueur","France",2130,200,"Tia
Maria","Liqueur","Jamaica",1570])
|
>demo.execute(A2,"update
LIQUORS set STOCK=? 其中 NAME=?", wineStock, wineName)
|
Update 在 批次
|
>demo.execute([1,3,5],"delete
从 ADVENTURE 其中 AID=?",~)
|
删除 在 批次
|
>demo.execute(A2,"insert
到 LIQUORS (LID, NAME, TYPE, PRODUCTION, STOCK) 值
(?,?,?,?,?)",wineID, wineName, wineType, wineProduction, wineStock)
|
插入 在 批次
