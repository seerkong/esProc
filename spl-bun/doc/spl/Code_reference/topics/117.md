# 2.8.6 管理 事务 提交 automatically 按 程序 代码

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=connect@e("demo")
=A1.execute@k(.…)
=A1.error()
>A1.commit()
>A1.rollback()
>A1.close()
```

## 提取内容
2.8.6
管理 事务 提交
automatically 按 程序 代码
|
=connect@e("demo")
|
建立连接
|
=A1.execute@k(.…)
|
=A1.error()
|
读取 错误 信息 generated 按
previous 数据库 事务 execution
|
if
A3==0
|
>A1.commit()
|
提交 如果 there 是 no errors
|
else
|
>A1.rollback()
|
Roll back 如果 there 是 errors
|
>A1.close()
|
关闭连接
