# 2.9.10 Cross-cellset call

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=size.(char(65+rand(26))).concat()
=call(“D:/demo.splx”,5)
```

## 提取内容
2.9.10 Cross-cellset call
|
=size.(char(65+rand(26))).concat()
|
Parmeter size 是 passed 在 从 外部
|
return
A1
|
返回 result 的 A1
|
=call(“D:/demo.splx”,5)
|
Call subprogram demo.splx 到 call 传递 在 parameter 5
