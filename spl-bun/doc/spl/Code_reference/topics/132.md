# 2.9.11 并行 处理

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.group(GENDER)
=A3.select(SALARY>10000)
```

## 提取内容
2.9.11 并行 处理
|
=demo.query("select
* 从 EMPLOYEE")
|
=A1.group(GENDER)
|
分组 按 gender
|
fork
A2
|
=A3.select(SALARY>10000)
|
获取 记录 按 指定 条件 使用
多个 threads 和 拼接 结果 在 A3
