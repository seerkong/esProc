# 3.1.5 执行 union, intersection, 和 difference 在 已排序 序列 按 归并 函数

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=A1.sort(STUDENTID)
=A2.sort(STUDENTID)
=[A3:A4].merge(STUDENTID)
=[A3:A4].merge@u(STUDENTID)
=[A3:A4].merge@i(STUDENTID)
=[A3:A4].merge@d(STUDENTID)
```

## 提取内容
3.1.5
执行 union, intersection,
和 difference 在 已排序 序列 按 归并 函数
|
=demo.query("select
CLASS,STUDENTID, SUBJECT, SCORE 从 SCORES 其中 CLASS=? 和 SUBJECT=? 和
STUDENTID<?","Class one", "Math",10)
|
=demo.query("select
CLASS,STUDENTID, SUBJECT,SCORE 从 SCORES 其中 CLASS=? 和 SUBJECT=? 和
STUDENTID>?","Class two", "Math",5)
|
=A1.sort(STUDENTID)
|
=A2.sort(STUDENTID)
|
=[A3:A4].merge(STUDENTID)
|
Concatenation
|
=[A3:A4].merge@u(STUDENTID)
|
Union
|
=[A3:A4].merge@i(STUDENTID)
|
Intersection
|
=[A3:A4].merge@d(STUDENTID)
|
Difference
