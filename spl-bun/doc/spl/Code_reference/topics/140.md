# 3.1.6 计算 平方 求和 和 方差

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=A1.sum(~*~)
=var(A1)
=demo.query("select
=var(A4.(SCORE))
```

## 提取内容
3.1.6
计算 平方 求和 和
方差
|
[1,2,3,4,5,6,7,8]
|
=A1.sum(~*~)
|
平方 求和
|
=var(A1)
|
方差
|
=demo.query("select
NAME,EVENT,SCORE from GYMSCORE")
|
=var(A4.(SCORE))
|
