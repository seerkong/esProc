# 3.1.9 计算 link relative ratio over 相邻 rows

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=A1.(~/~[-1]-1)
=demo.query("select
=A3.derive(AMOUNT/AMOUNT[-1]-1:
```

## 提取内容
3.1.9
计算 link relative ratio
over 相邻 rows
|
[1,2,3,4,5,6]
|
=A1.(~/~[-1]-1)
|
=demo.query("select
DATE,sum(CLOSING) AMOUNT from STOCKRECORDS GROUP BY DATE")
|
=A3.derive(AMOUNT/AMOUNT[-1]-1:
周期-over-周期)
|
