# 3.1.10 计算 滑动 平均 over 相邻 sets

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=A1.(~[-1,1].avg())
=demo.query("select
=A3.(CLOSING[-3,3].avg())
```

## 提取内容
3.1.10 计算 滑动 平均 over 相邻 sets
|
[1,2,3,4,5,6]
|
=A1.(~[-1,1].avg())
|
=demo.query("select
STOCKID, DATE,CLOSING 从 STOCKRECORDS 其中
STOCKID=?","000062")
|
=A3.(CLOSING[-3,3].avg())
|
