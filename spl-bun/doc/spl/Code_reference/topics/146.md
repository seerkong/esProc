# 3.1.12 Union 记录 序列 使用 不同 data structures

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=A1|A2
=A3.select(GENDER:"F").avg(AGE)
```

## 提取内容
3.1.12 Union 记录 序列 使用 不同 data structures
|
=demo.query("select
* 从 STUDENTS")
|
=demo.query("select
* 从 EMPLOYEE ").derive(间隔@y(BIRTHDAY,now()):AGE)
|
=A1|A2
|
=A3.select(GENDER:"F").avg(AGE)
|
