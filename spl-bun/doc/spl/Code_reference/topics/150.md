# 3.1.16 同步分段：用最大/最小值代表区间值

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=to(100).sort(rand())
=A1.median(:4)|A1.max()
=A1.((n=~,A2.pseg@r(n)))
=A3.(A2(~+1))
=A1.min()|A1.median(:4)
=A1.((n=~,A5.segp(n)))
```

## 提取内容
3.1.16 同步分段：用最大/最小值代表区间值
|
=to(100).sort(rand())
|
=A1.median(:4)|A1.max()
|
定义分段数量
|
=A1.((n=~,A2.pseg@r(n)))
|
确定每个值所属的分段
|
=A3.(A2(~+1))
|
用所在分段的最大值表示该值
分段
|
=A1.min()|A1.median(:4)
|
定义分段数量
|
=A1.((n=~,A5.segp(n)))
|
用所在分段的最小值表示该值
分段
