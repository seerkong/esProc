# 3.2 查找与定位

- 格式: 为 AI 阅读提取的 Markdown

## 提取内容
3.2 查找与定位
本章列出以下方面的代码示例： data
查找与定位, 包括 定位 成员, 定位 sub-序列,
定位 成员 matching 指定 条件 和 返回 其 位置, 查找
成员 matching 指定 条件 从 back 到 front, 停止 搜索 之后
第一/all 成员 matching 指定 条件 是 找到, 搜索 从
K th 成员, 查询 在 多个 字段, 加速 up 查询 在 已排序
序列/记录 序列 通过 binary 搜索, Aggregates 在 查询 结果, 过滤
表 序列, 执行 查询 在 表 序列/记录 序列 按 主
键 值, 获取 记录 使用 最大/最小 值 和 其 位置, 和 计算
link relative ratio 用于 选中的 成员.
