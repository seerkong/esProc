# 3.2.3 定位 成员 matching 指定 条件 和 返回 其 位置

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.select(GENDER=="M")
=A1.pselect(GENDER=="M")
```

## 提取内容
3.2.3
定位 成员 matching
指定 条件 和 返回 其 位置
|
=demo.query("select
EID,NAME, STATE, GENDER, BIRTHDAY,HIREDATE,DEPT, SALARY from EMPLOYEE")
|
=A1.select(GENDER=="M")
|
定位 all 成员 matching 指定
条件
|
=A1.pselect(GENDER=="M")
|
停止 搜索 之后 第一 matching
成员 是 找到, 和 返回 成员 位置
