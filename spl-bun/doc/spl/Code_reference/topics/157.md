# 3.2.4 查找 成员 matching 指定 条件 从 back 到 front

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.select@z(GENDER=="M")
=A1.pselect@z(GENDER=="M")
```

## 提取内容
3.2.4
查找 成员 matching 指定
条件 从 back 到 front
|
=demo.query("select
EID, NAME,STATE, GENDER, BIRTHDAY,HIREDATE,DEPT,SALARY from EMPLOYEE")
|
=A1.select@z(GENDER=="M")
|
=A1.pselect@z(GENDER=="M")
|
