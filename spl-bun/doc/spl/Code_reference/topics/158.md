# 3.2.5 停止 搜索 之后 第一/all 成员 matching 指定 条件 是 找到

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.pselect(EVENT:"UnevenBars")
=A1(A2).SCORE
=demo.query("select
=A4.select(GENDER=="M")
=A4.select@1(GENDER=="M")
=A4.pselect@a(GENDER=="M")
```

## 提取内容
3.2.5
停止 搜索 之后
第一/all 成员 matching 指定 条件 是 找到
|
=demo.query("select
NAME,EVENT, SCORE from GYMSCORE")
|
=A1.pselect(EVENT:"UnevenBars")
|
停止 搜索 之后 第一 matching
成员 是 找到
|
=A1(A2).SCORE
|
Scores 在 指定 event
|
=demo.query("select
EID,NAME,STATE, GENDER,BIRTHDAY, HIREDATE,DEPT, SALARY from EMPLOYEE")
|
=A4.select(GENDER=="M")
|
查找 all matching 成员
|
=A4.select@1(GENDER=="M")
|
查找 第一 matching 成员
|
=A4.pselect@a(GENDER=="M")
|
定位 all matching 成员
