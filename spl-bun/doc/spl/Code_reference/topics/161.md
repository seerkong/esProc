# 3.2.8 加速 up 查询 在 已排序 序列/记录 序列 通过 binary 搜索

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.select@b(GENDER:"M",DEPT:"R&D")
=A1.pselect@b(GENDER:"M",DEPT:"R&D")
```

## 提取内容
3.2.8
加速 up 查询 在 已排序
序列/记录 序列 通过 binary 搜索
|
=demo.query("select
* from EMPLOYEE order by GENDER,DEPT")
|
=A1.select@b(GENDER:"M",DEPT:"R&D")
|
=A1.pselect@b(GENDER:"M",DEPT:"R&D")
|
