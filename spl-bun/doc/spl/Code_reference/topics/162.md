# 3.2.9 Aggregates 在 查询 结果

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.select(NAME:"Apple").sum(UNITPRICE*QUANTITY)
```

## 提取内容
3.2.9
Aggregates 在 查询 结果
|
=demo.query("select
NAME,UNITPRICE,QUANTITY from RECEIPT")
|
=A1.select(NAME:"Apple").sum(UNITPRICE*QUANTITY)
|
