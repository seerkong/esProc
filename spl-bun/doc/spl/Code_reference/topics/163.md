# 3.2.10 过滤 表 序列

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.select(SALARY<6300)
=A1.select(SALARY>5000)
```

## 提取内容
3.2.10 过滤 表 序列
|
=demo.query("select
EID,NAME,STATE,GENDER,BIRTHDAY, HIREDATE, DEPT, SALARY from EMPLOYEE")
|
=A1.select(SALARY<6300)
|
=A1.select(SALARY>5000)
|
