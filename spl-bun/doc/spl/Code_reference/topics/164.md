# 3.2.11 执行 查询 在 表 序列/记录 序列 按 主 键 值

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.keys(CLASS,STUDENTID)
=A1.pfind(["Class
=A1(A3)
=A1.find(["Class
```

## 提取内容
3.2.11 执行 查询 在 表 序列/记录 序列 按 主 键
值
|
=demo.query("select
* 从 SCORES")
|
=A1.keys(CLASS,STUDENTID)
|
=A1.pfind(["Class
one",2])
|
=A1(A3)
|
=A1.find(["Class
one",2])
|
