# 3.2.12 获取 记录 使用 最大/最小 值 和 其 位置

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.sort(HIREDATE)
=A2.pmax(BIRTHDAY)
=A2(to(A3-1))
=A4.minp(BIRTHDAY).NAME
```

## 提取内容
3.2.12 获取 记录 使用 最大/最小 值 和 其 位置
|
=demo.query("select
EID,NAME,STATE,GENDER, BIRTHDAY,HIREDATE,DEPT,SALARY from EMPLOYEE ")
|
=A1.sort(HIREDATE)
|
排序
|
=A2.pmax(BIRTHDAY)
|
位置 的 youngest employee
|
=A2(to(A3-1))
|
记录 的 employees employed earlier
|
=A4.minp(BIRTHDAY).NAME
|
name 的 oldest employee
