# 3.2.13 计算 link relative ratio 用于 选中的 成员

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.pselect@a(CLOSING>10)
=A1.calc(A2,
=A2.new(A1(~).DATE:Date,A1(~).CLOSING:ClosingPrice,A3(#):Increase)
```

## 提取内容
3.2.13 计算 link relative ratio 用于 选中的 成员
|
=demo.query("select
* 从 STOCKRECORDS")
|
=A1.pselect@a(CLOSING>10)
|
定位 DATE s 在 which closing
prices 是 greater than 10
|
=A1.calc(A2,
CLOSING/CLOSING[-1]-1)
|
计算 corresponding increase
|
=A2.new(A1(~).DATE:Date,A1(~).CLOSING:ClosingPrice,A3(#):Increase)
|
Output result set
