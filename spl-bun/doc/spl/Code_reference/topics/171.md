# 3.3.3 成员 排名 top 10, 3rd, 2nd 从 底部 的 序列, 和 median

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.sort(-SCORE)
=A2(to(10))
=A2.m([3,-2])
=round(A2.len()/2)
=A2(A5)
```

## 提取内容
3.3.3
成员 排名 top 10, 3rd,
2nd 从 底部 的 序列, 和 median
|
=demo.query("select
NAME, EVENT,SCORE from GYMSCORE")
|
=A1.sort(-SCORE)
|
=A2(to(10))
|
成员 使用 scores 排名 top 10
|
=A2.m([3,-2])
|
成员 使用 score 排名
third, 和 one 使用 score 排名 第二-到-最后
|
=round(A2.len()/2)
|
=A2(A5)
|
Median score
