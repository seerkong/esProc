# 3.3.4 计算 link relative ratio 用于 top 3

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.sort(DATE)
=A2.psort(-CLOSING)
=A3(to(3))
=A4.(A2.calc(A4.~,
```

## 提取内容
3.3.4
计算 link relative ratio
用于 top 3
|
=demo.query("select
* from STOCKRECORDS where STOCKID=?","000062")
|
=A1.sort(DATE)
|
排序 按 DATE
|
=A2.psort(-CLOSING)
|
排序 按 CLOSING
|
=A3(to(3))
|
序列 数字 的 记录 用于
三个 日期 使用 highest closing prices
|
=A4.(A2.calc(A4.~,
CLOSING- CLOSING[-1]))
|
计算 increases growth rates 的
三个 天数
