# 3.3.6 Select 10 成员 随机

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.sort(rand())(to(10))
```

## 提取内容
3.3.6
Select 10 成员 随机
|
=demo.query("select
NAME,EVENT,SCORE from GYMSCORE")
|
=A1.sort(rand())(to(10))
|
