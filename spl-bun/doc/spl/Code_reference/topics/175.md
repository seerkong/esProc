# 3.3.7 计算 最大 连续 间隔

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.sort(DATE)
=A2.max(a=if(CLOSING/CLOSING[-1]>=1.05,
```

## 提取内容
3.3.7
计算 最大 连续
间隔
|
=demo.query("select
* from STOCKRECORDS where STOCKID=?","000062")
|
=A1.sort(DATE)
|
=A2.max(a=if(CLOSING/CLOSING[-1]>=1.05,
+1,0))
|
最大 连续 间隔 (天数) when
increase 是 greater than 5%
