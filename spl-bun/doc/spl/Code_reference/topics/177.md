# 3.3.9 排序 按 指定 order

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A2.align(A1,ABBR)
```

## 提取内容
3.3.9
排序 按 指定 order
|
[CA,IL,KY,CO,NY]
|
=demo.query("select
NAME,ABBR,CAPITAL,POPULATION from STATES")
|
=A2.align(A1,ABBR)
|
