# 3.3.10 创建 binary 搜索 index 用于 记录 序列

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.sort(SCORE)
=A2.select@b(SCORE:14.175)
=A1.psort(SCORE)
=A1(A4).pselect@b(SCORE:14.175)
=A4(A5)
```

## 提取内容
3.3.10 创建 binary 搜索 index 用于 记录 序列
|
=demo.query("select
NAME, EVENT,SCORE from GYMSCORE")
|
=A1.sort(SCORE)
|
创建 binary 搜索 index
|
=A2.select@b(SCORE:14.175)
|
返回 matching 记录 之后 binary
搜索 是 completed
|
=A1.psort(SCORE)
|
创建 index
|
=A1(A4).pselect@b(SCORE:14.175)
|
=A4(A5)
|
返回 序列 数量 的 记录
matching 指定 条件, 之后 binary 搜索 是 completed
