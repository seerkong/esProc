# 3.4 分组运算

- 格式: 为 AI 阅读提取的 Markdown

## 提取内容
3.4 分组运算
本章列出以下方面的代码示例：
common 分组 运算, 包括 获取 去重 值 的 字段, 删除
重复 成员, 删除 重复 相邻 成员, 拼接 分组
结果 到 表 序列, Set five 成员 在 每个 分组, 计算
aggregate 值 afer 分组, Ge 子集 的 分组 result, Refilter 或
re-排序 分组 result, Refilter 或 re-排序 subsets 的 分组 result,
Regroup subsets 的 分组 result, 执行 intragroup cross-row calculation,
获取 指定 成员 从 每个 grouped 子集, 查找 most appeared 成员,
获取 topN 从 每个 subgroup, 查找 连续 array, 分组 按 neighboring same
键 值, 创建 新 分组 用于 不同 条件, 和 分组 &
aggregate iteratively.
