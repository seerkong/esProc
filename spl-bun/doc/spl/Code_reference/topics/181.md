# 3.4.2 删除 重复 成员

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.id@u(TYPE)
=A1.group@1u(TYPE)
=[1,2,2,3,3,4,5,6,2,3].id@u()
```

## 提取内容
3.4.2
删除 重复 成员
|
=demo.query("select
NAME,TYPE, PRODUCTION from LIQUORS")
|
=A1.id@u(TYPE)
|
删除 repeated 成员 without changing
成员 order
|
=A1.group@1u(TYPE)
|
=[1,2,2,3,3,4,5,6,2,3].id@u()
|
[1,2,3,4,5,6]
