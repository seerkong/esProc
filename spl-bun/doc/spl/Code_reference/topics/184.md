# 3.4.5 Set five 成员 在 每个 分组

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.group(int((#-1)/5))
```

## 提取内容
3.4.5
Set five 成员 在 每个 分组
|
=demo.query("select
NAME,EVENT,SCORE from GYMSCORE")
|
=A1.group(int((#-1)/5))
|
