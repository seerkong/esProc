# 3.4.6 计算 aggregate 值 之后 分组

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.groups(EVENT:GymEvent
sum(SCORE):TotalScore)
=A1.group(EVENT)
=A3.new(EVENT:
```

## 提取内容
3.4.6
计算 aggregate 值 之后
分组
|
=demo.query("select
NAME,EVENT,SCORE from GYMSCORE")
|
=A1.groups(EVENT:GymEvent
sum(SCORE):TotalScore)
|
=A1.group(EVENT)
|
=A3.new(EVENT:
GymEvent,~.求和(SCORE): TotalScore)
|
