# 3.4.7 获取 子集 的 分组 result

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.group(DEPT)
=A2.maxp(~.avg(age(BIRTHDAY)))
```

## 提取内容
3.4.7
获取 子集 的 分组
result
|
=demo.query("select
EID,NAME,STATE, GENDER, BIRTHDAY,HIREDATE, DEPT,SALARY from EMPLOYEE")
|
=A1.group(DEPT)
|
=A2.maxp(~.avg(age(BIRTHDAY)))
|
记录 的 employees 在 department whose
平均 年龄 是 highest
