# 3.4.8 Refilter 或 re-排序 分组 result

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.group(EVENT)
=A2.select(~.avg(SCORE)>14.3)
=A3.sort(-(~.avg(SCORE)))
=A4(to(2))
=demo.query("select
=A6.groups(DEPT;count(age(BIRTHDAY)>40):Number)
=A7.select(Number>=20).(DEPT)
```

## 提取内容
3.4.8
Refilter 或 re-排序 分组
result
|
=demo.query("select
NAME,EVENT,SCORE from GYMSCORE")
|
=A1.group(EVENT)
|
=A2.select(~.avg(SCORE)>14.3)
|
=A3.sort(-(~.avg(SCORE)))
|
=A4(to(2))
|
Top 2 events 使用 最大 平均 score
|
=demo.query("select
EID,NAME,STATE,GENDER, BIRTHDAY,HIREDATE,DEPT,SALARY from EMPLOYEE")
|
=A6.groups(DEPT;count(age(BIRTHDAY)>40):Number)
|
=A7.select(Number>=20).(DEPT)
|
Department 使用 more than 20 employees
over 其 40s
