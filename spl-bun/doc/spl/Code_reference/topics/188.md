# 3.4.9 Refilter 或 re-排序 subsets 的 分组 result

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.group(EVENT)
>A2.(~=~.sort(-SCORE))
>A2.(~=~(to(2)))
=A2.(~.(NAME)).isect()
```

## 提取内容
3.4.9
Refilter 或 re-排序 subsets 的
分组 result
|
=demo.query("select
NAME,EVENT, SCORE from GYMSCORE")
|
=A1.group(EVENT)
|
>A2.(~=~.sort(-SCORE))
|
>A2.(~=~(to(2)))
|
=A2.(~.(NAME)).isect()
|
Athletes 使用 all event scores at top
