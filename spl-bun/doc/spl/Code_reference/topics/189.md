# 3.4.10 Regroup subsets 的 分组 result

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.group(DEPT)
>A2.(~=~.group(month(BIRTHDAY),day(BIRTHDAY)))
=A2.maxp(~.count())
=A4(1).DEPT
```

## 提取内容
3.4.10 Regroup subsets 的 分组 result
|
=demo.query("select
EID,NAME,STATE,GENDER, BIRTHDAY,HIREDATE,DEPT,SALARY from EMPLOYEE")
|
=A1.group(DEPT)
|
>A2.(~=~.group(month(BIRTHDAY),day(BIRTHDAY)))
|
Regroup subsets
|
=A2.maxp(~.count())
|
=A4(1).DEPT
|
