# 3.4.11 执行 intragroup cross-row calculation

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.group(STOCKID).(~.sort(DATE))
=0
=C5|A3.STOCKID
```

## 提取内容
3.4.11 执行 intragroup cross-row calculation
|
=demo.query("select
* 从 STOCKRECORDS")
|
=A1.group(STOCKID).(~.sort(DATE))
|
for
A2
|
=0
|
if
A3.pselect(B3= 如果( CLOSING/CLOSING[-1] >=1.05,B3+1,0):4)>0
|
Limit up 用于 三个 天数
|
=C5|A3.STOCKID
|
Stores result
