# 3.4.12 获取 指定 成员 从 每个 grouped 子集

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.group(TYPE).new(TYPE,~.m(-1):Last)
=demo.query("select
=A3.group(DEPT)
=A4.(~.minp(SALARY))
```

## 提取内容
3.4.12 获取 指定 成员 从 每个 grouped 子集
|
=demo.query("select
NAME,TYPE,PRODUCTION from LIQUORS")
|
=A1.group(TYPE).new(TYPE,~.m(-1):Last)
|
分组 和 aggregate directly
|
=demo.query("select
EID,NAME,STATE,GENDER, BIRTHDAY,HIREDATE,DEPT,SALARY from EMPLOYEE ")
|
=A3.group(DEPT)
|
分组 第一
|
=A4.(~.minp(SALARY))
|
Then aggregate
