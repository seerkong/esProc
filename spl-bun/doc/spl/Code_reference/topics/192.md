# 3.4.13 查找 most appeared 成员

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.group(DEPT)
=A2.maxp(~.count())
=A3(1).DEPT
```

## 提取内容
3.4.13 查找 most appeared 成员
|
=demo.query("select
EID,NAME,STATE,GENDER, BIRTHDAY,HIREDATE,DEPT,SALARY from EMPLOYEE ")
|
=A1.group(DEPT)
|
分组
|
=A2.maxp(~.count())
|
查找 分组 使用 most employees
|
=A3(1).DEPT
|
DEPT 使用 most employees
