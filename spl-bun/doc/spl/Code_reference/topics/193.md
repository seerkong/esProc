# 3.4.14 获取 topN 从 每个 subgroup

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.groups(DEPT;top(3;BIRTHDAY):TOP3_EMP)
```

## 提取内容
3.4.14 获取 topN 从 每个 subgroup
|
=demo.query("select
* 从 EMPLOYEE ")
|
=A1.groups(DEPT;top(3;BIRTHDAY):TOP3_EMP)
|
获取 3 eldest employees 在 每个 department
