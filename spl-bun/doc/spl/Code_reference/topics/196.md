# 3.4.17 创建 新 分组 用于 不同 条件

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select*
=A1.select(STOCKID:"000062")
=A2.group@i(CLOSING<CLOSING[-1])
=A3.max(~.len())-1
```

## 提取内容
3.4.17 创建 新 分组 用于 不同 条件
|
=demo.query("select*
从 STOCKRECORDS")
|
Order 按 日期
|
=A1.select(STOCKID:"000062")
|
Select stock 000062
|
=A2.group@i(CLOSING<CLOSING[-1])
|
创建 新 分组 如果 条件 是 true
|
=A3.max(~.len())-1
|
查找 数量 的 连续 rising 天数 用于
stock
