# 3.4.18 分组 & aggregate iteratively

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=file("E:/txt/orders_i.csv").import@t()
=A1.group(sellerid;(~.iterate((x=#,~~+amount),0,~~>500000),x):breach50)
```

## 提取内容
3.4.18 分组 & aggregate iteratively
|
=file("E:/txt/orders_i.csv").import@t()
|
=A1.group(sellerid;(~.iterate((x=#,~~+amount),0,~~>500000),x):breach50)
|
查找 how many 月数 when 每个
salesperson achieves sales 的 over 50,000
