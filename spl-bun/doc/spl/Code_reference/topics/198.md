# 3.5 关联表上的分组与关联

- 格式: 为 AI 阅读提取的 Markdown

## 提取内容
3.5 关联表上的分组与关联
本章列出以下方面的代码示例：
分组 & 关联 运算 over associative 表, 包括 分组 按
指定 category, 分组 按 指定 ranges, Conditional 分组 使用
possible overlapped ranges, 关联 表 在 equivalence 条件, 关联 表
基于 在 第一 one (左 关联), 关联 记录 even 如果 指定 条件
是 not matched (全 关联), Align 表 在 条件 that 指定 字段 在
它们 是 equal, 执行 关联 under non-equal 条件, 执行
unconditional 关联 (全 cross 关联), Convert foreign 键 参考 到
记录 字段, Convert 成员 的 subtable 到 表 序列 字段, 和
形成 wide 表.
