# 3.5.1 分组 按 指定 category

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A2.align@a(A1,PRODUCTION)
```

## 提取内容
3.5.1
分组 按 指定 category
|
[America,Jamaica,France,Scotland,England]
|
=demo.query("select
NAME,TYPE, PRODUCTION from LIQUORS")
|
=A2.align@a(A1,PRODUCTION)
|
分组 按 PRODUCTION
