# 3.5.2 分组 data 按 指定 ranges

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=create(Section,AgeGroup).record([A1:B5])
=demo.query("select
=A7.enum@r(A6.(Section),AGE)
=A8.new(A6(#).AgeGroup:AgeGroup,
```

## 提取内容
3.5.2
分组 data 按 指定 ranges
|
?<25
|
Below 25
|
?>=25 && ?<=30
|
25 到 30
|
?>30 && ?<=40
|
30 到 40
|
?>40 && ?<=50
|
40 到 50
|
?>50
|
over 50
|
=create(Section,AgeGroup).record([A1:B5])
|
=demo.query("select
EID,NAME,STATE,GENDER, BIRTHDAY,HIREDATE,DEPT, SALARY 从
EMPLOYEE").derive(age(BIRTHDAY):AGE)
|
=A7.enum@r(A6.(Section),AGE)
|
分组 按 AGE
|
=A8.new(A6(#).AgeGroup:AgeGroup,
~.计数():数量, ~.avg(AGE):AverageAge)
|
