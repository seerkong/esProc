# 3.5.3 Conditional 分组 使用 possible overlapped ranges

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A2.enum@r(A1,SALARY)
```

## 提取内容
3.5.3
Conditional 分组 使用
possible overlapped ranges
|
[?<5000,?>=7000,?>10000]
|
=demo.query("select
EID,NAME,STATE,GENDER, BIRTHDAY,HIREDATE,DEPT, SALARY from EMPLOYEE")
|
=A2.enum@r(A1,SALARY)
|
