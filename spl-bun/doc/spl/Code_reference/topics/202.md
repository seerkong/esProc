# 3.5.4 关联 表 在 equivalence 条件

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=join(A1:State,NAME;A2:Employee,STATE)
```

## 提取内容
3.5.4
关联 表 在 equivalence
条件
|
=demo.query("select
* 从 STATES")
|
=demo.query("select
* 从 EMPLOYEE")
|
=join(A1:State,NAME;A2:Employee,STATE)
|
