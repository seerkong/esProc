# 3.5.5 关联 表 基于 在 第一 one (左 关联)

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
one (left join)
=demo.query("select
=demo.query("select
=join@1(A1:State,NAME;A2:Employee,STATE)
```

## 提取内容
3.5.5
关联 表 基于 在 第一
one (左 关联)
|
=demo.query("select
* 从 STATES")
|
=demo.query("select
* 从 EMPLOYEE")
|
=join@1(A1:State,NAME;A2:Employee,STATE)
|
