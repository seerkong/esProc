# 3.5.6 关联 记录 even 如果 指定 条件 是 not matched (全 关联)

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=join@f(A1:State,NAME;A2:Employee,STATE)
```

## 提取内容
3.5.6
关联 记录 even 如果 指定
条件 是 not matched (全 关联)
|
=demo.query("select
* 从 STATES")
|
=demo.query("select
* 从 EMPLOYEE")
|
=join@f(A1:State,NAME;A2:Employee,STATE)
|
