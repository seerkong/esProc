# 3.5.7 Align 表 在 条件 that 指定 字段 在 它们 是 equal

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=demo.query("select
=join@1(A1:Employee,EID;A2:Attencance,EMPLOYEEID
```

## 提取内容
3.5.7
Align 表 在 条件 that
指定 字段 在 它们 是 equal
|
=demo.query("select
* 从 EMPLOYEE")
|
=demo.query("select
* 从 ATTENDANCE")
|
=demo.query("select
* 从 PERFORMANCE")
|
=join@1(A1:Employee,EID;A2:Attencance,EMPLOYEEID
A3:Performance,EMPLOYEEID)
|
