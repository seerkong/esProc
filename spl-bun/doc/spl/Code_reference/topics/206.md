# 3.5.8 执行 关联 under non-equal 条件

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=demo.query("select
=xjoin(A1:State,left(NAME,1)=="A";A2:City,POPULATION>
```

## 提取内容
3.5.8
执行 关联 under non-equal
条件
|
=demo.query("select
* 从 STATES")
|
=demo.query("select
* 从 CITIES")
|
=demo.query("select
* 从 GYMSCORE")
|
=xjoin(A1:State,left(NAME,1)=="A";A2:City,POPULATION>
1000000;A3:Score,EVENT=="Floor")
|
