# 3.5.10 Convert foreign 键 参考 到 记录 字段

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=A1.switch(STATEID,A2)
=A1.group(STATEID.REGIONID)
=A2.run(CAPITAL=A1.select@1(NAME==CAPITAL))
=A1.new(NAME,STATEID.CAPITAL.NAME:StateCapital)
=A1.select(STATEID.CAPITAL.POPULATION>1000000)
```

## 提取内容
3.5.10 Convert foreign 键 参考 到 记录 字段
|
=demo.query("select
* 从 CITIES").键(CID)
|
=demo.query("select
* 从 STATES 其中 STATEID<?",51).键(STATEID)
|
=A1.switch(STATEID,A2)
|
创建 参考 之间 main 表
和 subtable
|
=A1.group(STATEID.REGIONID)
|
Directly 访问 main 表 通过
参考 字段
|
=A2.run(CAPITAL=A1.select@1(NAME==CAPITAL))
|
=A1.new(NAME,STATEID.CAPITAL.NAME:StateCapital)
|
=A1.select(STATEID.CAPITAL.POPULATION>1000000)
|
