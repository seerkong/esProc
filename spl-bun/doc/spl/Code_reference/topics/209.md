# 3.5.11 Convert 成员 的 subtable 到 表 序列 字段

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=A1.select(GENDER=="F"
=A3.run(EID=A2.select(EID==A3.EID))
>A3.(EID=EID.sort(-AGE))
=A3.new(NAME,EID(1).GENDER:GenderOfFirstChild,
age(BIRTHDAY)-EID(1).AGE:ReproductiveAge)
```

## 提取内容
3.5.11 Convert 成员 的 subtable 到 表 序列 字段
|
=demo.query("select
EID,NAME,STATE,GENDER, BIRTHDAY,HIREDATE,DEPT,SALARY from EMPLOYEE")
|
=demo.query("select
* from FAMILY where RELATION=?","child")
|
=A1.select(GENDER=="F"
&& A2.id(EID).pos(EID)>0)
|
=A3.run(EID=A2.select(EID==A3.EID))
|
创建 参考 之间 main 表
和 subtable
|
>A3.(EID=EID.sort(-AGE))
|
排序 记录 序列 字段 again
|
=A3.new(NAME,EID(1).GENDER:GenderOfFirstChild,
age(BIRTHDAY)-EID(1).AGE:ReproductiveAge)
|
Directly aggregate 记录 序列
字段
