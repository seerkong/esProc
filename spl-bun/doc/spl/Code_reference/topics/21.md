# 2.2 字符串

- 格式: 为 AI 阅读提取的 Markdown

## 提取内容
2.2 字符串
本章列出以下方面的代码示例：
处理字符串，包括 生成定长字符串, 搜索 用于 和
替换 子串, 获取字符串片段, 字符串拼接, 大写 和
小写-分支 字母 识别 和 转换, 移除 空白 空格 在 两侧
两侧 的 字符串, 格式字符串匹配, 匹配 格式 字符串 使用 ' * ', 获取 Unicode 和
返回 字符 按 Unicode, 将字符串拆分为序列, 拼接
成员 的 序列 到 字符串, 删除字符串中的指定字符,
检查字符串是否由字母和数字组成, 计算
表达式 存储的 在 字符串, 使用参数计算字符串表达式,
Adjuste 字符串 表达式 during 编辑, 检查是否为字符串, 获取 MD5 签名字符串, 生成 随机 字符串 的
指定 长度, 匹配 字符串 按 正则表达式, 获取 子串 从 source
字符串, 从字符串中拆分单词/数字 和 按字符串解析字符串.
