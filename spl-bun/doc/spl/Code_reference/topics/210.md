# 3.5.12 形成 wide 表

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=A1.fname()\"NAME"
=A2.join(STATE,A1:NAME,${A3.concat@c()})
```

## 提取内容
3.5.12 形成 wide 表
|
=demo.query("select
* 从 STATES")
|
=demo.query("select
* 从 EMPLOYEE")
|
=A1.fname()\"NAME"
|
禁用
A1’s 键
|
=A2.join(STATE,A1:NAME,${A3.concat@c()})
|
关联
两个 表 按 键 和 添加 新 字段
