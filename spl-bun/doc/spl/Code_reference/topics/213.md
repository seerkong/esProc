# 4.1.1 IS NULL/NVL/COALESCE

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.select(DEPT!=null)
=A1.select(DEPT
==null)
=demo.query("select
=A4.(NAME).ifn()
```

## 提取内容
4.1.1
IS NULL/NVL/COALESCE
|
=demo.query("select
EID,NAME,STATE,GENDER,BIRTHDAY, HIREDATE,DEPT, SALARY from EMPLOYEE")
|
=A1.select(DEPT!=null)
|
Not null
|
=A1.select(DEPT
==null)
|
Null
|
=demo.query("select
NAME,UNITPRICE,QUANTITY from RECEIPT")
|
=A4.(NAME).ifn()
|
第一 non-null 成员
