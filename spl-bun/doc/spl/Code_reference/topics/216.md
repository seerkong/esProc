# 4.1.4 AND/OR/NOT, <>

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.select(CLIENT=="PWQ"
=A1.select(AMOUNT>5000
=A1.select(!(CLIENT=="TRADH"))
=A1.select(CLIENT!="TRADH")
```

## 提取内容
4.1.4
AND/OR/NOT, <>
|
=demo.query("select
* 从 SALES")
|
=A1.select(CLIENT=="PWQ"
|| CLIENT=="QUICK")
|
OR
|
=A1.select(AMOUNT>5000
&& AMOUNT<10000)
|
AND
|
=A1.select(!(CLIENT=="TRADH"))
|
NOT
|
=A1.select(CLIENT!="TRADH")
|
<>
