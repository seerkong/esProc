# 4.1.5 LIKE

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.select(like(CLIENT,"*AY*"))
```

## 提取内容
4.1.5
LIKE
|
=demo.query("select
* 从 SALES")
|
=A1.select(like(CLIENT,"*AY*"))
|
