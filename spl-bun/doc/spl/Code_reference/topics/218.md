# 4.1.6 COUNT/SUM/AVG/MAX/MIN

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.sum(AMOUNT)
=A1.count()
=A1.avg(AMOUNT)
=A1.max(AMOUNT)
=A1.min(AMOUNT)
```

## 提取内容
4.1.6
COUNT/SUM/AVG/MAX/MIN
|
=demo.query("select
* 从 SALES")
|
=A1.sum(AMOUNT)
|
=A1.count()
|
=A1.avg(AMOUNT)
|
=A1.max(AMOUNT)
|
=A1.min(AMOUNT)
|
