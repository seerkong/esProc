# 4.1.7 IN/EXISTS

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A2.select(A1.pos(EID)>0)
=demo.query("select
=A5.select(SCORE>75).group(STUDENTID)
=A6.select(~.(SUBJECT).pos(A4)!=null)
=A7.(STUDENTID)
```

## 提取内容
4.1.7
IN/EXISTS
|
[1,3,5,7,9]
|
=demo.query("select
* 从 EMPLOYEE")
|
=A2.select(A1.pos(EID)>0)
|
IN
|
[English,Math]
|
=demo.query("select
* 从 SCORES 其中 CLASS='Class one'")
|
=A5.select(SCORE>75).group(STUDENTID)
|
=A6.select(~.(SUBJECT).pos(A4)!=null)
|
EXISTS
|
=A7.(STUDENTID)
|
