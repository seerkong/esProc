# 4.2 数据查询

- 格式: 为 AI 阅读提取的 Markdown

## 提取内容
4.2 数据查询
本章列出以下方面的代码示例：
general data search, including SELECT * FROM …, WHERE …, SELECT … FROM, AS,
SELECT…, ORDER BY/ASC/DESC, DISTINCT, FIRST/LAST/TOP/BOTTOM, UNION/UNION
ALL/INTERSECT/MINUS, SELECT … FROM (SELECT … ), SELECT (SELECT … FROM) FROM,
和 CURSOR/FETCH.
