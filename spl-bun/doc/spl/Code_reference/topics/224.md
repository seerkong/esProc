# 4.2.4 AS

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.new(EID:EmployeeNo,NAME+"
```

## 提取内容
4.2.4
AS
|
=demo.query("select
* 从 EMPLOYEE")
|
=A1.new(EID:EmployeeNo,NAME+"
"+SURNAME: EmployeeName)
|
