# 4.2.5 SELECT …

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=[[1,"Beverage"]].new(~(1):TypeNumber,~(2):Name)
=create(TypeNumber,TypeName).record(
```

## 提取内容
4.2.5
SELECT …
|
=[[1,"Beverage"]].new(~(1):TypeNumber,~(2):Name)
|
创建 表 序列 containing only one
记录
|
=create(TypeNumber,TypeName).record(
[1,"Beverage",2,"Grain"])
|
创建空表序列, 和 填充
data 在 它
