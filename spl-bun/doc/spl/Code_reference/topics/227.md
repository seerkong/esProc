# 4.2.7 DISTINCT

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.id(CLIENT)
=A1.(CLIENT)
=A1.([CLIENT,SELLERID])
```

## 提取内容
4.2.7
DISTINCT
|
=demo.query("select
* 从 SALES")
|
=A1.id(CLIENT)
|
获取 去重 值
|
=A1.(CLIENT)
|
All 值 available
|
=A1.([CLIENT,SELLERID])
|
All available 值 在 字段
