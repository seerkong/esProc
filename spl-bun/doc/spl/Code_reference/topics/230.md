# 4.2.10 SELECT … FROM (SELECT … )

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.select(DEPT=="Sales")
=A2.count()
```

## 提取内容
4.2.10 SELECT … FROM (SELECT … )
|
=demo.query("select
* 从 EMPLOYEE")
|
=A1.select(DEPT=="Sales")
|
查询
|
=A2.count()
|
Aggregate result set
