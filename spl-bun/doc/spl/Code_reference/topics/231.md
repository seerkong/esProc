# 4.2.11 SELECT (SELECT … FROM) FROM

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=A1.derive()
=A1.run(EID=A2.select(EID:A1.EID))
=A1.new(NAME,EID.count():NumberOfMembers)
=A3.new(NAME,A2.select(EID:A3.EID).count():
```

## 提取内容
4.2.11 SELECT (SELECT … FROM) FROM
|
=demo.query("select
* 从 EMPLOYEE")
|
=demo.query("select
* 从 FAMILY")
|
=A1.derive()
|
=A1.run(EID=A2.select(EID:A1.EID))
|
计算 subtable 参考 第一
|
=A1.new(NAME,EID.count():NumberOfMembers)
|
=A3.new(NAME,A2.select(EID:A3.EID).count():
NumberOfMembers)
|
使用 direct-写入 method
