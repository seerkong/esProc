# 4.3.1 GROUP BY

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.groups(DEPT
sum(SALARY): SalarySum)
=A1.group(DEPT)
=A3.new(DEPT,~.count():EmployeeNumber)
```

## 提取内容
4.3.1
GROUP BY
|
=demo.query("select
* 从 EMPLOYEE")
|
=A1.groups(DEPT
sum(SALARY): SalarySum)
|
分组 和 aggregate together
|
=A1.group(DEPT)
|
第一 分组
|
=A3.new(DEPT,~.count():EmployeeNumber)
|
Then aggregate
