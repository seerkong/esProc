# 4.3.2 HAVING

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.groups(DEPT
sum(SALARY): SalarySum)
=A2.select(SalarySum
>200000)
=A1.group(DEPT)
=A4.select(~.count()>30)
```

## 提取内容
4.3.2
HAVING
|
=demo.query("select
* 从 EMPLOYEE")
|
=A1.groups(DEPT
sum(SALARY): SalarySum)
|
分组 和 aggregate
|
=A2.select(SalarySum
>200000)
|
过滤 aggregate result
|
=A1.group(DEPT)
|
分组
|
=A4.select(~.count()>30)
|
过滤 grouped subsets
