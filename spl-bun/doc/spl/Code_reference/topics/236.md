# 4.3.3 执行 equi-关联 在 same-level 表

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=join(A1:StateName,STATEID;A2:StateInfo,STATEID)
```

## 提取内容
4.3.3
执行 equi-关联 在 same-level
表
|
=demo.query("select
* 从 STATENAME")
|
=demo.query("select
* 从 STATEINFO")
|
=join(A1:StateName,STATEID;A2:StateInfo,STATEID)
|
