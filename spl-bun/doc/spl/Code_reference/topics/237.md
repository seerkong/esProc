# 4.3.4 执行 equi-关联 在 main 表 和 subtable

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=join(A1:State,NAME;A2:Employee,STATE)
```

## 提取内容
4.3.4
执行 equi-关联 在 main 表
和 subtable
|
=demo.query("select
* 从 STATES")
|
=demo.query("select
* 从 EMPLOYEE")
|
=join(A1:State,NAME;A2:Employee,STATE)
|
