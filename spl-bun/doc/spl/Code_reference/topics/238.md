# 4.3.5 Non-equi-joins

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=demo.query("select
=xjoin(A1:Employee,STATE=="New
```

## 提取内容
4.3.5
Non-equi-joins
|
=demo.query("select
* 从 EMPLOYEE")
|
=demo.query("select
* 从 LIQUORS")
|
=demo.query("select
* 从 RECEIPT")
|
=xjoin(A1:Employee,STATE=="New
York";A2:Liquor, STOCK>500;A3:Food,QUANTITY>2)
|
