# 4.3.6 LEFT JOIN/FULL JOIN

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=demo.query("select
=demo.query("select
=demo.query("select
=join@1(A2:State,NAME;A1:Employee,STATE)
=join@f(A2:State,NAME;A1:Employee,STATE)
=join@1(A1:Employee,EID;A3:Attendance,
```

## 提取内容
4.3.6
LEFT JOIN/FULL JOIN
|
=demo.query("select
* 从 EMPLOYEE")
|
=demo.query("select
* 从 STATES")
|
=demo.query("select
* 从 ATTENDANCE")
|
=demo.query("select
* 从 PERFORMANCE")
|
=join@1(A2:State,NAME;A1:Employee,STATE)
|
左 关联
|
=join@f(A2:State,NAME;A1:Employee,STATE)
|
全 关联
|
=join@1(A1:Employee,EID;A3:Attendance,
EMPLOYEEID;A4:Performance, EMPLOYEEID)
|
关联 使用 第一 表 在 alignment
