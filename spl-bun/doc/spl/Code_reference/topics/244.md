# 4.4.4 UPDATE … WHERE …

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.select(CLIENT:"HL").run(AMOUNT=
int(AMOUNT*1.1))
```

## 提取内容
4.4.4
UPDATE … WHERE …
|
=demo.query("select
ORDERID,CLIENT,SELLERID, ORDERDATE, AMOUNT from SALES")
|
=A1.select(CLIENT:"HL").run(AMOUNT=
int(AMOUNT*1.1))
|
Update data 在 certain 条件
