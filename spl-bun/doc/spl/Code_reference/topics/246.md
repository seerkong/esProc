# 4.4.6 ALTER TABLE

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.rename(EID:ID)
```

## 提取内容
4.4.6
ALTER TABLE
|
=demo.query("select
* 从 EMPLOYEE")
|
=A1.rename(EID:ID)
|
修改 字段 names
