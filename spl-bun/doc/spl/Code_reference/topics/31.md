# 2.2.10 将字符串拆分为序列

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
="12345678"
=len(A1)
=A1.split()
="a,[b,c],d"
=A4.split@cp()
=A4.split@c()
=A4.split@cb()
="a;[b;c];d".split@p(";")
="a b c"
=A1.split@t()
="a1b2c".split@r("(\\d)")
```

## 提取内容
2.2.10 将字符串拆分为序列
|
="12345678"
|
=len(A1)
|
=A1.split()
|
拆分 A1 到 序列 的 字符
|
="a,[b,c],d"
|
=A4.split@cp()
|
返回 序列 which 由...组成 的 三个
成员, [b,c], d 其中 成员 [b,c] 是 序列
|
=A4.split@c()
|
返回 序列 which 由...组成 的 三个
成员, [b,c], d 其中 成员 [ b,c] 是 字符串 instead 的
序列
|
=A4.split@cb()
|
返回 序列 which 由...组成 的 four
成员, [b,c], d. 引号 和 brackets will not be matched.
|
="a;[b;c];d".split@p(";")
|
使用 " ; " as 分隔符
|
="a b c"
|
=A1.split@t()
|
返回 序列
由...组成 的, b, c 和 执行 trim 在 两侧 两侧
|
="a1b2c".split@r("(\\d)")
|
返回 序列
由...组成 的, b, c 其中 分隔符 是 正则表达式
