# 2.2.11 将序列成员拼接为字符串

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=[1,"abc,def",[2,4],"{7,8}"]
=A1.concat@cq()
=A1.concat()
=A1.concat("&")
```

## 提取内容
2.2.11 将序列成员拼接为字符串
|
=[1,"abc,def",[2,4],"{7,8}"]
|
=A1.concat@cq()
|
使用 "," as 分隔符 到 拼接 成员, 在 which 字符串 will be
带引号
|
=A1.concat()
|
字符串 成员 don't need 到 be 带引号
和 separated when being
concatenated
|
=A1.concat("&")
|
使用 "&" as 分隔符
