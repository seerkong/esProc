# 2.2.13 检查字符串是否由字母和数字组成

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=len(A1)
=A1.split()
=A3.count(!isdigit(~)
```

## 提取内容
2.2.13 检查字符串是否由字母和数字组成
|
2345$#dfAgsdf23*
|
=len(A1)
|
=A1.split()
|
拆分 A1 到 序列 的 字符
|
=A3.count(!isdigit(~)
&&!isalpha(~))==0
|
检查 如果 它 是 alphanumeric 字符串
