# 2.2.20 使用正则表达式匹配字符串

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=A1.regex("(\\d),([0-9]*),([a-z]),([a-z]*)")
=A3.regex@c("(\\d),([0-9]*),([a-z]),([a-z]*)")
=A5.regex@u("(\\u5c0f\\u660e),(\\u4e2d\\u56fd)")
```

## 提取内容
2.2.20 使用正则表达式匹配字符串
|
4,233,,test
|
=A1.regex("(\\d),([0-9]*),([a-z]),([a-z]*)")
|
返回 序列 的
[4, 233, a, test]
|
4,233,A,tEst
|
=A3.regex@c("(\\d),([0-9]*),([a-z]),([a-z]*)")
|
返回 序列
由...组成 的 [4, 233, A, tEst]; 分支-insensitive
|
小明 , 中国
|
=A5.regex@u("(\\u5c0f\\u660e),(\\u4e2d\\u56fd)")
|
返回 序列 的
[ 小明 , 中国 ], where the
elements 是 Chinese chracters
