# 2.4 序列

- 格式: 为 AI 阅读提取的 Markdown

## 提取内容
2.4 序列
本章列出以下方面的代码示例：
handling 序列, 包括 检查是否为序列, 获取 序列 成员
或 sub-序列 在 reverse direction, 获取 序列 成员 或 sub-序列
在 cycles, 获取 sub-序列 but report no 错误 如果 指定 位置 是
beyond 范围, 生成 固定-长度 序列 由...组成 的 重复 成员,
重复复制序列生成新序列, 生成
连续 整数 序列, Exchange 位置 的 成员 groups 的
序列, 向序列插入一个或多个成员, 删除 one 或 多个
成员 从 序列, 修改序列中一个或多个成员, 修改
序列 成员(s) at 指定 位置(s) 和 supply 值 at
beyond-范围 位置(s), 将一个序列作为成员插入另一个序列,
按 ASCII 字典序比较序列, 获取逆序序列, 获取 &
计数 唯一 成员 在 序列, 将序列扩展到指定长度 和
序列相关逻辑运算.
