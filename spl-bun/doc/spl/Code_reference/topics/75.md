# 2.4.16 获取并计数序列中的唯一成员

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=["a","c","d","e","f","a","a","b"]
=A1.id()
=A1.id@o()
=A1.id@u()
=A1.icount()
=A1.icount@o()
```

## 提取内容
2.4.16 获取并计数序列中的唯一成员
|
=["a","c","d","e","f","a","a","b"]
|
=A1.id()
|
[a,b,c,d,e,f], 移除 duplicates 和 排序 成员
|
=A1.id@o()
|
[a,c,d,e,f,a,b], 移除 neighboring
重复 only
|
=A1.id@u()
|
[a,c,d,e,f,b], 移除 duplicates but won’t 执行 排序
|
=A1.icount()
|
6, 计数 成员 之后 duplicates 是 deleted
|
=A1.icount@o()
|
7, 计数 成员 之后 neighboring duplicates
是 deleted
