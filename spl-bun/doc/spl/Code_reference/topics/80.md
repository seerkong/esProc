# 2.5.2 判断是否为记录或表序列

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=create(fld1).record(A1)
=ifr(A1(1))
=ifr(A2(1))
=ift(A1)
=ift(A2)
```

## 提取内容
2.5.2
判断是否为记录或表序列
|
[1,2,3]
|
=create(fld1).record(A1)
|
=ifr(A1(1))
|
false, 判断 如果 它 是 记录
|
=ifr(A2(1))
|
true, 判断 如果 它 是 记录
|
=ift(A1)
|
false, 判断 如果 它 是 表 序列
|
=ift(A2)
|
true, 判断 如果 它 是 表 序列
