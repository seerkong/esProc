# 2.6.3 从表序列移除一个或多个记录

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
>A1.delete(2)
>A1.delete([4,6,1])
>A1.delete(A1.select(SCORE<15))
```

## 提取内容
2.6.3
移除 one 或 多个 记录
从 表 序列
|
=demo.query("select
NAME, EVENT, SCORE from GYMSCORE")
|
>A1.delete(2)
|
移除 第二 记录
|
>A1.delete([4,6,1])
|
移除 多个 记录
|
>A1.delete(A1.select(SCORE<15))
|
移除 记录 基于 在 指定
条件
