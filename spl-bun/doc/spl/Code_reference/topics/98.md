# 2.6.7 基于指定表序列/记录序列创建新表序列

- 格式: 为 AI 阅读提取的 Markdown

## 类代码片段
```spl
=demo.query("select
=A1.new(NAME,
=A1.new(NAME:State,
```

## 提取内容
2.6.7
创建 新 表 序列
基于 在 指定 表 序列/记录 序列
|
=demo.query("select
NAME, ABBR, CAPITAL,POPULATION from STATES")
|
=A1.new(NAME,
ABBR)
|
创建 新 表 序列
基于 在 original one
|
=A1.new(NAME:State,
ABBR,CAPITAL:Capital)
|
Change 字段 names
