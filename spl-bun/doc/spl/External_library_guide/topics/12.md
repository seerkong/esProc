# 3.9 FTP file systems

- 格式: 为 AI 阅读提取的 Markdown

## 提取内容
3.9 FTP file systems

1. 该外部库目录：`安装目录\esProc\extlib\FtpCli`。对应 Raqsoft 核心 jar：`scu-ftp-cli-2.10.jar`。

2. esProc 提供 `ftp_client()`、`ftp_cd()`、`ftp_dir()`、`ftp_put()`、`ftp_mput()`、`ftp_get()`、`ftp_mget()`、`ftp_close()` 访问 FTP 文件系统。可在【Help】-【Function reference】中查看用法。
