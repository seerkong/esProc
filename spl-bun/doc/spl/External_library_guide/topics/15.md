# 3.12 HDFS file systems

- 格式: 为 AI 阅读提取的 Markdown

## 提取内容
3.12 HDFS file systems

1. 该外部库目录：`安装目录\esProc\extlib\HdfsFileCli`。对应 Raqsoft 核心 jar：`scu-hdfsFile-cli-2.10.jar`。

commons-cli-1.2.jar
commons-collections-3.2.2.jar
commons-configuration2-2.1.1.jar
commons-lang3-3.7.jar
guava-11.0.2.jar
hadoop-auth-3.2.0.jar
hadoop-client-runtime-3.2.0.jar
hadoop-common-3.2.0.jar
hadoop-hdfs-3.2.0.jar
hadoop-mapreduce-client-core-3.2.0.jar
hadoop-mapreduce-client-jobclient-3.2.0.jar
htrace-core4-4.1.0.jar
log4j-1.2.17.jar
protobuf-java-2.5.0.jar
slf4j-api-1.7.25.jar
slf4j-log4j12-1.7.25.jar
stax2-api-3.1.4.jar
woodstox-core-5.0.3.jar

注：压缩包内已包含第三方 jar，用户可按具体场景选取。

- Hadoop 2.8.3 额外需要：
hadoop-hdfs-client-2.8.3.jar
htrace-core4-4.0.1-incubating.jar

- Hadoop 3.2.0 额外需要：
hadoop-client-runtime-3.2.0
hadoop-hdfs-client-3.2.0.jar
htrace-core4-4.1.0-incubating.jar
stax2-api-3.1.4.jar
woodstox-core-5.0.3.jar

此外，低版本请用 `commons-configuration2-2.1.1.jar` 替换 `commons-configuration-1.6.jar`。

2. 从网络下载以下四个文件并放到 `安装目录\bin`：

hadoop.dll
hadoop.lib
libwinutils.lib
winutils.exe

注：以上文件仅 Windows 需要，Linux 不需要。`winutils.exe` 需按系统位数选择 x86 或 x64。

3. 需要 JRE 1.7 及以上。若 esProc 内置 JRE 版本不足，请安装更高版本并在 `安装目录\esProc\bin\config.txt` 中配置 `java_home`；若版本满足可跳过。

4. 如默认内存不足，可手动调整。Windows 下通过 `.exe` 启动时改 `config.txt`，通过 `.bat` 启动时改批处理脚本；Linux 下改 `.sh`。
Windows 下 `config.txt` 示例：

java_home=C:\ProgramFiles\Java\JDK1.7.0_11;esproc_port=48773;btx_port=41735;gtm_port=41737;jvm_args= -Xms256m -XX:PermSize=256M
-XX:MaxPermSize=512M -Xmx9783m -Duser.language=zh

5. 在安装 esProc 的机器上编辑 hosts 文件，加入 HDFS 服务器 IP 与主机名。例如 IP 为 `192.168.0.8`、主机名为 `masters`：

（原文含示意图，图像资源未迁移）

6. esProc 提供 `hdfs_open()`、`hdfs_close()`、`hdfs_file()`、`hdfs_dir()`、`hdfs_exists()`、`hdfs_upload()`、`hdfs_download()` 等函数访问 HDFS 文件系统。可在【Help】-【Function reference】中查看用法。
