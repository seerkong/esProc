# 3.28 Webservice

- 格式: 为 AI 阅读提取的 Markdown

## 提取内容
3.28 Webservice

1. WebserviceCli 对应 Raqsoft 核心 jar：`scu-webservice-cli-2.10.jar`。
该外部库目录：`安装目录\esProc\extlib\WebserviceCli`。

jdom-1.1.3.jar

注：压缩包内已包含第三方 jar，用户可按具体场景选取。

2. esProc 提供 `ws_client()` 与 `ws_call()` 访问 Web Service。可在【Help】-【Function reference】中查看用法。
