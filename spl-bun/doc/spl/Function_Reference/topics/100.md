# atan()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
atan()
atan ( number )
atan(1)
atan(tan(pi()/2))
atan(tan(0))
```

## 提取内容
atan()
说明：
计算反正切值。
语法：
atan ( number )
注意：
该函数计算反正切值。
参数：
|
number
|
实数。
返回值：
数值型
value
示例：
|
atan(1)
|
0.7853981633974483
|
atan(tan(pi()/2))
|
1.5707963267948966
|
atan(tan(0))
|
0.0
相关函数：
asi n ()
a c o s ( )
