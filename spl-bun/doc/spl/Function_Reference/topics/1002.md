# webhdfs_file()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
webhdfs_file()
webhdfs_file( fileUrl , params )
=webhdfs_file("http://localhost:50070/webhdfs/v1/test/orders.txt":"UTF-8","user.name=root")
=A1.read()
=A1.import@t()
=A1.cursor().fetch ()
=webhdfs_file("http://localhost:50070/webhdfs/v1/test/orders.btx","user.name=root")
=A5.import@b()
=A5.cursor@b().fetch()
```

## 提取内容
webhdfs_file()
说明：
Implement file
read and write.
语法：
webhdfs_file( fileUrl , params )
注意：
The WebcrawlCli external library function (See External
Library Guide ) implements file read and write
using the HTTP request.
参数：
|
fileUrl
|
URL
of the target file.
|
params
|
Parameter
in the file URL.
返回值：
URL
of the target file
示例：
|
=webhdfs_file("http://localhost:50070/webhdfs/v1/test/orders.txt":"UTF-8","user.name=root")
|
Connect
to WebHDFS .
|
=A1.read()
|
Read
data from A1’s text file as a string .
|
=A1.import@t()
|
Read
content of the text file as records to form a table sequence to return .
|
=A1.cursor().fetch ()
|
Create
cursor for A1’s text file, and fetch records from the cursor .
|
=webhdfs_file("http://localhost:50070/webhdfs/v1/test/orders.btx","user.name=root")
|
=A5.import@b()
|
Read
content of A5’s bin file as records to form a table sequence to return .
|
=A5.cursor@b().fetch()
|
Create
cursor for A5’s bin file, and fetch records from the cursor .
