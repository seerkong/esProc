# f. write()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=file("D:/tmp.txt")
>A1.write("China")
>A1.write@a("Chinese")
=["China","America","England
>A1.write(A4)
>A1.write(string(now())+":start")
>A1.write@a(string(now())+":end")
>A1.write@a(string(now())+":startPrint")
>A1.write@a(string(now())+":endPrint")
=file("D:/test.btx") .read@b()
=file("D:/result.btx") .write@b(A10)
=file("D:/employee.txt").write@w(A4)
```

## 提取内容
f. write()
说明：
Write
a string or a sequence into a file.
语法：
|
f . write ( s )
|
f . write ( A )
|
Write
A , the sequence of strings, to file
f ; each member occupies one row.
注意：
The
function writes string s or the sequence of
strings A to file f . By default, the original file f will got overwritten, meaning contents in f will be first cleared and new contents are written.
参数：
|
f
|
file object, which can be a data file of txt, csv, btx or xls format.
|
s
|
string or BLOB value.
|
sequence of strings.
选项：
|
@a
|
Append
data into a file, instead of overwriting it. If the file has contents before
appending, then start a new line (with carriage return) to append.
|
@b
|
Write
as a binary file; in this case parameter s is a BLOB value.
|
@w
|
Use Windows-style \r\n line break; by
default, the line break is specified by OS .
示例：
|
=file("D:/tmp.txt")
|
>A1.write("China")
|
Overwrite tmp.txt and write string
China to it:
（原文含示意图，图像资源未迁移）
|
>A1.write@a("Chinese")
|
Use @a option to append string Chinese to tmp.txt :
（原文含示意图，图像资源未迁移）
|
=["China","America","England
"]
|
>A1.write(A4)
|
Write A4’s string sequence to tmp.txt :
（原文含示意图，图像资源未迁移）
|
>A1.write(string(now())+":start")
|
Simulate
to compose a log using write@a function.
（原文含示意图，图像资源未迁移）
|
>A1.write@a(string(now())+":end")
|
>A1.write@a(string(now())+":startPrint")
|
>A1.write@a(string(now())+":endPrint")
|
=file("D:/test.btx") .read@b()
|
Read data from a binary
file and return a BLOB value.
|
=file("D:/result.btx") .write@b(A10)
|
Generate result. btx using binary format data.
|
=file("D:/employee.txt").write@w(A4)
|
Use \r\n, the
Windows-style line break.
相关函数：
f .re a d ( )
