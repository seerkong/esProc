# xjoinx()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
xjoinx()
xjoinx ( cs i : F i , x i ,..;…)
=demo.cursor("select
=demo.query("select
=xjoinx(A1:Emp;A2:Family)
=A3.fetch()
=demo.cursor("select
=demo.cursor("select top 10 EID,NAME,RELATION,GENDER,AGE
=xjoinx(A1:Emp;A2:Family,EID==Emp.EID)
=A3.fetch()
=demo.cursor("select
=demo.query("select top 10 EID,NAME,RELATION,GENDER,AGE
=xjoinx@1(A1:Emp;A2:Family,EID==Emp.EID)
=A3.fetch()
```

## 提取内容
xjoinx()
说明：
Perform
cross join over table sequences represented by cursors and return a multicursor.
语法：
xjoinx ( cs i : F i , x i ,..;…)
注意：
Perform cross join on cursors/table
sequences cs i unconditionally to generate
a new cursor composed of F i ,… fields. Each F i
references a member of the corresponding original cursor cs i .
The operation will filter members of cs i
according to the filtering criterion x i if it is supplied. Parameter cs i is retraversable unicursors or table
sequences.
This is a delayed function.
选项：
|
@1
|
Perform
left join (注意： here is the number 1). Use null to participate in the cross
join if there are no matching members in cursors cs i .
参数：
|
F i
|
Names of fields of the result table sequence.
|
cs i
|
Unicursors
or table sequences to be joined.
|
x i
|
Filtering
expression.
Return
value:
多游标
示例：
|
=demo.cursor("select
EID,NAME,DEPT,GENDER from EMPLOYEE " )
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=demo.query("select
distinct RELATION from FAMILY " )
|
Return a table
sequence:
（原文含示意图，图像资源未迁移）
|
=xjoinx(A1:Emp;A2:Family)
|
Perform cross join
over A1 and A2.
|
=A3.fetch()
|
Fetch data from
cursor A3:
（原文含示意图，图像资源未迁移）
When
filter condition x i is specified:
|
=demo.cursor("select
top 10 EID,NAME,DEPT,GENDER from EMPLOYEE " )
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select top 10 EID,NAME,RELATION,GENDER,AGE
from FAMILY " )
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=xjoinx(A1:Emp;A2:Family,EID==Emp.EID)
|
Cross join with a
filter condition.
|
=A3.fetch()
|
Fetch data from
cursor A3:
（原文含示意图，图像资源未迁移）
Use
@1 option to enable left join:
|
=demo.cursor("select
top 10 EID,NAME,DEPT,GENDER from EMPLOYEE " )
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=demo.query("select top 10 EID,NAME,RELATION,GENDER,AGE
from FAMILY " )
|
Return a table
sequence:
（原文含示意图，图像资源未迁移）
|
=xjoinx@1(A1:Emp;A2:Family,EID==Emp.EID)
|
Left join-style
cross join.
|
=A3.fetch()
|
Fetch data from
cursor A3:
（原文含示意图，图像资源未迁移）
function:
x j o i n ()
