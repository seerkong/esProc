# xlscell()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
xlscell()
=file("xc1.xls").xlsopen()
=A1.xlscell("A1":"C4",1)
=A1.xlscell("B1","s2")
=A1.xlscell("C4":,)
=A1.xlscell@w("A1":"C3")
=A1.xlscell@wp("A1":"C3")
=file("xc2.xls").xlsopen()
=A1.xlscell@w("A1":,)
=A1.xlscell@n("A1":,)
=A1.xlscell@nw("A1":,)
=file("xc1.xls")
=A1.xlsopen()
=A2.xlscell("A3":,"s2";"aa\rbb\tcc")
=A1.xlswrite(A2)
=A2.xlscell@i("A3",;"OOOOO")
=A1.xlswrite(A2)
=[5,6]
=A2.xlscell("A6":,"s2";A7)
=A1.xlswrite(A2)
=[[7,49],[8,64]]
=A2.xlscell("A8":,"s2";A10)
=A1.xlswrite(A2)
=file("tp.xls")
=A1.xlscell@g("B2")
=file("tp_copy.xls").xlsopen()
=A3.xlscell@g("A3",;A2)
=file("tp_copy.xls").xlswrite(A3)
```

## 提取内容
xlscell()
说明：
Enter cell values to an Excel file object or
read values from it.
语法：
xo. xlscell ( a : b , s ; t )
注意：
The
function enters values to Excel sheet s
or read values from it. Parameter xo is an Excel file object read in the non-@r@w way.
When
parameter t is present and :b
is absent, a means entering string t in cell a ; a : means entering string t starting from cell a . Save the
entered data using the xlswrite() function.
If t
is absent, a : b means reading
values from cell a to cell b and return them as a string; when only parameter a is present, the function reads cell a
only; if the parameter is written as a :,
read cells through to the end. .
参数：
|
xo
|
An Excel file object read in a non-@r@w
way .
|
a
|
Cell a .
|
b
|
Cell b .
|
s
|
Sheet number, which
should not exceed 31 characters and contain special characters []:/\?*, or sheet name; it is the first page when omitted .
|
t
|
The to-be-populated data, which can be
omitted; it can be separated by the carriage return “\r”(which means the content will be
entered into the next row) or tab“\t” (which means the content will be
entered into the next column); t can be a string, a single
value, a sequence or a sequence of sequences .
选项：
|
@i
|
Insert-row style export; by
default, enter the specified data in a row next to the row containing cell a .
|
@w
|
Enable returning a sequence of
sequences of cell values at read-in .
|
@g
|
Parameter :b will be absent and parameter t is blob type when using this option to read in or configure an
image; support jpg a png only .
|
@p
|
Work with @w
to return a sequence of sequences; each sub-sequence is made up of column
values .
|
@n
|
Remove blank
spaces on both sides of a string; read an empty string as null when @w option
is also present .
返回值：
No return value/字符串/Sequence/BLOB
示例：
Read
data from an Excel 对象 when parameter t is absent:
|
=file("xc1.xls").xlsopen()
|
Read
the Excel 对象 and return its content as follows:
（原文含示意图，图像资源未迁移）
Below
is content of xc1.xls :
（原文含示意图，图像资源未迁移）
（原文含示意图，图像资源未迁移）
|
=A1.xlscell("A1":"C4",1)
|
Read content from cell A1 to cell C4 in the first
sheet of xc1.xls and return a string:
（原文含示意图，图像资源未迁移）
|
=A1.xlscell("B1","s2")
|
As
:b is absent, read content of cell B1 in xc1.xls ’s sheet s2,
and return a string :
（原文含示意图，图像资源未迁移）
|
=A1.xlscell("C4":,)
|
As
both parameter s and parameter b are absent, read content from
C4 to the end in xc1.xls ’s first sheet , and return a string :
（原文含示意图，图像资源未迁移）
|
=A1.xlscell@w("A1":"C3")
|
With
@w option, cell values are read and returned as a sequence of sequences:
（原文含示意图，图像资源未迁移）
|
=A1.xlscell@wp("A1":"C3")
|
With @wp options, columns are read and returned as a sequence of sequences:
（原文含示意图，图像资源未迁移）
Use
@n option to read value in the trim style:
|
=file("xc2.xls").xlsopen()
|
=A1.xlscell@w("A1":,)
|
Read content from xc2.xls
and return it as a sequence:
（原文含示意图，图像资源未迁移）
The 2 nd
member value has blank spaces at both ends; the 3 rd member value
is an empty string
|
=A1.xlscell@n("A1":,)
|
With @n option,
remove blank spaces at reading .
（原文含示意图，图像资源未迁移）
|
=A1.xlscell@nw("A1":,)
|
With @wn options,
read the empty string as null .
（原文含示意图，图像资源未迁移）
Enter
values to an Excel 对象:
|
=file("xc1.xls")
|
Below
is content of xc1.xls :
（原文含示意图，图像资源未迁移）
（原文含示意图，图像资源未迁移）
|
=A1.xlsopen()
|
Read
the Excel 对象 and return its content as follows:
（原文含示意图，图像资源未迁移）
|
=A2.xlscell("A3":,"s2";"aa\rbb\tcc")
|
Enter
string aa to cell A3 in sheet s2, string bb to cell A4 in the next row, and
string cc to cell B4 in the next column to the right .
|
=A1.xlswrite(A2)
|
Use
xlswrite function to save the written content and view the Excel file as
follows:
（原文含示意图，图像资源未迁移）
|
=A2.xlscell@i("A3",;"OOOOO")
|
Insert a row below cell A3 in the first
sheet, which will be row 4, and enter string OOOOO .
|
=A1.xlswrite(A2)
|
Use
xlswrite function to save the written content and view the Excel file as
follows:
（原文含示意图，图像资源未迁移）
|
=[5,6]
|
Return
a sequence .
|
=A2.xlscell("A6":,"s2";A7)
|
Enter
members of A7’s sequence column by column starting from cell A6 in sheet s2 .
|
=A1.xlswrite(A2)
|
Use
xlswrite function to save the written content and view the Excel file as
follows:
（原文含示意图，图像资源未迁移）
|
=[[7,49],[8,64]]
|
Return
a sequence of sequences .
|
=A2.xlscell("A8":,"s2";A10)
|
Enter
sequence members of A10’s sequence row by row starting from cell A8 in sheet
s2 .
|
=A1.xlswrite(A2)
|
Use
xlswrite function to save the written content and view the Excel file as
follows:
（原文含示意图，图像资源未迁移）
Read
or set up an image:
|
=file("tp.xls")
|
Below
is content of tp.xls :
（原文含示意图，图像资源未迁移）
|
=A1.xlscell@g("B2")
|
Return
a blob type value and view the image as follows:
（原文含示意图，图像资源未迁移）
|
=file("tp_copy.xls").xlsopen()
|
Open
tp_copy.xls , which is a blank Excel file, and return an Excel 对象 .
|
=A3.xlscell@g("A3",;A2)
|
Write
the blob value returned by A2 to cell A3 in tp_copy.xls .
|
=file("tp_copy.xls").xlswrite(A3)
|
Use
xlswrite function to save the written content and view tp_copy.xls as
follows:
（原文含示意图，图像资源未迁移）
