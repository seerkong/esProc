# xo. xlsexport()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select * from scores")
=file("E11.xlsx")
=A2.xlsopen()
=A3.xlsexport@t(A1)
=A3.xlsexport@t(A1,CLASS:class,STUDENTID:sid;"Class")
=A3.xlsexport@a(A1,STUDENTID,SUBJECT,SCORE
=A3.xlsexport@ w (A 7 )
=A3.xlsexport@ wp (A 7 )
=file("e7.xls").xlsimport@s(;1)
=A3.xlsexport@ w (A 10 )
=A3.xlsexport@ wp (A 10 )
=A2.xlswrite(A3)
```

## 提取内容
xo. xlsexport()
说明：
Write a sequence/cursor to an Excel file
object.
语法：
xo .xlsexport (A, x :Fi,..;s)
注意：
The
function writes a sequence to sheet s
in an Excel file. Parameter A can be
a cursor when parameter xo is an
Excel file object retrieved using @w option.
参数：
|
xo
|
An Excel file object read in the non-@r
way .
|
A sequence/cursor .
|
x
|
To-be-exported fields; all fields will be
exported when omitted. It is a sequence number for locating the field when
specified as # .
|
F i
|
Result
field names; use the original field names when this parameter is absent.
|
s
|
Sheet name or sheet number. Create the
sheet if the sheet name or number does not exist; and write data to the first
sheet when omitted. The sheet name should not exceed 31 characters and
contain special characters []:/\?*, and its first and last characters should
not be a single quotation mark .
选项：
|
@t
|
Export headers; headers will overwrite the
last row if there is data already .
|
@a
|
Append data according to the current format
if the page exists; overwrite the existing data by default .
|
@w
|
Use
this option when parameter A is
a sequence of sequences or a string delimited by CR/tab; it does not work
with @t option, and does not need parameters x:F .
|
@p
|
When
parameter A is
a sequence of sequences, use this option to transpose “first-column-then-row”
form of data structure before writing data to a target Excel file; the option
must work with @w option. Ignore this option when parameter A is a string delimited by CR/tab.
示例：
|
=demo.query("select * from scores")
|
Return a table sequence :
（原文含示意图，图像资源未迁移）
|
=file("E11.xlsx")
|
=A2.xlsopen()
|
Read E11.xlsx and return an
Excel 对象 .
|
=A3.xlsexport@t(A1)
|
Write A1’s table sequence into an Excel 对象 .
|
=A3.xlsexport@t(A1,CLASS:class,STUDENTID:sid;"Class")
|
Create a sheet named Class in an Excel 对象 , rename
CLASS field and STUDENTID field in A1’s table sequence and write them into
sheet Class .
|
=A3.xlsexport@a(A1,STUDENTID,SUBJECT,SCORE
"STUSCORE")
|
There is a sheet named STUSCORE in E11.xlsx that has data in it, so
append STUDENTID, SUBJECT, SCORE columns in A1’s table sequence to this page .
|
[[1,2,3],[4,5,6],[7,8,9]]
|
=A3.xlsexport@ w (A 7 )
|
Write
A7’s sequence of sequences to A3’s Excel file:
（原文含示意图，图像资源未迁移）
|
=A3.xlsexport@ wp (A 7 )
|
Transpose
the “first-column-then-row” content and write it to the Excel file:
（原文含示意图，图像资源未迁移）
|
=file("e7.xls").xlsimport@s(;1)
|
Return
a string delimited by CR/tab:
（原文含示意图，图像资源未迁移）
|
=A3.xlsexport@ w (A 10 )
|
Write
A10’s string to A3’s Excel file:
（原文含示意图，图像资源未迁移）
|
=A3.xlsexport@ wp (A 10 )
|
As
A10 returns a string delimited by CR/tab, ignore @p option and the export
result is the same as A11.
|
=A2.xlswrite(A3)
|
Save the Excel 对象 in E11.xlsx using
the xlswrite() function .
