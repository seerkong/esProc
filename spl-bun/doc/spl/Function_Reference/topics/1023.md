# xlsopen()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
xlsopen()
=file("D:/ex1.xls")
=A1.xlsopen("123")
=A2.xlsimport()
=file("ex2.xlsx").xlsopen@r()
>A4.xlsclose()
```

## 提取内容
xlsopen()
说明：
Read an Excel file and return an Excel file object.
语法：
f. xlsopen ( p )
注意：
The function reads Excel file f
and returns an Excel file object, which can be saved as a read-only table
sequence, which includes three attributes: stname (page name), nrows (number of
rows) and ncols (number of columns). When f
is large, use @r and @w options to retrieve data from it.
参数：
|
f
|
An Excel file.
|
p
|
The password for opening an Excel file; can be omitted if there isn’t a password.
选项：
|
@r
|
Read data in a stream style; support only xlsx files.
|
@w
|
Write data in a stream style; doesn’t support returning the index and
support only xlsx files; this option and @r option are mutually exclusive.
Return
value:
Excel file object
示例：
|
=file("D:/ex1.xls")
|
=A1.xlsopen("123")
|
Read the xls file whose password is 123 and return an Excel
object :
（原文含示意图，图像资源未迁移）
|
=A2.xlsimport()
|
Retrieve a table sequence
from the Excel 对象; when there isn’t the parameter, retrieve data from the
first sheet.
|
=file("ex2.xlsx").xlsopen@r()
|
Read the xlsx file without a
password in the stream style and return an Excel 对象 .
|
>A4.xlsclose()
|
Close the Excel 对象.
