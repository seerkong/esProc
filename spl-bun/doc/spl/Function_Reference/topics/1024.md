# xlswrite()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
xlswrite()
=file("E1.xls").xlsopen()
=file("T2.xls").xlswrite(A1,"123")
=file("E3.xlsx")
=A1.xlsopen()
=demo.query("select
=A2.xlsexport@t(A3)
=A2.xlsmove
=A1.xlswrite(A2)
>A2.xlsclose()
```

## 提取内容
xlswrite()
说明：
Write data of an Excel file
object to a new Excel file.
语法：
f. xlswrite ( xo,p )
注意：
The function writes data of an
Excel file object to file f . It is
used to write data retrieved from an Excel file object in the non- @r@w method.
参数：
|
f
|
The Excel file object into which data will
be written.
|
xo
|
The Excel 对象 from which data is
retrieved in the non- @r@w method .
|
p
|
For specifying the password to access an
Excel file.
示例：
Write data of an Excel 对象 to a new Excel file:
|
=file("E1.xls").xlsopen()
|
Retrieve
data from E1.xls and return an Excel 对象.
|
=file("T2.xls").xlswrite(A1,"123")
|
Write
data of A1’s Excel 对象 to T2.xls and set 123 as the new file’s
password.
Perform update on Excel
object and save the changes:
|
=file("E3.xlsx")
|
=A1.xlsopen()
|
Retrieve
data from E3.xlsx and return an Excel 对象.
|
=demo.query("select
* from scores")
|
Return
a table sequence.
|
=A2.xlsexport@t(A3)
|
Write
data of A3’s table sequence to the first sheet of E3.xlsx .
|
=A2.xlsmove
("Sheet3")
|
Delete
a sheet named Sheet3 from E3.xlsx .
|
=A1.xlswrite(A2)
|
Save
updates on the Excel 对象 to a new Excel file.
|
>A2.xlsclose()
|
Close
the Excel 对象.
