# xor()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
xor()
xor( xi,… )
xor( A )
xor(6,11)
xor([6,11])
xor(decimal(6),decimal(11))
```

## 提取内容
xor()
说明：
Perform
XOR operation on integers.
语法：
xor( xi,… )
xor( A )
注意：
The
function performs XOR operation on integers. It supports a decimal without the
fractional part.
参数：
|
sequence.
|
x i
|
The
numerical expression based on which you perform the XOR operation.
返回值：
Integer
示例：
|
xor(6,11)
|
xor([6,11])
|
xor(decimal(6),decimal(11))
|
13,
which is the decimal type.
