# year()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
year()
year( dateExp )
year(datetime("19800227","yyyyMMdd"))
year(19000)
```

## 提取内容
year()
说明：
Get
the year from a date value.
语法：
year( dateExp )
注意：
The
function gets the year from the date dateExp .
参数：
|
dateExp
|
date or a date expression in standard format; it can be an integer, which we
can regard as the value of computing days@o().
返回值：
Integer
示例：
|
year(datetime("19800227","yyyyMMdd"))
|
year("1972-11-08
10:20:30")
|
year(datetime("2006-01-15
13:20:45"))
|
year(19000)
|
相关函数：
mo n th()
da y ( )
hou r ( )
minu t e()
seco n d ()
millis e cond()
