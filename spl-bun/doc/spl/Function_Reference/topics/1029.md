# ym2_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ym2_close()
ym2_close ( preObj )
=ym2_env("F:/ymodel")
=ym2_pcfload("F:/tmp/train.pcf")
=ym2_predict(A2,"F:/tmp/train.csv")
>ym2_close(A3)
```

## 提取内容
ym2_close()
说明：
Release resources.
语法：
ym2_close ( preObj )
注意：
YmodelCli external library function (See YModel User Reference ).
The function closes the YModel
service to release resources.
When parameter preObj is
present, clear the specified prediction result data and closes YModel service.
参数：
|
preObj
|
Prediction result object.
示例：
|
=ym2_env("F:/ymodel")
|
Initialize
environment and start Python service.
|
=ym2_pcfload("F:/tmp/train.pcf")
|
Return
a pcf object.
|
=ym2_predict(A2,"F:/tmp/train.csv")
|
Perform
prediction using a .csv file.
|
>ym2_close(A3)
|
Clear
prediction data to release resources.
