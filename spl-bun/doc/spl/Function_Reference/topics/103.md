# T.attach(T’)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.attach(T’)
=file("D:\\emp1.ctx")
=A1.create(#EID,NAME)
=demo.cursor("select
=A2.append(A3)
=A2.attach(t1,
=demo.cursor("select
=A5.append(A6)
=A2.attach(t2,GENDER,SALARY)
=demo.cursor("select
=A8.append(A9)
=file("emp1.ctx",["192.168.0.116:8281"])
=A1.open()
=A2.attach(t1)
```

## 提取内容
T.attach(T’)
说明：
Retrieve
an attached table from a cluster composite table.
语法：
T. attach ( T’ )
注意：
The function retrieves attached table T’ , which should already exist, from cluster composite table T .
Only supported by esProc Enterprise Edition.
参数：
|
T
|
cluster composite table.
|
T’
|
An
attached table.
Return
value:
Attached
table
示例：
Add
an attached table to composite table emp1.ctx :
|
=file("D:\\emp1.ctx")
|
=A1.create(#EID,NAME)
|
Create
the composite table’s base table .
|
=demo.cursor("select
EID,NAME from employee ")
|
=A2.append(A3)
|
Append
records to the base table .
|
=A2.attach(t1,
SURNAME,DEPT )
|
Add
attached table t1 to the base table .
|
=demo.cursor("select
EID,SURNAME,DEPT from employee")
|
=A5.append(A6)
|
Append
records to attached table t1 .
|
=A2.attach(t2,GENDER,SALARY)
|
Add
attached table t2 to the base table .
|
=demo.cursor("select
EID,GENDER,SALARY from employee ")
|
=A8.append(A9)
|
Append
records to attached table t2 .
Put
emp1.ctx in node 192.168.0.116:8281:
|
=file("emp1.ctx",["192.168.0.116:8281"])
|
Open
a cluster file .
|
=A1.open()
|
Return
a cluster composite table .
|
=A2.attach(t1)
|
Retrieve
attached table t1 .
