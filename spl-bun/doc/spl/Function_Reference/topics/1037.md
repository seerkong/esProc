# zeros()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
zeros()
zeros ( n 1 ,n 2 ,…)
=zeros(4,3)
=zeros(3)
=zeros(4,3,5)
```

## 提取内容
zeros()
说明：
Create a multidimensional
matrix where all the elements are zero.
语法：
zeros ( n 1 ,n 2 ,…)
注意：
A MathCli external library
function (See External Library Guide ) where n i
defines sizes of dimensions in order. The function creates a new n * n
square matrix when there is only one parameter n .
参数：
|
n i
|
A positive integer .
返回值：
Sequence
示例：
|
=zeros(4,3)
|
（原文含示意图，图像资源未迁移）
Generate
a 4*3 matrix whose elements are all zero .
|
=zeros(3)
|
（原文含示意图，图像资源未迁移）
Generate
a matrix of size 3 whose elements are all zero .
|
=zeros(4,3,5)
|
（原文含示意图，图像资源未迁移）
Generate
a multidimensional matrix whose elements are all zero .
