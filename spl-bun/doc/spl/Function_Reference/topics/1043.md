# zip_encrypt()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
zip_encrypt()
zip_encrypt( zip, mode )
=zip_open("D:/e1.zip","123")
=zip_encrypt(A1,0)
=zip_encrypt(A1,1)
>zip_close(A1)
```

## 提取内容
zip_encrypt()
说明：
Set encryption mode for a zip file.
语法：
zip_encrypt( zip, mode )
注意：
The
ZipCli external library function (See External Library Guide ) sets an
encryption mode for a zip file with a password. Values of parameter mode is a closed interval [0,4].
参数：
|
zip
|
zip file object .
|
mode
|
0:
Won’t encrypt
1:
Standard encryption
2:
AES encryption with key length 128
3:
AES encryption with key length 192
4:
AES encryption with key length 256
Other
values: Won’t encrypt .
Return
value:
Boolean
示例：
|
=zip_open("D:/e1.zip","123")
|
Open a zip file .
|
=zip_encrypt(A1,0)
|
Parameter mode is 0, so the function won’t encrypt the zip file .
|
=zip_encrypt(A1,1)
|
Parameter mode is 1; it is a standard encryption .
|
>zip_close(A1)
|
Close a zip file .
