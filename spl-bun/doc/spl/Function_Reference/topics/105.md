# T.attach(T’,C…)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.attach(T’,C…)
=file("D:/ctb.ctx")
=A1.create(#k1,v1)
=A2.attach(table2,#k2,v2)
=create(k1,v1).record([1,10,2,20,3,30,4,40,10,100])
=create(k1,k2,v2).record([1,1,"a",3,1,"c"])
=A2.append(A4.cursor())
=A3.append(A5.cursor())
>A2.close()
```

## 提取内容
T.attach(T’,C…)
说明：
Add
an attached table to the base table of a composite table.
Synatx:
T. attach ( T’,C… )
注意：
The
function adds attached table T’ to a
composite table’s base table T . A composite table can have one or multiple
attached tables. The attached table’s dimensions include dimensions of its base
table; its column names should be different from the base table’s column names.
参数：
|
T
|
base table.
|
T’
|
An
attached table.
|
Attached
table column, can be omitted; return an attached table named T’ if it is absent; a C preceded by # is a dimension.
Return
value:
Attached
table
示例：
Add an attached table to
the base table:
|
=file("D:/ctb.ctx")
|
=A1.create(#k1,v1)
|
Create
the composite table’s base table, where k1 is the dimension and v1 is the
base table’s column .
|
=A2.attach(table2,#k2,v2)
|
Add
an attached table named table2 to the base table returned by A2; the
attached table’s dimension fields are k1 and k2 and its column is v2 .
|
=create(k1,v1).record([1,10,2,20,3,30,4,40,10,100])
|
Create
a table sequence .
|
=create(k1,k2,v2).record([1,1,"a",3,1,"c"])
|
Create
a table sequence .
|
=A2.append(A4.cursor())
|
Append
cursor’s records to A2’s base table .
|
=A3.append(A5.cursor())
|
Append
cursor’s records to A3’s attached table .
|
>A2.close()
|
Close
the composite table .
