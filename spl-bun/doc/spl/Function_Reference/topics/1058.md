# Difference between sequences

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=[1,2,3,4]\[2,3]
=[1,2,3,3]\[1,3]
=[1,2,3]\3
```

## 提取内容
Difference between sequences
说明：
Generate
a new sequence by subtracting members of another sequence from a sequence.
语法：
A \ B
A \ x
注意：
The
operation generates a new sequence by subtracting the members (or single
values) of B from sequence A in order.
参数：
|
A sequence whose length is n .
|
A sequence
whose length is m or a single value which can be regarded as [ B ] .
返回值：
new sequence by subtracting the members (or single values) of B from sequence A in order.
示例：
|
=[1,2,3,4]\[2,3]
|
[1,4]
|
=[1,2,3,3]\[1,3]
|
[2,3] , the duplicate members will not be subtracted repeatedly .
|
=[1,2,3]\3
|
[1,2]
相关函数：
Concat e na t ion
o f s e q u e n ces
Inters e ction o f s e que n ces
Union of Se que n ces
Multiplication of se q u ences
Alignment
arithmetic operations
cm p ( )
