# Multiplication of sequences

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=[1,2,3]*3
=3*[1,2,3]
=[1,2,3]*0
cmp()
```

## 提取内容
Multiplication of sequences
说明：
Generate
a new sequence by d uplicating
members of a sequence.
语法：
A * k
k * A
注意：
The function generates a
new sequence by multiplying the given sequence by an integer k , that is - d uplicating members of the sequence for k times.
If k <=0 , then return an empty sequence.
参数：
|
sequence.
|
k
|
positive integer.
返回值：
new sequence generated by duplicating the members of a given sequence for multiple times.
示例：
|
=[1,2,3]*3
|
[1,2,3, 1,2,3,
1,2,3] .
|
=3*[1,2,3]
|
[1,2,3, 1,2,3, 1,2,3] .
|
=[1,2,3]*0
|
[] .
相关函数：
Difference between
sequences
Union of sequences
Concatenation of
sequences
Alignment
arithmetic operations
cmp()
