# 记录 constant

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=arg1
```

## 提取内容
记录 constant
说明：
JSON-style record style.
语法：
{ Fi:…,…}
注意：
The
record constant is in JSON style. It can be used in both a constant cell or a
parameter. The expression will be computed when it is located in a larger
computing expression, and it will be identified as a string when used as a
constant. In the latter case, only the basic simple data types will be
identified.
参数：
|
Fi
|
Column
name in a record.
Return
value:
record
示例：
Define
a cellset parameter:
（原文含示意图，图像资源未迁移）
|
=arg1
|
Return result as follows:
（原文含示意图，图像资源未迁移）
|
{name:Jone,score:435}
|
Return result as follows:
（原文含示意图，图像资源未迁移）
