# Identifier

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
>arg1=5
>'a b'=4
>'a=b'=3
=arg1
='a b'
='a=b'
=file("emp.xls").xlsimport@t()
=A1.('NA
```

## 提取内容
Identifier
说明：
Identifier
is commonly referred to as the variable name. We can define an identifier, or a
variable name.
语法：
string1
' string2 '
注意：
Normally the variable name
can be used without having to be specifically defined. But if the variable name
contains the space, equal sign, and other misleading characters, it needs to be
defined specifically. There’s the same principle when handling spaces in
commands line.
参数：
|
string1
|
Ordinary
identifier.
|
'string2'
|
If
the string contains any misleading characters such as = ,
space, + , and - , it must be
enclosed in single quotation marks.
返回值：
variable value specified by the variable name - string1 or ' string2 '
示例：
|
>arg1=5
|
An
ordinary identifier.
|
>'a b'=4
|
Identifier
that contains a space.
|
>'a=b'=3
|
Identifier
that contains equal signs.
|
=arg1
|
='a b'
|
='a=b'
|
=file("emp.xls").xlsimport@t()
|
Import an Excel
file whose second column name is NA ME, which contains a space .
（原文含示意图，图像资源未迁移）
|
=A1.('NA
ME')
|
An identifier containing a space and quoted
by single quotation marks .
（原文含示意图，图像资源未迁移）
