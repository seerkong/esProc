# base64()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
base64()
base64 ( x : cs , cs' )
=base64("i\xb7\x1d\xfb\xef\xff")
=base64(A1)
=base64("6ZuG566X5Zmo","utf-8")
=base64("6ZuG566X5Zmo":"GBK")
```

## 提取内容
base64()
说明：
Base64-encode
the given data.
语法：
base64 ( x : cs , cs' )
注意：
The
function retrieves data from a BLOB using charset cs and encodes it into a base64 string, or it encodes a base64
string into a string using charset cs’ .
By default, perform the former operation.
参数：
|
x
|
BLOB
data or an ordinary string.
|
cs
|
Charset.
Return
value:
BLOB
data/Base64 string
示例：
|
=base64("i\xb7\x1d\xfb\xef\xff")
|
Encode
the given string as BLOB data .
|
=base64(A1)
|
Encode
the BLOB data as a base64 string .
（原文含示意图，图像资源未迁移）
|
=base64("6ZuG566X5Zmo","utf-8")
|
（原文含示意图，图像资源未迁移）
|
=base64("6ZuG566X5Zmo":"GBK")
|
（原文含示意图，图像资源未迁移）
