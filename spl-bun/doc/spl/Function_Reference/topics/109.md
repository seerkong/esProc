# between()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
between()
between( x , a:b )
=between(4,5:6)
=between(4,3:6)
=between(4,4:6)
=between@l(5,5:6)
=between@r(6,5:6)
=between@b(5,6:9)
=between([1,2,3],[1,2]:[5,6])
=between("1990-06-08","1989-01-01":"1992-12-31")
if ( )
```

## 提取内容
between()
说明：
Check
if a parameter value is within a specified interval.
语法：
|
between( x , a:b )
|
Find
if x falls between a and b . When no option is present the interval enclosed by a and b is closed
注意：
The
function checks whether x falls
between a and b . Both two endpoints are included when options are absent.
参数：
|
x/a/b
|
An
expression, which can be numeric, string, date or sequence.
选项：
|
@l
|
The
interval is left-open and right-closed.
|
@r
|
The
interval is right-open and left-closed.
|
@ b
|
Make
the function returns -1 if x < a ; returns 1 if b < x ; and return 0
for the rest of the cases.
Return
value:
Boolean
示例：
|
=between(4,5:6)
|
false
|
=between(4,3:6)
|
true
|
=between(4,4:6)
|
true
|
=between@l(5,5:6)
|
false
|
=between@r(6,5:6)
|
false
|
=between@b(5,6:9)
|
-1
|
=between([1,2,3],[1,2]:[5,6])
|
true
|
=between("1990-06-08","1989-01-01":"1992-12-31")
|
true
functions:
if ( )
ca s e ()
st r i n g()
