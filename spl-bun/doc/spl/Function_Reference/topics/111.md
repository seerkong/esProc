# A.bi()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.bi()
A.bi()
A.bi@r(Rec)
=T("D://house_prices_train.csv")
=A1.(MSZoning)
=A2.bi()
=A2.bi@r(A3(2))
```

## 提取内容
A.bi()
说明：
Split a low-frequency
categorical enumerated sequence variable that contains a number of categories
not greater than 6 into multiple binary variables during modeling.
语法：
|
A.bi()
|
During modeling, split low-frequency categorical
enumerated variable A that contains a number of categories not greater
than 6 into multiple binary variables, and return a binary sequence
consisting of a table sequence of splitting result and a sequence of
splitting process records Rec .
|
A.bi@r(Rec)
|
During scoring, split low-frequency categorical
enumerated variable A that contains a number of categories not greater
than 6 into multiple binary variables according to the sequence of splitting
process records Rec , and return result as a table sequence .
注意：
The MathCli external
library function (See External Library Guide ) splits
a low-frequency categorical enumerated sequence variable that contains a number
of categories not greater than 6 into multiple binary variables during
modeling.
参数：
|
A sequence, which is a low-frequency categorical
enumerated variable that contains a number of categories no greater than 6 .
|
Rec
|
A sequence of splitting process records .
返回值：
Sequence/Table sequence
示例：
|
=T("D://house_prices_train.csv")
|
=A1.(MSZoning)
|
A variable containing a number of categories no greater than 6 .
|
=A2.bi()
|
（原文含示意图，图像资源未迁移）
A3(1) A table sequence of splitting result
A3(2) A sequence of splitting process records Rec .
|
=A2.bi@r(A3(2))
|
（原文含示意图，图像资源未迁移）
Split A2’s variable into multiple variables according to A3’s sequence
of splitting process records Rec .
|
