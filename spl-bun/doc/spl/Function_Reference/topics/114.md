# bits()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
bits()
bits( x i,… )
=bits(1,0,1,1)
=bits([1,0,1,1])
=bits("1","0","1","1")
=bits("1011")
=bits@h("A",1,1,5)
=bits@d("1","0","1","5")
=bits@b(true,false,true)
=bits@r(0XBB0D8196)
=67546523567
=bits@n(67546523567)
=bits@sd(1212)
=bits@sh(1212)
```

## 提取内容
bits()
说明：
Convert
a number represented by a certain number system to a decimal number.
语法：
bits( x i,… )
注意：
The
function converts a number of a certain number system to a decimal integer
according to a specific rule. Parameter x i represents the value in the i th
position of the number counted from left to right. When no option is present
the function converts a binary number to a decimal number.
When
there is only one parameter x i and
x i is a sequence, write
the parameter in the form of x 1
,
x 2 ,… x i .
If
there is only a single x i
and it is a string, split it into a sequence of single characters first, and
then perform the conversion.
number
system
参数：
|
x i
|
An
integer/string.
选项：
|
@h
|
Convert
x i to integer decimal
number according to the rules of hexadecimal number system if it is the
string.
|
@d
|
First
convert x i to an integer
if it is the string and then perform the operation according to rules of
decimal number system.
|
@b
|
Convert
to 0 if parameter x i is false
and to 1 if the parameter is true, and then convert them to a decimal number
according to the rules of binary system.
|
@s
|
Should
work with other options to return the string forming the number of the corresponding
number system.
|
@n
|
Convert
to a long integer.
|
@r
|
Put
lower bits before higher bits.
Return
value:
数值型
value/string
示例：
Convert from binary to decimal:
|
=bits(1,0,1,1)
|
11.
As no options work, covert binary number 1011 to decimal.
|
=bits([1,0,1,1])
|
11.
As the one parameter is a sequence, take members of the sequence as parameter
values; in this case the operation is equivalent to bits(1,0,1,1).
|
=bits("1","0","1","1")
|
11.
As the parameters are strings, first convert them to an integer and to
decimal according to rules of binary system.
|
=bits("1011")
|
11.
As the one parameter is a string, split it into a sequence of single
characters; in this case the operation is equivalent to bits([1,0,1,1]).
Convert hexadecimal to
decimal:
|
=bits@h("A",1,1,5)
|
@h
option works to convert hexadecimal number A115 to decimal and the result is 41237.
With @d option:
|
=bits@d("1","0","1","5")
|
1015.
As parameters are strings, first convert them to an integer and then to
decimal.
With @b option:
|
=bits@b(true,false,true)
|
5.
The operation convert binary number 101 to decimal.
Put lower bits before
higher bits:
|
=bits@r(0XBB0D8196)
|
Convert to long integer:
|
=67546523567
|
=bits@n(67546523567)
|
Return
a long type:
（原文含示意图，图像资源未迁移）
With @s option:
|
=bits@sd(1212)
|
1212.
Get the decimal string forming 1212.
|
=bits@sh(1212)
|
4bc.
Get the hexadecimal string forming 1212.
