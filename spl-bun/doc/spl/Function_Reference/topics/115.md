# A.bits()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.bits()
A.bits()
=192.(rand(2))
=A1.bits()
=[1,0,1,1].bits()
=[true,false,true,true].bits@b()
```

## 提取内容
A.bits()
说明：
Convert
a sequence of bit values to a sequence of long numeric values.
语法：
A.bits()
注意：
The
function converts a sequence of binary bits A to a sequence of long
numeric values, during which every 64 members is transformed to one long
numeric member.
参数：
|
sequence of binary bits.
选项：
|
@b
|
When
A consists of Boolean members, convert to 1 for true and to 0 for
false.
Return
value:
sequence of long numeric values
示例：
|
=192.(rand(2))
|
Randomly
generate a sequence of binary bits .
|
=A1.bits()
|
Convert A1 to a sequence
of long numeric values. For instance:
（原文含示意图，图像资源未迁移）
|
=[1,0,1,1].bits()
|
Return
[-5764607523034234880] .
|
=[true,false,true,true].bits@b()
|
Return
[-5764607523034234880] .
