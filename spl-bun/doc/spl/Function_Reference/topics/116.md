# B.bits(n)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
B.bits(n)
B.bits( n )
=192.(rand(2))
=A1.bits()
=A2.bits(61)
=A2.bits@b(61)
=192.(A2.bits(~))
```

## 提取内容
B.bits(n)
说明：
The
inverse operation of A.bits() function; get value of the n th bit.
语法：
B.bits( n )
注意：
The
function gets value of the nth bit; it is the inverse operation of A.bits()
function.
参数：
|
The
value returned by A.bits().
|
n
|
An
integer representing the n th bit.
选项：
|
@b
|
Judge
whether value of the n th bit in sequence B is 1; the function returns true when the bit value is 1; and
returns false when it is 0.
Return
value:
0/1/Boolean
示例：
|
=192.(rand(2))
|
Randomly
generate a sequence of binary bits .
|
=A1.bits()
|
Convert A1 to a sequence
of long numeric values. For instance:
（原文含示意图，图像资源未迁移）
|
=A2.bits(61)
|
Return 1 .
|
=A2.bits@b(61)
|
Judge
whether the 61 th bit member of A2’s sequence is 1 and return true
|
=192.(A2.bits(~))
|
Return
same result as A1 does .
