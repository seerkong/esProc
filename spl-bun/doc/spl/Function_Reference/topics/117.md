# bit1(x)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
bit1(x)
bit1( x )
=bit1(5)
=bit1(A2)
```

## 提取内容
bit1(x)
说明：
Get the
number of 1s in a complement in binary.
语法：
bit1( x )
注意：
The function gets the number of 1s in complement of x
in binary.
When x is a sequence, find the total number of
1’s in all members’ complements of all
members in binary.
参数：
|
x
|
A long integer or a sequence of long integers .
Return
value:
Integer
示例：
|
=bit1(5)
|
The value of converting 5 to binary is 101 and the
result is 2 .
|
[1,3,5,6]
|
=bit1(A2)
|
Return 7 .
