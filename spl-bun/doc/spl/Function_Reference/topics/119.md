# blob()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
blob()
blob ( b )
=blob([4,7,3])
=blob(A1)
=blob@4([4,7,3])
=blob@r4(A3)
```

## 提取内容
blob()
说明：
Convert
a blob object into an integer sequence, or vice versa.
语法：
blob ( b )
注意：
The function converts the
blob object into a data column or an integer sequence into a blob object. Each
byte of blob object corresponds to a member of the would-be integer sequence.
参数：
|
b
|
blob object or an integer sequence.
选项：
|
@4
|
Every
4 bytes correspond to one int.
|
@8
|
Every
8 bytes correspond to one int.
|
@f
|
Every
4 bytes correspond to one float.
|
@d
|
Every
8 bytes correspond to one double.
|
@r
|
All
the above options use little-endian; by default, use big-endian.
Return
value:
A blob
object or an integer sequence
示例：
|
=blob([4,7,3])
|
Return
a blob object:
（原文含示意图，图像资源未迁移）
|
=blob(A1)
|
Return
an integer sequence:
（原文含示意图，图像资源未迁移）
|
=blob@4([4,7,3])
|
=blob@r4(A3)
|
Return
an integer sequence; @r4 options enable using the little-endian and every 4
bytes correspond to one int .
（原文含示意图，图像资源未迁移）
