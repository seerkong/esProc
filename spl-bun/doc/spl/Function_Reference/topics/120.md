# bool()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
bool()
bool ( expression )
=bool(123)
=bool(A1)
=bool("false")
=bool(1==2)
=bool(null)
```

## 提取内容
bool()
说明：
Convert
a data object to a Boolean value.
语法：
bool ( expression )
注意：
The function converts the
data type of a given expression expression
to a Boolean type. It returns false when the parameter value is null, “false”
(case-sensitive), and false; otherwise, it returns true.
参数：
|
expression
|
constant or an expression.
Return
value:
Boolean
示例：
|
=bool(123)
|
true
|
=bool(A1)
|
true
|
=bool("false")
|
false
|
=bool(1==2)
|
false
|
=bool(null)
|
false
