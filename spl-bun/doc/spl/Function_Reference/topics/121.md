# break {a}

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=[]
>A1=A1|[A2]
=create(ID,Dept)
=demo.query("select
>A6.insert(0,A8.EID:ID,A8.DEPT:Dept)
```

## 提取内容
break {a}
说明：
Break
the current loop.
语法：
break { a }
注意：
The
function breaks a loop whose master cell is a . If a is absent, break
the current loop.
参数：
|
a
|
The
master cell of a loop block. If a is omitted, the
function exits the current loop.
示例：
|
D
|
=[]
|
for 5
|
The
result of A1 after computation is a sequence [1, 1, 2, 2, 3, 3] ; break A2 is to control the loop of the A2 level.
|
for 2
|
if A2==4
|
break A2
|
>A1=A1|[A2]
|
=create(ID,Dept)
|
（原文含示意图，图像资源未迁移）
|
=demo.query("select
* from EMPLOYEE")
|
for A7
|
if A8.NAME=="Alexis"
|
Extract
the data of DEPT from the EMPLOYEE table and insert them to a table sequence uninterruptedly until
the record whose NAME value is " Alexis " appears .
|
break
|
>A6.insert(0,A8.EID:ID,A8.DEPT:Dept)
|
A6 returns final result
as follows:
（原文含示意图，图像资源未迁移）
