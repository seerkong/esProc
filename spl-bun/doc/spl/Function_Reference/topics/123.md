# C

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=create(k1,v1).record([1,10,2,20,3,30,4,40,10,100]) .sort(k1)
=create(k1,k2,v2).record([1,1,"a",2,1,"c"])
=file("D:/ctb.ctx")
=A3.create(#k1,v1)
=A4.attach(table2,#k2,v2)
=A4.append(A1.cursor())
=A5.append(A2.cursor())
=A4.cursor(k1,v1,table2.v2).fetch()
=A5.cursor(k1,v1,v2).fetch()
```

## 提取内容
说明：
column in a table, its parent table, or a table above them.
注意：
It
is a column in a table, its parent table, or a higher level parent table; can
be referenced directly by name without being prefixed by the table name. For
example, if table A1 is table B1 ’s parent table, then a column of
table A1 can be directly referenced
by name from table B1 , instead of
using a format like A1.C .
Note : If one of table B1 ’s
columns is referenced from table A1 ,
the format should be B1.C .
示例：
|
=create(k1,v1).record([1,10,2,20,3,30,4,40,10,100]) .sort(k1)
|
Generate a table
sequence:
（原文含示意图，图像资源未迁移）
|
=create(k1,k2,v2).record([1,1,"a",2,1,"c"])
|
Generate a table
sequence:
（原文含示意图，图像资源未迁移）
|
=file("D:/ctb.ctx")
|
=A3.create(#k1,v1)
|
Create
the composite table’s primary table
|
=A4.attach(table2,#k2,v2)
|
Add
subtable table2 to the primary
table .
|
=A4.append(A1.cursor())
|
=A5.append(A2.cursor())
|
=A4.cursor(k1,v1,table2.v2).fetch()
|
Reference
column v2 of the subtable table2 from its parent table .
（原文含示意图，图像资源未迁移）
|
=A5.cursor(k1,v1,v2).fetch()
|
Reference
columns k1 and v1 of the parent table from the subtable .
（原文含示意图，图像资源未迁移）
