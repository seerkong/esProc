# T.f(C)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.f(C)
=create(k1,v1).record([1,10,2,20,3,30,4,40,10,100])
=create(k1,k2,v2).record([1,1,"a",2,4,"c",2,8,"b"])
=file("D:/ckv.ctx")
=A3.create(#k1,v1)
=A4.attach(table2,#k2,v2)
=A4.append(A1.cursor())
=A5.append(A2.cursor())
=A4.cursor(k1,table2.sum(k2):k2_sum).fetch()
=A4.cursor(table2.count(k2)).fetch()
=A4.cursor(table2.max(k2)).fetch()
=A4.cursor(table2.min(k2)).fetch()
=A4.cursor(table2.avg(k2)).fetch()
=A5.cursor().top(2;k2)
=A5.cursor().derive(iterate(~~*2,10):F1).fetch()
```

## 提取内容
T.f(C)
说明：
Perform
an aggregate operation over an attached table’s column.
语法：
T .f( C )
注意：
The
function performs an aggregation operation over a specified column of an
attached table.
参数：
|
T
|
An
attached table.
|
f
|
An
aggregate operation performed over the attached table; support count , sum , max , min , avg , top and iterate .
|
An
attached table’s column.
示例：
|
=create(k1,v1).record([1,10,2,20,3,30,4,40,10,100])
|
Create a table
sequence:
（原文含示意图，图像资源未迁移）
|
=create(k1,k2,v2).record([1,1,"a",2,4,"c",2,8,"b"])
|
Create a table
sequence:
（原文含示意图，图像资源未迁移）
|
=file("D:/ckv.ctx")
|
=A3.create(#k1,v1)
|
Create
the composite table’s base table .
|
=A4.attach(table2,#k2,v2)
|
Add
an attached table to the base table .
|
=A4.append(A1.cursor())
|
Append
data to the base table .
|
=A5.append(A2.cursor())
|
Append
data to the attached table .
|
=A4.cursor(k1,table2.sum(k2):k2_sum).fetch()
|
Perform
a sum operation over each value of column k2 in attached table table2 , and name the new column k2_sum .
（原文含示意图，图像资源未迁移）
|
=A4.cursor(table2.count(k2)).fetch()
|
Perform
count operation over each value of column k2 in attached table table2 .
（原文含示意图，图像资源未迁移）
|
=A4.cursor(table2.max(k2)).fetch()
|
Perform
max operation over each value of column k2 in attached table table2 .
（原文含示意图，图像资源未迁移）
|
=A4.cursor(table2.min(k2)).fetch()
|
Perform
min operation over each value of column k2 in attached table table2 .
（原文含示意图，图像资源未迁移）
|
=A4.cursor(table2.avg(k2)).fetch()
|
Perform
average operation over each value of column k2 in attached table table2 .
（原文含示意图，图像资源未迁移）
|
=A5.cursor().top(2;k2)
|
Return
the records corresponding to the first two smallest values in column k2 in
entity table table2 .
（原文含示意图，图像资源未迁移）
|
=A5.cursor().derive(iterate(~~*2,10):F1).fetch()
|
Perform
an iteration over entity table table2 .
（原文含示意图，图像资源未迁移）
