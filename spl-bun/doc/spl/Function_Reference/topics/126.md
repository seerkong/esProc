# T{x:C,…}

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=create(k1,v1).record([1,10,2,20,3,30,4,40,10,100])
=create(k1,k2,v2).record([1,1,"a",2,4,"c",2,8,"b"])
=file("D:/ckv.ctx")
=A3.create(#k1,v1)
=A4.attach(table2,#k2,v2)
=A4.append(A1.cursor())
=A5.append(A2.cursor())
=A4.cursor(k1,table2{k2,v2}:t2).fetch()
```

## 提取内容
T{x:C,…}
说明：
Return
an attached table’s records as a sub table sequence.
语法：
T{x:C,…}
说明：
The
function returns an attached table’s records that match the current primary key
value as a sub table sequence.
参数：
|
T
|
An
attached table.
|
x
|
An
attached table’s column.
|
Column
alias; can be omitted.
示例：
|
=create(k1,v1).record([1,10,2,20,3,30,4,40,10,100])
|
Create a table
sequence:
（原文含示意图，图像资源未迁移）
|
=create(k1,k2,v2).record([1,1,"a",2,4,"c",2,8,"b"])
|
Create a table
sequence:
（原文含示意图，图像资源未迁移）
|
=file("D:/ckv.ctx")
|
=A3.create(#k1,v1)
|
Create
the composite table’s base table .
|
=A4.attach(table2,#k2,v2)
|
Add
an attached table to the base table .
|
=A4.append(A1.cursor())
|
Append
data to the base table .
|
=A5.append(A2.cursor())
|
Append
data to the attached table .
|
=A4.cursor(k1,table2{k2,v2}:t2).fetch()
|
Return records of
attached table table2 as a table sequence .
（原文含示意图，图像资源未迁移）
（原文含示意图，图像资源未迁移）
（原文含示意图，图像资源未迁移）
