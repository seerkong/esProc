# cand()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cand()
cand ( x 1 ,…, x n )
=[2<10,3>4,1!=1]
=A1.cand()
=[1==1,1<2].cand()
=[20,1<2].cand()
=[null,1<2].cand()
=cand(null,1<2)
=demo.query("select
=A1.cand(DEPT)
=A1.cand(GENDER=="F")
```

## 提取内容
cand()
说明：
Perform the logical AND operation over
members of a sequence.
语法：
|
A .cand (x)
|
cand ( x 1 ,…, x n )
|
Equivalent to A . cand () , where x 1 ,…, x n are members of sequence A .
注意：
The function performs a logical AND
operation and returns true when expression x is true with all members of sequence
A ; otherwise, it returns false.
Operators in a logical operation is Boolean
type; will automatically convert to Boolean type if they are not.
参数：
|
A sequence .
|
x
|
An
expression; cannot be omitted when sequence A is a table sequence or a record
sequence.
返回值：
Boolean
示例：
When
A is a
sequence:
|
=[2<10,3>4,1!=1]
|
Return
members true, false, false.
|
=A1.cand()
|
false.
|
=[1==1,1<2].cand()
|
true.
|
=[20,1<2].cand()
|
true
automatically convert to Boolean when an operator isn’t Boolean type.
|
=[null,1<2].cand()
|
false.
|
=cand(null,1<2)
|
False,
which is same as A5.
When
A is a table
sequence or a record sequence:
|
=demo.query("select
top 10 EID,NAME,GENDER,DEPT from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.cand(DEPT)
|
Judge
whether DEPT values in table sequence A1 are true and return true.
|
=A1.cand(GENDER=="F")
|
Judge
whether DEPT values in table sequence A1 are “F” and return false.
