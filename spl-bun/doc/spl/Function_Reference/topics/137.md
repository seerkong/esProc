# case()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
case()
case( x , x 1 : y 1 ,…, x k : y k ; y )
=3
=case(A1,1:"Dept
=4
=case(A3,1:"Dept
=case(A3,1:"Dept
=case(A3,1:"Dept
if( )
```

## 提取内容
case()
说明：
Return
the corresponding value of the expression whose value is equal to the result of
a specific expression.
语法：
case( x , x 1 : y 1 ,…, x k : y k ; y )
注意：
This
function computes from left to right, beginning from the conditional expression x . Then match the result of expression x with each expression after it in turn. If there is any expression x k that is equal to the result of x , return the result of y k
and stop computation.
If
there is no eligible expression x k but there exists a default
expression y , then return the result of y ; otherwise, return
null.
参数：
|
x
|
Conditional
expression.
|
x k
|
An
expression, which is the object matched with conditional expression x .
|
y k
|
Result
expression returned when conditional expression x matches with expression x k .
|
y
|
Default
expression returned when conditional expression x does not match any of expressions x 1 , ... x k .
示例：
|
=3
|
=case(A1,1:"Dept
1",2:"Dept 2",3:"Dept 3";"Admin Dept")
|
Return Dept 3 .
|
=4
|
=case(A3,1:"Dept
1",2:"Dept 2",3:"Dept 3",4:;"Admin Dept")
|
Return null .
|
=case(A3,1:"Dept
1",2:"Dept 2",3:"Dept 3";"Admin Dept")
|
Return Admin Dept .
|
=case(A3,1:"Dept
1",2:"Dept 2",3:"Dept 3";)
|
Return null .
相关函数：
if( )
bet w een()
