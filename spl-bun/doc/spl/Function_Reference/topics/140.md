# ceil()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ceil()
ceil( numberExp , { nExp } )
>0 : Move the
=0 : Indicate the
ceil(3450001.004,0)
ceil(3450001.004,-1)
ceil(3450001.004,-2)
ceil(3450001.004,1)
ceil(3450001.004,2)
ceil@s(-3450001.004,2)
```

## 提取内容
ceil()
说明：
Truncate
data at the specified position and carry the remaining part if any.
语法：
ceil( numberExp , { nExp } )
注意：
The
function truncates data numberExp at the
specified position nExp , and carry the
remaining part (if any).
参数：
|
numberExp
|
Data
to be truncated.
|
nExp
|
An
integer specifying the truncate position; taken as 0 when it is omitted:
>0 : Move the
decimal point to the right for nExp places
<0 : Move the
decimal point to the left for nExp places
=0 : Indicate the
current decimal place
选项：
|
@s
|
This option is a must when parameter numberExp
is a negative number.
返回值：
数值型
type
示例：
|
ceil(3450001.004,0)
|
3450002.0
|
ceil(3450001.004,-1)
|
3450010.0
|
ceil(3450001.004,-2)
|
3450100.0
|
ceil(3450001.004,1)
|
3450001.1
|
ceil(3450001.004,2)
|
3450001.01
|
ceil@s(-3450001.004,2)
|
-3450001.01
functions:
f l o o r()
ro u n d ()
