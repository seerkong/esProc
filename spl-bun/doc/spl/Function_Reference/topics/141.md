# cellname()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cellname()
cellname( r,c )
cellname(1,1)
cellname(4,3)
cellname(124,198)
```

## 提取内容
cellname()
说明：
Return an Excel cell name according to
specific row and column numbers.
语法：
cellname( r,c )
注意：
The
function finds the name of an Excel cell through row parameter r and column parameter c .
参数：
|
r
|
The
r th row in an Excel
sheet, which is an integer greater than 0.
|
c
|
The
c th column in an Excel
sheet, which is an integer greater than 0.
Return
value:
字符串
示例：
|
cellname(1,1)
|
Result:
A1.
|
cellname(4,3)
|
Result:
C4.
|
cellname(124,198)
|
Result:
GP124.
