# ch.()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.()
=demo.cursor("select EID,NAME,BIRTHDAY,SALARY from EMPLOYEE where
=channel()
=channel()
=channel()
=A2.(age(BIRTHDAY))
=A2.fetch()
=B2.(SALARY)
=B2 .(~*1.2)
=B2.fetch()
=C2.()
=C2.fetch()
=A1.push(A2,B2,C2)
=A1.fetch()
=A2.result()
=B2.result()
=C2.result()
```

## 提取内容
ch.()
说明：
Attach a computation to a channel and return
the original channel.
语法：
|
ch . ( x )
|
ch .()
|
Return
the channel ch itself.
注意：
The
function attaches a computation to channel ch ,
which will compute expression x over
each of its members, and returns the original channel.
This
is an attachment function.
参数：
|
ch
|
channel.
|
x
|
An
expression, which is a field name generally or a legal expression consisting
of field names; use ~ to reference the current record.
Return
value:
管道
示例：
|
=demo.cursor("select EID,NAME,BIRTHDAY,SALARY from EMPLOYEE where
EID<10")
|
=channel()
|
=channel()
|
=channel()
|
Create
channels A2, B2 and C2.
|
=A2.(age(BIRTHDAY))
|
Attach
a computation to channel A2, which will compute age(BIRTHDAY) on BIRTHDAY’s
members, and return result to the channel.
|
=A2.fetch()
|
Execute
result fetching function in channel A2 and keep the current data in channel.
|
=B2.(SALARY)
|
Attach
a computation to channel B2, which will compute SALARY column, and return the
original channel B2.
|
=B2 .(~*1.2)
|
Attach
a computation to channel B2, which will use ~ to reference the current member
of the sequence to compute SALARY* 1.2, and return the original channel B2 .
|
=B2.fetch()
|
Execute
result fetching function in channel B2 and keep the current data in channel.
|
=C2.()
|
Return
channel C2.
|
=C2.fetch()
|
Execute
result fetching function in channel C2 and keep the current data in channel.
|
=A1.push(A2,B2,C2)
|
Be
ready to push data in A1’s cursor into channel A2, B2 and C2, but the action
needs to wait.
|
=A1.fetch()
|
Fetch
data from cursor A1 while pushing data into the channel to execute the
attached computation and keep the result.
（原文含示意图，图像资源未迁移）
|
=A2.result()
|
Get channel A2’s
result:
（原文含示意图，图像资源未迁移）
|
=B2.result()
|
Get channel B2’s
result:
（原文含示意图，图像资源未迁移）
|
=C2.result()
|
（原文含示意图，图像资源未迁移）
Get channel C2’s
result:
