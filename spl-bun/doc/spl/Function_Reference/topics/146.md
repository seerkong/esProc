# channel()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
channel()
channel()
=demo.cursor("select
=channel()
>A2.select(STUDENTID==1)
>A2.fetch()
=A1.push(A2)
=A1.fetch()
=A2.result()
```

## 提取内容
channel()
说明：
Create
a channel.
语法：
|
channel()
|
Create
a channel.
注意：
The
function creates a channel.
Return
value:
管道
示例：
|
=demo.cursor("select
* from SCORES where CLASS = 'Class one'")
|
=channel()
|
Create a channel .
|
>A2.select(STUDENTID==1)
|
>A2.fetch()
|
=A1.push(A2)
|
Push data in A1’s
cursor to A2’s channel .
|
=A1.fetch()
|
=A2.result()
|
（原文含示意图，图像资源未迁移）
