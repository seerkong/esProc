# channel(cs)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
channel(cs)
channel ( cs )
=demo.cursor("select
=channel(A1)
>A2.select(STUDENTID==1)
>A2.fetch()
=A1.fetch()
=A2.result()
```

## 提取内容
channel(cs)
说明：
Create
a channel and push data in a specified cursor into it.
语法：
channel ( cs )
注意：
The
function creates a channel, pushes data in cursor cs into it, and returns a channel. This is equivalent to cs.push(channel()) .
参数：
|
cs
|
cursor.
Return
value:
管道
示例：
|
=demo.cursor("select
* from SCORES where CLASS = 'Class one'")
|
=channel(A1)
|
Create a
channel and be ready to push data
in A1’s cursor into the new channel, which is equivalent to =A1.push(channel()) , and return a
channel .
|
>A2.select(STUDENTID==1)
|
Attach a
computation to A2’s channel .
|
>A2.fetch()
|
Keep data in A2’s
channel .
|
=A1.fetch()
|
Push data into
A2’s channel when data is fetched from the cursor .
|
=A2.result()
|
Get data from A2’s
channel .
（原文含示意图，图像资源未迁移）
