# channel(ch)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
channel(ch)
channel( ch )
=demo.cursor("select
=channel()
=channel(A2)
>A2.select(STUDENTID==1)
>A3.select(STUDENTID==2)
=A2.fetch()
=A3.fetch()
=A1.push(A2)
=A1.fetch()
=A2.result()
=A3.result()
```

## 提取内容
channel(ch)
说明：
Create
a channel and push data in another channel into it.
语法：
channel( ch )
注意：
The
function creates a channel, pushes data in channel ch into it, and returns a channel. This is equivalent to ch.push(channel()) .
参数：
|
ch
|
channel.
Return
value:
管道
示例：
|
=demo.cursor("select
* from SCORES where CLASS = 'Class one'")
|
=channel()
|
Create a channel .
|
=channel(A2)
|
Create a
channel and be ready to push data
in A2’s channel into the new channel, which is equivalent to =A2.push(channel()) .
|
>A2.select(STUDENTID==1)
|
Attach a computation to A2’s channel to
select records where STUDENTID is 1 .
|
>A3.select(STUDENTID==2)
|
Attach a computation to A3’s channel to
select records where STUDENTID is 2 .
|
=A2.fetch()
|
Keep the current
data in A2’s channel .
|
=A3.fetch()
|
Keep the current
data in A3’s channel .
|
=A1.push(A2)
|
Ready to push A1’s
cursor into 管道 A2 .
|
=A1.fetch()
|
Perform the push
action defined in A8 when data is fetched from the cursor .
|
=A2.result()
|
Get data from
channel A2 .
（原文含示意图，图像资源未迁移）
|
=A3.result()
|
Get data from
channel A3 .
（原文含示意图，图像资源未迁移）
