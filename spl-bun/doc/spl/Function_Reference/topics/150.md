# chardetect()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
chardetect()
chardetect( fn,cs )
>www="http://www.baidu.com"
=chardetect(www)
=chardetect@v("abc 一二三 123")
>file1="d:/UTF8.xml"
>file2="d:/UTF16LE.xml"
=chardetect(file1)
=file(file2)
=chardetect(A7)
=chardetect@v(" 你好 ")
=chardetect@av(" 你好 ")
=chardetect@v(" 你好 ",["Big5","CP949"])
=chardetect@va(" 你好 ",["Big5","CP949"])
```

## 提取内容
chardetect()
说明：
Get
the character encoding standard used by a string or a text file.
语法：
chardetect( fn,cs )
注意：
The
function identifies the character encoding standard used by text file fn when
no options are used. Standards it supports include UTF-8, GBK, UTF-16LE and
UTF-16BE. Identify the character encoding as GB18030 for text files where the
original encoding standards are GBK, GB2312 or GB18030.
There
could be multiple character set values when trying to identify the character
set used for a specified string or binary code representing a (Traditional)
Chinese character, a Japanese character or a Korean character because there are
overlaps between character sets for the three languages.
The
function returns the first character set value by default. When parameter cs
is present, return the first eligible character set value in the list.
fn is interpreted as a URL if it begins
with "http://" or "https://".
选项：
|
@v
|
Get the character encoding from fn if it is a string or a binary
variable .
|
@a
|
Return the list of all eligible
character encoding standards; return the first eligible one by default .
参数：
|
fn
|
A string or binary value, name of the text file to be identified or
object/URL of the text file to be identified
|
cs
|
The list of available character encoding standards; can be omitted
返回值：
charset value or a sequence of charset values
示例：
|
>www="http://www.baidu.com"
|
=chardetect(www)
|
UTF-8.
|
=chardetect@v("abc 一二三 123")
|
GB-2312.
|
>file1="d:/UTF8.xml"
|
Use
character set UTF-8.
|
>file2="d:/UTF16LE.xml"
|
Use
UTF-16LE character set .
|
=chardetect(file1)
|
UTF-8
parameter fn is file name.
|
=file(file2)
|
=chardetect(A7)
|
UTF-16LE
parameter fn is file object.
|
=chardetect@v(" 你好 ")
|
GB2312.
|
=chardetect@av(" 你好 ")
|
Return a list of all eligible character encoding
standards.
（原文含示意图，图像资源未迁移）
|
=chardetect@v(" 你好 ",["Big5","CP949"])
|
Return the first eligible character set value
in the cs list: Big5.
|
=chardetect@va(" 你好 ",["Big5","CP949"])
|
Return all eligible character sets in the cs
list.
（原文含示意图，图像资源未迁移）
