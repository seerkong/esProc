# chi2inv()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
chi2inv()
chi2inv( p , v )
=chi2inv(0.95,10)
```

## 提取内容
chi2inv()
说明：
Compute the chi-square
inverse cumulative distribution.
语法：
chi2inv( p , v )
注意：
The MathCli external library function (See External Library Guide ) computes the chi-square inverse cumulative
distribution.
参数：
|
P
|
The p-value within the
interval (0,1) .
|
v
|
Degrees of freedom (DF) .
返回值：
Double-precision number
示例：
|
=chi2inv(0.95,10)
|
（原文含示意图，图像资源未迁移）
