# clear()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
clear()
=demo.query("select
=demo.query("select
=[]
>B1=A1.select(EID>6)
=[]
>A3=A1.len()+10
```

## 提取内容
clear()
说明：
Clear
the cell value.
语法：
clear a : b , c , d : e
注意：
The
function clears values and #@ in certain cells. With only a: specified, it clears the cell values in a code block where a is the master cell.
参数：
|
a : b , c , d : e
|
The
cells whose values need to be cleared.
示例：
|
=demo.query("select
* from EMPLOYEE")
|
clear A1
|
Clear
value of cell A1 .
|
=demo.query("select
* from EMPLOYEE")
|
=[]
|
>B1=A1.select(EID>6)
|
=[]
|
if A1.len()>10
|
>A3=A1.len()+10
|
clear A1:B2
|
Clear
values of cells from A1to B2 .
|
clear A4:
|
Clear
cell values in a code block whereA4 is the master cell .
