# clipboard()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
clipboard()
clipboard ( s )
=clipboard("runqian")
=clipboard()
=clipboard@e()
```

## 提取内容
clipboard()
说明：
Set
system clipboard content.
语法：
clipboard ( s )
注意：
The
function sets the content of the system clipboard; return the clipboard content
in string format if parameter s is
absent.
参数：
|
s
|
The
content to be copied to clipboard, whose data type can be table sequence,
string, numeric, date, time, and datetime; can be omitted.
选项：
|
@e
|
If
the clipboard content comes from Excel, record it to return; otherwise the
function will return the content previously copied.
返回值：
Boolean/字符串
示例：
|
=clipboard("runqian")
|
Set
system clipboard content as runqian .
|
=clipboard()
|
Return
the clipboard content since parameter s
is absent .
（原文含示意图，图像资源未迁移）
|
=clipboard@e()
|
Copy
content in an Excel file .
（原文含示意图，图像资源未迁移）
Return
the records after A3’s is executed .
（原文含示意图，图像资源未迁移）
