# cs.close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.close()
=demo.cursor("select * from SCORES")
=A1.fetch(10)
=A1.close()
```

## 提取内容
cs.close()
说明：
Close
a cursor.
语法：
cs . close ()
注意：
The
function clears the file handles and database connections used by cursors and
their root cursors. By default, all of them will be closed automatically when
fetch operation finishes .
参数：
|
cs
|
A cursor .
示例：
|
=demo.cursor("select * from SCORES")
|
Return
a cursor of retrieved data .
|
=A1.fetch(10)
|
Retrieve
some records .
|
=A1.close()
|
Close
the cursor .
function:
cs . fet c h ()
d b . cur s o r ()
