# db.close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
db.close()
=connect("demo")
>A1.close()
```

## 提取内容
db.close()
说明：
Close
a datasource connection.
语法：
db . close ()
注意：
Close
a database connection. By default, data will be committed before closing the
database.
参数：
|
db
|
Database
connection, which supports esproc-JDBC source.
示例：
|
=connect("demo")
|
>A1.close()
|
Close
the connection to database demo .
注意：
connect and close must be used in
pairs; otherwise, the connection cannot be closed.
相关函数：
conn e c t ()
db .err o r ()
db .com m i t()
db .rollba c k ( )
