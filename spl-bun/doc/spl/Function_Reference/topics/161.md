# T.close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.close()
=connect("demo").cursor("select
=file("emp.ctx")
=A2.create(#EID,NAME,GENDER,SALARY)
=A3.append(A1)
=A3.close()
```

## 提取内容
T.close()
说明：
Close
a composite table.
语法：
T . close ()
注意：
The
function closes an entity table and its attached tables in a composite table. The
operation is not a necessity when there isn’t a write operation.
参数：
|
T
|
composite table .
示例：
|
=connect("demo").cursor("select
EID,NAME,GENDER,SALARY from
employee")
|
Return
a single cursor .
|
=file("emp.ctx")
|
Generate
a composite table
|
=A2.create(#EID,NAME,GENDER,SALARY)
|
Create
A2’s base table .
|
=A3.append(A1)
|
Append
data of A1’s cursor to the base table .
|
=A3.close()
|
Close
the composite table .
