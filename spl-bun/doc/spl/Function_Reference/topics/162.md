# T.close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.close()
=file("dept.ctx":[1],["192.168.0.104:8281"])
=A1.open()
=A2.attach(t1)
=A2.memory(DEPTID,DEPTNAME;DEPTID<10)
=A2.close()
```

## 提取内容
T.close()
说明
Close
a cluster table.
语法：
T . close ()
注意：
The
function closes a cluster table. It clears memory when T is a cluster
in-memory table created by T .memory().
Only
supported by esProc Enterprise Edition.
参数：
|
T
|
cluster table .
示例：
|
=file("dept.ctx":[1],["192.168.0.104:8281"])
|
=A1.open()
|
Open
a cluster table .
|
=A2.attach(t1)
|
Return
an existing attached table t1 in A2’ composite table.
|
=A2.memory(DEPTID,DEPTNAME;DEPTID<10)
|
Generate
a cluster in-memory table from table t1 .
|
=A2.close()
|
Close
the cluster table and clear memory .
