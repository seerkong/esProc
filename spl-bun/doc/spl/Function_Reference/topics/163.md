# T.close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.close()
=create(file).record(["STOCKRECORDS.ctx"])
=pseudo(A1)
>A2.close()
```

## 提取内容
T.close()
说明：
Close
a pseudo table.
语法：
T . close ()
注意：
The
function closes a pseudo table.
Only
supported by esProc Enterprise Edition.
参数：
|
T
|
pseudo table .
示例：
|
=create(file).record(["STOCKRECORDS.ctx"])
|
=pseudo(A1)
|
Return
a pseudo table object .
|
>A2.close()
|
Close
the pseudo table .
