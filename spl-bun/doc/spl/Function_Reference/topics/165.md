# cmps()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cmps()
cmps ( x i ,…; y i ,…)
cmps ( x i ,…; y i ,…; z i ,…)
=cmps(2*2;1+3)
=cmps(3,2,1;1,8,9)
=cmps("s";"a")
=cmps([3],[4,5];[3,1],[2,9])
=cmps(abs(-9);abs(3))
=cmps(5;2;9)
=cmps(5,6;5,6;7,8)
=cmps@l(5,6;5,6;7,8)
=cmps(7,8;4,5;7,8)
=cmps@r(7,8;4,5;7,8)
```

## 提取内容
cmps()
说明：
Compare values of parameters.
语法：
|
cmps ( x i ,…; y i ,…)
|
Compare parameter x i
and parameter y i . Return 0 when x i ,… and y i ,… are equal, otherwise compare members of them
from left to right in order; return 1 when x i
is greater than y i ,
and return -1 when x i is
less than y i .
The
syntax is equivalent to cmp ( [ x i ,… ], [ y i ,… ] ) .
|
cmps ( x i ,…; y i ,…; z i ,…)
|
Judge whether x i falls between y i and z i
and make the comparison between members of parameters from left to right.
Return -1 when x i is less
than y i , return 1 when is x i greater than z i , and return 0 when both cases
cannot hold. The syntax is equivalent to between@b ([ x i ,… ], [ y i ,… ]:[ z i ,… ]) .
注意：
The function compares
values of parameters. Cannot make the comparison when parameter types are
different.
参数：
|
x i
|
single value/expression/sequence.
|
y i
|
single value/expression/sequence.
|
z i
|
single value/expression/sequence.
选项：
|
@l
|
Work when parameter z i is present, and
make the comparison by excluding parameter y i .
|
@r
|
Work when parameter z i is present, and
make the comparison by excluding parameter z i .
Return
value:
0/1/-1
示例：
|
=cmps(2*2;1+3)
|
=cmps(3,2,1;1,8,9)
|
=cmps("s";"a")
|
=cmps([3],[4,5];[3,1],[2,9])
|
-1
|
=cmps(abs(-9);abs(3))
|
=cmps(5;2;9)
|
=cmps(5,6;5,6;7,8)
|
=cmps@l(5,6;5,6;7,8)
|
-1
|
=cmps(7,8;4,5;7,8)
|
=cmps@r(7,8;4,5;7,8)
|
