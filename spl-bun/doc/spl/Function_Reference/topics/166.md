# cmpx()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cmpx()
cmpx ( it 1 , it 2 )
=cmpx(date("2025-12-12"),date("2025-12-9"))
=cmpx(datetime("2025-12-2
=cmpx(subx(date("2025-12-12"),date("2025-12-9")),itx(,3))
=cmps(1,2)
=time("11:02:10")>time("13:02:10")
```

## 提取内容
cmpx()
说明：
Perform comparison operation on
parameters.
语法：
|
cmpx ( it 1 , it 2 )
|
Compare
parameter it 1 and parameter it 2 . When it 1 is greater than it 2 , return 1
when it 1
is equal to it 2 , return 0; when it 1 is less
than it 2 ,
return -1.
|
it 1 ? it 2
|
?
is the comparison operator, which can be >, == or < , and return Boolean
value.
注意：
The function performs comparison operation
on parameter it 1 and
parameter it 2 .
参数：
|
it 1 / it 2
|
date/time/datetime/time interval constant/numeric value.
Return
value:
-1/0/1/Boolean
示例：
|
=cmpx(date("2025-12-12"),date("2025-12-9"))
|
Compare
two date parameters. As parameter 1 is greater than parameter 2, return 1.
|
=cmpx(datetime("2025-12-2
20:10:10"),datetime("2025-12-9
21:10:11"))
|
Compare
two datetime parameters and return -1.
|
=cmpx(subx(date("2025-12-12"),date("2025-12-9")),itx(,3))
|
Compare
the difference (the number of days) between 2025-12-12 and 2025-12-9 with 3
days and return 0.
|
=cmps(1,2)
|
-1
|
=time("11:02:10")>time("13:02:10")
|
f alse
