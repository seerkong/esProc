# cor()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cor()
cor ( x 1 ,…, x n )
=[2<10,3>4,1!=1]
=A1.cor()
=[1!=1,3<2].cor()
=[20,1>2].cor()
=[null,1<2].cor()
=cor(20,1>2)
=demo.query("select
=A1.cor(DEPT=="HR")
```

## 提取内容
cor()
说明：
Perform logical OR operation over members of
a sequence.
语法：
|
A . cor ( x )
|
cor ( x 1 ,…, x n )
|
Equivalent to A .cor() , where x 1 ,…, x n are members of sequence A
注意：
The logical OR operation. The function
returns true when at least one member of sequence A is true; otherwise, it returns false.
Operators in a logical operation is Boolean
type; will automatically convert to Boolean type if they are not.
参数：
|
A sequence
|
x
|
An
expression; cannot be omitted when sequence A is a table sequence or a record
sequence
返回值：
Boolean
示例：
When
A is a sequence:
|
=[2<10,3>4,1!=1]
|
Return
members true, false, false .
|
=A1.cor()
|
true .
|
=[1!=1,3<2].cor()
|
false .
|
=[20,1>2].cor()
|
true
automatically convert to Boolean when members of the sequence are non-Boolean
type .
|
=[null,1<2].cor()
|
true .
|
=cor(20,1>2)
|
True,
which is same as A4 .
When
A is a table sequence or a record sequence:
|
=demo.query("select
top 10 EID,NAME,GENDER,DEPT from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.cor(DEPT=="HR")
|
Judge
whether there is a record whose DEPT value is HR in table sequence A1 and
return true .
