# db.commit()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
db.commit()
=create(ID,NAME,GENDER,AGE).record([3,
=connect@e("demo")
>A2.execute@k(A1,"update
=A2.error()
>A2.commit()
>A2.rollback()
>A2.close()
```

## 提取内容
db.commit()
说明：
Commit
the database transaction manually.
语法：
db . commit ()
注意：
The
function commits the transaction manually, which is the same as commit() of the
Connection class in Java.
参数：
|
db
|
数据源连接 .
选项：
|
@k
|
After
the execution is completed, the transaction won't be committed. If this
option is omitted, the transaction will be committed.
示例：
|
=create(ID,NAME,GENDER,AGE).record([3,
"AAA","F",14])
|
=connect@e("demo")
|
Create
the connection and automatically control the commit and rollback operations
through @e option .
|
>A2.execute@k(A1,"update
STUDENTS2 set NAME=?,GENDER=?,AGE=? where ID=?",NAME,GENDER,AGE,ID)
|
Execute
the sql statement and update table STYDENTS2 according to A1’s records,
during which @k option is used to not to commit the transaction.
|
=A2.error()
|
Read
the error code generated by the execution of the previous sql .
|
if A4==0
|
>A2.commit()
|
Commit
if there isn’t an error.
|
else
|
>A2.rollback()
|
Roll
back if there is an error .
|
>A2.close()
|
Close
the connection .
相关函数：
db .clos e ()
db . err o r()
conn e c t ()
db .rollba c k( )
