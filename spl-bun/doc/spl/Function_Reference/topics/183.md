# concat()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
concat()
concat( x i ,… )
=demo.query("select * from SCORES where SCORE>90")
=A1.(SCORE)
=A1.(SUBJECT)
=concat(A2,A3)
=concat(2,3,"a")
```

## 提取内容
concat()
说明：
Concatenate
parameters into a string.
语法：
concat( x i ,… )
注意：
The
function concatenates parameters x i ,… into a string in which quotation marks will not be used.
参数：
|
x i
|
Any value that can be converted to a string
if it is a sequence, it will be broken up before
concatenation.
Return
value:
字符串
示例：
|
=demo.query("select * from SCORES where SCORE>90")
|
（原文含示意图，图像资源未迁移）
|
=A1.(SCORE)
|
=A1.(SUBJECT)
|
=concat(A2,A3)
|
979691939797979691939797PEEnglishPEEnglishMathMathPEEnglishPEEnglishMathMath.
|
=concat(2,3,"a")
|
23a .
function:
s .s p lit()
