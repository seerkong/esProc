# concat(x;d)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
concat(x;d)
concat( x;d )
=demo.query("select EID,NAME,DEPT from employee")
=A1.groups(DEPT;concat(NAME;"_"):ALL_NAME)
=A1.groups(DEPT;concat@c(NAME):ALL_NAME)
=A1.groups(DEPT;concat@q(NAME):ALL_NAME)
=A1.groups(DEPT;concat@i(NAME):ALL_NAME)
=A1.groups(DEPT;concat@i(NAME;"|"):ALL_NAME)
=create(id,num).record([1,11,1,,1,33,2,232,2,577,2,null])
=A1.groups(id;concat(num;";"))
=A1.groups(id;concat@0(num;";"))
```

## 提取内容
concat(x;d)
说明：
Use
a delimiter to concatenate values of an aggregation field.
语法：
concat( x;d )
注意：
The
function concatenates values of aggregation field x using the delimiter d
concatenate them without using a delimiter when parameter d is absent.
It can only work as the aggregate function in the groups() function.
参数：
|
x
|
Aggregation
field name.
|
d
|
delimiter.
选项：
|
@c
|
Concatenate
with the comma.
|
@i
|
Enclose
each to-be-concatenated value with the single quotation marks.
|
@q
|
Enclose
each to-be-concatenated value with the double quotation marks; won’t quote
them when the option is absent.
|
@0
|
Discard nulls and empty strings.
Return
value:
字符串
示例：
|
=demo.query("select EID,NAME,DEPT from employee")
|
Return
a table sequence .
（原文含示意图，图像资源未迁移）
|
=A1.groups(DEPT;concat(NAME;"_"):ALL_NAME)
|
Group
A1’s table sequence by DEPT field, concatenate NAME values in each group with
the delimiter _ and return them to ALL_NAME column .
（原文含示意图，图像资源未迁移）
|
=A1.groups(DEPT;concat@c(NAME):ALL_NAME)
|
Work
with @c option to use the comma as the delimiter .
（原文含示意图，图像资源未迁移）
|
=A1.groups(DEPT;concat@q(NAME):ALL_NAME)
|
Work
with @q option to enclose each to-be-concatenated value with the double
quotation marks .
（原文含示意图，图像资源未迁移）
|
=A1.groups(DEPT;concat@i(NAME):ALL_NAME)
|
Work
with @i option to enclose each to-be-concatenated value with the single
quotation marks .
（原文含示意图，图像资源未迁移）
|
=A1.groups(DEPT;concat@i(NAME;"|"):ALL_NAME)
|
Work
with @i option to enclose each to-be-concatenated value with the single
quotation marks, and use | as the delimiter .
（原文含示意图，图像资源未迁移）
When @0 option is present:
|
=create(id,num).record([1,11,1,,1,33,2,232,2,577,2,null])
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A1.groups(id;concat(num;";"))
|
Group records of A1’s table sequence and perform an
aggregation operation by id, and use semicolon to concatenate each
aggregation field value:
（原文含示意图，图像资源未迁移）
|
=A1.groups(id;concat@0(num;";"))
|
With @0 option, nulls and empty strings are
discarded when concatenating each aggregation field value:
（原文含示意图，图像资源未迁移）
