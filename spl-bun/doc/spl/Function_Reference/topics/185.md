# A.concat(d)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.concat(d)
=[1, ["a","b"],[2,"c"]]
=A1.concat()
=A1.concat(":")
=A1.concat@q()
=A1.concat@c()
=A1.concat@i()
=[[1,2,3], ["a","b"],[2,"c"]]
=A7.concat@n("-")
=A7.concat@nc()
=[123,"hello",789,"world"].concat@y([3,8,6,5])
=[123,,456,null,789].concat(";")
=[123,,456,null,789].concat@0(";")
```

## 提取内容
A.concat(d)
说明：
Concatenate
members of a sequence with the delimiter as a string.
Syntax ：
A .concat( d )
注意：
The
function concatenates members of A
delimited by d and returns result as into
a string; the sub-sequences of the string will be handled in a same way.
Concatenate the members without a delimiter when parameter d is omitted.
选项：
|
@q
|
Add
quotation marks to string members when concatenating them into a string. If
this option is omitted, do not use the quotation marks.
|
@c
|
Concatenate
with the comma.
|
@i
|
Enclose
string members to be concatenated with single quotes.
|
@n
|
If members of the
sequence are also sequences, create a new line after concatenate members of
each sequence members with the delimiter. The operations are equivalent to A .(~.concat(d)).concat("\n") .
If there are other options, perform the concatenation in the inner layer of
the function.
|
@y
|
Use
this option when parameter d is
an array. In the array, each member is regarded as representing the length of
the corresponding segment of characters, and the result is their combination
of the specified lengths. For a numeric member of A , add 0s before it to represent the
extra digits; and for a string member, add white spaces after it to reach the
specified number of characters.
|
@0
|
Discard nulls and empty member strings.
参数：
|
sequence of strings.
|
d
|
Delimiter.
Return
value:
字符串
示例：
|
=[1, ["a","b"],[2,"c"]]
|
=A1.concat()
|
Concatenate
members of sequence A1 without using the separator; the result is 1[ab][2c] .
|
=A1.concat(":")
|
Concatenate
members of sequence A1 using colon as the separator; the result is 1:[a:b]:[2:c] .
|
=A1.concat@q()
|
Use
@q option to do the concatenation with the quotation marks retained; the
result is 1["a""b"][2"c"] .
|
=A1.concat@c()
|
Use
@c option to do the concatenation with comma being the separator; the result
is 1,[a,b],[2,c] .
|
=A1.concat@i()
|
Use
@i option to do the concatenation with members enclosed by single quotation
marks; the result is 1['a''b'][2'c'] .
|
=[[1,2,3], ["a","b"],[2,"c"]]
|
Return
a sequence of sequences.
|
=A7.concat@n("-")
|
Use
@n option to join members of each sub-sequence in sequence A7 with the
separator “-” and use the line break for a new sub-sequence; the result is as
follows:
（原文含示意图，图像资源未迁移）
|
=A7.concat@nc()
|
Use
@nc options to do the concatenation; below is the result:
（原文含示意图，图像资源未迁移）
|
=[123,"hello",789,"world"].concat@y([3,8,6,5])
|
As @y option is present, add white spaces
after the corresponding string and 0s before the corresponding number to
reach the specified number of characters. Below is the returned result:
（原文含示意图，图像资源未迁移）
|
=[123,,456,null,789].concat(";")
|
Return 123;;456;;789.
|
=[123,,456,null,789].concat@0(";")
|
Use @o option to discard nulls and empty
strings and return 123;456;789.
