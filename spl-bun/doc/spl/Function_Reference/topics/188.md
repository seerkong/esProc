# ch.conj()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.conj()
=demo.cursor("select * from GYMNASTICSWOMEN")
=channel()
=A2.conj(create(ID,NAME,COUNTRY,SUBJECT,SCORES).record([ID,NAME,COUNTRY,"VAULT"
=A2.fetch()
=A1.push(A2)
=A1.fetch()
=A2.result()
```

## 提取内容
ch.conj()
说明：
Attach
a computation to a channel, which will split each of its records and perform
union on the splitting result.
语法：
ch .conj( x )
注意：
The
function attaches a computation to channel ch ,
which will split each of its records according to result of computing
expression x into a sequence or a record sequence, union the result
members or records, and return the original channel ch .
This
is an attachment computation.
参数：
|
ch
|
channel.
|
x
|
An
expression that returns a record sequence/table sequence.
Return
value:
管道
示例：
|
=demo.cursor("select * from GYMNASTICSWOMEN")
|
Return a cursor.
（原文含示意图，图像资源未迁移）
The above is content of GYMNASTICSWOMEN table.
|
=channel()
|
Create
a channel.
|
=A2.conj(create(ID,NAME,COUNTRY,SUBJECT,SCORES).record([ID,NAME,COUNTRY,"VAULT"
,VAULT,ID,NAME,COUNTRY,"UNEVENBARS",UNEVENBARS,ID,NAME,COUNTRY,
"BALANCEBEAM",BALANCEBEAM,ID,NAME,COUNTRY,"FLOOR",FLOOR]))
|
Attach
a computation to channel A2, which will perform column-to-row transposition
on VAULT, UNEVENBARS, BALANCEBEAM and FLOOR fields, and return the original
channel.
|
=A2.fetch()
|
Execute
the result fetching function in channel A2 and keep the current data in
channel.
|
=A1.push(A2)
|
Be
ready to push data in A1’s cursor into the channel, but the action needs to
wait.
|
=A1.fetch()
|
Fetch
data from cursor A1 while pushing data into the channel to execute the
attached computation and keep the result.
|
=A2.result()
|
Get channel A2’s
result:
（原文含示意图，图像资源未迁移）
