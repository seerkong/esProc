# CS.conj()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
CS.conj()
=connect("demo").cursor("SELECT top 3 * FROM scores where SUBJECT='English'
=connect("demo").cursor("SELECT top 3 * FROM scores where SUBJECT='Math'
=connect("demo").cursor("SELECT top 3 * FROM scores where SUBJECT='PE' ")
=[A1,A2,A3]
=A4.conj()
=A5.fetch()
```

## 提取内容
CS.conj()
说明：
Union
the members of a cursor sequence, and return result as a multicursor .
语法：
CS. conj ()
注意：
The
function concatenates members of CS ,
a 游标序列, and returns result as a multicursor. This is actually
the merging of data in the cursors. Each cursor in CS must have the same structure.
This
is a delayed function.
参数：
|
CS
|
游标序列.
返回值：
多游标
示例：
|
=connect("demo").cursor("SELECT top 3 * FROM scores where SUBJECT='English'
")
|
Return a cursor,
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 3 * FROM scores where SUBJECT='Math'
")
|
Return a cursor,
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 3 * FROM scores where SUBJECT='PE' ")
|
Return a cursor,
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=[A1,A2,A3]
|
Return a sequence
consisting of the three cursors.
|
=A4.conj()
|
Concatenate
records of members cursors in A4’s sequence and return a cursor .
|
=A5.fetch()
|
Fetch data from
cursor A5:
（原文含示意图，图像资源未迁移）
相关函数：
A.merg e ()
CS.me r ge()
