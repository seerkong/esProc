# A.contain( x i ,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.contain( x i ,… )
=[1,2,8,4,5,6,7,8]
=A1.contain(8)
=A1.contain(9)
= [1,2,3,4,5,6,7,8,9] .contain@b(8)
=[[1,2,3,4,5,6,7,8],[3]].contain ([3])
=A6.contain( "aaa" )
=A1.contain(8,1)
=A1.contain(8,9)
=to(A1)
>A2(1)=0
=A1.step(A4,A4).to(2,)
>A2(C5)=0
=A2.select(~>0)
=A7.contain@h(4447)
A.pos()
```

## 提取内容
A.contain( x i ,… )
说明：
Check
whether a given data object is a member of a certain sequence.
语法：
A . contain ( x i ,… )
注意：
The
function checks whether parameter x i ,… are members of
sequence A or not. Return true if all
parameters are members of sequence A ,
and false if not.
参数：
|
sequence object or an expression that returns one.
|
x i
|
Data
object, which can be a number, a string or a sequence.
选项：
|
@b
|
The
option means that sequence A is
ordered, based on which a binary search is performed in an ascending or
descending order.
|
@h
|
Use
HASH method to increase computing efficiency; for scenarios where the
sequence has a large amount of data.
Return
value:
Boolean
示例：
|
=[1,2,8,4,5,6,7,8]
|
=A1.contain(8)
|
true .
|
=A1.contain(9)
|
false .
|
= [1,2,3,4,5,6,7,8,9] .contain@b(8)
|
Use binary search when the sequence is ordered
the result is true .
|
=[[1,2,3,4,5,6,7,8],[3]].contain ([3])
|
true .
|
[1,3,6,aaa]
|
=A6.contain( "aaa" )
|
true .
|
=A1.contain(8,1)
|
true .
|
=A1.contain(8,9)
|
false .
|
=to(A1)
|
>A2(1)=0
|
for A2
|
if A4>0
|
=A1.step(A4,A4).to(2,)
|
>A2(C5)=0
|
=A2.select(~>0)
|
A7’s
result is a sequence consisting of prime numbers within 10,000 .
|
=A7.contain@h(4447)
|
true
judge whether 4447 is a prime number, during which @h option is used to
increase computing efficiency .
functions:
A.pos()
