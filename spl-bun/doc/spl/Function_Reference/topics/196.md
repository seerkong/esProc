# P.corskew()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.corskew()
P.corskew(cn)
P.corskew@r(cn, Rec)
=T("D:
=T("D:
=A1.(LotArea).skew()
=A1.corskew@c("LotArea")
=A1.(Ln_LotArea).skew()
=A2.corskew@r(5,A4(2))
```

## 提取内容
P.corskew()
说明：
Correct skewness of a
numeric variable.
语法：
|
P.corskew(cn)
|
Rec During modeling, correct skewness and handle outliers for numeric
variable cn in table sequence P , and return the result sequence
of correction and a sequence of correction and handling process records Rec
|
P.corskew@r(cn, Rec)
|
During scoring, correct skewness
of numeric variable cn according to correction process records Rec
注意：
MathCli external library function (See External Library Guide ).
No null values are allowed in the to-be-corrected data.
参数：
|
P
|
A table sequence/record sequence
|
cn
|
A string/number, which is the name of column (or the column number
starting from 1) – the to-be-pre-processed variable – in a table sequence or
record sequence
|
Rec
|
A sequence of correction and
handling process records
选项：
|
@c
|
Modify the original data to the
corrected result after execution
返回值：
Sequence
示例：
|
=T("D:
//house_prices_train.csv")
|
Import modeling data .
|
=T("D:
//house_prices_test.csv")
|
Import scoring data .
|
=A1.(LotArea).skew()
|
（原文含示意图，图像资源未迁移）
Find the skewness of
numeric variable “LotArea” .
|
=A1.corskew@c("LotArea")
|
（原文含示意图，图像资源未迁移）
Correct skewness of
numeric variable:
A4(1) Correction result
A4(2) Correction process
records .
|
=A1.(Ln_LotArea).skew()
|
（原文含示意图，图像资源未迁移）
The corrected skewness .
|
=A2.corskew@r(5,A4(2))
|
（原文含示意图，图像资源未迁移）
Correct skewness of
corresponding variable in the scoring data according to A4’s correction
process records Rec .
