# cos()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cos()
cos( numberExp )
cos(pi())
cos(pi(2))
tan ( )
```

## 提取内容
cos()
说明：
Compute
the cosine value.
语法：
cos( numberExp )
注意：
The
function computes the cosine value.
参数：
|
numberExp
|
An
angle’s value, which is measured in radians here.
返回值：
Float
type
示例：
|
cos(pi())
|
-1.0
|
cos(pi(2))
|
1.0
相关函数：
si n ()
tan ( )
