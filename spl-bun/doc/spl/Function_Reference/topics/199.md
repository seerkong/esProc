# count()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
count()
count( x 1 ,…, x n )
=[1,2,3,"w"].count()
=[1,null,3,4].count()
=[1,null,3,4].count(~|0)
=count(1,null,3,4)
=demo.query("select
=A1.count()
=A1.count(FATHER)
```

## 提取内容
count()
说明：
Count
the number of members of a sequence.
语法：
|
A .count( x )
|
count( x 1 ,…, x n )
|
Equivalent to A .count () , where x 1 ,…, x n are members of sequence A .
注意：
The
function computes expression x with members of
sequence A and returns the
number of non-null members.
参数：
|
sequence.
|
x
|
An
expression; can be omitted.
返回值：
Integer
示例：
When
A is a sequence:
|
=[1,2,3,"w"].count()
|
4.
|
=[1,null,3,4].count()
|
3; ignore null members.
|
=[1,null,3,4].count(~|0)
|
4 ; piece zero together with
members of the given sequence and return number of non-null members.
|
=count(1,null,3,4)
|
3, which is same as A2.
When
A is a table sequence or a record
sequence:
|
=demo.query("select
top 10 * from AREA ")
|
（原文含示意图，图像资源未迁移）
|
=A1.count()
|
Find
number of records in table sequence A1 and return 10.
|
=A1.count(FATHER)
|
Find
number of records where FATHER value is non-null in table sequence A1 and
return 9 .
