# covm()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
covm()
covm ( A )
=covm(A1)
```

## 提取内容
covm()
说明：
Calculate
the covariance matrix for a matrix.
语法：
covm ( A )
注意：
The
MathCli external library function (See
External Library Guide ) computes covariance matrix for matrix A .
参数：
|
Matrix.
Return
value:
Sequence
示例：
|
[[1,2,3],[3,1,1]]
|
=covm(A1)
|
（原文含示意图，图像资源未迁移）
