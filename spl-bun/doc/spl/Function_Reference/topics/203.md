# create ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
create ()
create( F i ,… )
=create(id,name,gender)
```

## 提取内容
create ()
说明：
Create
an empty table sequence.
语法：
create( F i ,… )
注意：
The
function creates an empty table sequence taking F i ,… as its fields.
参数：
|
F i
|
Field
name.
返回值：
An
empty table sequence
示例：
|
=create(id,name,gender)
|
（原文含示意图，图像资源未迁移）
