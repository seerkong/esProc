# create(#Fi,…)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
create(#Fi,…)
create ( #F i ,… )
=create(#id,#name,gender)
```

## 提取内容
create(#Fi,…)
说明：
Create
an empty table sequence with one or more keys set.
语法：
create ( #F i ,… )
注意：
The
function creates an empty table sequence consisting of fields F i ,… . A field name headed by a
hash (#) acts as a key. For the time being, the function cannot generate a time
key directly.
参数：
|
F i
|
Field
name.
Return
value:
Empty
table sequence
示例：
|
=create(#id,#name,gender)
|
（原文含示意图，图像资源未迁移）
id field and name field are
keys.
