# f.create( C,…; x;b )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
f.create( C,…; x;b )
=file("employee1.ctx")
=A1.create(#EID,NAME,GENDER)
=connect("demo").cursor("select
=connect("demo").cursor("select
=[A3,A4].cursor()
=file("emp.ctx":[1,2])
=A6.create@y(#EID,NAME,GENDER,SALARY;if(GENDER=="F",1,2))
=A7.append@i(A5)
=file("1.emp.ctx").open().cursor().fetch()
=file("2.emp.ctx").open().cursor().fetch()
=file("D:/orders.txt").import@t(SID,OID,OTime,Amount)
=file("D:/orders.ctx")
=A2.create@py(SID,#OID,OTime,Amount)
=A3.append(A1)
=A3.cursor(;;1,10).fetch()
=A3.cursor(;;2,10).fetch()
=A3.close()
=file("transaction.ctx")
=A1.create@yt(#UID,#Time,Change,Amount)
=file("transaction.txt").cursor@t().sortx(UID,Time)
=A2.append@i(A3)
=A2.import()
=connect("demo").cursor("select EID,NAME,GENDER from employee")
=A1.derive(null:Defiled)
=A2.new(EID,Defiled,NAME,GENDER)
=file("ed.ctx":[1,2])
=A4.create@yd(#EID,Defiled,NAME,GENDER;if(GENDER=="F",1,2))
=A5.append@ix(A3)
=create(EID,Defiled,NAME,GENDER).record([1,true,, 2,false,
=file("ed.ctx":[3])
=A8.create@yd(#EID,Defiled,NAME,GENDER;3)
=A9.append@i(A7)
=file("ed.ctx":[1,2,3])
=A11.open()
=A12.cursor@w()
=A13.fetch()
```

## 提取内容
f.create( C,…; x;b )
说明：
Create
a composite table based on a file.
语法：
f . create ( C,…;x;b )
注意：
The
function creates a composite table using composite table file f and generates a
multizone composite table if f is a
homo-name files group. Parameter C is a column, which is a dimension if
it is preceded by #, of the would-be composite table, and parameter x is
a zone table expression. The dimension and all fields before it must be
ordered.
Only supported by esProc Enterprise
Edition.
参数：
|
f
|
composite table file object or a homo-name files group object.
|
column of the would-be composite table.
|
x
|
zone table expression, whose result is the integer representing the
corresponding zone table.
|
b
|
块大小, whose unit is byte. Default
value is the “Composite table block size” configured in the configuration
options; when esProc is integrated into a third-party application, the
parameter’s default value is value of blockSize configured in raqsoftConfig.xml file .
选项：
|
@u
|
Do
not compress the file; default is to compress it.
|
@r
|
Generate
a row-wise file while default is columnar storage, which does not support the
multicursor.
|
@y
|
Force to re-create
the file even if the target file already exists; defalut is to terminate
computation .
|
@p
|
Use
the first field as the grouping key. By default, use dimension fields as the
segmentation key.
|
@v
|
When
columnar storage is used to generate the composite table, check whether each
of its columns is pure during data maintenance and save the data type. Only
supported by esProc Enterprise Edition.
|
@t
|
Create
key for the composite table (including multizone composite table) using
dimensions and the last key field is the time key; in this case no attached
table is allowed.
|
@d
|
Used
on a multizone composite table; the first field after key fields is regarded
as the update mark field, whose values fall in three types: null representing
to-be-added, true representing to-be-deleted, and false meaning
to-be-modified; the program retrieves data from a multizone composite table
according to the update mark field value.
Return
value:
Composite
table/Homo-name files group
示例：
|
=file("employee1.ctx")
|
Generate
composite table file employee1.ctx .
|
=A1.create(#EID,NAME,GENDER)
|
Create
A1’s base table whose columns are EID, NAME and GENDER; EID is the dimension.
|
=connect("demo").cursor("select
EID,NAME,GENDER,SALARY from
employee where GENDER='M' ").sortx(EID)
|
=connect("demo").cursor("select
EID,NAME,GENDER,SALARY from
employee where GENDER='F' ").sortx(EID)
|
=[A3,A4].cursor()
|
Return
a multicursor.
|
=file("emp.ctx":[1,2])
|
Generate
a homo-name files group, which contains two files 1.emp.ctx and 2.emp.ctx .
|
=A6.create@y(#EID,NAME,GENDER,SALARY;if(GENDER=="F",1,2))
|
Create
a multizone composite table; if(GENDER=="F",1,2) is zone table
expression and @y forces to re-create the target file even if it already
exists.
|
=A7.append@i(A5)
|
Append
data in A5’s multicursor to A7’s multizone composite table; each part of the
multicursor uniquely corresponds to A7’s one zone table.
|
=file("1.emp.ctx").open().cursor().fetch()
|
View
data in zone table 1.emp.ctx .
|
=file("2.emp.ctx").open().cursor().fetch()
|
View
data in zone table 2.emp.ctx .
Use
the first field as the segmentation key:
|
=file("D:/orders.txt").import@t(SID,OID,OTime,Amount)
|
Return
a table sequence, whose content is as follows:
（原文含示意图，图像资源未迁移）
There is a large amount of data in the table
sequence where OID values are unique.
|
=file("D:/orders.ctx")
|
Generate
composite table file orders.ctx .
|
=A2.create@py(SID,#OID,OTime,Amount)
|
Create
the base table for composite table orders.ctx and use @p option to
make the first field SID the segmentation key. 记录s whose segmentation key
values are equal will be put into the same column block.
|
=A3.append(A1)
|
Write
data in A1’s table sequence to the composite table.
|
=A3.cursor(;;1,10).fetch()
|
Retrieve
data from the composite table segment by segment. 记录s where values of
segmentation key SID are equal will be put into the same column block, making
sure that SID value of the last record of the n th segment is
different from that of the first record of the (n+1) th segment.
Dividing the table sequence into 10 segments and retrieve the 1 st
segment for example, the ending part is as follows:
（原文含示意图，图像资源未迁移）
|
=A3.cursor(;;2,10).fetch()
|
Divide
the table sequence into 10 segments and retrieve the 2 nd segment,
the beginning part is as follows:
（原文含示意图，图像资源未迁移）
|
=A3.close()
|
Close
the composite table.
Set
the time key:
|
=file("transaction.ctx")
|
=A1.create@yt(#UID,#Time,Change,Amount)
|
Create
the composite table’s base table; @t option works to set UID as the base key
and Time as the time key .
|
=file("transaction.txt").cursor@t().sortx(UID,Time)
|
Return
a cursor ordered by UID and Time .
|
=A2.append@i(A3)
|
Append
cursor A3’s data to the base table .
|
=A2.import()
|
Retrieve
data of the base table .
（原文含示意图，图像资源未迁移）
When
update mark field is present:
|
=connect("demo").cursor("select EID,NAME,GENDER from employee")
|
=A1.derive(null:Defiled)
|
=A2.new(EID,Defiled,NAME,GENDER)
|
Return
a cursor, whose content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("ed.ctx":[1,2])
|
Homo-name
files group: 1.ed.ctx and 2.ed.ctx .
|
=A4.create@yd(#EID,Defiled,NAME,GENDER;if(GENDER=="F",1,2))
|
Create
a composite table, set EID as the key, use @d option to make Defield field
the deletion mark, and put records where GENDER is F to 1.ed.ctx and
the other records to 2.ed.ctx .
|
=A5.append@ix(A3)
|
Append
cursor A1’s data to the multizone composite table .
|
=create(EID,Defiled,NAME,GENDER).record([1,true,, 2,false,
"ABC","F" ]).cursor()
|
Return
a cursor, whose content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("ed.ctx":[3])
|
=A8.create@yd(#EID,Defiled,NAME,GENDER;3)
|
Add
zone table 3.ed.ctx
|
=A9.append@i(A7)
|
Append
cursor A7’s data to zone table 3.ed.ctx .
|
=file("ed.ctx":[1,2,3])
|
=A11.open()
|
Open
A11’s multizone composite table .
|
=A12.cursor@w()
|
Return
the multizone composite table as a cursor, use @w option to handle the update
mark, which means the record where EID field value is 1 recognized as the
to-be-deleted and the one where EID is 2 is recognized as to-be-modified .
|
=A13.fetch()
|
Fetch
data in cursor A13 .
（原文含示意图，图像资源未迁移）
We can see that
the record where EID is 1 isn’t retrieved from the multizone composite table
and NAME value of the one where EID is 2 is modified .
