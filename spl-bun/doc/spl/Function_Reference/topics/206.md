# P.create()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.create()
=demo.query("select
=A1.to()
=A2.create()
=demo.query("select
=A1.keys(DEPT)
=A1.to()
=A3.create()
```

## 提取内容
P.create()
说明：
Generate
a new empty table sequence by copying data structure of a specific record sequence.
语法：
P .create()
注意：
If
parameter P has the key, just
copy it.
参数：
|
P
|
record sequence.
Return
value:
Empty
table sequence
示例：
Generated
from a record sequence:
|
=demo.query("select
top 1 * from DEPARTMENT")
|
（原文含示意图，图像资源未迁移）
|
=A1.to()
|
Generate
a record sequence .
|
=A2.create()
|
（原文含示意图，图像资源未迁移）
Create
an empty table sequence that has same data structure as A2’s .
When
there is the key:
|
=demo.query("select
top 1 * from DEPARTMENT ")
|
（原文含示意图，图像资源未迁移）
|
=A1.keys(DEPT)
|
=A1.to()
|
Generate
a record sequence CITIES.ctx.
|
=A3.create()
|
（原文含示意图，图像资源未迁移）
Create
an empty table sequence that has same data structure as A3’s while copying
the key .
