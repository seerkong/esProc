# T.create ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.create ()
=demo.query("select
=A1.create()
=demo.query("select
=A1.keys(DEPT)
=A1.create()
```

## 提取内容
T.create ()
说明：
Create
an empty table sequence by duplicating data structure of a specific table
sequence.
语法：
T .create()
注意：
If
table sequence T has a key , then
duplicate the key at the same time.
参数：
|
T
|
table sequence.
返回值：
new empty table sequence
示例：
Created
from a normal table sequence:
|
=demo.query("select
top 1 * from DEPARTMENT")
|
（原文含示意图，图像资源未迁移）
|
=A1.create()
|
（原文含示意图，图像资源未迁移）
Create
an empty table of same structure as A1 .
Copy the key
|
=demo.query("select
* from DEPARTMENT ")
|
（原文含示意图，图像资源未迁移）
|
=A1.keys(DEPT)
|
=A1.create()
|
（原文含示意图，图像资源未迁移）
Create
an empty table sequence that has not only the same data structure, but also
the same key, as table sequence A1 .
