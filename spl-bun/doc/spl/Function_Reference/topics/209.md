# r.create()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.create()
=demo.query("select top 1 * from DEPARTMENT")
=A1(1).create()
=demo.query("select * from DEPARTMENT ")
=A1.keys(DEPT)
=A1(1).create()
```

## 提取内容
r.create()
说明：
Create
a table sequence by copying the data structure of a specified record.
语法：
r .create()
注意：
If
there’s a key defined in record r ,
then copy the key at the same time.
参数：
|
r
|
record.
Return
value:
Empty
table sequence
示例：
Generate an empty table
sequence from an average record:
|
=demo.query("select top 1 * from DEPARTMENT")
|
（原文含示意图，图像资源未迁移）
|
=A1(1).create()
|
（原文含示意图，图像资源未迁移）
Create
an empty table of same structure as A1(1) .
Generate an empty table
sequence from a record with the key:
|
=demo.query("select * from DEPARTMENT ")
|
（原文含示意图，图像资源未迁移）
|
=A1.keys(DEPT)
|
=A1(1).create()
|
（原文含示意图，图像资源未迁移）
Generate
an empty table sequence by copying the data structure of the record A1(1) and the key .
