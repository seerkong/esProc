# cs.(x)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.(x)
=connect("demo").cursor("select
=A1.(age(HIREDATE))
=A1.fetch()
=A1.reset()
=A1.(2*~+1)
=A1.fetch()
=demo.cursor("select
=A1.()
=A1.skip()
```

## 提取内容
cs.(x)
说明：
Attach
the action of computing expression to a cursor and return the original cursor.
语法：
cs .( x )
注意：
The
function computes expression x on
each member of the cursor and returns the original cursor.
This
is a delayed function.
参数：
|
cs
|
cursor.
|
x
|
An
expression that is generally a single field name or a legal expression
composed of multiple field names; both can use ~ to reference the current
record; can be omitted.
Return
value:
游标
示例：
|
=connect("demo").cursor("select
top 5 EID,NAME,HIREDATE from EMPLOYEE where EID<10")
|
Return
a cursor.
|
=A1.(age(HIREDATE))
|
Attach
a computation to cursor A1 – compute the length of service according to HIERDATE,
and return the original cursor.
|
=A1.fetch()
|
Fetch
data from cursor A1 on which the attached computation is already executed (it
would be better to fetch data in batches when data amount involved is huge):
（原文含示意图，图像资源未迁移）
|
=A1.reset()
|
Reset
cursor A1 and move it back to the beginning.
|
=A1.(2*~+1)
|
Attach
a computation to cursor A1 – compute length of service*2+1 and ~ references
the current record, and return the original cursor.
|
=A1.fetch()
|
Fetch
data from cursor A1 on which A5’s computation is already executed:
（原文含示意图，图像资源未迁移）
When
parameter x is absent:
|
=demo.cursor("select
top 3 DEPT,MANAGER from DEPARTMENT")
|
Return
a cursor.
|
=A1.()
|
As
parameter x is absent, no computation is performed on cursor A1 and
the function returns the same cursor.
|
=A1.skip()
|
Get
the number of records in cursor A1 and return 3.
