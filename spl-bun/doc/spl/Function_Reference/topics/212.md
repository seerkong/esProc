# T.cuboid(f,F i ,…;y,…)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.cuboid(f,F i ,…;y,…)
=file("empcub.ctx")
=A1.create@y(#EID,NAME,GENDER,DEPT,SALARY,BIRTHDAY)
=demo.cursor("select
=A2.append(A3)
=file("cub1")
=A2.cuboid(A5,DEPT,GENDER;avg(age(BIRTHDAY)),count(EID))
=A2.cgroups(DEPT,GENDER;avg(age(BIRTHDAY)):AvgAge,count(EID):IdNum;;A5)
```

## 提取内容
T.cuboid(f,F i ,…;y,…)
说明：
Generate
preaggregation file for a composite table/multizone composite table.
语法：
T. cuboid ( f , F i ,…; y ,…)
注意：
The
function generates preaggregation file f for
composite table/multizone composite table T .
The preaggregation file will not update itself accordingly when the composite
table is updated.
参数：
|
T
|
composite table/multizone composite table.
|
f
|
Preaggregation
file object.
|
F i
|
Grouping
field.
|
y
|
Aggregate
expression; aggregate functions supported are sum/count/avg/max/min/top.
Return
value:
Preaggregation
file name
示例：
|
=file("empcub.ctx")
|
=A1.create@y(#EID,NAME,GENDER,DEPT,SALARY,BIRTHDAY)
|
Create
a composite table file .
|
=demo.cursor("select
EID,NAME,GENDER,DEPT,SALARY,BIRTHDAY from employee ")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.append(A3)
|
Append
table sequence A3’s data to the composite table .
|
=file("cub1")
|
Preaggregation
file object .
|
=A2.cuboid(A5,DEPT,GENDER;avg(age(BIRTHDAY)),count(EID))
|
Generate
preaggregation file cub1 for the composite table file, where DEPT and
GENDER are grouping fields, and compute average age and find the number of
employees in each group .
|
=A2.cgroups(DEPT,GENDER;avg(age(BIRTHDAY)):AvgAge,count(EID):IdNum;;A5)
|
Use
the preaggregation file to perform grouping & aggregation on the
composite table and return following result:
（原文含示意图，图像资源未迁移）
