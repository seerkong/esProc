# cum()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cum()
cum ( x;G i ,… )
=demo.query("select * from SCORES ")
=A1.derive(cum(SCORE+10;CLASS):F1)
=A1.derive(cum(SCORE+10;CLASS,STUDENTID):F1)
```

## 提取内容
cum()
说明：
An
iterative loop to get a cumulative value over records in which one of its
fields has same values.
语法：
cum ( x;G i ,… )
注意：
The function performs an
iterative loop over records whose G i field contains same
values to compute expression x and
add its previous value to the current one. Each time when the value of G i
field changes, perform a new round of calculation.
参数：
|
x
|
An
expression.
|
G i
|
Field
name.
Return
value:
The
result of the given expression’s current value plus its previous one
示例：
|
=demo.query("select * from SCORES ")
|
=A1.derive(cum(SCORE+10;CLASS):F1)
|
Compute
expression SCORE+10 on records having same CLASS field values in A1’s table
sequence, get sum of the current result and the preceding result, and make
the sum value of the new field F1:
（原文含示意图，图像资源未迁移）
|
=A1.derive(cum(SCORE+10;CLASS,STUDENTID):F1)
|
Compute
expression SCORE+10 on records having same CLASS and STUDENTID field values
in A1’s table sequence, get sum of the current result and the preceding
result, and make the sum value of the new field F1:
（原文含示意图，图像资源未迁移）
