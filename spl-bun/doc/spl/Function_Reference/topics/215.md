# A.cumulate()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.cumulate()
=[1,2,3,4]
=A1.cumulate()
=connect("demo").query("select EID,NAME,SALARY,DEPT from employee")
=A3.group(DEPT;sum(SALARY):TolSalary)
=A4.(TolSalary).cumulate()
```

## 提取内容
A.cumulate()
说明：
Compute
sum of members of a sequence.
语法：
A . cumulate ( )
注意：
The
function computes sum of members of sequence A .
参数：
|
sequence.
Return
value:
Sequence
示例：
|
=[1,2,3,4]
|
=A1.cumulate()
|
Return 【 1,3,6,10 】 .
|
=connect("demo").query("select EID,NAME,SALARY,DEPT from employee")
|
=A3.group(DEPT;sum(SALARY):TolSalary)
|
Compute
sum of salaries in each department of table sequence A3 .
（原文含示意图，图像资源未迁移）
|
=A4.(TolSalary).cumulate()
|
Compute sum of values of TolSalary column in sequence A4 .
（原文含示意图，图像资源未迁移）
