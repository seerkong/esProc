# cursor()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cursor()
cursor ( spl ,…)
=connect("demo").query("select * from
=B2.derive(avg(VAULT,UNEVENBARS,BALANCEBEAM,FLOOR):Average)
=[5,10,20,25]
=cursor("test.splx",A1)
=A2.fetch()
=call("test.splx",A1)
=cursor@c(A1,5)
=A3.fetch()
```

## 提取内容
cursor()
说明：
Call a cellset file and return the result
set returned from the execution of the file as a cursor.
语法：
cursor ( spl ,…)
注意：
The
function calls a cellset file and returns the result set returned from the execution
of the file as a cursor. When there are multiple return statements in the spl
file, first merge the result sets returned by the return statements, and then return the merge result as a cursor.
These result sets of return must be
of the same structure, or error will be reported. Use either an absolute path
or a relative path to search for the spl file. With the relative path,
the search order is Class path –> Search path –> Main directory. The
cursor the function returns is irreversible.
参数：
|
spl
|
Cellset
file name.
|
…
|
spl ’s parameter.
选项：
|
@c
|
The
spl can be represented by a cell that corresponds to a subroutine
defined by func() function.
返回值：
游标
示例：
Here’s test.splx cellset file under the main directory in which arg1 is the cellset parameter:
|
for arg1
|
=connect("demo").query("select * from
GYMNASTICSWOMEN where ID=?",A1)
|
=B2.derive(avg(VAULT,UNEVENBARS,BALANCEBEAM,FLOOR):Average)
|
return B3
|
Perform
a loop computation and return the result sets .
|
=[5,10,20,25]
|
=cursor("test.splx",A1)
|
（原文含示意图，图像资源未迁移）
Cll
test.spl , pass A1’s sequence as the
value of parameter arg1 , and return
result as a cursor .
|
=A2.fetch()
|
（原文含示意图，图像资源未迁移）
Fetch records from
the cursor; the result is the union of returned result sets by the loop
computation of test. splx .
|
=call("test.splx",A1)
|
（原文含示意图，图像资源未迁移）
Use call function
to call test. splx but only the result set of the first loop
is returned .
|
func fc(arg1)
|
arg1 is cellset parameter
|
return arg1*100
|
=cursor@c(A1,5)
|
Call
the subroutine code block whose master cell is A1, and pass parameter value 5
to it .
|
=A3.fetch()
|
500 .
