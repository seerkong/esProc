# cursor cs cursor … cursor

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=to(100000).new(rand(1000):f1,rand(1000):f2).cursor()
=A2.groups(f1:count(1))
=A3.groups(f2:count(1))
```

## 提取内容
cursor cs cursor … cursor
说明：
Define
a channel for each cursor according to a specified cursor.
语法：
cursor
cs
cursor
…
cursor
注意：
The
statement defines a channel for each cursor according to cursor cs (the value
of the cursor cell is a channel), traverses cs to perform a calculation over
the channel in the code block and write the result into the cell of cursor.
参数：
|
cs
|
cursor.
|
…
|
code block.
Return
value:
Result
set of channel
示例：
|
=to(100000).new(rand(1000):f1,rand(1000):f2).cursor()
|
Generate
a cursor .
|
cursor A1
|
=A2.groups(f1:count(1))
|
Traverse
cursor A1, calculate A2’s channel in B2’s code block and write the result to
A2 .
|
cursor
|
=A3.groups(f2:count(1))
|
Same
as the above .
