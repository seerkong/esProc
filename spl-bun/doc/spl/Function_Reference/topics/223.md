# cs.cursor@m()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.cursor@m()
=demo.cursor("select * from EMPLOYEE")
=A1. cursor @m (3)
=demo.cursor("select EID,NAME,GENDER,DEPT, SALARY from
=A1.cursor@m(0)
=file("emp.ctx").open()
=A3.append(A2)
```

## 提取内容
cs.cursor@m()
说明：
Generate a multicursor using a
unicursor.
语法：
cs. cursor@m (n)
注意：
The
function generates a multicursor using a unicursor cs . It fetches data
from cs and parallelly allocates them to n subcursors.
参数：
|
cs
|
unicursor.
|
n
|
The number of subcursors, whose default
value is 【 Default
Number of Subcursors in a 多游标 】 configured in the designer.
When esProc is integrated into a third-party application, the default number
of subcursors is cursorParallelNum value set up in raqsoftConfig.xml .
When
n =0, the function generates a
buffer cursor, to which retrieval threads buffer data to it. This often
happens when cursor data is being appended to a composite table.
Return
value:
Unicursor/多游标/Buffer
cursor
示例：
|
=demo.cursor("select * from EMPLOYEE")
|
=A1. cursor @m (3)
|
Generate
a multicursor.
|
=demo.cursor("select EID,NAME,GENDER,DEPT, SALARY from
employee")
|
=A1.cursor@m(0)
|
Generate
a buffer cursor.
|
=file("emp.ctx").open()
|
Open
a composite table file.
|
=A3.append(A2)
|
Append
data of cursor A2 to emp.ctx, during which the retrieval threads buffers data
when cursor data is outputting.
