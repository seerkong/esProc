# f.cursor()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
f.cursor()
=file("D://Student.txt").cursor@tx()
=create(CLASS,STUDENTID,SUBJECT,SCORE)
=A1.skip(5)
=A1.fetch(3)
>A2.insert(0:B5,CLASS,STUDENTID,SUBJECT,SCORE)
=file("D://Department.txt").cursor@t(Dept,Manager;,"/")
=A10.fetch()
=file("D://Department5.txt").cursor@t(;1:2)
=A11.fetch()
=file("D:// EMPLOYEE.
=A12.fetch()
=file("D://EMPLOYEE.btx").cursor@b(;1:2)
=A13.fetch()
=file("D://Department.txt").cursor@ts()
= A14.fetch()
=file("D://StuName.txt").cursor@i()
=A15.fetch()
=file("D://EMPLOYEE1.txt").cursor@tc()
=A16.fetch()
=file("D://Department3.txt").cursor@e(EID)
=A17.fetch()
=file("D://Department2.txt").cursor@tq(;,"|")
=A18.fetch()
=file("D://Department.txt").cursor@tm(DEPT:string,MANAGER:int;3,"/")
=file("D://Sale1.txt").cursor(#1,#3)
=A20.fetch()
=file("D:/Dep3.txt").cursor@cqo()
=A21.fetch()
=file("D:/Dep1.txt").cursor@k()
=A22.fetch()
=file("D:/Department1.txt").cursor@t(id:int,name;,"|")
=A23.fetch()
=file("D:/Department1.txt").cursor@td(id:int,name;,"|")
=A24.fetch()
=file("D:/Department1.txt").cursor@tv(id:int,name;,"|")
=A25.fetch()
=file("D:/Dep2.txt").cursor@tdn(id:int,name,surname;,"|")
=A26.fetch()
=file("D:/Desktop/DemoData/txt/City.txt").cursor@w()
=A27.fetch()
=file("D://t1.txt").cursor@c()
=A28.fetch()
=file("D://t1.txt").cursor@cp()
=A29.fetch()
=file("D://t1.txt").cursor@cpa()
=A30.fetch()
=file("D://t2.txt").cursor@l()
=A31.fetch()
=file("D://t3.txt").cursor@f()
=A32.fetch()
=file("um.txt")
=A1.cursor().fetch()
=A1.cursor@y(UID:int:1,Amount:int:5,Udate:string:18).fetch()
=A1.cursor@y(UID:int:1,Amount:int:5,Udate:datetime:"yyyy-MM-ddHH:mm:ss").fetch()
=A1.cursor@ye(UID:int:1,UName::,Amount:int:5,Udate:string:18).fetch()
f.import()
```

## 提取内容
f.cursor()
说明：
Create a cursor based on a file.
语法：
f .cursor()
f .cursor( F i :type ,…; k:n , s )
注意：
The function creates a cursor based
on file f and returns it. The cursor
will be automatically closed after a full data scan.
参数：
|
f
|
File
object, only supporting the textual
file object .
|
F i
|
Fields
to be retrieved; all fields will be retrieved by default. The to-be-retrieved
field(s) can be represented by their sequence numbers headed by the sign #.
|
type
|
Field
types, including bool, int, long, float, decimal, string, date, time and
datetime. Data type of the first row will be used by default.
|
s
|
User-defined
separator. The default is tab. When the parameter is omitted, the comma
preceding it can be omitted, too.
|
k
|
The
segment number.
|
n
|
The number of
segments. Retrieve the whole file when both k and n are omitted .
选项：
|
@t
|
Use
the first row of f as the field names. If omitted, use _1,_2,… as the default field names.
|
@b
|
Retrieve
data from a binary file written in the method of export, with the support of
parameters F i , k and n , and without support of parameters type and s . Ignore
options @t, @s, @i, @q, @a, @n, @k,
@p, @f, @l, @m, @c, @o, @d, @v and @r. The segmental retrieval could result
in empty segment in cases when a file has only a very small number of records.
|
@e
|
Make
the function return null when F i doesn’t
exist in the file; raise an error when the option is absent.
|
@x
|
Delete
the source file automatically on closing the cursor; the cursor that the
function returns is irreversible when this option works.
|
@s
|
Not
split the to-be-retrieved field when it is imported as a cursor whose content
is a table sequence consisting of strings of a single field; in this case the
parameters will be ignored.
|
@i
|
If
the result set has only one column, the content of the returned cursor will
be a sequence.
|
@q
|
Remove
the quotation marks, if any, from both ends of data items, including the
field names, and handle escape sequences; but will keep quotation marks
within the data item.
|
@c
|
Use comma as
the separator when parameter s
is absent .
|
@m
|
With the
option, the f.cursor@m( Fi:type,…;n,s )
function returns a multicursor; here
parameter n is the number
of segments; use the value
of 【 Default subcursor count in a multicursor 】 defined in the
designer as the number subcursors if the option is absent ; when integrating esProc into
a third-party application, use cursorParallelNum value configured in raqsoftConfig.xml file as the default .
|
@o
|
Use quotation
marks as the escape character .
|
@k
|
Retain white
spaces on both sides of the data item; without it white spaces on both ends
will be automatically deleted .
|
@d
|
Delete a
record if it contains unmatching data types or data formats and start
examining data by type, or if the parentheses and the quotation marks in it
do not match when @p option and @q option respectively are present .
|
@n
|
Ignore and
discard rows whose number of columns don’t match the first row .
|
@v
|
Throw an
exception, terminate the execution and output the content of the problem
record when errors appear in @d check and @n check .
|
@w
|
Read
each row, including the column headers row, as a sequence and return a cursor
of sequence made up of sequences.
|
@a
|
Treat
single quotes as what they are; left not handled by default, and can work
with @q@p.
|
@p
|
Enable
handling the matching of parentheses (not including the separators within the
parentheses) and quotes, as well as the escape sequences outside of the
quotes.
|
@f
|
Split
the file content into a string by the separator without parsing.
|
@l
|
Allow
line continuation if there is an escape character \ at the end of the line.
|
@y
|
Use
fixed-length method to parse data. In this case, fmt is interpreted as the field length. When type is the datetime type, use fmt to compute length of the corresponding field.
This
option and @y option are mutually exclusive. With it, Fi is interpreted as result set field names. When the parameter
is absent, skip the corresponding field while maintaining the original order
of the fields in the file.
When
@e is also present, a field without fmt
is regarded as an added empty field; otherwise error will be reported.
返回值：
游标/多游标
示例：
|
=file("D://Student.txt").cursor@tx()
|
Return
the cursor for retrieving data, take the record in the first row as field
names, and delete the file automatically when closing the cursor.
|
=create(CLASS,STUDENTID,SUBJECT,SCORE)
|
Construct
a new table sequence.
|
for
|
if A3==1
|
=A1.skip(5)
|
If
the loop number is 1, then skip 5 consecutive rows.
|
=A1.fetch(3)
|
Retrieve
data from cursorA1, 3 rows each time.
|
if B5==null
|
Jump
out from the loop when B5 is null.
|
Break
|
else
|
>A2.insert(0:B5,CLASS,STUDENTID,SUBJECT,SCORE)
|
Insert
records in B5 into A2.
|
=file("D://Department.txt").cursor@t(Dept,Manager;,"/")
|
=A10.fetch()
|
Below
is Department.txt :
（原文含示意图，图像资源未迁移）
Contents of Department. txt are separated with the slashes and read out according to the specified
fields of DEPT and MANAGER.
（原文含示意图，图像资源未迁移）
|
=file("D://Department5.txt").cursor@t(;1:2)
|
=A11.fetch()
|
With
F i and s omitted, the function separates the
cursor into 2 segments and retrieves the first one.
|
=file("D:// EMPLOYEE.
btx").cursor@b(GENDER;1:2)
|
=A12.fetch()
|
Retrieve
the GENDER field of the bin file, EMPLOYEE.btx (a
segmented binary file) exported through f.export@z(); by
default the exported binary file includes field names.
|
=file("D://EMPLOYEE.btx").cursor@b(;1:2)
|
=A13.fetch()
|
Retrieve
the segmented binary file, EMPLOYEE1.btx ,
exported by f.export@b(), divide
the file contents into 2 parts, and get the first part.
|
=file("D://Department.txt").cursor@ts()
|
= A14.fetch()
|
（原文含示意图，图像资源未迁移）
Won’t
split fields, the cursor contains a table sequence consisting of a single
string field.
|
=file("D://StuName.txt").cursor@i()
|
=A15.fetch()
|
（原文含示意图，图像资源未迁移）
StuName.txt is a file c ontaining only one field, so the content of the
cursor is a sequence.
|
=file("D://EMPLOYEE1.txt").cursor@tc()
|
=A16.fetch()
|
（原文含示意图，图像资源未迁移）
Retrieve the first segment of
GENDER field of comma-seperated EMPLOYEE1.txt .
|
=file("D://Department3.txt").cursor@e(EID)
|
=A17.fetch()
|
（原文含示意图，图像资源未迁移）
Return null since
EID field can’t be found in Department3.txt ; without @e option, error will be reported, saying EID: field is not found .
|
=file("D://Department2.txt").cursor@tq(;,"|")
|
=A18.fetch()
|
Below
is Department2.txt :
（原文含示意图，图像资源未迁移）
B19’s result:
（原文含示意图，图像资源未迁移）
|
=file("D://Department.txt").cursor@tm(DEPT:string,MANAGER:int;3,"/")
|
The cursor is
devided into 3 segments and the result of A19 is returned as a multicursor .
|
=file("D://Sale1.txt").cursor(#1,#3)
|
=A20.fetch()
|
Below is the Sale1.txt file:
（原文含示意图，图像资源未迁移）
Below is the result of B20:
（原文含示意图，图像资源未迁移）
|
=file("D:/Dep3.txt").cursor@cqo()
|
=A21.fetch()
|
Here is the Dep3.txt file:
（原文含示意图，图像资源未迁移）
With @o option, two double quotation marks
are treated as one and return the result as follows:
（原文含示意图，图像资源未迁移）
|
=file("D:/Dep1.txt").cursor@k()
|
=A22.fetch()
|
（原文含示意图，图像资源未迁移）
Retain the whitespace characters on both
sides of the data item.
|
=file("D:/Department1.txt").cursor@t(id:int,name;,"|")
|
=A23.fetch()
|
Return id and name fields of Department1.txt .
（原文含示意图，图像资源未迁移）
|
=file("D:/Department1.txt").cursor@td(id:int,name;,"|")
|
=A24.fetch()
|
（原文含示意图，图像资源未迁移）
Delete a record containing unmatching data
types; rows where id value is a are
deleted.
|
=file("D:/Department1.txt").cursor@tv(id:int,name;,"|")
|
=A25.fetch()
|
（原文含示意图，图像资源未迁移）
Verify data
type matching, and, and if error reports, throw an exception, terminate the
execution and output the content of the problem record; data type doesn’t
match with rows where id value is a .
|
=file("D:/Dep2.txt").cursor@tdn(id:int,name,surname;,"|")
|
=A26.fetch()
|
Here’s the file Dep2.txt :
（原文含示意图，图像资源未迁移）
Ignore row 6 and row 8 because the number
of columns doesn’t match that in row 1.
（原文含示意图，图像资源未迁移）
|
=file("D:/Desktop/DemoData/txt/City.txt").cursor@w()
|
=A27.fetch()
|
Use @w option to
read each row as a sequence and return a cursor of sequence whose members are
sequences .
（原文含示意图，图像资源未迁移）
|
=file("D://t1.txt").cursor@c()
|
=A28.fetch()
|
Below is t1.txt :
（原文含示意图，图像资源未迁移）
With @c option,
use the comma as the default separator and return result as follows:
（原文含示意图，图像资源未迁移）
|
=file("D://t1.txt").cursor@cp()
|
=A29.fetch()
|
With @p option,
parentheses and quotation marks matching will be handled during parsing .
（原文含示意图，图像资源未迁移）
|
=file("D://t1.txt").cursor@cpa()
|
=A30.fetch()
|
With @a option,
single quotes are treated as quotes .
（原文含示意图，图像资源未迁移）
|
=file("D://t2.txt").cursor@l()
|
=A31.fetch()
|
Below is t2.txt :
（原文含示意图，图像资源未迁移）
With @1 option,
allow line continuation when there is an escape character at the end of the
line .
（原文含示意图，图像资源未迁移）
|
=file("D://t3.txt").cursor@f()
|
=A32.fetch()
|
With @f option,
just splt the file as a string using the separator .
（原文含示意图，图像资源未迁移）
Use fixed-length method to
parse data:
|
=file("um.txt")
|
=A1.cursor().fetch()
|
Create
a cursor based on um.txt , fetch data from the cursor, and return the
following result:
（原文含示意图，图像资源未迁移）
|
=A1.cursor@y(UID:int:1,Amount:int:5,Udate:string:18).fetch()
|
With @y option, use the fixed-length method to parse
data. The 1 st column’s length is 1, its data type is int, and the
column name is UID; the 2 nd column’s length is 5, its data type is
int, and the column name is Amount; the 3 rd column’s length is 18,
its data type is string, and the column name is Udate. Below is the returned
result:
（原文含示意图，图像资源未迁移）
|
=A1.cursor@y(UID:int:1,Amount:int:5,Udate:datetime:"yyyy-MM-ddHH:mm:ss").fetch()
|
As data of the 3 rd column is datetime type,
compute its length according to the datetime format and return the following
result:
（原文含示意图，图像资源未迁移）
|
=A1.cursor@ye(UID:int:1,UName::,Amount:int:5,Udate:string:18).fetch()
|
With
@e option, parameter fmt ’s absence
makes the 2 nd column be treated as empty. Below is the returned
result:
（原文含示意图，图像资源未迁移）
相关函数：
cs . f etch()
cs .skip( )
db . cursor()
f.import()
