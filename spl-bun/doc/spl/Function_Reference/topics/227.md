# T.cursor(x,…;wi,...)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.cursor(x,…;wi,...)
=file(“D:/tb4.ctx” :[1] ,“169.254.121.62:8281”)
=A1.open()
=A2.attach(table3)
=A2.cursor()
=A3.cursor(NAME,GENDER;EID<6)
=file(“tb5.ctx” :[2] ,“192.168.31.72:8281”)
=A6.open()
=A7.cursor@m(;;3)
=A7.cursor()
=A9.memory()
=A10.cursor()
```

## 提取内容
T.cursor(x,…;wi,...)
说明：
Return
the specified column(s) in a cluster entity table/cluster in-memory table as a
cluster cursor.
语法：
T. cursor (x ,…;wi,... )
注意：
The
function computes data in a cluster entity table/cluster in-memory table with
expression x and the filtering
condition w and returns the results
as a cluster cursor.
Only
supported by esProc Enterprise Edition.
选项：
|
@m
|
The function with this option generates
a cluster multicursor synchronously segmented into n parts. n is a
positive integer representing the number of segments to be divided; the
function returns an ordinary cursor if n <2
use the value of 【 Default
number of subcursors in a multicursor 】 set
in 【 Tool 】 - 【 Options 】 if n is absent .
参数：
|
T
|
cluster entity table/cluster in-memory table; cannot be a duplicate table.
|
x
|
An
expression.
|
wi
|
Filtering
condition; multiple conditions are separated by comma(s) and their
relationships are AND.
Return
value:
Cluster
(multi)cursor
示例：
|
=file(“D:/tb4.ctx” :[1] ,“169.254.121.62:8281”)
|
Open a cluster table
file.
|
=A1.open()
|
Return
a cluster composite table.
|
=A2.attach(table3)
|
Retrieve
cluster entity table table3 .
|
=A2.cursor()
|
Return
a cluster cursor.
|
=A3.cursor(NAME,GENDER;EID<6)
|
Return
NAME field and GENER field where EID<6 as a cluster cursor.
|
=file(“tb5.ctx” :[2] ,“192.168.31.72:8281”)
|
=A6.open()
|
=A7.cursor@m(;;3)
|
Generate
a cluster multicursor that is segmented into 3 parts.
|
=A7.cursor()
|
=A9.memory()
|
Generate
a cluster in-memory table.
|
=A10.cursor()
|
Return
a cluster cursor.
