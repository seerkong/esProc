# T.cursor(x:C,…;wi,...;mcs)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.cursor(x:C,…;wi,...;mcs)
=to(10000).new(#:k1,rand():c1).sort@o(k1)
=to(10000).new(#:k1,rand(10000):c2).sort@o(k1)
=to(10000).new(#:k1,rand()*1000:c3).sort@o(k1)
=A2.cursor()
=A3.cursor()
=A4.cursor()
=file("D:\\cs1.ctx")
=A8.create(#k1,c1)
=A9.append(A5)
=A9.attach(table1,c2)
=A11.append(A6)
=A11.cursor@m(;;2)
=A9.attach(table2,c3)
=A14.append(A7)
=A14.cursor@v(;;A13)
=connect("demo").cursor("select EID,NAME from employee")
=file("emp.ctx")
=A2.create@y(#EID,NAME)
=A3.append(A1)
=A4.cursor@m(;;3)
=connect("demo").cursor("select EID,NAME,GENDER,SALARY
=file("emp.ctx":[1,2])
=A7.create@y(#EID,NAME,GENDER,SALARY;if(GENDER=="F",1,2))
=A8.append@x(A6)
=A8.cursor(EID,NAME,GENDER,SALARY;SALARY>5000;A5)
=file("emp.ctx")
=A1.open()
=A2.import()
=file("emp1.ctx").open().cursor@m(;;2)
=5.new(~:ID,~*~:Num).keys(ID)
=A2.cursor(EID,NAME;EID=A5.find(EID);A4)
=A2.cursor(EID,NAME;EID=A5.pfind(EID);A4)
=A2.cursor(EID,NAME;EID:A5;A4)
=A2.cursor(NAME,SALARY;EID:A5;A4)
=A2.cursor(EID,NAME;EID:A5:null;A4)
=A2.cursor(EID,NAME;EID:A5:#;A4)
=connect("demo").query("select
=A2.cursor(EID,NAME;(EID=A5.find(EID),NAME=A12.find(NAME),EID!=null
=connect("demo").cursor("select EID,NAME from employee")
=file("e-mcs.ctx")
=A2.create@y(#EID,NAME)
=A3.append(A1)
=A4.cursor@m(;;3)
=connect("demo").cursor("select EID,NAME,GENDER from employee")
=A6.derive(:Defiled)
=A7.new(EID,Defiled,NAME,GENDER)
=file("ecd.ctx":[1,2])
=A9.create@yd(#EID,Defiled,NAME,GENDER;if(GENDER=="F",1,2))
=A10.append@ix(A8)
=create(EID,Defiled,NAME,GENDER).record([0,true,,,1,true,,,
=file("ecd.ctx":[3])
=A13.create@yd(#EID,Defiled,NAME,GENDER;3)
=A14.append@i(A12)
=file("ecd.ctx":[1,2,3]).open()
=A16.cursor(;;A5)
=A17.fetch()
=A16.cursor@w(;;A5)
=A19.fetch()
```

## 提取内容
T.cursor(x:C,…;wi,...;mcs)
说明：
Synchronously
segment an entity table/in-memory table/multizone composite table according to
a multicursor and return a multicursor.
语法：
T. cursor (x: C,…;wi,...;mcs )
注意：
The
function synchronously segments entity table/in-memory table/multizone
composite table T according to
multicursor mcs , during which T ’s first field and mcs ’s first field will be matched, and returns a multicursor. When T
is a multizone composite table, merge zone tables by their dimensions.
参数：
|
T
|
An
entity table/in-memory table/multizone composite table; should not be composite
table stored in row-wise.
|
x
|
An
expression.
|
The
column alias.
|
wi
|
Filtering
condition; retrieve the whole set when this parameter is absent; separate
multiple conditions by comma(s) and their relationships are AND. Besides
regular filtering expressions, you can also use the following five types of
syntax in a filtering condition, where K is a field in the entity
table:
1 ． K=w
w usually uses expression T i .find( K )
or T i .pfind( K ), where T i is a
table sequence. When value of w is null or false, the corresponding record in
the entity table will be filtered away; when w is expression T i .find( K )
and the to-be-selected fields C,... contain K , T i ’ s referencing field will be assigned to K ; when w is
expression T i .pfind( K ) and the to-be-selected fields
C,... contain K , sequence numbers of K values in T i
will be assigned to K .
2 ． (K1=w1, … K i =w i ,w)
K i =w i is an
assignment expression. Generally, parameter w i can use
expression T i .find( K i ) or T i .pfind( K ),
where T i is a table sequence; when w i is
expression T i .find( K i ) and the
to-be-selected fields C,... contain K i , T i ’s
referencing field will be assigned to K i correspondingly
when w i is expression T i .pfind( K i )
and the to-be-selected fields C,... contain K i , sequence
numbers of K i values in T i will be
assigned to K i .
w is a filter expression; you can
reference K i in w .
3 ． K:T i
Ti
is a table sequence. Compare K i value in the entity table
with key values of T i and discard records whose K i
value does not match; when the to-be-selected fields C,... contain
K , T i ’s referencing field will be assigned to K .
4 ． K:T i :null
Filter
away all records that satisfy K : T i .
5 ． K:T i :#
Locate
records according to sequence numbers, compare sequence numbers of records in
table sequence T i according to the entity table’s K
values, and discard non-matching records; when the to-be-selected fields C,...
contain K, T i ’s referencing field will be assigned to K .
|
mcs
|
multicursor generated from an entity table.
选项：
|
@k
|
Perform matching using the multicursor’s
first field of the key .
|
@v
|
Generate a pure table sequence-based
column-wise cursor, which has higher performance than regular cursors; only
supported by esProc Enterprise Edition .
|
@x
|
Automatically close the entity table
after data is fetched from the cursor .
|
@g
|
When
T is a multizone composite table and if group() operation will be executed on
the result cursor, use this option to speed up the computation.
|
@w
|
Used
on a multizone composite table with the update mark
Perform
update merge; when zone tables share a same key value, ignore the record
contained in the zone table with a smaller number; segmentation is performed
according to the way zone table 1 is split
Handle
the update mark and do not return records with a deletion mark to the cursor
but if the key value of a record with the deletion mark is unique in the
multizone composite table, just retain it
This
option enables retrieving key field(s) as well as the deletion mark field, if
there is one, forcefully.
|
@p
|
Perform
MERGE by the first field when parameter T is a multizone composite
table.
Return
value:
多游标
示例：
|
for
|
=to(10000).new(#:k1,rand():c1).sort@o(k1)
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=to(10000).new(#:k1,rand(10000):c2).sort@o(k1)
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=to(10000).new(#:k1,rand()*1000:c3).sort@o(k1)
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.cursor()
|
Return
a cursor.
|
=A3.cursor()
|
Return
a cursor.
|
=A4.cursor()
|
Return
a cursor.
|
=file("D:\\cs1.ctx")
|
Generate
a composite table file.
|
=A8.create(#k1,c1)
|
Create
the composite table’s base table.
|
=A9.append(A5)
|
Append
data in A5’s cursor to the base table.
|
=A9.attach(table1,c2)
|
Create
attached table table1 on base table.
|
=A11.append(A6)
|
Append
data in A6’s cursor to table1 .
|
=A11.cursor@m(;;2)
|
Segment
the attached table and return a multicursor.
|
=A9.attach(table2,c3)
|
Create
attached table table2 on base table.
|
=A14.append(A7)
|
Append
data in A7’s cursor to table2 .
|
=A14.cursor@v(;;A13)
|
Segment
table2 according to A13’s multicursor.
When
T is a multizone composite table:
|
=connect("demo").cursor("select EID,NAME from employee")
|
=file("emp.ctx")
|
Generate
a composite table file.
|
=A2.create@y(#EID,NAME)
|
Create
the composite table’s base table.
|
=A3.append(A1)
|
Append
data in A1’s cursor to the composite table’s base table.
|
=A4.cursor@m(;;3)
|
Generate
a multicursor having 3 subcursors according to the composite table.
|
=connect("demo").cursor("select EID,NAME,GENDER,SALARY
from employee")
|
=file("emp.ctx":[1,2])
|
Generate
a homo-name files group.
|
=A7.create@y(#EID,NAME,GENDER,SALARY;if(GENDER=="F",1,2))
|
Create
a multizone composite table.
|
=A8.append@x(A6)
|
Append
data in A6’s cursor to the multizone composite table and its content is as
follows:
（原文含示意图，图像资源未迁移）
|
=A8.cursor(EID,NAME,GENDER,SALARY;SALARY>5000;A5)
|
Split A8’s multizone
composite table synchronously according to A5’s multicursor, perform merge by
default and return a cursor as follows:
（原文含示意图，图像资源未迁移）
We can see that
data in two zone tables are merged by their dimensions .
Use
special types of filtering conditions:
|
=file("emp.ctx")
|
=A1.open()
|
Open
the composite table file.
|
=A2.import()
|
As
no parameters are present, return all data in the entity table.
（原文含示意图，图像资源未迁移）
|
=file("emp1.ctx").open().cursor@m(;;2)
|
Return
a multicursor.
|
=5.new(~:ID,~*~:Num).keys(ID)
|
Generate
a table sequence using ID as the key.
（原文含示意图，图像资源未迁移）
|
=A2.cursor(EID,NAME;EID=A5.find(EID);A4)
|
Use
K = w filtering mode; in this case w is T i .find( K ) and entity table records making EID=A4.find(EID) get null or
false are discarded; EID is the selected field, to which table sequence A5’s
referencing field is assigned.
（原文含示意图，图像资源未迁移）
|
=A2.cursor(EID,NAME;EID=A5.pfind(EID);A4)
|
Use
K = w filtering mode; in this case w is T i .pfind( K ) and entity table records making EID=A4.pfind(EID) get null or
false are discarded; EID is the selected field, to which its sequence numbers
in table sequence A5 are assigned.
（原文含示意图，图像资源未迁移）
|
=A2.cursor(EID,NAME;EID:A5;A4)
|
Use
K : T i filtering mode; compare the
entity table’s EID values with the table sequence’s key values and discard
entity table records that cannot match .
（原文含示意图，图像资源未迁移）
|
=A2.cursor(NAME,SALARY;EID:A5;A4)
|
This
is a case where K isn’t selected
EID isn’t the selected field, so only filtering is performed.
（原文含示意图，图像资源未迁移）
|
=A2.cursor(EID,NAME;EID:A5:null;A4)
|
Use
K : T i :null
filtering mode; compare the entity
table’s EID values with the table sequence’s key values and discard entity
table records that can match .
（原文含示意图，图像资源未迁移）
|
=A2.cursor(EID,NAME;EID:A5:#;A4)
|
Use K : T i :# filtering
mode; compare with sequence numbers of table sequence’s records according to
the entity table’s EID values, and discard records that cannot match.
（原文含示意图，图像资源未迁移）
|
=connect("demo").query("select
top 2 NAME,GENDER from employee").keys(NAME)
|
Return
a table sequence using NAME as the key.
（原文含示意图，图像资源未迁移）
|
=A2.cursor(EID,NAME;(EID=A5.find(EID),NAME=A12.find(NAME),EID!=null
&&NAME!=null);A4)
|
Use
( K 1 = w 1 , … K i = w i , w ) filtering mode; return records that
meet all conditions.
（原文含示意图，图像资源未迁移）
Use
@w option to recognize update mark:
|
=connect("demo").cursor("select EID,NAME from employee")
|
=file("e-mcs.ctx")
|
Generate
a composite table file.
|
=A2.create@y(#EID,NAME)
|
Create
the composite table’s base table.
|
=A3.append(A1)
|
Append
cursor A1’s data to the composite table’s base table.
|
=A4.cursor@m(;;3)
|
Create
a 3-subcursor multicursor based on the composite table.
|
=connect("demo").cursor("select EID,NAME,GENDER from employee")
|
=A6.derive(:Defiled)
|
=A7.new(EID,Defiled,NAME,GENDER)
|
Return
a cursor whose content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("ecd.ctx":[1,2])
|
Define
a homo-name files group: 1.ec.ctx and 2.ec.ctx .
|
=A9.create@yd(#EID,Defiled,NAME,GENDER;if(GENDER=="F",1,2))
|
Create
a multizone composite table, set EID as its key, use @d option to make
Defiled as the update mark field, and put records where GENDER is F to 1.ed.ctx
and the other records to 2.ed.ctx .
|
=A10.append@ix(A8)
|
Append
cursor A8’s data to A10’s multizone composite table.
|
=create(EID,Defiled,NAME,GENDER).record([0,true,,,1,true,,,
2,false,"BBB","F"])
|
Return
a table sequence whose content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("ecd.ctx":[3])
|
=A13.create@yd(#EID,Defiled,NAME,GENDER;3)
|
Add
a new zone table 3.ecd.ctx , where Defiled is the update mark field.
|
=A14.append@i(A12)
|
Append
table sequence A12’s records to zone table 3.ecd.ctx .
|
=file("ecd.ctx":[1,2,3]).open()
|
=A16.cursor(;;A5)
|
Split
A16’s multizone composite table synchronously according to multicursor A5,
and return a multicursor.
|
=A17.fetch()
|
Fetch
data from cursor A17.
（原文含示意图，图像资源未迁移）
|
=A16.cursor@w(;;A5)
|
Use
@w option to split multizone composite table ecd.ctx into multiple
segments according to multicursor A5, return the segmented composite table as
a multicursor, recognize the update mark – that is:
won’t
return the record whose EID value is 1 and whose update mark is true (meaning
to-be-deleted) to the result multicursor
Modify
the record whose EID value is 2 and whose update mark is false (meaning
to-be-modified)
Retain
the record whose primary key value is unique though it is marked by a
deletion mark – that is, return the record whose EID value is 0 to the result
multicursor.
|
=A19.fetch()
|
Fetch
data from cursor A19.
（原文含示意图，图像资源未迁移）
