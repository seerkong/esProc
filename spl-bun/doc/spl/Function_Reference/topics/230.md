# T.cursor( x i : C i ,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.cursor( x i : C i ,… )
T.cursor( x i : C i ,… )
=create(file).record(["D:/file/pseudo/empT.ctx"])
=pseudo(A1)
=A2.cursor()
=A3.fetch()
=A2.cursor(EID:eid,NAME,SALARY:salary)
=A5.fetch()
mcs.cursor(n)
=demo.query("select
=A1.cursor(3)
=A2.cursor()
```

## 提取内容
T.cursor( x i : C i ,… )
说明：
Get a cursor based on a pseudo table object.
语法：
T.cursor( x i : C i ,… )
注意：
The function gets a cursor based on pseudo table T by specifying field
expressions x i and field names C i , which,
by default, field names in the pseudo table. It gets a cursor using all fields
of the pseudo table when paramters x i : C i are
absent.
Only
supported by esProc Enterprise Edition.
参数：
|
T
|
pseudo table .
|
x i
|
A field
expression .
|
C i
|
Field
name in a result table sequence .
返回值：
游标
示例：
|
=create(file).record(["D:/file/pseudo/empT.ctx"])
|
=pseudo(A1)
|
Generate a pseudo table object .
|
=A2.cursor()
|
Generate a cursor from A2’s pseudo table using all its
fields, since no parameters are present .
|
=A3.fetch()
|
Below is data in A3’s cursor:
（原文含示意图，图像资源未迁移）
|
=A2.cursor(EID:eid,NAME,SALARY:salary)
|
Retrieve fields EID, NAME and SALARY from the pseudo
table to generate a cursor with new field names eid, NAME and salary
respectively .
|
=A5.fetch()
|
Below is data in A5’s cursor:
（原文含示意图，图像资源未迁移）
mcs.cursor(n)
说明：
Merge
subcursors in a multicursor into a unicursor or a new multicursor with a
smaller number of parallel subcursors.
语法：
mcs .cursor( n )
注意：
The
function merges subcursors in a multicursor into a unicursor, which is the
default, or a new multicursor with a smaller number, which is specified by
parameter n , of parallel subcursors.
参数：
|
mcs
|
multicursor.
|
n
|
Subcursor
column name.
Return
value:
游标/多游标
示例：
|
=demo.query("select
* from EMPLOYEE").cursor@m(5)
|
Return
a multicursor having 5 subcursors.
|
=A1.cursor(3)
|
Merge subcursors in the multicursor and
convert it to a 3-subcursor multicursor .
|
=A2.cursor()
|
Convert
a multicursor to a unicursor.
