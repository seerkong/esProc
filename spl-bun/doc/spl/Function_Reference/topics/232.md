# date(datetimeExp)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
date(datetimeExp)
date( datetimeExp )
date(now())
time( da t e t imeExp )
```

## 提取内容
date(datetimeExp)
说明：
Get
the date part of the datetime value.
语法：
date( datetimeExp )
注意：
The
function gets the date part of the datetimeExp .
参数：
|
datetimeExp
|
日期time
value.
返回值：
日期
示例：
|
date(now())
|
2013-12-09
相关函数：
da t e ( )
datet i me( d atet i meE x p )
time( da t e t imeExp )
dat e t i me()
tim e ()
