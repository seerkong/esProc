# datetime(dt)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
datetime(dt)
datetime( dt )
=datetime(now())
=datetime@m(now())
=datetime@s(now())
=datetime@h(now())
date( dat e t i meExp )
time( dat e ti m eExp )
```

## 提取内容
datetime(dt)
说明：
Adjust
the precision of the datetime expression and return it.
语法：
datetime( dt )
注意：
The
function adjusts and returns the precision of a specified datetime value . By default, the precision is the day.
参数：
|
dt
|
日期time
value.
选项：
|
@m
|
Accurate
to minute.
|
@s
|
Accurate
to second.
|
@h
|
Accurate
to hour.
返回值：
日期time
value
示例：
|
=datetime(now())
|
2013-12-09 00:00:00
|
=datetime@m(now())
|
2013-12-09 16:56:00
|
=datetime@s(now())
|
2013-12-09 16:56:45
|
=datetime@h(now())
|
2013-12-09 16:00:00
functions:
dat e ( )
date( dat e t i meExp )
time( dat e ti m eExp )
datetim e ()
tim e ()
