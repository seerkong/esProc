# day()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
day()
day( dt )
=day(datetime("19800227","yyyyMMdd"))
=day(datetime(12345))
=day(datetime("2006-01-15
=day@w(datetime("19800227","yyyyMMdd"))
=day@w(datetime("2006-01-15
=day(150)
```

## 提取内容
day()
说明：
Get
the day from a date value.
语法：
day( dt )
注意：
The
function gets the day from
date type data dt .
参数：
|
dt
|
日期
expression whose result is a date or a string of datetime format. can be an
integer, which is regarded as the value of computing days@o().
选项：
|
@w
|
Get
the day of the week from the specified date. For Sunday, return 1; For
Monday, return 2, and so on.
返回值：
Integer
示例：
|
=day(datetime("19800227","yyyyMMdd"))
|
=day(datetime(12345))
|
=day(datetime("2006-01-15
10:20:30"))
|
=day@w(datetime("19800227","yyyyMMdd"))
|
Return
4, which represents Wednesday .
|
=day@w(datetime("2006-01-15
10:20:30"))
|
Return
1, which represents Sunday .
|
=day(150)
|
相关函数：
ye a r ( )
mon t h ()
hou r ()
min u t e ()
secon d ()
millise c ond()
