# T.delete()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.delete()
=demo.query("select
=A1.delete(1)
=A1.delete([2,4,6])
=A1.select(EID>5)
=A1.delete(A4)
=A1.delete@n(1)
```

## 提取内容
T.delete()
说明：
Delete
specified records from a table sequence.
语法：
|
T . delete ( k )
|
Delete
the k th record .
|
T . delete ( p )
|
Delete
records whose sequence numbers exist in p .
|
T. delete ( A )
|
Delete
records that exist in sequence A .
注意：
The
function deletes the specified records from table sequence T . The deleted records will be always saved in the delete buffer.
参数：
|
T
|
table sequence.
|
k
|
positive integer, which specifies the position of a record to be deleted.
|
p
|
An
integer sequence with the length of n , which specifies the positions of the records to be deleted .
|
sequence, which specifies the records to be deleted.
选项：
|
@n
|
Return
the deleted record or a record sequence of the deleted records.
返回值：
Table
sequence
示例：
|
=demo.query("select
* from EMPLOYEE")
|
=A1.delete(1)
|
Delete
the first record .
|
=A1.delete([2,4,6])
|
Delete
the second, the fourth and the sixth records.
|
=A1.select(EID>5)
|
=A1.delete(A4)
|
Delete
the records whose EID is more than 5 .
|
=A1.delete@n(1)
|
Return
the deleted record .
注意：
We
use the store address, instead of the field names or field values, to judge
whether the records specified by A exist in T . So to delete the specified records, we generally use the function T .delete( T .select(…)) to locate them.
相关函数：
T .modi f y()
T .ins e rt()
A .de l ete ( )
