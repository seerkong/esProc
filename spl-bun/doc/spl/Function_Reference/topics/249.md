# A.delete ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.delete ()
=["a","c","d","e","f"]
=A1.delete([2,4,5])
=demo.query("select
=A3.delete@n(2)
=["a","c","d","e","f"].delete([3,-5])
```

## 提取内容
A.delete ()
说明：
Delete
specified members from a sequence.
语法：
|
A .delete( k )
|
Delete
the k th member .
|
A .delete( p )
|
Delete
members whose sequence numbers exist in p ; count backward when a
member of sequence p is less than 0.
注意：
The
function deletes the k th member or the members whose sequence numbers exist in p
from sequence A .
参数：
|
sequence.
|
k
|
positive integer that indicates the position of a member to be deleted in the
sequence.
|
p
|
An
integer
sequence with the length of n that specifies the
positions of the members to be deleted .
选项：
|
@n
|
Return
the deleted record or a record sequence of the deleted records.
返回值：
Sequence
示例：
|
=["a","c","d","e","f"]
|
=A1.delete([2,4,5])
|
[a,d]
|
=demo.query("select
* from STUDENTS")
|
=A3.delete@n(2)
|
Return the deleted record .
|
=["a","c","d","e","f"].delete([3,-5])
|
Count backward to
get the value [c,e,f] since one
member of the sequence parameter is less than 0 .
相关函数：
A .inse r t()
A .m o dify()
T .dele t e()
