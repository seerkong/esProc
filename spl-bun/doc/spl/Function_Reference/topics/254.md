# A.derive()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.derive()
=demo.query("select
=A1.derive(interval@y(BIRTHDAY, HIREDATE):EntryAge,
age(HIREDATE):WorkAge)
=A1.derive@m(age(HIREDATE):WorkAge)
=file("D:\\txt_files\\data1.txt").import@t()
=A4.derive@i(SCORE:score_not_null)
=demo.query("select
=demo.query("select
>A7.switch(DEPT,A6:DEPT)
=A7.derive(SALARY*5:BONUS)
=A7.derive@x(SALARY*5:BONUS)
=demo.query("select NAME,BIRTHDAY,HIREDATE from EMPLOYEE")
=A11.derive@o(age(HIREDATE):WorkAge)
=demo.query("select
=A1.derive(cum(SCORE;CLASS,STUDENTID):F1)
=A1.derive@z(cum(SCORE;CLASS,STUDENTID):F1)
```

## 提取内容
A.derive()
说明：
Add
one or more fields to a table sequence or a record sequence.
语法：
A . derive ( x i : F i ,…)
注意：
The
function adds Fi ,… field(s) to table
sequence / record sequence A , traverses each record of A , assigns each F i
with value x i
and returns a new table sequence consisting of the original fields and the new
field(s) Fi .
参数：
|
F i
|
字段名, which shouldn’t have the same name as any of the original fields in A .
|
x i
|
Expression,
whose results are used as the values of the derived fields .
|
table sequence/record sequence .
选项：
|
@m
|
Use
parallel algorithm to handle data-intensive or computation-intensive tasks to
enhance performance; and no definite order for the records in the result set .
|
@i
|
Won’t
generate a record if there is expression x i and its result is null .
|
@x( …;n )
|
Unfold
original fields whose values are records into n levels; default of n
is 2 .
|
@o
|
When
parameter A is a table sequence, add columns directly to it instead of
generating a new table sequence .
|
@z
|
Perform
the inverse operation; only applies to non-pure sequences .
返回值：
Table
sequence
示例：
|
=demo.query("select
NAME,BIRTHDAY,HIREDATE from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.derive(interval@y(BIRTHDAY, HIREDATE):EntryAge,
age(HIREDATE):WorkAge)
|
（原文含示意图，图像资源未迁移）
|
=A1.derive@m(age(HIREDATE):WorkAge)
|
Use
the @m option to increase performance of big data handling.
|
=file("D:\\txt_files\\data1.txt").import@t()
|
Below is the file data1.txt :
（原文含示意图，图像资源未迁移）
|
=A4.derive@i(SCORE:score_not_null)
|
If
the SCORE value is null, the corresponding record won’t be generated.
（原文含示意图，图像资源未迁移）
|
=demo.query("select
* from DEPARTMENT")
|
（原文含示意图，图像资源未迁移）
|
=demo.query("select
NAME,GENDER,DEPT,SALARY from EMPLOYEE")
|
>A7.switch(DEPT,A6:DEPT)
|
Switch
values of DEPT of A7’s table over with corresponding records.
|
=A7.derive(SALARY*5:BONUS)
|
Add
a new field:BONUS.
（原文含示意图，图像资源未迁移）
|
=A7.derive@x(SALARY*5:BONUS)
|
Use
@x option to unfold the DEPT field whose values are records; the default
unfolding levels are 2.
（原文含示意图，图像资源未迁移）
|
=demo.query("select NAME,BIRTHDAY,HIREDATE from EMPLOYEE")
|
Return a table
sequence .
|
=A11.derive@o(age(HIREDATE):WorkAge)
|
Add the new column
directly to the original table sequence rather than generating a new one
here the function returns same result as A11. Following is the result:
（原文含示意图，图像资源未迁移）
Perform
the inverse operation:
|
=demo.query("select
* from SCORES ")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A1.derive(cum(SCORE;CLASS,STUDENTID):F1)
|
Add F1 field and perform
iterative operation in a loop function to cumulatively sum SCORE values in
records having same CLASS and STUDENTID values, and use the result sums as F1
values:
（原文含示意图，图像资源未迁移）
|
=A1.derive@z(cum(SCORE;CLASS,STUDENTID):F1)
|
Use @z option to
perform the inverse operation:
（原文含示意图，图像资源未迁移）
注意：
The
difference between new() and derive(): The new() constructs a new table
sequence without changing the original one. By comparison, the derive() copies
the original fields and then adds new fields.
