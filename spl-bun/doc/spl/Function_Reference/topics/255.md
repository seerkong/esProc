# r.derive()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.derive()
=demo.query("select top 9 class,studentid,subject,score from
=A1(1)
=A2.derive("MARY":NAME,"F":gender)
```

## 提取内容
r.derive()
说明：
Add fields to a record.
语法：
r . derive ( x i : F i ,…)
注意：
The function adds F i ,… fields to record r ,
assigns x i to F i , and returns a new
record consisting of the original fields and the new fields.
参数：
|
F i
|
字段名s, which should not
be namesake with the existing fields of record r .
|
x i
|
Expressions, whose values are
used as field values .
|
r
|
A record .
返回值：
记录
示例：
|
=demo.query("select top 9 class,studentid,subject,score from
scores")
|
Return a
table sequence:
（原文含示意图，图像资源未迁移）
|
=A1(1)
|
Return
the 1 st record of table sequence A1:
（原文含示意图，图像资源未迁移）
|
=A2.derive("MARY":NAME,"F":gender)
|
Add new fields NAME and gender
to A2’s record and return the new record:
（原文含示意图，图像资源未迁移）
