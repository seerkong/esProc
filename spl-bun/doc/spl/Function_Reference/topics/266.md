# dism()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
dism()
dism( X,Y,C )
=covm(A1)
=dism(A1(1),A1(3),A2)
```

## 提取内容
dism()
说明：
Calculate
Mahalanobis distance between two vectors on covariance matrix.
语法：
dism( X,Y,C )
注意：
The
MathCli external library function (See
External Library Guide ) computes Mahalanobis distance between vector X and vector Y on covariance matrix C .
There is a same number of columns in vector X
/ Y and covariance matrix C .
参数：
|
X/Y
|
Vectors
of same dimension with the covariance matrix C .
|
covariance matrix .
Return
value:
数值型
value
示例：
|
[[1, 0, 2], [-1, 5, 0],
[0, 3, -9], [3, 2, 4], [-1, 1, 2]]
|
（原文含示意图，图像资源未迁移）
Generate
a matrix containing five samples.
|
=covm(A1)
|
（原文含示意图，图像资源未迁移）
Calculate
the general covariance matrix.
|
=dism(A1(1),A1(3),A2)
|
（原文含示意图，图像资源未迁移）
Calcualte
Mahalanobis distance between the 1 st sample and the 3 rd sample.
