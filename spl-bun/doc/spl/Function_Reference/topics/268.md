# T.dup()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.dup()
=file("D:/test0.ctx","169.254.121.62:8281")
=A1.open()
=A2.attach(table)
=A3.memory()
=A4.dup()
```

## 提取内容
T.dup()
说明：
Join
up a cluster in-memory table into a local in-memory table.
语法：
T. dup ()
注意：
The
function joins up cluster in-memory table T
to generate a local in-memory table.
Only
supported by esProc Enterprise Edition.
参数：
|
T
|
cluster in-memory table .
返回值：
In-memory table
示例：
|
=file("D:/test0.ctx","169.254.121.62:8281")
|
Open
a cluster file.
|
=A1.open()
|
Return
a cluster table.
|
=A2.attach(table)
|
Retrieve
cluster table table .
|
=A3.memory()
|
Generate
a cluster memory table.
|
=A4.dup()
|
Join up the cluster in-memory table to return
a local in-memory table.
（原文含示意图，图像资源未迁移）
