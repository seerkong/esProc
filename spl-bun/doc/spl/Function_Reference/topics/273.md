# dynadb.query()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
dynadb.query()
dynadb.query( sql )
dynadb.query( A,sql )
=dyna_open("http://localhost:8000",
=A1.query("select
=A1.query("select
=A1.query("select
=A1.query(A5,
>A1.close()
```

## 提取内容
dynadb.query()
说明：
Query
data stored in DynamoDB.
语法：
dynadb.query( sql )
dynadb.query( A,sql )
注意：
The
DynamodbCli external library function (See
External Library Guide ) queries data in DynamoDB using a SQL
statement.
参数：
|
sequence on which the sql statement is executed circularly while calculating
different parameter values
to
pass to sql for execution .
|
sql
|
SQL statement .
返回值：
Table
sequence
示例：
|
=dyna_open("http://localhost:8000",
"us-east-2")
|
=A1.query("select
* from emp")
|
=A1.query("select
* from emp where lname in (?,?,?) and gender =
?","Gary","Eddy","Lee","F")
|
=A1.query("select
* from emp where gender =?","M")
|
[201,'Joshua',205,'Emma',212,'Ryan']
|
=A1.query(A5,
"select * from emp where pid = ? and fname=?")
|
>A1.close()
