# P.enum()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.enum()
=["?<=60","?>=60
=demo.query("select
=A2.enum(A1,SCORE)
=A2.enum@r(A1,SCORE)
=A2.enum@p(A1,SCORE)
=["?<=60","?>=60
=A2.enum@n(A6,SCORE)
```

## 提取内容
P.enum()
说明：
Generate
a new record sequence by performing enumeration grouping on an record sequence
according to another record sequence.
语法：
P . enum( E , y )
注意：
The
function generates a new record sequence by grouping the record sequence P according to the record sequence/sequence E .
参数：
|
P
|
记录
sequence to be grouped .
|
E
|
记录
sequence/sequence .
|
y
|
field name or an expression in P . y is allowed to be omitted .
选项：
|
@ r
|
Repetitive
enum, that is, allocate a certain record of P to multiple groups .
|
@ p
|
The
return value is composed of sequence numbers of members that exist in P .
|
@n
|
Group
P ’s records according to E ’s members and return the result
groups. In the result set, there is a group to store the unaligned member(s) .
Function keyword:
|
?
|
It
represents the value of y .
返回值：
记录
sequence
示例：
E is a sequence:
|
=["?<=60","?>=60
&& ?<=90","?>=90"]
|
Use
y to replace ?
in the group condition when computing.
|
=demo.query("select
* from SCORES")
|
=A2.enum(A1,SCORE)
|
groups in total, and a member will not appear in multiple groups.
|
=A2.enum@r(A1,SCORE)
|
groups in total; 60 and 90 are allocated to multiple groups.
|
=A2.enum@p(A1,SCORE)
|
groups in total; return sequence numbers of members that exist in A2 by
group.
|
=["?<=60","?>=60
&& ?<=90"]
|
=A2.enum@n(A6,SCORE)
|
groups in total, group 1 for those not greater than 60, group 2 for those
greater than 60 and not greater than 90, and group 3 for the remaining
values.
注意：
p .enum() is mainly used to conduct the enum grouping on a single record
sequence. The grouping result is a sequence composed of multiple record sets,
and each record set is a group. This sequence is in an order exactly equal to
that of E . Therefore, you
can retrieve the associated group information from E via the sequence numbers. The group result is not a record
sequence, so no new data structure will be generated.
相关函数：
E .pen u m()
