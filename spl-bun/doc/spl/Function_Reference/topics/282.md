# env()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
env()
env ( v,x )
=env(arg1,12)
=arg1*10
=env(arg1)
=env@j(arg2,3)
```

## 提取内容
env()
说明：
Assign
value to the global variable.
语法：
env ( v,x )
注意：
The
function assigns value x to the
global variable v . It will lock v when calculating x to make sure the global variable won’t be changed during the
process. With x omitted, the global
variable will be deleted.
参数：
|
v
|
The
global variable .
|
x
|
Variable
value .
选项：
|
@j
|
Set
the global variable v as a task
variable .
Return
value:
The
variable value or null
示例：
|
=env(arg1,12)
|
assign 12 to the global variable arg1.
|
=arg1*10
|
120; use the global variable .
|
=env(arg1)
|
Delete
the global variable arg1 as the variable value is omitted
|
=env@j(arg2,3)
|
Set
the global variable arg2 as a task variable.
