# A.eq(B)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.eq(B)
= [1,2,5]
=[5,2,1]
=A1.eq(A2)
=["a","b","c"]
=["b","a","c"]
=A4.eq(A5)
=[5,2,2]
=A7.eq(A1)
```

## 提取内容
A.eq(B)
说明：
Judge
if a given sequence can be generated by adjusting the positions of the members
of another sequence.
语法：
A. eq ( B )
注意：
Judge
if sequence A can be generated by changing positions of the members of sequence B.
参数：
|
sequence expression .
|
sequence expression .
返回值：
Boolean
示例：
|
= [1,2,5]
|
=[5,2,1]
|
=A1.eq(A2)
|
true
|
=["a","b","c"]
|
=["b","a","c"]
|
=A4.eq(A5)
|
true
|
=[5,2,2]
|
=A7.eq(A1)
|
false
