# db.error()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
db.error()
=file("D://files//student.txt")
=A1.import@t()
=connect@e("demo")
>A3.execute@k(A2,"update
=A3.error@m()
>file("D:\\log.txt").write@a(A5)
>A3.close()
```

## 提取内容
db.error()
说明：
Obtain
the last error information from a database connection.
语法：
db . error ()
注意：
The
function returns the error information corresponding to the previous command
related to the database. 0 indicates error free
otherwise, the error code is returned. The error message can only be called one
time.
注意：
The connection to db should be one
created using connect@e, otherwise the
function cannot return the error information or
error code.
参数：
|
db
|
Database connection.
选项：
|
@m
|
Use
this option to return information in string .
示例：
|
=file("D://files//student.txt")
|
Data
in the file student.txt is
not correct
（原文含示意图，图像资源未迁移）
|
=A1.import@t()
|
=connect@e("demo")
|
Create
a connection and auto-control the commit and rollback operations.
|
>A3.execute@k(A2,"update
STUDENTS2 set NAME=?,GENDER=?,AGE=? where ID=?",NAME,GENDER,AGE,ID)
|
The
transaction is not committed.
|
=A3.error@m()
|
Return
the error message corresponding to sql command execution.
|
>file("D:\\log.txt").write@a(A5)
|
Append
error message to the log.
（原文含示意图，图像资源未迁移）
|
>A3.close()
|
Close
the connection.
相关函数：
db .cl o s e ()
conn e ct()
db .co m m i t()
db .rollba c k()
