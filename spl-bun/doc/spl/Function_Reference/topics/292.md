# es_open()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
es_open()
es_open ( host,..., user:passwd )
=es_open("localhost:9200”)
=es_open("http://localhost:9200","user":"un1234")
=es_open@s("https://localhosts:9200","user":"un1234")
```

## 提取内容
es_open()
说明：
Connect
to Elasticsearch (ES) server in the default http protocol.
语法：
es_open ( host,..., user:passwd )
注意：
The ElasticsearchCli external library function (See External Library Guide ) connects
to the ES server. Parameters user and
passwd can be omitted if there are
not username and password.
选项：
|
@s
|
The https protocol .
参数：
|
host
|
URL of the ES server, the format is:
http(s)://hostname:port, in which http(s) can be omitted; the default is http
protocol .
|
user
|
Username to connect to an ES server .
|
passwd
|
密码 to connect to an ES server .
Return
value:
REST Client object
示例：
|
=es_open("localhost:9200”)
|
Connect
to the ES server whose hostname is localhost
and port number is 9200 in the http
protocol.
|
=es_open("http://localhost:9200","user":"un1234")
|
Connect
to the ES server whose hostname is localhost
and port number is 9200 in the http
protocol; the username is user and
password is un1234 .
|
=es_open@s("https://localhosts:9200","user":"un1234")
|
Connect
to the ES server in https protocol; other parameters are the same as the
above.
