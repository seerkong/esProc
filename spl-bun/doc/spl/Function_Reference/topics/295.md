# es_put()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
es_put()
es_put ( fd, endpoint, entity ， header )
=es_open("localhost:9200","user":"un1234")
=es_put@f(A1, "/shirts", "D: /put_index.txt")
=es_put@f(A1, "/shirts/doc/2",
```

## 提取内容
es_put()
说明：
Execute
the put command and return a table sequence.
语法：
es_put ( fd, endpoint, entity ， header )
注意：
The ElasticsearchCli external library function (See External Library Guide ) executes
the put command. Parameter header can
be omitted; the REST API address can reach the index, the type or the record.
选项：
|
@f
|
Parameter entity can be a file .
参数：
|
fd
|
A REST Client object .
|
endpoint
|
Rest API address .
|
entity
|
A parameter
transmitted through http body, which is a JSON string or the name of JSON
file .
|
header
|
A variable parameter
that passes http header; it is a key type JSON string .
返回值：
Table sequence
示例：
|
=es_open("localhost:9200","user":"un1234")
|
Connect
to the ES server.
|
=es_put@f(A1, "/shirts", "D: /put_index.txt")
|
Create
an index for records under the shirts index.
|
=es_put@f(A1, "/shirts/doc/2",
"D:/put_index_record.txt")
|
Insert
a record whose id is 2 under doc type in the newly-created shirts index.
注意： Here’s the file put_ index.txt :
{
"mappings": {
"_doc": {
"properties": {
"brand": { "type": "keyword"},
"color": { "type": "keyword"},
"model": { "type": "keyword"}
}
The file put_shirt_ record.txt :
{
"brand":
"coop",
"color": "blue",
"model":
"girl"
}
