# db.execute()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
db.execute()
=demo.execute("insert
=demo.execute("delete
=demo.execute("update
=[["'TecSupport
=demo.execute(A4,"insert
=demo.query("select
=file("D://data_dept.txt").cursor@t()
=demo.execute(A7,"insert
=demo.execute("insert
```

## 提取内容
db.execute()
说明：
Execute
a SQL statement through a specified database connection.
语法：
|
db . execute ( sql {, args …} )
|
Execute the
SQL statement sql in database db . args,… is the parameter of sql , which can also be a sequence consisting of parameter values .
|
db . execute ( A/cs , sql {, args ,…} )
|
Execute
the SQL
statement sql in database db over sequence A/cs . args,… is the parameter of sql . The method can reduce the frequency
of database access. Here args
shouldn’t be a a sequence type parameter .
注意：
The
function executes a SQL statement sql
through the specified database connection. It is generally used to execute the
SQL statements that change the records in a database (such as update and
insert).
参数：
|
db
|
数据源连接.
|
sql
|
SQL statement in the form of select
* from table , for example .
|
args
|
An
argument value passed into the specified SQL command sql . It can be a constant or an expression. The comma is used to
separate multiple arguments .
|
sequence. the SQL statement is executed on each member of A . Generally args is computed against each member of A and the value is passed to the SQL statement for execution .
|
cs
|
cursor .
选项：
|
@k
|
After
the execution is completed, the transaction won't be committed. by default
the transaction will be committed .
|
@ s
|
No
preparation will be done for transaction processing .
示例：
|
=demo.execute("insert
into DEPARTMENT (DEPT, MANAGER)values(?,?)","TecSupport",5)
|
Insert
a record into the table.
|
=demo.execute("delete
from DEPARTMENT where DEPT='TecSupport'")
|
Delete
the records whose DEPT values are '' TecSupport '' .
|
=demo.execute("update
DEPARTMENT set MANAGER = ? where DEPT='Sales'","7")
|
Modify
the MANAGER value of
the record whose DEPT value is 'Sales' into 7.
|
=[["'TecSupport
",5],[" AppSupport",9]].new(~(1):Dept,~(2): Manager)
|
（原文含示意图，图像资源未迁移）
|
=demo.execute(A4,"insert
into DEPARTMENT (DEPT, MANAGER) values (?,?)", #1,#2)
|
# 1 and # 2 stand for the first and second columns
respectively in A4 .
|
=demo.query("select
* from DEPARTMENT")
|
（原文含示意图，图像资源未迁移）
|
=file("D://data_dept.txt").cursor@t()
|
Below
is data_dept.txt :
（原文含示意图，图像资源未迁移）
|
=demo.execute(A7,"insert
into DEPARTMENT (DEPT, MANAGER)values (?,?)", #1,#2)
|
A7 is a cursor .
|
=demo.execute("insert
into DEPARTMENT(DEPT, MANAGER) values(?)",["TecSupport",9])
|
a args is a sequence of parameter
values. It is automatically converted into =demo.execute("insert
into DEPARTMENT(DEPT, MANAGER) values(?,?)","TecSupport",9) before execution. Same result
as A1’s.
相关函数：
db .qu e r y ()
db .pr o c ( )
