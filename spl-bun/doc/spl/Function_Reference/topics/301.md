# f.exists()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
f.exists()
=file("D://p1.splx").exists()
```

## 提取内容
f.exists()
说明：
Check
if a file exists.
语法：
f .exists()
注意：
The
function checks if file f exists
return true if it exists, and false if it doesn’t exist.
参数：
|
f
|
file oject .
返回值：
Boolean
示例：
|
=file("D://p1.splx").exists()
|
Return
true if the file exists.
相关函数：
f . da t e()
f . si z e ( )
movefil e ()
