# A.export()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.export()
=demo.query("select
=A1.export()
=A1.export(;"|")
=A1.export@t(EID:id,NAME:name;",")
=A5.export()
=A1.export@c()
=A1.export@w()
=["12\r34","aa\nbb"]
=A9.export()
=A9.export@q()
=xml(A12)
=A13.export()
=file("D://t4.txt").import@coq()
=A15.export@coq()
```

## 提取内容
A.export()
说明：
Convert
a sequence into a string.
语法：
A .export ( x: F ,… ;s )
注意：
The
function separates each selected fields x of records in table
sequence/record sequence/sequence A with the user-defined separator and
returns the result as a string. The name of the resulting field in the string
is F. If x isn’t specified, then export all fields. Return a one-field
string without a field name if A is a simple sequence; use separator
when A 's members are sequences. A serial byte value is stored as
an integer.
If
parameters x:F are absent and if A is a sequence of record, then
these records should be of same structure.
The
function returns JSON strings when members of each member of A is
record, sequence or table sequence; and returns a hexadecimal string when
records or members of A are serial byte keys.
注意：
In the returned string, use line break to separate records and custom delimiter
to separate fields. By default, use TAB to separate both.
参数：
|
table sequence/record sequence/sequence to be exported .
|
x
|
Fields
to be exported; if omitted, then export all fields of A .
|
F
|
Name
of the resulting field in the string; if omitted, then use the original field
name .
|
s
|
User-defined
field separator; default is TAB .
选项：
|
@t
|
The
column names will be written to the string as the first record .
|
@c
|
Use comma as
the separator when parameter s
is absent .
|
@w
|
Use
Windows-style \r\n line break; by default, the line break is specified by the
operating system .
|
@q
|
Enclose
the exported text field values and headers with quotation marks .
|
@o
|
Perform escaping according to the Excel rule,
which treats two double quotation marks as one and does not escape the other
characters; need to work with @q .
返回值：
字符串
示例：
|
=demo.query("select
EID,NAME from EMPLOYEE")
|
=A1.export()
|
（原文含示意图，图像资源未迁移）
Parameters
x , F , and s are omitted .
|
=A1.export(;"|")
|
（原文含示意图，图像资源未迁移）
Specify
the separator as "|".
|
=A1.export@t(EID:id,NAME:name;",")
|
（原文含示意图，图像资源未迁移）
Specify
to-be-exported fields and the separator; make column names the first record
and write it at the beginning of the exported string.
|
[1,23,34,45]
|
A sequence .
|
=A5.export()
|
（原文含示意图，图像资源未迁移）
|
=A1.export@c()
|
（原文含示意图，图像资源未迁移）
With @c option, use default separator
comma when parameter s is absent .
|
=A1.export@w()
|
=["12\r34","aa\nbb"]
|
=A9.export()
|
（原文含示意图，图像资源未迁移）
|
=A9.export@q()
|
（原文含示意图，图像资源未迁移）
With @q option, the exported content
contains quotation marks.
|
<xml><row><DEPTID>1</DEPTID><DEPTNAME>sale</DEPTNAME>
<FATHER>12</FATHER></row><row><DEPTID>10</DEPTID>
<DEPTNAME>create</DEPTNAME><FATHER>12</FATHER></row></xml>
|
=xml(A12)
|
Return a sequence
of table sequences:
（原文含示意图，图像资源未迁移）
Expand members of
the sequence as follows:
（原文含示意图，图像资源未迁移）
Then expand
sub-members of the sequence as follows:
（原文含示意图，图像资源未迁移）
|
=A13.export()
|
Return JSON strings .
（原文含示意图，图像资源未迁移）
|
=file("D://t4.txt").import@coq()
|
Below is content of t4.txt :
（原文含示意图，图像资源未迁移）
A15’s result:
（原文含示意图，图像资源未迁移）
|
=A15.export@coq()
|
With @o option,
two double quotation marks are treated as one .
（原文含示意图，图像资源未迁移）
相关函数：
f .i m p o r t()
f.exp o rt()
