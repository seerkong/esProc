# f.export(A/cs,x:F,…;s)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
f.export(A/cs,x:F,…;s)
=demo.query( " select
=file( " Dep.txt " ).export(A1; " | " )
=demo.query( " select
=file( " Dep.txt " ) . export@t(A1)
=demo.query( " select
=file( " Dep.txt " ).export@a(A1)
=demo. cursor ("select EID,NAME,BIRTHDAY,SALARY from
=file("emp.txt").export@t(A1,NAME,age(BIRTHDAY):Age)
=file("emp 2 .txt").export@t(A1, #1,#2 )
=demo.query( " select
=file( " Dep.btx " ).export@b(A1)
=demo.query("select
=A1.array()
=file("emp_b.btx").export@b(A2)
=file("emp_wb.btx").export@wb(A2)
=10000.new(~:id,rand(10):xb).sort(xb)
=file("stest.btx").export@b(A1;xb)
=file("myfile.txt").export(A1)
=demo.query("select EID,NAME,DEPT from EMPLOYEE order by
=demo.query("select DEPT,MANAGER from
=A1.switch(DEPT,A2:DEPT)
=file("empswtich.txt").export@t(A3)
=demo.query("select EID,NAME,DEPT,SALARY from EMPLOYEE")
=A1.group(DEPT).new(~:DEPT)
=file("empj.txt").export(A2)
=demo.query( " select
=file( " Dep.txt " ).export@qt(A1,#1)
=A1.import@coq()
=file("o.txt").export@coq(A2)
```

## 提取内容
f.export(A/cs,x:F,…;s)
说明：
Write
data of a sequence/cursor to a file.
语法：
f . export ( A/cs , x: F ,… ;s)
注意：
The function computes
expression x on sequence/cursor A/cs and writes the data to file f
in the form of text.
Write
all fields to the file when parameter x is absent; if values of a
to-be-exported field are referenced records, just write their primary key
values to f . F is the field name of x in
the result set.
When
file f does not exist, automatically
create the file (cannot automatically create the path directory). The created
file is of text format by default.
When
A is a sequence and parameter x
is absent, write data to file f in
the form of single-column text without field name.
By
default, write data as JSON strings when members of each member of A are
sequences/records/table sequences.
If
A contains a serial byte field, write
the serial byte values as hexadecimal strings; use the original data type with
the binary export. The unexportable fields (such as images) in A can’t be written into file f .
Format of the txt file: separate records by
carriage return; separate fields by the custom delimiter, which is tab by
default.
参数：
|
f
|
file object, which can be a data file of txt, csv and btx format . Use f.xlsexport() to write data of an Excel file.
|
A/cs
|
The
sequence/cursor to be exported; use separators to export when members are
sequences .
|
x
|
The
field to be exported; if omitted, all the fields of A that can be textualized will be exported; the sign # is used to
represent a field with a sequence number .
|
F
|
Result
field name; if omitted, then use x .
|
s
|
The
user-defined separator used in the text file, and the default separator is
tab .
选项：
|
@t
|
Export
field description, or the headers, as the first row of the file .
|
@a
|
Append.
The appended records and the original records should be of the same
structure, otherwise error will be reported. Ignore @t option if there is
data in the original file; overwrite the original file by default .
|
@b
|
Convert
into binary file to speed up the processing and ignore @t options; won’t
segment A if it is small enough, otherwise it will be segmented when
being converted into the binary format
With
the option, parameter s represents a field or field expression of
sequence A ; when parameter s is present, A is regarded
as ordered by s and create a segment only when s is changed; s
should be different for the newly-added data at append .
|
@c
|
Use comma as
the separator when parameter s
is absent .
|
@w
|
Use Windows-style \r\n line break; by
default, the line break is specified by the operating system; export a
sequence of sequences as structured data where the first row is field names
when @b option is also present.
|
@q
|
Enclose the exported text field values and
headers with quotation marks.
|
@o
|
Perform
escaping according to the Excel rule, which identifies two double quotation
marks as one and does not escape the other characters .
示例：
Write a
table sequence to txt file:
|
=demo.query( " select
* from DEPARTMENT " )
|
=file( " Dep.txt " ).export(A1; " | " )
|
Write
all fields in table sequence A1 to file, use | as the separator, and generate
a file as follows:
（原文含示意图，图像资源未迁移）
Use @t
option to write the first row as the title:
|
=demo.query( " select
* from DEPARTMENT " )
|
=file( " Dep.txt " ) . export@t(A1)
|
Generate
a file as follows:
（原文含示意图，图像资源未迁移）
Use @a
option to append data to file:
|
=demo.query( " select
* from DEPARTMENT " )
|
=file( " Dep.txt " ).export@a(A1)
|
Append
A1’s content to its end.
（原文含示意图，图像资源未迁移）
Export
specified fields:
|
=demo. cursor ("select EID,NAME,BIRTHDAY,SALARY from
EMPLOYEE")
|
=file("emp.txt").export@t(A1,NAME,age(BIRTHDAY):Age)
|
Export
cursor A1’s NAME field and BIRTHDAY field; set Age as the result field name
for computing BIRTHDAY field expression.
（原文含示意图，图像资源未迁移）
|
=file("emp 2 .txt").export@t(A1, #1,#2 )
|
Use
#1 and #2 to represent the first and the second fields in A1.
（原文含示意图，图像资源未迁移）
Use @b
option to write data to bin file:
|
=demo.query( " select
* from DEPARTMENT " )
|
=file( " Dep.btx " ).export@b(A1)
|
The
bin file to which data is written has column titles by default.
（原文含示意图，图像资源未迁移）
Use @wb
options to export a sequence of sequences as structured data:
|
=demo.query("select
EID,NAME,DEPT,SALARY from EMPLOYEE")
|
Return a table sequence:
（原文含示意图，图像资源未迁移）
|
=A1.array()
|
Return a sequence of sequences:
（原文含示意图，图像资源未迁移）
|
=file("emp_b.btx").export@b(A2)
|
Export A2 to bin file emp_b.btx , whose
content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("emp_wb.btx").export@wb(A2)
|
Use @wb options to export A2’s sequence of
sequences to bin file emp_wb.btx in the form of structured data. Below
is the file content:
（原文含示意图，图像资源未迁移）
Use @b
option to export as a segmented bin file:
|
=10000.new(~:id,rand(10):xb).sort(xb)
|
Generate
a table sequence ordered by xb field.
|
=file("stest.btx").export@b(A1;xb)
|
Write
data as a bin file segmented by xb field, during which a new segment is
created when xb field value is changed.
Write
data of a sequence and parameter x is absent:
|
[a,s,d,f]
|
=file("myfile.txt").export(A1)
|
Generate
a single-column file without the field name.
（原文含示意图，图像资源未迁移）
When
values of the to-be-exported field in the table sequence are referenced
records:
|
=demo.query("select EID,NAME,DEPT from EMPLOYEE order by
GENDER")
|
=demo.query("select DEPT,MANAGER from
DEPARTMENT").keys(MANAGER)
|
=A1.switch(DEPT,A2:DEPT)
|
DEPT
values are referenced records.
（原文含示意图，图像资源未迁移）
|
=file("empswtich.txt").export@t(A3)
|
The
exported file is as follows:
（原文含示意图，图像资源未迁移）
Write
data as JSON string:
|
=demo.query("select EID,NAME,DEPT,SALARY from EMPLOYEE")
|
=A1.group(DEPT).new(~:DEPT)
|
Return
a sequence whose members are records:
（原文含示意图，图像资源未迁移）
|
=file("empj.txt").export(A2)
|
Export
the file as JSON strings:
（原文含示意图，图像资源未迁移）
Use @q
option to generate a text file where both field values and column headers are
quoted:
|
=demo.query( " select
* from DEPARTMENT " )
|
=file( " Dep.txt " ).export@qt(A1,#1)
|
（原文含示意图，图像资源未迁移）
Use @o
option to treat two continuous quotation marks as one:
|
f1,f2,f3
2,"dd""ff",3
|
=A1.import@coq()
|
（原文含示意图，图像资源未迁移）
|
=file("o.txt").export@coq(A2)
|
As
@o option is present, treat the two quotation marks in the string as one and
the exported t5.txt is as follows:
（原文含示意图，图像资源未迁移）
相关函数：
f .im p ort()
