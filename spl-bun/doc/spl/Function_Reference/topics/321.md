# ch.fetch()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.fetch()
=demo.cursor("select * from SALES")
=channel()
=channel()
=A2.select(ORDERID>100)
=A3.select(ORDERID>100)
=A2.fetch()
=A3.fetch( "sales.btx" )
=A1.push(A2,A3)
=A1.fetch()
=A2.result()
```

## 提取内容
ch.fetch()
说明：
Fetch
and store the existing data in a channel.
语法：
ch . fetch ( f )
注意：
The
function fetches and stores the existing data in channel ch ; write result of
data retrieval to bin file f when parameter f
is present . It is a result set generation function.
参数：
|
ch
|
管道 .
|
f
|
bin file .
Return
value:
管道
示例：
|
=demo.cursor("select * from SALES")
|
=channel()
|
Create
a channel.
|
=channel()
|
=A2.select(ORDERID>100)
|
Attach
ch.select() operation to the channel.
|
=A3.select(ORDERID>100)
|
=A2.fetch()
|
Attach ch.fetch()
function that returns the final result to A2’s channel while retaining the
exisitng data in the channel .
|
=A3.fetch( "sales.btx" )
|
Attach ch.fetch()
function that gets the final result set to A3’s channel, and define writing
result to bin file sales.btx in the main directory .
|
=A1.push(A2,A3)
|
Push
A1’s data in into the channel.
|
=A1.fetch()
|
=A2.result()
|
（原文含示意图，图像资源未迁移）
Get
the result of performing the operation from the channel.
