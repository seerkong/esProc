# r.field(F)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.field(F)
=demo.query("select * from EMPLOYEE")(1).field(2)
=demo.query("select * from
=demo.query("select * from
=demo.query("select * from
A.field ( F,x)
```

## 提取内容
r.field(F)
说明：
Get
the value of a specified field in a record.
语法：
r .field( F )
注意：
The
function gets value of the F th field in record r , or value of string parameter F . Count backward if F <0. Return null if F exceeds the boundary number of the
fields or it does not exist.
参数：
|
r
|
record .
|
F
|
The
sequence number of a field, or the string parameter in which a variable can
be used to reference the field name .
返回值：
The
field value
示例：
|
=demo.query("select * from EMPLOYEE")(1).field(2)
|
Return
the second field value of the first record in table sequence EMPLOYEE .
|
=demo.query("select * from
EMPLOYEE")(1).field(-2)
|
Return the second-to-last field value in the first
record of the table sequence EMPLOYEE .
|
=demo.query("select * from
EMPLOYEE")(1).field(arg1)
|
arg1 is a variable .
|
=demo.query("select * from
EMPLOYEE")(1).field("SALARY")
|
Return
SALARY value in the first record of table sequelcne EMPLOYEE .
functions:
A .fiel d ( F )
A.field ( F,x)
r.fiel d ( F ,x)
