# r.field(F,x)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.field(F,x)
=demo.query("select * from EMPLOYEE")
=A1(1).field(2,"Admin1")
=A1(1).field(-1,10000)
>arg1="SURNAME"
=A1(1).field(arg1,"Lily")
=A1.field(-1)+=1
A.field ( F, x)
```

## 提取内容
r.field(F,x)
D escription:
Modify
a value of the specified field of a record .
语法：
r . field( F,x )
注意：
The
function modifies the value of the F th field of record r , or the value of string parameter F , into x . Count backward
if F <0. Do not execute if F exceeds the boundary number of the
fields or it does not exist.
参数：
|
r
|
A record.
|
F
|
The sequence number of a field , or the string parameter in which a variable can be used to
reference the field name.
|
x
|
Expression
whose computing result is the value of the F th field, or the value of the string
parameter F .
返回值：
None
示例：
|
=demo.query("select * from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1(1).field(2,"Admin1")
|
Change
the value of the 2nd field of the record 1 to Admin1 .
|
=A1(1).field(-1,10000)
|
Modify
the value of record 1’s last field into 10,000.
|
>arg1="SURNAME"
|
=A1(1).field(arg1,"Lily")
|
Modify the value of record 1’s SURNAME field into Lily .
|
{Au:0,Ag:0,Cu:0}
|
=A1.field(-1)+=1
|
Change
the last field value in A1’s record according to “original value +1”. Below
is the modified record:
（原文含示意图，图像资源未迁移）
相关函数：
A .fiel d ( F )
A.field ( F, x)
r .field( F )
