# A.field(F)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.field(F)
=demo.query("select
=A1.field(2)
=A1.field(-2)
=A1.field("NAME")
>arg1="NAME"
=A1.field(arg1)
=demo.query("select * from EMPLOYEE where EID < 20")
=A7.group(STATE;~.avg(SALARY):avg,~:group)
=A8.field@r(3)
r.field( F, x)
A.field(F,x)
```

## 提取内容
A.field(F)
说明：
Get
values of a specified field of
a sequence.
语法：
A .field( F )
注意：
The
function loops through parameter A to
get the value of the F th
field from each record, or the value of every string parameter F , and returns a sequence consisting of
these values. Count backward if F <0.
Return null if F exceeds the boundary
number of the fields or it does not exist.
参数：
|
sequence.
|
F
|
The
sequence number of a field, or the string parameter in which a variable can
be used to reference the field name.
选项：
|
@r
|
When the return value is a record or record
sequence, loop its members iteratively to get desired values.
返回值：
Sequence
示例：
|
=demo.query("select
* from EMPLOYEE")
|
=A1.field(2)
|
Return
a sequence composed of values of the second field.
|
=A1.field(-2)
|
Return
a sequence comprising values of the second-to-last field.
|
=A1.field("NAME")
|
Return
a sequence composed of values of NAME field.
|
>arg1="NAME"
|
=A1.field(arg1)
|
Same
as A4.
|
=demo.query("select * from EMPLOYEE where EID < 20")
|
=A7.group(STATE;~.avg(SALARY):avg,~:group)
|
（原文含示意图，图像资源未迁移）
|
=A8.field@r(3)
|
After
A8’s 3 rd column is returned, go on to return the 3 rd
column of the return value.
（原文含示意图，图像资源未迁移）
相关函数：
r .fie l d ( F )
r.field( F, x)
A.field(F,x)
