# A.field(F,x)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.field(F,x)
=demo.query("select top
=A1.field(9,8000)
=A1.field("SALARY",7000)
>arg1="SALARY"
=A1.field(arg1,7000)
=A1.field(9,[8000,6500,9000])
=create(ID,age).record([1,20,2,18])
=A1.field(-1)+=1
r.field(F, x )
```

## 提取内容
A.field(F,x)
说明：
Modify
the value of a specified field in a sequence.
语法：
A . field( F,x )
注意：
The function assigns
members of sequence x sequentially to
values of F th field in
sequence A , or values of string
parameter F . Count backward if F <0. Do not execute if F exceeds the boundary number of the
fields or it does not exist.
参数：
|
sequence.
|
F
|
The sequence number of a field , or the string parameter in which a variable can be used to
reference the field name.
|
x
|
sequence whose length is equal to the number of values in F th field, or a single
value, which can be considered as a sequence in which members have the same value
and their number is equal to the number of values in F th field.
返回值：
None
示例：
|
=demo.query("select top
3 * from EMPLOYEE")
|
=A1.field(9,8000)
|
Modify
the value of the ninth field; parameter x
is a single value, and it will be regarded as the sequence [8000,8000,8000]
here’s
A1’s result:
（原文含示意图，图像资源未迁移）
|
=A1.field("SALARY",7000)
|
Modify
all values of SALARY field
into 7000.
|
>arg1="SALARY"
|
=A1.field(arg1,7000)
|
Same
as A3.
|
=A1.field(9,[8000,6500,9000])
|
Here
parameter x is a sequence, and this
is A1’s result:
（原文含示意图，图像资源未迁移）
|
=create(ID,age).record([1,20,2,18])
|
Return a sequence
（原文含示意图，图像资源未迁移）
|
=A1.field(-1)+=1
|
Change the last field value in A1’s record
according to “original value +1”. Below is the modified sequence:
（原文含示意图，图像资源未迁移）
相关函数：
r .fiel d ( F )
A .fie l d ( F )
r.field(F, x )
