# file(fn:cs,h)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
file(fn:cs,h)
=file("D:/City.txt":"UTF-8",["192.168.46.1:8281"])
=A1.import@t()
=file("tmp.txt",["192.168.31.165:8281"])
>A3.write("hello
```

## 提取内容
file(fn:cs,h)
说明：
Return
a remote file from a node.
语法：
file (
fn : cs,h )
注意：
The
function returns remote file fn from
node h . As a local file, the remote
one is both readable and writable.
参数：
|
fn
|
To-be-loaded
file represented by an absolute path or a relative path that is relative to
the main directory.
|
cs
|
Character
set, which is a JVM built-in one; use the system default when the parameter
is absent.
|
h
|
node.
Return
value:
File
object
示例：
|
=file("D:/City.txt":"UTF-8",["192.168.46.1:8281"])
|
Return
City.txt file under D:/ directory in node 192.168.46.1:8281
using character set UTF-8.
|
=A1.import@t()
|
Read
in content of A1’s file.
|
=file("tmp.txt",["192.168.31.165:8281"])
|
Return
tmp.txt file under the main directory in node 192.168.31.165:8281.
|
>A3.write("hello
world!")
|
Write
string "hello world!" to A3’s file.
