# file(fn:cs,hs)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
file(fn:cs,hs)
=file("City.txt":"UTF-8",["192.168.46.1:8281","192.168.31.165:8281"])
=A1.read()
```

## 提取内容
file(fn:cs,hs)
说明：
Return
a remote file from a sequence of nodes.
语法：
file (
fn : cs,hs )
注意：
The
function searches the local machine for remote file fn from a sequence of nodes hs
it searches the nodes in turn if it cannot find the targe file locally. The
returned file is read-only.
参数：
|
fn
|
To-be-loaded
file represented by an absolute path or a relative path that is relative to
the main directory.
|
cs
|
Character
set, which is a JVM built-in one; use the system default when the parameter
is absent.
|
hs
|
sequence of nodes.
Return
value:
File
object
示例：
|
=file("City.txt":"UTF-8",["192.168.46.1:8281","192.168.31.165:8281"])
|
Find
City.txt file under the main directory in the sequence of nodes
["192.168.46.1:8281","192.168.31.165:8281"] using character set UTF-8.
|
=A1.read()
|
Read
in content of A1’s file.
