# file(fn:z,f)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
file(fn:z,f)
file ( f n:z,f )
=["192.168.126.1:8281","192.168.31.165:8281"]
=file("emCp.ctx":[2,3],A1)
=file("embak.ctx",A2)
```

## 提取内容
file(fn:z,f)
说明：
Generate
a new cluster homo-name files group with same distribution as an existing
homo-name files group.
语法：
file ( f n:z,f )
注意：
The
function generates a new cluster homo-name files group fn using the distribution style of homo-name files group f . Parameter z is zone table
number, and, when it is absent, the function use the zone table number(s) in f .
参数：
|
fn
|
would-be cluster homo-name files group.
|
z
|
Zone
table number, which is an integer or an integer sequence.
|
f
|
cluster homo-name files group.
Return
value:
Cluster
homo-name files group
示例：
|
=["192.168.126.1:8281","192.168.31.165:8281"]
|
sequence of nodes.
|
=file("emCp.ctx":[2,3],A1)
|
Return
a homo-name files group.
|
=file("embak.ctx",A2)
|
Generate
a cluster homo-name files group having the same distribution type as A2’s; as
parameter z is absent, use the corresponding value in A2, that is,
[2,3].
