# A.f ind()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select
=A1.keys(NAME,DEPT)
=A1.find(["Alexis","Sales"])
=demo.query("select
=A4.keys(EID)
=A4.find@b(3)
=A4.find@k(2,15,69,220)
=A4.find(2,15,69,220)
=demo.query("select
=A9.keys@t(STATE,HIREDATE)
=A9.find(["Florida",date("2006-03-12")])
=A9.find("Florida")
```

## 提取内容
A.f ind()
说明：
Find
a record according to its primary key value.
语法：
A . find ( k )
注意：
The
function finds records from a record sequence/table sequence whose primary key
values are k . Use the index table if
there is one . When
parameter A has a time key,
among records that have same base key values and where the time key value is
not greater than the specified k value, find the record whose time key value is
the largest. If there isn’t time key value in the primary key, take the time
calculated through now() function, which is the latest time.
参数：
|
record sequence/table sequence.
|
k
|
Primary key, which will be written as a
sequence if it is a multi-field primary key .
选项：
|
@b
|
Use
the binary search. A must be
ordered by the primary key; otherwise, the result will be wrong. T he index table will be ignored.
|
@k
|
Find
multiple records; return members of A
matching the sequence of key values specified by parameter k ; by default, the function returns the first-found record whose
primary key is k .
返回值：
记录s
示例：
|
=demo.query("select
* from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.keys(NAME,DEPT)
|
=A1.find(["Alexis","Sales"])
|
（原文含示意图，图像资源未迁移）
A sequence is used
since the key contains two fields.
|
=demo.query("select
* from EMPLOYEE")
|
This
table sequence is ordered by the EID field.
|
=A4.keys(EID)
|
=A4.find@b(3)
|
（原文含示意图，图像资源未迁移）
@b is used to enable the binary search in order to speed up the
computation.
|
=A4.find@k(2,15,69,220)
|
（原文含示意图，图像资源未迁移）
With @k option, the function returns members matching the specified
key values .
|
=A4.find(2,15,69,220)
|
（原文含示意图，图像资源未迁移）
Return the record whose primary key value is
matched as @k option is absent.
|
=demo.query("select
EID,NAME,STATE,HIREDATE from EMPLOYEE where GENDER='M'")
|
（原文含示意图，图像资源未迁移）
|
=A9.keys@t(STATE,HIREDATE)
|
Set STATE as the base key and
HIREDATE as the time key .
|
=A9.find(["Florida",date("2006-03-12")])
|
（原文含示意图，图像资源未迁移）
Among records where STATE is “Florida” and
HIREDATE is earlier than 2006-03-12, find the one whose time key value is the
latest.
|
=A9.find("Florida")
|
（原文含示意图，图像资源未迁移）
Calculate the time key value using now(),
which is the latest date.
相关函数：
A .pfin d ( )
