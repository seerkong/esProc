# T.find(k,…)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.find(k,…)
=demo.cursor("select EID,NAME,GENDER from employee where EID<
=A1.memory()
=A2.keys(EID,NAME)
=A2.find([8,"Megan"])
=A2.keys(EID,GENDER)
=A2.find@k([[8,"F"],[2,"F"]])
=A2.keys(EID)
=A2.find(3)
```

## 提取内容
T.find(k,…)
说明：
Find
the record(s) according to the specified primary key value(s) from an in-memory
table.
语法：
T . find ( k,… )
注意：
The
function finds records where the primary key value is k from memory table T .
Use binary search if there isn’t an index.
参数：
|
T
|
An
in-memory table.
|
k
|
The
primary key; write multiple keys as a sequence.
选项：
|
@k
|
Find multiple records according
to multiple primary keys and return them as a record sequence; by default, the function returns the
first-found record whose primary key is k ; write multiple primary keys in the format of [[k1,k2],[...],...] .
Return
value:
记录/记录
sequence
示例：
|
=demo.cursor("select EID,NAME,GENDER from employee where EID<
10")
|
Return a cursor.
|
=A1.memory()
|
Return an in-memory table.
（原文含示意图，图像资源未迁移）
|
=A2.keys(EID,NAME)
|
Set EID and NAME as the keys of the in-memory
table
|
=A2.find([8,"Megan"])
|
The two primary keys are written as a
sequence.
（原文含示意图，图像资源未迁移）
|
=A2.keys(EID,GENDER)
|
Set EID and GENDER as the keys of the in-memory
table
|
=A2.find@k([[8,"F"],[2,"F"]])
|
Use @k option to find multiple records.
（原文含示意图，图像资源未迁移）
|
=A2.keys(EID)
|
Set EID as the key of the in-memory table.
|
=A2.find(3)
|
Find the record where EID is 3.
（原文含示意图，图像资源未迁移）
