# T.fname(i )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.fname(i )
=demo.query("select
=A1.fname(2)
=A1.fname()
```

## 提取内容
T.fname(i )
说明：
Get
the names of fields of the table sequence according to sequence numbers of
these fields.
语法：
T . fname( i )
注意：
The
function gets the name of the field whose sequence number is i from table sequence T. Return all field names in T If no parameter is present .
参数：
|
T
|
Table
sequence .
|
i
|
Sequence
number of a field .
返回值：
字符串/Sequence
示例：
|
=demo.query("select
EID,NAME,DEPT,SALARY from EMPLOYEE")
|
=A1.fname(2)
|
Return
“NAME”.
|
=A1.fname()
|
Return
a sequence comprising all field names in A1.
相关函数：
r .fna m e ()
