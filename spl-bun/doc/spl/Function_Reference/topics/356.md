# r.fname(i )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.fname(i )
=demo.query("select
=A1(1).fname(2)
=A1(1).fname()
```

## 提取内容
r.fname(i )
说明：
Get
field names from records according to sequence numbers of these fields.
语法：
r . fname( i )
注意：
The
function gets the name of the field whose sequence number is i from record r . If no parameter is present, then return all field names in r.
参数：
|
r
|
记录 .
|
i
|
Sequence
number of a field .
返回值：
Character
string/Sequence
示例：
|
=demo.query("select
* from EMPLOYEE")
|
=A1(1).fname(2)
|
“NAME”,
the name of the 2nd field in the first record is NAME.
|
=A1(1).fname()
|
Return
a sequence comprising all field names of the 1st record in A1.
相关函数：
T .fn a me ()
