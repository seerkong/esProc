# T.fno(F )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.fno(F )
=demo.query("select
=A1.fno(EID)
=A1.fno()
```

## 提取内容
T.fno(F )
说明：
Get
the sequence number of a field in a table sequence.
语法：
T .fno( F )
注意：
The
function gets the sequence number of field F
in table sequence T . If there is no parameter, the number of
fields in T will be returned; if the
specified field cannot be found, a null value will be returned.
参数：
|
T
|
Table
sequence .
|
F
|
Field
name .
返回值：
Integer/Null
示例：
|
=demo.query("select
* from EMPLOYEE")
|
=A1.fno(EID)
|
1 , which indicates
that the sequence number of field "EID" is 1 .
|
=A1.fno()
|
9 , which indicates
that there are 9 fields in the table sequence.
functions:
r .fno ( )
