# r.fno(F )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.fno(F )
=demo.query("select
=A1 (2) .fno(EID)
=A1 (2) .fno()
```

## 提取内容
r.fno(F )
说明：
Get
the sequence number of a field of a record in a table sequence.
语法：
r .fno( F )
注意：
The function gets the sequence
number of field F of record r in the table sequence
if there is no parameter, return the number of fields in the table sequence in
which r locates. If the field cannot be found,
a null value will be returned.
参数：
|
r
|
A record .
|
F
|
Field
name .
返回值：
The
sequence number of a field
示例：
|
=demo.query("select
* from EMPLOYEE")
|
=A1 (2) .fno(EID)
|
1 , which indicates
that the sequence number of the field " EID " is 1 .
|
=A1 (2) .fno()
|
9 , which indicates
that there are 9 fields in the table " Employee ".
functions:
T .fn o ()
