# for a,b,s

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
>A1=A1+A2
```

## 提取内容
for
a,b,s
说明：
Execute a loop according to the specified scope
and step.
语法：
for a,b,s
注意：
The statement executes the code block in loops
from a to b with a step of s.
参数：
|
a
|
An integer .
|
b
|
An integer .
|
s
|
An integer to indicate the step. Its value is 1 by default .
示例：
|
for 1,11,5
|
>A1=A1+A2
|
Add up integers from 1 to 11 with a step of 5,
i.e. every 4 integers, and store the accumulated value in A1 .
