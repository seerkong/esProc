# for x

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
>A1=A1+A2
=0
>A1=A1+#A2
=15
>B1=B1+#A2
>A1=A1-1
=demo.query("select
=A1.select(STATE=="Texas")
=age(A3.BIRTHDAY)
>B1=max(B1,B3)
=to(10)
>B1=B1+~
```

## 提取内容
for x
说明：
Start
a loop.
语法：
for x
注意：
The
statement starts a loop executing the code block.
参数：
|
x
|
A sequence , an
integer, or a logic expression. Loop
through the code block against x or to(x) , or if x is true. If x is empty, then the loop
will be endless .
When the parameter is a table sequence, you can use a
loop function symbol in the for x code block.
示例：
x is an integer:
|
Save
the cumulative total 55 in A1 .
|
for 10
|
>A1=A1+A2
|
Add
up integers from 1 to 10 .
x is empty:
|
=0
|
5050 .
|
for
|
Loop
will be endless if x is empty. When
the loop count reaches 100 , then break the loop.
|
>A1=A1+#A2
|
if #A2==100
|
break
|
x is a Boolean expression:
|
=15
|
for A1>10
|
If
A1 >10 , then
start a loop through the code block; otherwise, quit the loop.
|
>B1=B1+#A2
|
>A1=A1-1
|
x is a sequence:
|
=demo.query("select
* from EMPLOYEE")
|
Get
the oldest age among all Texas employees in B1 after the loop is over.
|
=A1.select(STATE=="Texas")
|
for A2
|
=age(A3.BIRTHDAY)
|
A3
performs loop over each Texas employee while computing their age.
|
>B1=max(B1,B3)
|
The
oldest age is stored in B1.
Use a loop function symbol when parameter x
is a table sequence:
|
=to(10)
|
for A1
|
A2 executes the loop
operation to compute the cumulative sum of members of A1’s sequence and
return 55, which is written to cell B1. ~ in B3 is a loop function symbol
representing the current member value of the cell A,
|
>B1=B1+~
