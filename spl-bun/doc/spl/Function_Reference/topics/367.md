# fork A i ,…

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=connect("demo")
=B2.query("select
=B2.close()
=connect("demo")
=B2.query("select
=B2.close()
=file("PersonnelInfo.txt")
=A1.import@t(;A2:4)
call ( )
```

## 提取内容
fork
A i ,…
说明：
Use
multithreads to execute the code block in a cellset.
语法：
fork Ai , …
注意：
Ai is a sequence parameter, whose length determines the number of
parallel threads. The statement makes multiple duplicates of the current
cellset and its context to execute them respectively using multiple threads. A
sequence parameter will be separated to have its members respectively used as
the value of current cell for the execution of each thread; and a single-value
parameter will be duplicated to be distributed to each thread. Form a sequence
of values returned by the code block and enter them into the fork cell.
Use #c to represent
parallel thread number in the code block.
参数：
|
Ai
|
sequence .
示例：
Single parameter:
|
fork
[[1,20,6,14,5],[32,8]]
|
Return the return results
of multithreads once the fork code block is executed.
（原文含示意图，图像资源未迁移）
|
=connect("demo")
|
=B2.query("select
* from EMPLOYEE where EID in (?) ",A1)
|
EID
is the parameter.
|
=B2.close()
|
return B3
|
Multiple parameters:
|
fork
[[1,20,6,14,5],[32,8]],"F"
|
/
|
Return the return results
of multithreads once the fork code block is executed.
（原文含示意图，图像资源未迁移）
|
=connect("demo")
|
=B2.query("select
* from EMPLOYEE where EID in (?) and GENDER=?",A1(1),A1(2))
|
EID
is the first sequence parameter, GENDER is the second sequence parameter.
|
=B2.close()
|
return B3
|
Use parallel thread number:
|
=file("PersonnelInfo.txt")
|
fork to(4)
|
（原文含示意图，图像资源未迁移）
|
=A1.import@t(;A2:4)
|
Retrieve
records from data file A1 using multiple threads
|
return #A2|B3
|
#A2
represents the parallel thread number.
functions:
cal l x()
call ( )
