# fork cs

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=file("employee.txt")
=A1.cursor@mt()
=A2.select().fetch()
```

## 提取内容
fork cs
说明：
Perform
parallel computation over a multicursor.
语法：
fork cs
注意：
The function performs
parallel computation over multicursor cs . Within a parallel computing code block, the referenced cell
represents the current thread of cursor.
参数：
|
cs
|
multicursor .
示例：
|
=file("employee.txt")
|
=A1.cursor@mt()
|
Create
a multicursor based on employee.txt
and return it.
|
fork B1
|
Perform
parallel computation over the multicursor.
|
=A2.select().fetch()
|
Get
records from each thread of cursor by loop
