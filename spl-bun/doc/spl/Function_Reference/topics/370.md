# fork … fork …

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=connect("demo")
=B2.query("select *
=connect("demo")
=B5.query("select *
```

## 提取内容
fork … fork …
说明：
Execute
multiple code blocks in parallel.
语法：
fork
…
fork
…
注意：
The function executes
multiple code blocks in parallel and assigns the result of executing each code
block to the corresponding fork cell.
示例：
|
fork
[[1,20,6,14,5],[32,8]]
|
//fork
|
=connect("demo")
|
=B2.query("select *
from EMPLOYEE where EID in (?) ",A1)
|
fork
[[1,20,6,14,5],[32,8]],"F"
|
//fork
|
=connect("demo")
|
=B5.query("select *
from EMPLOYEE where EID in (?) and GENDER=?",A4(1),A4(2))
