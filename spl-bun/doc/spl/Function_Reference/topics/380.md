# ftp_open()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ftp_open()
ftp_open( server:port,user,pwd )
=ftp_open("192.168.75.1":21,"Administrator","admin")
=ftp_cd(A1,"/workPath")
=ftp_put(A1,"test.txt","G:/Test.txt")
>ftp_close(A1)
```

## 提取内容
ftp_open()
说明：
Create
an FTP client.
语法：
ftp_open( server:port,user,pwd )
注意：
FtpCli
external library function (See External Library Guide ) . By
default, the FTP server operates in an active mode.
选项：
|
@d
|
Start
the FTP passive mode .
|
@s
|
Create
an SFTP client .
参数：
|
server
|
An
FTP server’s IP address .
|
port
|
An
FTP server’s port number, which is by default 21 and can be omitted .
|
user
|
The
username to access an FTP server .
|
pwd
|
The
password to access an FTP server .
示例：
|
=ftp_open("192.168.75.1":21,"Administrator","admin")
|
Create
an FTP client.
|
=ftp_cd(A1,"/workPath")
|
=ftp_put(A1,"test.txt","G:/Test.txt")
|
>ftp_close(A1)
|
function:
ftp_clos e ()
