# ftp_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ftp_close()
ftp_close( client )
=ftp_open("192.168.75.1":21,"Administrator","admin")
=ftp_cd(A1,"/folder1")
=ftp_put(A1,"Test1.docx","G:/Test.docx")
>ftp_close(A1)
ftp_open()
```

## 提取内容
ftp_close()
说明：
Exit
an FTP client program.
语法：
ftp_close( client )
注意：
This
FtpCli external library function (See External Library Guide ) closes
an FTP connection.
参数：
|
client
|
An
FTP client object .
示例：
|
=ftp_open("192.168.75.1":21,"Administrator","admin")
|
Create
an FTP client.
|
=ftp_cd(A1,"/folder1")
|
=ftp_put(A1,"Test1.docx","G:/Test.docx")
|
Upload
a file to the FTP server.
|
>ftp_close(A1)
|
Exit
the FTP client program.
function:
ftp_open()
