# ftp_mget()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ftp_mget()
ftp_mget( client,serverFolder,localFolder,multiServerFiles )
=ftp_open("192.168.75.1":21,"Administrator","admin")
=ftp_cd(A1,"/
=ftp_mget@f(A1,"folder1","G:/download","*.doc")
=ftp_mget@f(A1,"folder1","G:/download","*.xls","*.xlsx")
=ftp_mget@f(A1,"folder1","G:/download","**")
=ftp_mget@t(A1,"folder2","G:/download","*.docx")
>ftp_close(A1)
ftp_mput()
ftp_put()
```

## 提取内容
ftp_mget()
说明：
Download
multiple files from a FTP server.
语法：
ftp_mget( client,serverFolder,localFolder,multiServerFiles )
注意：
FtpCli
external library function (See External Library Guide ) . When
parameter serverFolder is a relative path, it is relative to the current
working directory; when it is preceded by a slash sign (/), it is an absolute
path. When the parameter is absent, the comma must be retained. If the
parameter is an empty string or is absent, the folder path is relative to the
current working directory.
When
parameter localFolder is a relative path, it is relative to the path configured
in esProc Options -> Main directory. When the parameter is absent, the comma
must be retained. If the parameter is an empty string or is absent, the folder
path is relative to the path configured in esProc Options -> Main directory.
For
a wildcard character, the question mark (?) signifies a single character, an
asterisk (*) signifies any character, and double asterisk (**) signifies any
character plus a directory.
选项：
|
@f
|
Overwrite
a namesake file .
|
@t
|
Skip
a namesake file .
参数：
|
client
|
An
FTP client object .
|
serverFolder
|
server folder .
|
localFolder
|
local folder .
|
multiServerFiles
|
Wildcard-specified
server files; can be duplicated .
示例：
|
=ftp_open("192.168.75.1":21,"Administrator","admin")
|
=ftp_cd(A1,"/
WorkingDirectory ")
|
=ftp_mget@f(A1,"folder1","G:/download","*.doc")
|
Download
files suffixed by doc under folder1 directory to G:/download directory while
overwriting namesake files.
|
=ftp_mget@f(A1,"folder1","G:/download","*.xls","*.xlsx")
|
Download
files suffixed by xls and xlsx under folder1 directory to G:/download
directory.
|
=ftp_mget@f(A1,"folder1","G:/download","**")
|
Download
all files under folder1 directory
to G:/download directory.
|
=ftp_mget@t(A1,"folder2","G:/download","*.docx")
|
Download
files suffixed by docx under folder2 directory to G:/download directory while skipping
namesake files.
|
>ftp_close(A1)
|
function:
ftp_mput()
ftp_put()
