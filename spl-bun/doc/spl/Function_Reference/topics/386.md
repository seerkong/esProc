# ftp_put()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ftp_put()
ftp_put( client,remoteFile,localFile )
=ftp_open("192.168.75.1":21,"Administrator","admin")
=ftp_cd(A1,"/WorkingDirectory")
=ftp_put(A1,"记录s_Upload.docx","G:/记录s.docx")
>ftp_close(A1)
```

## 提取内容
ftp_put()
说明：
Upload
a file to the FTP server.
语法：
ftp_put( client,remoteFile,localFile )
注意：
FtpCli
external library function (See External Library Guide ) . If the
parameter localFile is a relative
path, it is relative to the path to the current splx file.
选项：
|
@f
|
Overwrite
the local namesake file .
参数：
|
client
|
An
FTP client object .
|
remoteFile
|
remote file .
|
localFile
|
local file .
示例：
|
=ftp_open("192.168.75.1":21,"Administrator","admin")
|
=ftp_cd(A1,"/WorkingDirectory")
|
=ftp_put(A1,"记录s_Upload.docx","G:/记录s.docx")
|
Upload
a file to the FTP server.
|
>ftp_close(A1)
|
相关函数：
ftp_ g e t()
