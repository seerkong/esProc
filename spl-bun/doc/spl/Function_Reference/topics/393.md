# gcd()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
gcd()
gcd ( x i ,… )
gcd ( A )
gcd(7,1)
gcd([14,7,77])
```

## 提取内容
gcd()
说明：
Return
the greatest common divisor.
语法：
gcd ( x i ,… )
gcd ( A )
注意：
The
function calculates the greatest common divisor of members of [ x 1 , x 2 ,… ].
参数：
|
A sequence .
|
x i
|
A numeric expression, which will be ignored if its value is not a number .
返回值：
数值型
value
示例：
|
gcd(7,1)
|
gcd([14,7,77])
|
