# gcs_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
gcs_close()
gcs_close (fd)
=file("F: /gcs .json").read()
=gcs_open(A1)
>gcs_close(A 2 )
```

## 提取内容
gcs_close()
说明：
Close
Google Cloud Storage connection.
语法：
gcs_close (fd)
注意：
This
GcsCli external library function (See External Library Guide ) closes the GCS connection to
release resources.
参数：
|
fd
|
GCS
connection .
示例：
|
=file("F: /gcs .json").read()
|
=gcs_open(A1)
|
Connect
to the GCS service.
|
>gcs_close(A 2 )
|
Close
the GCS connection.
