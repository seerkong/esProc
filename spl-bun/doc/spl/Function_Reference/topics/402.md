# A.group(x i ,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.group(x i ,… )
=A1.group(~%2)
=A1.group(~%2,~%3)
=[6,9,16,5,1,7,8].group@s(~%2)
=A1.group((#-1)\3)
=demo.query("select NAME,BIRTHDAY,GENDER from EMPLOYEE")
=A1.group(GENDER)
=A2.new(GENDER:Gender,~.count():Number)
=A2.new(GENDER:Gender,~.avg(age(BIRTHDAY)):Average)
=demo.query("select
=A1.group(GENDER,DEPT)
=A1.group@o(GENDER)
=A1.group@1(GENDER)
=A1.group@n(if(GENDER=="F",1,2))
=A1.group@u(GENDER,DEPT)
=A1.group@i(GENDER=="F")
=A1.group@p(GENDER,DEPT)
=file("D:\\Salesman.txt").import@t()
=A9.group@0(Gender)
=file("D:/emp10.txt").import@t()
=A11.group@h(DEPT)
=A1.group@n(if(DEPT=="HR":1,DEPT=="Sales":2;0))
=demo.query@v("select
=A1.group@v(GENDER,DEPT)
A.id( )
A.group(x:F,…;y: G ,…)
```

## 提取内容
A.group(x i ,… )
说明 ：
Perform
equi-grouping according one or more fields or expressions.
Syntax ：
A . group ( x i ,… )
Note ：
The
function performs equi-grouping on a sequence according to expression x i ,… . The result is a sequence/record sequence consisting of groups.
Option ：
|
@o
|
Group
records by comparing adjacent ones, which is equal to the merging operation,
and the result set won’t be sorted .
|
@1
|
Get
the first record of each group to form a record sequence and return it
(Please note that 1 is a number instead of a letter); can work with @v option .
|
@n
|
x gets assigned with group numbers which can be used to define the
groups; discard groups that make x<1 .
@n and @0 are mutually exclusive .
|
@u
|
Do
not sort the result set by x . It
doesn’t work with @o/@n .
|
@i
|
x is a Boolean expression. If the result of x is true, then start a new group. This option is equivalent to
A.group@o(a+=if(x,1,0)), in which a=0 and there is only one x .
|
@0
|
Discard
the group over which the result of grouping expression x is null; discard empty groups when @n is also present. Use it
when there’s only one expression x .
|
@s
|
Perform
a concatenation of sequences/records sequences after the grouping. It is
equivalent to A.group(xi,…).conj()
can work with @v option .
|
@p
|
Return
a sequence of integer sequences, each of which contains the positions of
members in each group in sequence A .
|
@h
|
Used
over a grouped table with each group ordered to speed up grouping .
|
@v
|
When
parameter A is a pure table
sequence, return a set of pure table sequences; only supported by esProc Enterprise
Edition.
Parameter ：
|
A sequence .
|
x i
|
Grouping
expression .
Return value ：
Sequence/记录
sequence
Example ：
Group a sequence:
|
[6,9,12,15,16,5,1,7,8]
|
=A1.group(~%2)
|
[[6,12,16,8],[
9,15,5,1,7]] The series is divided into two groups. Divide the number of members
in both groups by 2, one of the remainders is 0 and the other is 1.
|
=A1.group(~%2,~%3)
|
[[6,12],[16],[8],[9,15],[1,7],[5]].
Group
the series according to multiple expressions.
|
=[6,9,16,5,1,7,8].group@s(~%2)
|
Divide
the sequence into a group of odd numbers and a group of even numbers, and
then concatenate them.
（原文含示意图，图像资源未迁移）
|
=A1.group((#-1)\3)
|
Group sequence A1
every 3 members .
（原文含示意图，图像资源未迁移）
Reuse the grouping result:
|
=demo.query("select NAME,BIRTHDAY,GENDER from EMPLOYEE")
|
=A1.group(GENDER)
|
Group A1’s table
sequence by GENDER:
（原文含示意图，图像资源未迁移）
[ Each group is a sequence:
（原文含示意图，图像资源未迁移）
|
=A2.new(GENDER:Gender,~.count():Number)
|
（原文含示意图，图像资源未迁移）
Count
members of each group.
|
=A2.new(GENDER:Gender,~.avg(age(BIRTHDAY)):Average)
|
（原文含示意图，图像资源未迁移）
Get different statistical
results using the same grouping result.
Group data by multiple fields:
|
=demo.query("select
NAME,GENDER,DEPT,BIRTHDAY from EMPLOYEE")
|
=A1.group(GENDER,DEPT)
|
Group
records by multiple fields.
|
=A1.group@o(GENDER)
|
记录s
won’t be sorted; only the adjacent ones will be compared and group them
together if they match. Same records that are not adjacent may be put into
different groups, so there may be overlapped groups and the returned result
is a set of sequences.
（原文含示意图，图像资源未迁移）
|
=A1.group@1(GENDER)
|
Return
the first record of each group.
（原文含示意图，图像资源未迁移）
|
=A1.group@n(if(GENDER=="F",1,2))
|
x gets assigned with group numbers which can be used to define the
groups directly.
[[Rebecca,Ashley,Rachel,…],[Matthew,Ryan,Jacob,…]]
|
=A1.group@u(GENDER,DEPT)
|
Do
not sort result set by the sorting field.
|
=A1.group@i(GENDER=="F")
|
（原文含示意图，图像资源未迁移）
Start a new group when GENDER=="F" .
|
=A1.group@p(GENDER,DEPT)
|
（原文含示意图，图像资源未迁移）
Return
a sequence of integer sequences, each of which contains the positions of
records in a group (grouped by GENDER field and DEPT field) in the original
table sequence.
|
=file("D:\\Salesman.txt").import@t()
|
（原文含示意图，图像资源未迁移）
|
=A9.group@0(Gender)
|
（原文含示意图，图像资源未迁移）
Group
the table sequence by GENDER field, while discarding groups with null values.
|
=file("D:/emp10.txt").import@t()
|
For data fiel emp10.txt , every 10 records are ordered
by DEPT .
（原文含示意图，图像资源未迁移）
|
=A11.group@h(DEPT)
|
As A11 is
segmented and ordered by DEPT, @h option is used to speed up grouping .
（原文含示意图，图像资源未迁移）
|
=A1.group@n(if(DEPT=="HR":1,DEPT=="Sales":2;0))
|
Use @n option to discard groups of records that
make the grouping expression less than 1. That is, give up those
where DEPT is neither HR nor Sales .
（原文含示意图，图像资源未迁移）
When parameter A is
a table sequence:
|
=demo.query@v("select
* from EMPLOYEE order by GENDER,DEPT ")
|
Return
a pure table sequence.
|
=A1.group@v(GENDER,DEPT)
|
@v
option enables returning a set of pure table sequences.
Related functions ：
A.id( )
A.group(x:F,…;y: G ,…)
