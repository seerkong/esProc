# T. group ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.group( x i ,… )
=create(file).record(["D:/file/pseudo/empT.ctx"])
=pseudo(A1)
=A2.group(GENDER,DEPT)
=A3.import()
=A2.group@i(GENDER=="F")
=A5.import()
=A2.group@1(GENDER)
=A7.cursor().fetch()
```

## 提取内容
T. group ()
说明：
Define
a computation, which will group records by comparing the grouping field in each
with its next neighbor, on a pseudo table and return a new pseudo table.
语法：
T.group( x i ,… )
注意：
The function defines a computation on pseudo
table T , which compares value of expression x i only with its next neighbor, and
returns a new pseudo table.
Only supported by esProc Enterprise Edition.
参数：
|
T
|
A pseudo table .
|
x i
|
Grouping
expression; use comma to separate multiple grouping fields or expressions .
选项：
|
@i
|
x is a Boolean expression. Begin a new group when a record makes it
return true. In this case there should be only one x .
|
@1
|
Get
the first record of every group to form a record sequence and return it; here
it is number 1, instead of letter l .
返回值：
Pseudo table object
示例：
|
=create(file).record(["D:/file/pseudo/empT.ctx"])
|
=pseudo(A1)
|
Generate a pseudo table
object, whose data is as follows:
（原文含示意图，图像资源未迁移）
记录s are ordered by DEPT field and GENDER
field.
|
=A2.group(GENDER,DEPT)
|
Define a computation on
A2’s pseudo table, which will put records where both GENDER and DEPT values
are same into the same group, and return a new pseudo table .
|
=A3.import()
|
Import data from A3’s
pseudo table while executing the computation in A3 defined on A2’s pseudo
table, and return the following pseudo table:
（原文含示意图，图像资源未迁移）
|
=A2.group@i(GENDER=="F")
|
With @i option, create a
new group whenever the record meets the condition GENDER=="F" .
|
=A5.import()
|
Get data from A5’s pseudo
table .
（原文含示意图，图像资源未迁移）
|
=A2.group@1(GENDER)
|
With @1 option, return
the first record of each group .
|
=A7.cursor().fetch()
|
Fetch data from A7’s
pseudo table:
（原文含示意图，图像资源未迁移）
