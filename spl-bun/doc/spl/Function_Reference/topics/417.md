# cs.groupn(x;C)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.groupn(x;C)
=demo.cursor("select
=channel()
=channel()
=A1.groupn(if(GENDER=="F",1,2);[A2,A3])
=A2.fetch()
=A3.fetch()
=A1.fetch()
=A2.result()
=A3.result()
```

## 提取内容
cs.groupn(x;C)
说明：
Group
records in a cursor and push them respectively into a sequence of channels.
语法：
cs. groupn ( x ;C)
注意：
The
function groups records in cursor cs by
grouping expression x , pushes them
respectively into a sequence of channels, and returns the original cursor.
参数：
|
cs
|
游标
records .
|
x
|
Grouping
expression .
|
sequence of channels .
返回值：
游标
示例：
|
=demo.cursor("select
NAME,GENDER,DEPT,BIRTHDAY from EMPLOYEE")
|
=channel()
|
Create
a channel.
|
=channel()
|
Create
a channel.
|
=A1.groupn(if(GENDER=="F",1,2);[A2,A3])
|
Group
cursor records, and push one group where GENDER is F into A2’s channel and
the other group into A3’s channel.
|
=A2.fetch()
|
Keep
data in the channel in place.
|
=A3.fetch()
|
Keep
data in the channel in place.
|
=A1.fetch()
|
=A2.result()
|
Get
result from A2’s channel.
（原文含示意图，图像资源未迁移）
|
=A3.result()
|
Get
result from A3’s channel.
（原文含示意图，图像资源未迁移）
