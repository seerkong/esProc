# ch.groupn( x;C )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.groupn( x;C )
=demo.cursor("select
=channel(A1)
=channel()
=channel()
=A2.groupn(if(GENDER=="F",1,2);[A3,A4])
=A3.fetch()
=A4.fetch()
=A1.fetch()
=A3.result()
=A4.result()
```

## 提取内容
ch.groupn( x;C )
说明：
Group
records in channel ch and push them
respectively into a sequence of channels.
语法：
ch. groupn ( x ;C)
注意：
The
function groups records in channel ch
by grouping expression x and pushes
them respectively into a sequence of channels.
参数：
|
ch
|
管道 .
|
x
|
Grouping
expression .
|
sequence of channels .
返回值：
管道
示例：
|
=demo.cursor("select
NAME,GENDER,DEPT,BIRTHDAY from EMPLOYEE")
|
=channel(A1)
|
Push
cursor records into a sequence of channels and return a channel.
|
=channel()
|
Create
a channel.
|
=channel()
|
Create
a channel.
|
=A2.groupn(if(GENDER=="F",1,2);[A3,A4])
|
Group
records of A2’s channel, and push one group where GENDER is F into A3’s
channel and the other group into A4’s channel.
|
=A3.fetch()
|
Attach
ch.fetch() function that gets the final result set to A3’s channel but keep
data in A3’s channel in place
|
=A4.fetch()
|
Attach
ch.fetch() function that gets the final result set to A4’s channel but keep
data in A4’s channel in place.
|
=A1.fetch()
|
=A3.result()
|
Get
result from A3’s channel.
（原文含示意图，图像资源未迁移）
|
=A4.result()
|
Get
result from A4’s channel.
（原文含示意图，图像资源未迁移）
