# A. groups()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select * from SCORES where CLASS = 'Class one'")
=A1.groups(STUDENTID:StudentID;sum(SCORE):TotalScore)
=demo.query("select * from SCORES")
=A3.groups(CLASS:Class,STUDENTID:StudentID;sum(SCORE):TotalScore)
=A3.groups@m(STUDENTID:StudentID;sum(SCORE):TotalScore)
=A3.groups@o(STUDENTID:StudentID;sum(SCORE):TotalScore)
=demo.query("select * from STOCKRECORDS where
=A7.groups@n(if(STOCKID=="000062",1,2):StockID;sum(CLOSING):TotalPrice)
=demo.query("select * from EMPLOYEE")
=A9.groups@u(STATE:State;count(STATE):TotalScore)
=A9.groups@i(STATE=="California":IsCalifornia;count(STATE):count)
=A3.groups(CLASS:Class,STUDENTID:StudentID;iterate(~~*2,10): Score1)
=file("D:\\Salesman.txt").import@t()
=A13.groups@0(Gender:Gender;sum(Age):Total)
=file("D:/emp10.txt").import@t()
=A15.groups@h(DEPT:dept;sum(SALARY):bouns)
=A1.groups(STUDENTID:StudentID;SUBJECT,sum(SCORE):SUMSCORE)
=demo.query("select * from SCORES where CLASS = 'Class three'")
=A18.groups@t(STUDENTID:StudentID;sum(SCORE):TotalScore)
=demo.query("select
=A1.groups@z(STUDENTID:StudentID;sum(SCORE):TotalScore)
=demo.query("select
=A1.groups(DEPT;minp(SALARY))
=A1.groups@e(DEPT;minp(SALARY))
=connect("demo").query("select
=A1.groups(DEPT;conj(NAME))
=A1.groups(DEPT;union(STATE))
A.group( x i, … )
A.group(x:F,...;y:G,…)
```

## 提取内容
A. groups()
说明：
Group
a table sequence and then get the aggregate result cumulatively.
语法：
A. groups ( x : F, …; y : G, …)
注意：
The function groups and
aggregates table sequence A by
expression x to generate a new table sequence with F ,.. G … as the fields.
Namely, during the traversal through members of A , they will be placed to the corresponding result set one by one
while a result set is aggregated cumulatively. Compared with the method of first
grouping and then aggregation represented by A.group ( x : F,… ; y : G, …)
function, the function has a better performance.
选项：
|
@o
|
Group
records by comparing adjacent ones, which is equal to the merging operation,
and the result set won’t be sorted.
|
@n
|
x gets assigned with group numbers which can be used to define the
groups. @n and @o are mutually exclusive.
|
@u
|
Do
not sort the result set by x. It doesn’t work with @o/@n.
|
@i
|
x is a Boolean expression. If the result of x is true, then start a new group. There is only one x .
|
@m
|
Use
parallel algorithm to handle data-intensive or computation-intensive tasks
no definite order for the records in the result set; can’t be used with @o
and @i options.
|
@0
|
Discard
the group over which the result of grouping expression x is null.
|
@h
|
Used
over a grouped table with each group ordered to speed up grouping.
|
@t
|
Return
an empty table sequence with data structure if the grouping and aggregate
operation over the sequence returns null.
|
@z(…;…)
|
Split
the sequence according to groups during parallel computation, and the
multiple threads share a same result set.
|
@e
|
Return
a table sequence consisting of results of computing function y
expression x is a field of sequence A and y is a
function on A ; the result of y must be one record of A
and y only supports maxp, minp and top@1 when it is an aggregate
function.
参数：
|
sequence.
|
x
|
Grouping
expression
|
F
|
Field
name of the result table sequence.
|
G
|
Summary
field name in the result table sequence.
|
y
|
y is the function with which A
is traversed. When y is an
aggregate function, it only supports sum,
count, max, min, top, avg, iterat, icount, median, maxp, minp, concat, var,
conj and union . When the function work with iterate( x,a;G i ,… ) function, the latter’s parameter Gi should
be omitted. When y isn’t an
aggregate function, perform calculation over only the first record in each
group.
返回值：
Post-grouping
table sequence
示例：
|
=demo.query("select * from SCORES where CLASS = 'Class one'")
|
（原文含示意图，图像资源未迁移）
|
=A1.groups(STUDENTID:StudentID;sum(SCORE):TotalScore)
|
（原文含示意图，图像资源未迁移）
Group
by a single field.
|
=demo.query("select * from SCORES")
|
=A3.groups(CLASS:Class,STUDENTID:StudentID;sum(SCORE):TotalScore)
|
（原文含示意图，图像资源未迁移）
group
by multiple fields.
|
=A3.groups@m(STUDENTID:StudentID;sum(SCORE):TotalScore)
|
Use
@m option to increase performance of big data handling.
|
=A3.groups@o(STUDENTID:StudentID;sum(SCORE):TotalScore)
|
（原文含示意图，图像资源未迁移）
Only
compare and merge with the neighboring element, and the result set is not
sorted.
|
=demo.query("select * from STOCKRECORDS where
STOCKID<'002242'")
|
=A7.groups@n(if(STOCKID=="000062",1,2):StockID;sum(CLOSING):TotalPrice)
|
（原文含示意图，图像资源未迁移）
The
value of x is the group number.
|
=demo.query("select * from EMPLOYEE")
|
=A9.groups@u(STATE:State;count(STATE):TotalScore)
|
Do not sort result
set by the sorting field.
|
=A9.groups@i(STATE=="California":IsCalifornia;count(STATE):count)
|
（原文含示意图，图像资源未迁移）
Start a new group
when STATE=="California".
|
=A3.groups(CLASS:Class,STUDENTID:StudentID;iterate(~~*2,10): Score1)
|
（原文含示意图，图像资源未迁移）
Perform iterate
operation within each group .
|
=file("D:\\Salesman.txt").import@t()
|
（原文含示意图，图像资源未迁移）
|
=A13.groups@0(Gender:Gender;sum(Age):Total)
|
（原文含示意图，图像资源未迁移）
Discard groups
where Gender values are nulls .
|
=file("D:/emp10.txt").import@t()
|
For data file emp10.txt , every 10 records are
ordered by DEPT .
（原文含示意图，图像资源未迁移）
|
=A15.groups@h(DEPT:dept;sum(SALARY):bouns)
|
A15 is grouped and
ordered by DEPT, for which @h option is used to speed up grouping .
（原文含示意图，图像资源未迁移）
|
=A1.groups(STUDENTID:StudentID;SUBJECT,sum(SCORE):SUMSCORE)
|
Parameter y isn’t an aggregate function, so the
function performs operation over the first record .
（原文含示意图，图像资源未迁移）
|
=demo.query("select * from SCORES where CLASS = 'Class three'")
|
Return an empty
table sequence .
|
=A18.groups@t(STUDENTID:StudentID;sum(SCORE):TotalScore)
|
Return an empty
table sequence with the data structure .
（原文含示意图，图像资源未迁移）
|
=demo.query("select
* from SCORES")
|
=A1.groups@z(STUDENTID:StudentID;sum(SCORE):TotalScore)
|
Split
A1’s sequence according to groups during parallel computation.
|
=demo.query("select
EID,NAME,GENDER,DEPT,SALARY from employee")
|
=A1.groups(DEPT;minp(SALARY))
|
（原文含示意图，图像资源未迁移）
Execute
aggregate function minp() and return A3’s records.
|
=A1.groups@e(DEPT;minp(SALARY))
|
（原文含示意图，图像资源未迁移）
Return
a table sequence consisting of result records of computing minp(SALARY).
|
=connect("demo").query("select
* from employee")
|
=A1.groups(DEPT;conj(NAME))
|
Group A1’s table sequence by DEPT and for each group
concatnate NAME values:
（原文含示意图，图像资源未迁移）
|
=A1.groups(DEPT;union(STATE))
|
Group A1’s table sequence by DEPT and for each group
compute union of STATE values:
（原文含示意图，图像资源未迁移）
function:
A.group( x i, … )
A.group(x:F,...;y:G,…)
