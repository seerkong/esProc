# ch.groups ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.groups ()
=demo.cursor("select * from EMPLOYEE ")
=channel()
=channel()
=channel()
=channel()
=A1.push(A2,A3,A4,A5)
=A2.groups(;sum(SALARY):TotalSalary)
=A3.groups(DEPT:dept;sum(SALARY):TotalSalary)
=A4.groups@n(if(GENDER=="F",1,2):SubGroups;sum(SALARY):TotalSalary)
=A5.groups@u(STATE:State;count(STATE):count)
A1.select(month(BIRTHDAY)==2)
A11.fetch()
=A2.result()
=A3.result()
=A4.result()
=A5.result()
```

## 提取内容
ch.groups ()
说明：
Group
records in a channel.
语法：
ch. groups ( x:F, …; y : G …)
注意：
The function groups records in channel ch
according to grouping expression x , by which the records are ordered, to
get a channel having F ,... G ,… fields.
Sort records in the new channel by x .
Values of G
field are the results of computing expression y over each group. It aims to fetch the grouping result set from
the channel.
选项：
|
@n
|
With
the option the value of expression x
is a group number, which points to the desired group .
|
@u
|
Won’t
sort the resulting set by expression x
the option and @n are mutually exclusive .
参数：
|
ch
|
管道 .
|
x
|
Grouping
expression, by which an aggregation over the whole grouped set is performed
if x:F is omitted. In that case the
semicolon “;” should not be omitted .
|
F
|
Field
names of the resulting table sequence .
|
y
|
An
aggregate function on channel ch , which only supports sum, count, max,
min, top, avg, iterate, concat, var, conj and union; the parameter Gi should
be given up if function iterate( x,a;G i ,… ) is used .
|
G
|
The
aggregate fields in the resulting table sequence .
Return
value:
管道
示例：
|
=demo.cursor("select * from EMPLOYEE ")
|
=channel()
|
Create
a channel.
|
=channel()
|
Create
a channel.
|
=channel()
|
Create
a channel.
|
=channel()
|
Create
a channel.
|
=A1.push(A2,A3,A4,A5)
|
Be
ready to push the data in A1’s cursor into channel A2, A3 and A5, but the action
needs to wait.
|
=A2.groups(;sum(SALARY):TotalSalary)
|
As
x:F is omitted, calculate the sum
of salaries of all employees.
|
=A3.groups(DEPT:dept;sum(SALARY):TotalSalary)
|
Group
and sort records by DEPT field.
|
=A4.groups@n(if(GENDER=="F",1,2):SubGroups;sum(SALARY):TotalSalary)
|
The
value of x is group number; put
records where GENDER is “F” into the first group and others into the second
group, and then aggregate each group.
|
=A5.groups@u(STATE:State;count(STATE):count)
|
Won’t
sort the resulting set by grouping field
|
A1.select(month(BIRTHDAY)==2)
|
A11.fetch()
|
Attach
a fetch operation to A11’s cursor.
|
=A2.result()
|
（原文含示意图，图像资源未迁移）
|
=A3.result()
|
（原文含示意图，图像资源未迁移）
|
=A4.result()
|
（原文含示意图，图像资源未迁移）
|
=A5.result()
|
（原文含示意图，图像资源未迁移）
