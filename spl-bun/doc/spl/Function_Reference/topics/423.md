# cs.gr oups()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=file("emp1.ctx","192.168.0.111:8281")
=A1.open()
=A2.cursor()
=A3.groups(Dept:dept;count(Name):count)
=file("emp.ctx":[1,2],
=A2. open ()
=A3.cursor()
=A4.groups(GENDER:gender;sum(SALARY):totalSalary)
=A3.cursor()
=A6.groups@c(GENDER:gender;sum(SALARY):totalSalary).dup()
A.group(xi,…)
A.group(x: F ,…;y:G,…)
A.groups()
```

## 提取内容
cs.gr oups()
说明：
Group
records in a cluster cursor, sort them by the grouping field and perform
aggregation over each group and add each aggregate to the result set.
语法：
cs. groups ( x:F, …; y : G …; n )
注意：
The
function groups records in a cluster cursor by expression x , sorts result by the grouping field, and calculates the aggregate
value on each group.
This
creates a new table sequence consisting of fields F ,... G ,… and sorted by the grouping field x . The G field gets values
by computing y on each group.
Only
supported by esProc Enterprise Edition.
选项：
|
@c
|
Perform
the group operation over data in every node and compose the result sets into
a cluster in-memory table in the segmentation way of the cursor; support a
cluster dimension table .
参数：
|
cs
|
记录s
in a cluster cursor .
|
x
|
Grouping
expression; if omitting parameters x:F,
aggregate the whole set; in this case, the semicolon “;” must not be omitted .
|
F
|
F ield name in the
result table sequence .
|
y
|
An
aggregate function on cs , which only supports sum, count, max, min, top,
avg, iterate, concat, var, conj and union; when the function works with
iterate( x,a;G i ,… ) function, the latter’s
parameter Gi should be omitted .
|
G
|
Aggregate
field name in the result table sequence .
|
n
|
The
specified maximum number of groups; stop executing the function when the
number of data groups is bigger than n to
prevent memory overflow; the parameter is used in scenarios when it is
predicted that data will be divided into a large number of groups that are greater
than n .
返回值：
Table
sequence/Cluster in-memory table
示例：
|
=file("emp1.ctx","192.168.0.111:8281")
|
Below
is emp1.ctx :
（原文含示意图，图像资源未迁移）
|
=A1.open()
|
Open
a cluster composite table.
|
=A2.cursor()
|
Return a cluster cursor .
|
=A3.groups(Dept:dept;count(Name):count)
|
Group
data by DEPT and perform aggregation.
（原文含示意图，图像资源未迁移）
|
[192.168.0.110:8281,192.168.18.143:8281]
|
=file("emp.ctx":[1,2],
A1)
|
=A2. open ()
|
Open
a cluster composite table.
|
=A3.cursor()
|
Create
a cluster cursor.
|
=A4.groups(GENDER:gender;sum(SALARY):totalSalary)
|
Group
data by GENDER and perform aggregation and return result as a table sequence.
（原文含示意图，图像资源未迁移）
|
=A3.cursor()
|
=A6.groups@c(GENDER:gender;sum(SALARY):totalSalary).dup()
|
Retain
the way of segmentation of the distributed cursor and return a cluster
in-memory table.
（原文含示意图，图像资源未迁移）
相关函数：
A.group(xi,…)
A.group(x: F ,…;y:G,…)
A.groups()
cs.gro u p x()
