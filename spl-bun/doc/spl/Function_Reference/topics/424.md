# cs.groups()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.groups()
=demo.cursor("select
=A1.groups(;sum(SCORE):TotalScore)
=demo.cursor("select
=A3.groups(GENDER:gender;sum(AGE):TotalAge)
=demo.cursor("select
=A5.groups@n(if(STOCKID=="000062",1,2):SubGroups;sum(CLOSING):ClosingPrice)
=demo.cursor("select
=A7.groups@u(STATE:State;count(STATE):Total)
=demo.cursor("select
=A9.groups@o(STATE:State;count(STATE):Total)
=demo.cursor("select
=A11.groups@i(STATE=="California":IsCalifornia;count(STATE):count)
=file("D:/emp10.txt").cursor@t()
=A13.groups@h(DEPT:DEPT;sum(SALARY):bouns)
=demo.query("select * from employee")
=A15.cursor@m(3)
=A16.groups(STATE:state;sum(SALARY):salary)
=demo.cursor("select
=A1.groups@t(STUDENTID:StudentID;sum(SCORE):TotalScore)
=demo.cursor("select
=A3.groups@0(FATHER)
=demo.cursor("select
=A1.groups@z(STUDENTID:StudentID;sum(SCORE):TotalScore;5)
=demo.cursor("select
=A1.groups@e(SUBJECT;maxp(SCORE))
=connect("demo").cursor("select
=connect("demo").cursor("select
```

## 提取内容
cs.groups()
说明：
Group
records in a cursor.
语法：
cs. groups ( x:F, …; y : G …)
注意：
The
function groups records in a cluster cursor by expression x , sorts result by the grouping field, and calculates the aggregate
value on each group. This creates a new table sequence consisting of fields F ,... G ,… and sorted by the grouping field x . The values of F field
are the value of x field of the first
record in each group and G field gets
values by computing y on each group. The
aggregation over a cluster cursor will first be performed by the main process
on the local machine and the result will then be returned to the machine that
initiates the invocation; the process is called reduce.
选项：
|
@n
|
The
value of grouping expression is group number used to locate the group .
|
@u
|
Do
not sort the result set by the grouping expression; it doesn’t work with @n .
|
@o
|
Compare
each record only with its neighboring record to group, which is equivalent to
the merge operation, and won’t sort the result set .
|
@i
|
With
this option, the function only has one parameter x that is a Boolean expression; start a new group if its result
is true .
|
@h
|
Used
over a grouped table with each group ordered to speed up grouping .
|
@0
|
Discard
groups on which expression x gets empty result .
|
@t
|
When
empty data is obtained from the cursor, the function returns an empty table
sequence having only the data structure .
|
@z(…;…;n)
|
Split
the sequence according to groups during parallel computation, and the
multiple threads share a same result set; in this case HASH space will not be
dynamically adjusted; parameter n is HASH space size, whose value can
be default .
|
@e
|
Return
a table sequence consisting of results of computing function y
expression x is a field of cursor cs and y is a function
on cs ; the result of y must be one record of cs and y
only supports maxp, minp and top@1 when it is an aggregate function .
参数：
|
cs
|
记录s
in a cursor .
|
x
|
Grouping
expression; if omitting parameters x:F,
aggregate the whole set; in this case, the semicolon “;” must not be omitted .
|
F
|
F ield name in the
result table sequence .
|
y
|
An
aggregate function on cs , which only supports sum, count, max, min, top,
avg, iterate, concat, var, conj and union; when the function works with
iterate( x,a;G i ,… ) function, the latter’s
parameter Gi should be omitted .
|
G
|
Aggregate
field name in the result table sequence .
Return
value:
Table
sequence
示例：
|
=demo.cursor("select
* from SCORES where CLASS = 'Class one'")
|
=A1.groups(;sum(SCORE):TotalScore)
|
（原文含示意图，图像资源未迁移）
As
parameters x:F absent, calculate
the total score of all students.
|
=demo.cursor("select
* from FAMILY")
|
=A3.groups(GENDER:gender;sum(AGE):TotalAge)
|
（原文含示意图，图像资源未迁移）
Group
and order data by specified fields.
|
=demo.cursor("select
* from STOCKRECORDS where STOCKID<'002242'")
|
=A5.groups@n(if(STOCKID=="000062",1,2):SubGroups;sum(CLOSING):ClosingPrice)
|
（原文含示意图，图像资源未迁移）
The
value of grouping expression is group number; put records whose STOCKID is “000062”
to the first group and others to the second group; and meanwhile aggregate
each group.
|
=demo.cursor("select
* from EMPLOYEE")
|
=A7.groups@u(STATE:State;count(STATE):Total)
|
The
result set won’t be sorted by the grouping field.
|
=demo.cursor("select
* from EMPLOYEE")
|
=A9.groups@o(STATE:State;count(STATE):Total)
|
Compare
each record with its next neighbor and won’t sort the result set.
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select
* from EMPLOYEE")
|
=A11.groups@i(STATE=="California":IsCalifornia;count(STATE):count)
|
（原文含示意图，图像资源未迁移）
Start
a new group if the current record meets the condition STATE=="California".
|
=file("D:/emp10.txt").cursor@t()
|
For
data file emp10.txt , every 10
records are ordered by DEPT
（原文含示意图，图像资源未迁移）
|
=A13.groups@h(DEPT:DEPT;sum(SALARY):bouns)
|
As A13 is grouped
and ordered by DEPT, use @h option to speed up grouping .
（原文含示意图，图像资源未迁移）
|
=demo.query("select * from employee")
|
=A15.cursor@m(3)
|
Return a
multicursor .
|
=A16.groups(STATE:state;sum(SALARY):salary)
|
Group the
multicursor with groups function .
（原文含示意图，图像资源未迁移）
When there are
empty values:
|
=demo.cursor("select
* from SCORES where CLASS = 'Class three'")
|
=A1.groups@t(STUDENTID:StudentID;sum(SCORE):TotalScore)
|
Return an empty
table sequence with the original data structure .
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select
* from DEPT")
|
Below
is content of DEPT table:
（原文含示意图，图像资源未迁移）
|
=A3.groups@0(FATHER)
|
Discard groups on
which Father field expression gets empty value .
（原文含示意图，图像资源未迁移）
Use
@z option to enable parallel processing:
|
=demo.cursor("select
* from SCORES")
|
=A1.groups@z(STUDENTID:StudentID;sum(SCORE):TotalScore;5)
|
Split
A1’s sequence according to groups during parallel computation; HASH space
size is 5.
Use
@e option to return a table sequence consisting of results of computing
function y :
|
=demo.cursor("select
* from SCORES")
|
=A1.groups@e(SUBJECT;maxp(SCORE))
|
（原文含示意图，图像资源未迁移）
Return
a table sequence consisting of result records of computing maxp(SCORE).
Perform
grouping and compute concatenation and union:
|
=connect("demo").cursor("select
* from employee").groups(DEPT;conj(NAME))
|
Group records in the cursor by DEPT and for each
group concatenate NAME values:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("select
* from employee").groups(DEPT;union(STATE))
|
Group records in the cursor by DEPT and for each
group union up STATE values:
（原文含示意图，图像资源未迁移）
