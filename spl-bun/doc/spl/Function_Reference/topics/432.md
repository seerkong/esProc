# hbase_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hbase_close()
hbase_close (con)
=hbase_open(" hdfs://192.168.0.8:9000 ")
=hbase_close(A1)
```

## 提取内容
hbase_close()
说明：
Close
a HBase connection.
语法：
hbase_close (con)
注意：
This
HbaseCli external library function (See
External Library Guide ) closes HBase connections.
参数：
|
con
|
Database
connection string .
示例：
|
=hbase_open(" hdfs://192.168.0.8:9000 ")
|
=hbase_close(A1)
|
Close
the HBase connection.
function:
hbase_ope n ()
