# hbase_cmp()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hbase_cmp()
hbase_cmp (str[,r][,b])
=hbase_open(" hdfs://192.168.0.8:9000 ")
=hbase_cmp("13")
=hbase_filter("ValueFilter","eq",A2)
=hbase_scan(A1,"emp";filter:A4)
=hbase_cmp@p("136")
=hbase_scan(A1,"emp";filter:hbase_filter("ValueFilter","eq",A5))
=hbase_cmp@r("J","i")
=hbase_scan(A1,"emp";filter:hbase_filter("ValueFilter","eq",A7))
=hbase_cmp@s("sp")
=hbase_scan(A1,"emp";filter:hbase_filter("SingleColumnValueFilter","company","name","=",A9))
hbase_open ( )
```

## 提取内容
hbase_cmp()
说明：
Compare
data with the HBase-supplied comparator.
语法：
hbase_cmp (str[,r][,b])
注意：
This
HbaseCli external library function (See
External Library Guide ) compares the current value and the threshold
with the HBase-supplied comparator.
选项：
|
@p
|
Match
the current value with the prefix .
|
@n
|
Check
if the current value is null but won’t perform matching .
|
@r
|
Use
a regular expression to match table data .
|
@s
|
Use
the threshold and table data, both of which are string type, to match up a
string .
|
@l
|
Convert
a string type or a date/time/datetime type parameter to a Long type one .
|
@b
|
Perform
bit-level comparison using AND, OR and XOR operations .
参数：
|
str
|
A string .
|
r
|
Only use this parameter with @r option; its
value can only be one of the following letters:
i=case insensitive (Case-insensitive)
m=multiline, (^ and $ can match
up a line break within a string)
d=dotall,
(The dot symbol can match up a line break)
u=unicode case, (Case-insensitive in matching up
Unicode characters )
q=canon_eq (Consider same
characters in different character encodings as equal)
x=unix_lines, (Consider \n as a line
break only)
l=literal,
(Text mode)
c=comments (Comment mode)
The value must be in quotation marks .
|
b
|
Only use this parameter with @b option; its
value can only be one of the following: AND, OR, XOR; the value must be in
quotation marks .
返回值：
Comparator
handle
示例：
|
=hbase_open(" hdfs://192.168.0.8:9000 ")
|
=hbase_cmp("13")
|
=hbase_filter("ValueFilter","eq",A2)
|
=hbase_scan(A1,"emp";filter:A4)
|
（原文含示意图，图像资源未迁移）
|
=hbase_cmp@p("136")
|
=hbase_scan(A1,"emp";filter:hbase_filter("ValueFilter","eq",A5))
|
（原文含示意图，图像资源未迁移）
|
=hbase_cmp@r("J","i")
|
=hbase_scan(A1,"emp";filter:hbase_filter("ValueFilter","eq",A7))
|
（原文含示意图，图像资源未迁移）
|
=hbase_cmp@s("sp")
|
=hbase_scan(A1,"emp";filter:hbase_filter("SingleColumnValueFilter","company","name","=",A9))
|
（原文含示意图，图像资源未迁移）
function:
hbase_open ( )
hbase_filt e r()
hbase_sca n ()
