# hbase_filter()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hbase_filter()
hbase_filter (filterName,filterArg)
hbase_filter(ColumnCountGetFilter,n)
hbase_filter(ColumnPaginationFilter,limit,columnOffset)
hbase_filter(Column前缀Filter,str)
hbase_filter(ColumnRangeFilter,minColumn,boolean,maxColumn,boolean)
hbase_filter(DependentColumnFilter,family,qualifier)
hbase_filter(FamilyFilter,ifequal,my-family)
hbase_filter(First键OnlyFilter)
hbase_filter(First键ValueMatchingQualifiersFilter,column)
hbase_filter(FuzzyRowFilter,fuzzy键sData)
hbase_filter(InclusiveStopFilter,stopRow键)
hbase_(键OnlyFilter)
hbase_filter(MultipleColumn前缀Filter,prefixes1,prefixes2,...prefixesN)
hbase_filter(PageFilter,n)
hbase_filter(前缀Filter,str)
hbase_filter(QualifierFilter,cmp,cmpColumn)
hbase_filter(RowFilter,cmp,cmpRow)
hbase_filter(ValueFilter,cmp,cmpValue)
hbase_filter(RandomRowFilter,n)
hbase_filter(SkipFilter,hbase_filter(ValueFilter,ifequal,cmpValue))
hbase_filter(WhileMatchFilter,hbase_filter())
hbase_filter(TimestampsFilter,timeStamp,boolean)
=hbase_open(" hdfs://192.168.0.8:9000 ")
=hbase_scan(A1,"emp")
=hbase_filter("ColumnCountGetFilter",3)
=hbase_scan(A1,"emp";filter:A3)
=hbase_filter("ColumnPaginationFilter",2, 2)
=hbase_scan(A1,"emp";filter:A5)
=hbase_filter("Column前缀Filter","na")
=hbase_scan(A1,"emp";filter:A7)
=hbase_filter("ColumnRangeFilter","age", true,
=hbase_scan(A1,"emp";filter:A9)
=hbase_filter("DependentColumnFilter","family","age")
=hbase_scan(A1,"emp";filter:A11)
=hbase_filter("FamilyFilter","=",hbase_cmp@s("family"))
=hbase_scan(A1,"emp";filter:A13)
=hbase_filter("键OnlyFilter")
=hbase_scan(A1,"emp";filter:A15)
=hbase_filter("First键OnlyFilter")
=hbase_scan(A1,"emp";filter:A17)
=hbase_filter("FuzzyRowFilter","row1",[1,1,0,0])
=hbase_scan(A1,"emp";filter:A19)
=hbase_filter("InclusiveStopFilter","row3")
=hbase_scan(A1,"emp";filter:A21)
=hbase_filter("MultipleColumn前缀Filter","na","tel","position")
=hbase_scan(A1,"emp";filter:A23)
=hbase_filter("前缀Filter","ro")
=hbase_scan(A1,"emp";filter:A25)
=hbase_filter("QualifierFilter","eq",
hbase_cmp@s("name"))
=hbase_scan(A1,"emp";filter:A27)
=hbase_filter("RowFilter","eq",
hbase_cmp("row1"))
=hbase_scan(A1,"emp";filter:A29)
=hbase_filter("ValueFilter","=",
hbase_cmp("C++"))
=hbase_scan(A1,"emp";filter:A31)
=hbase_filter("SingleColumnValueFilter","family","tel"
=hbase_scan(A1,"emp";filter:A33)
=hbase_filter("SingleColumnValueExcludeFilter","family","tel"
=hbase_scan(A1,"emp";filter:A35)
=hbase_filter("SkipFilter",hbase_filter("ValueFilter","=",
hbase_cmp@s("aaa")))
=hbase_scan(A1,"emp";filter:A37)
=hbase_filter("First键ValueMatchingQualifiersFilter","name")
=hbase_scan(A1,"emp";filter:A39)
=hbase_filter("TimestampsFilter",1488855959195,1488855959219,1488855959145,false)
=hbase_scan(A1,"emp";filter:A41)
=hbase_filter("RandomRowFilter",0.5)
```

## 提取内容
hbase_filter()
说明：
Filter
data with the HBase-supplied filter.
语法：
hbase_filter (filterName,filterArg)
Different filterName values represent different numbers and types of
specific parameters, as shown in the following:
|
hbase_filter(ColumnCountGetFilter,n)
|
Get
the first n values from each row
|
hbase_filter(ColumnPaginationFilter,limit,columnOffset)
|
For
each row, get field values, whose number is limit , starting from the (columnOffset+1) th
column
|
hbase_filter(Column前缀Filter,str)
|
For
each row, get field values whose corresponding column names have a prefix which
can match up str
|
hbase_filter(ColumnRangeFilter,minColumn,boolean,maxColumn,boolean)
|
For
each row, get values whose corresponding column names fall in a certain
range; the parameter boolean
indicates an open interval – when the value is false, or a closed interval –
when the value is true
|
hbase_filter(DependentColumnFilter,family,qualifier)
|
Specify
a column and perform filtering to get columns whose timestamp is different
|
hbase_filter(FamilyFilter,ifequal,my-family)
|
Specify
a column family, whose name is my-family ,
to perform the filtering; the value of ifequal
can be “=”, “!=”, etc.
|
hbase_filter(First键OnlyFilter)
|
Get
the first value from each row
|
hbase_filter(First键ValueMatchingQualifiersFilter,column)
|
Get
the first value from each row that satisfies the specified column qualifier
|
hbase_filter(FuzzyRowFilter,fuzzy键sData)
|
Perform
matching over fuzzy row key. Take a list parameter (List>) to do this. The
list has a pair of byte arrays, in which the first one is the row key format,
and the second one’s value is 0 or 1. 0 means a byte at same position in row
key must match; 1 means a byte at same position in row key can be different.
|
hbase_filter(InclusiveStopFilter,stopRow键)
|
Stop
data scanning if a specified row is reached
|
hbase_(键OnlyFilter)
|
Will
only return the key components, like rowkey, column family name, column name
and timestamp
|
hbase_filter(MultipleColumn前缀Filter,prefixes1,prefixes2,...prefixesN)
|
Allow
to specify multiple column prefixes
|
hbase_filter(PageFilter,n)
|
Pigination
filter for displaying n rows
|
hbase_filter(前缀Filter,str)
|
Specify
a prefix for rowkey
|
hbase_filter(QualifierFilter,cmp,cmpColumn)
|
To
filter based on column qualifier; cmp
is a comparison operator like equal (or=) and not equal (or !=); cmpColumn is column qualifier filtered
out through comparator.
|
hbase_filter(RowFilter,cmp,cmpRow)
|
To
filter based on row key; cmp is a
comparison operator like equal (or=) and not equal (or !=); cmpColumn is row key filtered out
through comparator.
|
hbase_filter(ValueFilter,cmp,cmpValue)
|
To
filter based on vlaue ; cmp is a
comparison operator like equal (or=) and not equal (or !=); cmpColumn is a value filtered out
through comparator.
|
hbase_filter(SingleColumnValueExcludeFilter,family,columnName,ifequal,cmpValue)/
hbase_filter(SingleColumnValueFilter,family,columnName,ifequal,cmpValue
|
Filter
rows based on specified column value; family
is the column family name, ifequal
is comparison operator, and cmpValue
is the result filtered out through comparator. columnName column will be excluded with SingleColumnValueExcludeFilter , while included with SingleColumnValueFilter .
|
hbase_filter(RandomRowFilter,n)
|
Filter
rows randomly and return a random result set with a certain probability. To
perform same RandomRowFilter on
same data set for multiple times, different result sets will be returned.
Paramameter n is float type; all
rows will be filtered away if n <=0,
and all will be kept if n >=1.
|
hbase_filter(SkipFilter,hbase_filter(ValueFilter,ifequal,cmpValue))
|
wrapper filter that filters an entire row if any of the cell is ineligible
work with ValueFilter.
|
hbase_filter(WhileMatchFilter,hbase_filter())
|
wrapper filter that stops scanning as soon as the filtering condition is met.
|
hbase_filter(TimestampsFilter,timeStamp,boolean)
|
Return
only values whose timestamps match up the specified timestamp.
注意：
This
HbaseCli external library function (See
External Library Guide ) filters data with the HBase-supplied filter.
参数：
|
filterName
|
HBase
filter name .
|
filterArg
|
Filter
parameter .
返回值：
Filter handle
示例：
|
=hbase_open(" hdfs://192.168.0.8:9000 ")
|
=hbase_scan(A1,"emp")
|
（原文含示意图，图像资源未迁移）
All
data of the emp table.
|
=hbase_filter("ColumnCountGetFilter",3)
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A3)
|
（原文含示意图，图像资源未迁移）
rowkey is a row name, and doesn’t belong to data columns.
|
=hbase_filter("ColumnPaginationFilter",2, 2)
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A5)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("Column前缀Filter","na")
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A7)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("ColumnRangeFilter","age", true,
"name",false)
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A9)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("DependentColumnFilter","family","age")
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A11)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("FamilyFilter","=",hbase_cmp@s("family"))
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A13)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("键OnlyFilter")
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A15)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("First键OnlyFilter")
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A17)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("FuzzyRowFilter","row1",[1,1,0,0])
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A19)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("InclusiveStopFilter","row3")
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A21)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("MultipleColumn前缀Filter","na","tel","position")
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A23)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("前缀Filter","ro")
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A25)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("QualifierFilter","eq",
hbase_cmp@s("name"))
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A27)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("RowFilter","eq",
hbase_cmp("row1"))
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A29)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("ValueFilter","=",
hbase_cmp("C++"))
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A31)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("SingleColumnValueFilter","family","tel"
,"eq",hbase_cmp@s("13"))
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A33)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("SingleColumnValueExcludeFilter","family","tel"
,"eq",hbase_cmp@s("13"))
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A35)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("SkipFilter",hbase_filter("ValueFilter","=",
hbase_cmp@s("aaa")))
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A37)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("First键ValueMatchingQualifiersFilter","name")
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A39)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("TimestampsFilter",1488855959195,1488855959219,1488855959145,false)
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A41)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("RandomRowFilter",0.5)
|
Filer
rows at random.
function:
hbase_cm p ()
hbase_sca n ()
