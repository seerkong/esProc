# hbase_filterlist()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hbase_filterlist()
hbase_filterlist (filterHandle1,filterHandle2,...filterHandleN)
=hbase_open(" hdfs://192.168.0.8:9000 ")
=hbase_filter("ColumnPaginationFilter",2, 0)
=hbase_scan(A1,"emp";filter:A2)
=hbase_filter("Column前缀Filter","na")
=hbase_scan(A1,"emp";filter:A4)
=hbase_filterlist(A2,A4)
=hbase_filterlist@o(A2,A4)
=hbase_scan(A1,"emp";filter:A6)
=hbase_scan(A1,"emp";filter:A7)
```

## 提取内容
hbase_filterlist()
说明：
Filter
data with multiple filters.
语法：
hbase_filterlist (filterHandle1,filterHandle2,...filterHandleN)
注意：
This
HbaseCli external library function (See
External Library Guide ) can accommodate another hbase_filterlist()
function within it. By default, the filtering conditions in all filters must be
met at the same time.
选项：
|
@o
|
Every
filter is independent in filtering data.
参数：
|
filterHandle
|
Filter
handle.
返回值：
Filter
handle
示例：
|
=hbase_open(" hdfs://192.168.0.8:9000 ")
|
=hbase_filter("ColumnPaginationFilter",2, 0)
|
=hbase_scan(A1,"emp";filter:A2)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("Column前缀Filter","na")
|
=hbase_scan(A1,"emp";filter:A4)
|
（原文含示意图，图像资源未迁移）
|
=hbase_filterlist(A2,A4)
|
=hbase_filterlist@o(A2,A4)
|
=hbase_scan(A1,"emp";filter:A6)
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A7)
|
（原文含示意图，图像资源未迁移）
function:
hbase_fi l ter()
hbase_ s can()
