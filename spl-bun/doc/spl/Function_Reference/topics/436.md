# hbase_get()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hbase_get()
=hbase_open(" hdfs://192.168.0.8:9000 ")
=hbase_scan(A1,"emp")
=hbase_get(A1,"emp","row3","company:name"::cname,"company:position":string:cpos,"family:tel":string)
```

## 提取内容
hbase_get()
说明：
Query
data in HBase database and return a one-row table sequence.
语法：
hbase_get (client,tableName,rowName,family:column:type:alias,family2:column2:type:alias2,...;filter:f,timeRange:[t1,t2]).
注意：
This
HbaseCli external library function (See
External Library Guide ) queries an HBbase database to return a
sing-row table sequence.
参数：
|
client
|
Database
connection information.
|
tableName
|
Table
name.
|
rowName
|
Row
name.
|
family
|
Column
family name.
|
column
|
Colum
name.
|
type
|
Data
type; can be omitted.
|
alias
|
Another
name of a column; can be omitted.
|
filter:f
|
“ filter: ”
is a fixed format, and parameter f
means filter handle; can be omitted.
|
timeRange:[t1,t2]
|
‘“ timeRange: ” ’ is a fixed format, and parameters t1 and t2 specify a
range of timestamp; can be omitted.
返回值：
Table
sequence
示例：
|
=hbase_open(" hdfs://192.168.0.8:9000 ")
|
=hbase_scan(A1,"emp")
|
（原文含示意图，图像资源未迁移）
Query
all table data.
|
=hbase_get(A1,"emp","row3","company:name"::cname,"company:position":string:cpos,"family:tel":string)
|
（原文含示意图，图像资源未迁移）
Find
a row whose rowkey is row3 , and
rename column names.
相关函数：
hbase_ope n ()
hbase_sca n ()
