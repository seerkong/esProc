# hbase_scan()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hbase_scan()
=hbase_open(" hdfs://192.168.0.8:9000 ")
=hbase_scan(A1,"emp")
=hbase_scan@c(A1,"emp")
=A3.fetch()
=hbase_filter("SingleColumnValueFilter","family","tel","=",hbase_cmp@s("13"))
=hbase_scan(A1,"emp";filter:A5)
=hbase_scan(A1,"emp","company:name":string:cname,"company:position":string:cpos,"family:tel":string;filter:A5,startRow:row3,stopRow:row7)
=hbase_scan(A1,"emp","company:name":string:cname,"company:position":string:cpos,"family:tel":string;filter:A5,row前缀:row1)
=hbase_scan(A1,"emp","company:name":string:cname,"company:position":string:cpos,"family:tel":string;filter:A5,startRow:row3
=hbase_scan(A1,"emp","company:name":string:cname,"company:position":string:cpos,"family:tel":string;filter:A5,startRow:row3,
hbase_cmp( )
```

## 提取内容
hbase_scan()
说明：
Query
a HBase database and return result as a cursor or a table sequence.
语法：
hbase_scan (client,tableName,family:column:type:alias,family2:column2:type:alias2,...;row前缀:x,filter:f,startRow:startrow,stopRow:stoprow,timeRange:[t1,t2]).
注意：
This
HbaseCli external library function (See
External Library Guide ) gets data from an HBase database and by
default returns a table sequence. Return a cursor when there’s @c option.
选项：
|
@c
|
Return
a cursor .
参数：
|
client
|
Database
connection information.
|
tableName
|
Table
name.
|
family
|
Column
family name.
|
column
|
Column
name.
|
Type
|
Data
type; can be omitted.
|
alias
|
Another
name of a column; can be omitted.
|
row前缀:x
|
“row前缀:” is a fixed format; parameter x
is the prefix of row键 .
|
filter:f
|
Filter
handle.
|
startRow:startrow
|
“startRow:” is a fixed format; parameter startrow
represents the starting row, which will be included in data retrieval.
|
stopRow:stoprow
|
“stopRow:” is a fixed format; parameter stoprow
represents the ending row, which will be excluded in data retrieval.
|
timeRange:[t1,t2]
|
“timeRange:[,]” is a fixed format; parameter t1
is the starting timestamp whose value will be retrieved, and parameter t2 is the ending timestamp whose value
won’t be retrieved.
返回值：
Table
sequence/游标
示例：
|
=hbase_open(" hdfs://192.168.0.8:9000 ")
|
=hbase_scan(A1,"emp")
|
（原文含示意图，图像资源未迁移）
Query
all table data.
|
=hbase_scan@c(A1,"emp")
|
（原文含示意图，图像资源未迁移）
Return
a cursor.
|
=A3.fetch()
|
（原文含示意图，图像资源未迁移）
|
=hbase_filter("SingleColumnValueFilter","family","tel","=",hbase_cmp@s("13"))
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp";filter:A5)
|
（原文含示意图，图像资源未迁移）
Return
data filtered out by A5.
|
=hbase_scan(A1,"emp","company:name":string:cname,"company:position":string:cpos,"family:tel":string;filter:A5,startRow:row3,stopRow:row7)
|
（原文含示意图，图像资源未迁移）
Get
data from row3 to row7 , which is excluded, from A5’s
filtered data.
|
=hbase_scan(A1,"emp","company:name":string:cname,"company:position":string:cpos,"family:tel":string;filter:A5,row前缀:row1)
|
（原文含示意图，图像资源未迁移）
Get
a row whose prefix is row1 from
A5’s filtered data.
|
=hbase_scan(A1,"emp","company:name":string:cname,"company:position":string:cpos,"family:tel":string;filter:A5,startRow:row3
,stopRow:row7,timeStamp:1488855958071)
|
（原文含示意图，图像资源未迁移）
|
=hbase_scan(A1,"emp","company:name":string:cname,"company:position":string:cpos,"family:tel":string;filter:A5,startRow:row3,
stopRow:row7,timeRange:[1488855958071,1488855958255])
|
（原文含示意图，图像资源未迁移）
function:
hbase_ope n ()
hbase_cmp( )
