# hdfs_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hdfs_close()
hdfs_close( hd )
=hdfs_open("hdfs://192.168.0.8:9000")
=hdfs_close(A1)
```

## 提取内容
hdfs_close()
说明：
Close
the HDFS connection.
语法：
hdfs_close( hd )
注意：
This
HdfsFileCli external library function (See External Library Guide ) closes
a connection to the HDFS file system.
参数：
|
hd
|
An hd connection.
示例：
|
=hdfs_open("hdfs://192.168.0.8:9000")
|
Connect
to the HDFS file system.
|
=hdfs_close(A1)
|
Close
the HDFS connection.
