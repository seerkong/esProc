# hive_open()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hive_open()
hive_open ( hdfsUrl,thriftUrl ,dbname ,hdfsUserName )
=hive_open("hdfs://192.168.0.8:9000","thrift://192.168.0.8:9083","hive","asus")
```

## 提取内容
hive_open()
说明：
Create
a Hive database connection.
语法：
hive_open ( hdfsUrl,thriftUrl ,dbname ,hdfsUserName )
注意：
This HiveCli external
library function (See External Library Guide )
connects to a Hive database. The format of the parameter hdfsUrl is hdfs://ip:port, which represents the HDFS file’s IP and port
number. The format of the parameter thriftUrl is thrift://ip:port, which represents the Thrift file’s IP and port
number.
参数：
|
hdfsUrl
|
HDFS
file connection information .
|
thriftUrl
|
Thrift
file connection information .
|
dbname
|
database name .
|
hdfsUserName
|
HDFS
file user name .
Return
value:
游标
示例：
|
=hive_open("hdfs://192.168.0.8:9000","thrift://192.168.0.8:9083","hive","asus")
|
Connect
to the hive database.
function:
hive_clo s e ()
hive_curs o r ()
hive_que r y ( )
