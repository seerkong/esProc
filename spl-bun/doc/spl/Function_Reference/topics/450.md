# hive_cursor()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hive_cursor()
hive_cursor ( con,sql )
=hive_client("hdfs://192.168.0.8:9000","thrift://192.168.0.8:9083","hive","asus")
=hive_cursor(A1, "select
=hive_close(A1)
hive_close ( )
hive_open()
```

## 提取内容
hive_cursor()
说明：
Query
a Hive database and return the result as a cursor.
语法：
hive_cursor ( con,sql )
注意：
This
HiveCli external library function (See
External Library Guide ) performs a SQL query and returns the result
as a cursor.
参数：
|
con
|
Database
connection string
|
sql
|
SQL query statement, like select * from table
Return
value:
游标
示例：
|
=hive_client("hdfs://192.168.0.8:9000","thrift://192.168.0.8:9083","hive","asus")
|
Connect
to the hive database.
|
=hive_cursor(A1, "select
* from table")
|
Return
a cursor.
|
=hive_close(A1)
|
function:
hive_close ( )
hive_open()
hive_que r y()
