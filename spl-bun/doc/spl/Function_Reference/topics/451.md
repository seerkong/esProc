# hive_db()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hive_db()
hive_db ( con )
=hive_open("hdfs://192.168.0.8:9000","thrift://192.168.0.8:9083","hive","asus")
=hive_db(A1)
=hive_close(A1)
```

## 提取内容
hive_db()
说明：
Obtain a list of Hive databases.
语法：
hive_db ( con )
注意：
HiveCli external library function (See External Library Guide for uses).
It obtains a list of Hive databases.
参数：
|
con
|
Database connection string.
返回值：
Table sequence
示例：
|
=hive_open("hdfs://192.168.0.8:9000","thrift://192.168.0.8:9083","hive","asus")
|
Connect to Hive database.
|
=hive_db(A1)
|
Return a table sequence.
|
=hive_close(A1)
|
