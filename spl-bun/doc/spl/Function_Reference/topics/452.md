# hive_execute()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hive_execute()
hive_execute(con,sql)
=hive_client("hdfs://192.168.0.8:9000/","thrift://192.168.0.8:9083","hive","asus")
=hive_execute(A1, "create table stu2( id string, name string,age
=hive_execute(A1, "drop table stu")
=hive_execute(A1, "alter table stu2 add columns (ncol string)")
=hive_execute(A1, "insert into stu2 values
=hive_execute(A1,"insert overwrite table stu select * from stu2
hive_open()
```

## 提取内容
hive_execute()
说明：
Execute
Hive’s SQL-like queries.
语法：
hive_execute(con,sql)
注意：
This HiveCli external
library function (See External Library Guide ) supports
executing Hive’s SQL-like statements, including create table , drop table ,
insert into , insert overwrite , and create
view .
参数：
|
con
|
Database
connection string .
|
sql
|
SQL query statement .
Return
value:
Boolean
示例：
|
=hive_client("hdfs://192.168.0.8:9000/","thrift://192.168.0.8:9083","hive","asus")
|
Connect
to the hive database.
|
=hive_execute(A1, "create table stu2( id string, name string,age
string)")
|
Create
a table
|
=hive_execute(A1, "drop table stu")
|
Drop
a table.
|
=hive_execute(A1, "alter table stu2 add columns (ncol string)")
|
Add
a column to the table.
|
=hive_execute(A1, "insert into stu2 values
('2','dfff','21','lll')")
|
Insert
data into the table.
|
=hive_execute(A1,"insert overwrite table stu select * from stu2
|
Overwrite
table stu with data in table stu2 .
function:
hive_open()
