# hive_query()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hive_query()
hive_query ( con,sql )
=hive_client("hdfs://192.168.0.8:9000","thrift://192.168.0.8:9083","hive","asus")
=hive_query(A1, "select
=hive_close(A1)
hive_open()
```

## 提取内容
hive_query()
说明：
Query
a Hive database.
语法：
hive_query ( con,sql )
注意：
This HiveCli external library function (See External Library Guide ) executes a certain SQL query on a Hive
database, and returns the query result as a table sequence.
参数：
|
con
|
Database
connection string .
|
sql
|
SQL query statement, like select *
from tabl e .
Return
value:
Table
sequence
示例：
|
=hive_client("hdfs://192.168.0.8:9000","thrift://192.168.0.8:9083","hive","asus")
|
Connect
to the hive database.
|
=hive_query(A1, "select
* from table")
|
Query
data in table.
|
=hive_close(A1)
|
function:
hive_clo s e ( )
hive_cur s o r ()
hive_open()
