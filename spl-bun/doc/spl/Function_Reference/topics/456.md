# hosts(i;j)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hosts(i;j)
hosts ( i; j )
=hosts(2;"task2")
=hosts(;"task2")
=hosts(0;"task2")
=hosts(;"task2")
```

## 提取内容
hosts(i;j)
说明：
Set
or get the number of memory zone holding a specified task on the current node.
语法：
hosts ( i; j )
注意：
The
function gets the number of memory zone accommodating task j on the
current node when parameter i is absent; sets the number of memory zone
holding task j on the current node when both parameter i and
parameter j are present; and clear the existing memory zone number when i
is 0. It is usually used in the initiation file init.splx .
Only
supported by esProc Enterprise Edition.
参数：
|
i
|
An
integer that represents memory zone number .
|
j
|
Task
name; can be absent .
Return
value:
Boolean/Integer
示例：
|
=hosts(2;"task2")
|
Set the number of memory zone holding task
2 on the current node as 2, and return true .
|
=hosts(;"task2")
|
Get the number of memory zone holding task
2 on the current node as 2, and return true .
|
=hosts(0;"task2")
|
Delete the number of memory zone holding
task 2 and return true since parameter i is 0 .
|
=hosts(;"task2")
|
Then get the number of memory zone holding
task 2 on the current node as 2, and return false .
