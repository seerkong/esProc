# hosts(n,h s;j)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
hosts(n,h s;j)
hosts ( n,hs; j )
=file("emp.ctx":i)
=B2.open().cursor().fetch()
>env(EMP,B3)
=hosts(2,A1;"task1")
=memory(A2,EMP)
=B4.cursor().fetch()
>output("ERROR!")
=hosts(0,A1;"task1")
```

## 提取内容
hosts(n,h s;j)
说明：
Return
a sequence of available nodes.
Syntax ；
hosts ( n,hs; j )
注意：
The
function finds the number of memory zone holding task j across a node
list hs , and returns a sequence of available nodes in the format of
1,…,n.
If
the target memory zone cannot be found on a certain node, execute initiation
using init.splx on it where the function gets null. The function returns null
if it cannot find the sufficient number of available nodes.
The
function returns a sequence of all nodes, where the unavailable nodes are
recorded as nulls.
Only
supported by esProc Enterprise Edition.
参数：
|
n
|
The
number of nodes .
|
hs
|
node sequence .
|
j
|
Task
name .
Return
value:
node sequence
示例：
Parameter
configuration for init.splx on node 192.168.46.1:
（原文含示意图，图像资源未迁移）
Parameter
configuration for init.splx on node 192.168.31.39:
（原文含示意图，图像资源未迁移）
init.splx script:
|
if i>0
|
=file("emp.ctx":i)
|
1.emp.ctx and 2.emp.ctx are
respectively stored in the main paths of node 192.168.46.1 and node
192.168.31.39.
|
=B2.open().cursor().fetch()
|
>env(EMP,B3)
|
Store
B3’s result in global variable EMP .
Start
node 192.168.46.1:8281 and node 192.168.31.39:8281 , search for the
available nodes, and compute data stored in the specified memory zone:
|
[192.168.46.1:8281,192.168.31.39:8281,192.168.46.1:8282]
|
=hosts(2,A1;"task1")
|
Return
a sequence of nodes containing task 1 in memory zones 1 and 2 from A1’s node
sequence.
（原文含示意图，图像资源未迁移）
|
if A2!=null
|
Judge
whether A2 can find an enough number of available nodes after it is executed,
and return null if it cannot.
|
=memory(A2,EMP)
|
Generate
a cluster composite table using the global variables EMP stored in the
memory zones of all nodes found in A2.
|
=B4.cursor().fetch()
|
Query data in A4’s cluster composite table .
|
else
|
>output("ERROR!")
|
=hosts(0,A1;"task1")
|
With parameter n being 0, return a
sequence of all nodes the unavailable one is recorded as null .
（原文含示意图，图像资源未迁移）
