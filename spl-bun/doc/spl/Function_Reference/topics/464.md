# P.i()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.i()
=demo.query("select
=A1.i()
```

## 提取内容
P.i()
说明：
Convert
a specified table sequence to a pure table sequence.
语法：
P . i ( )
注意：
The
function converts table sequence P to a pure record sequence or a pure table sequence that uses
columnwise storage. A pure table sequence is a table sequence where all fields
are made pure.
Error
is reported when field values of a pure table sequence are modified.
Only
supported by esProc Enterprise Edition.
参数：
|
P
|
table sequence .
Return
value:
Pure
record sequence/Pure table sequence
示例：
|
=demo.query("select
EID,NAME,GENDER,DEPT,BIRTHDAY from employee")
|
Return
a table sequence whose all fields are pure fields.
（原文含示意图，图像资源未迁移）
|
=A1.i()
|
Convert
A1’s table sequence to a pure table sequence.
