# icount()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
icount()
icount ( x 1 ,…, x n )
=["a","c","d","e","f","a","a"]
=A1.icount()
=[1,4,9,16,25,36,49,64,81,100,81]
=A3.icount@n()
=A3.icount@b()
=icount("a","c","d","e","f","a","a")
=demo.query("select
=A1.icount@o(DEPT)
```

## 提取内容
icount()
说明：
Count
the number of distinct members in a sequence.
语法：
|
A. icount(x)
|
icount ( x 1 ,…, x n )
|
Equivalent to A .icount() , where x 1 ,…, x n are members of sequence A; do not support working
with options
注意：
The
function calculates the number of distinct members in sequence A .
参数：
|
sequence .
|
x
|
An
expression; cannot be omitted when A is a table sequence or a record sequence .
选项：
|
@o
|
Assume sequence A is ordered
and only remove duplicates by comparing neighboring members .
|
@n
|
Judge whether a member is distinct or not
according to position when parameter A is a number sequence .
|
@b
|
Judge whether a member is distinct or not
according to the long type bit when parameter A is a number sequence .
Return
value:
Integer
示例：
When
A is a sequence:
|
=["a","c","d","e","f","a","a"]
|
=A1.icount()
|
=[1,4,9,16,25,36,49,64,81,100,81]
|
An
integer sequence.
|
=A3.icount@n()
|
10 ; judge whether a
member is distinct or not according to the position.
|
=A3.icount@b()
|
10 ; judge whether a
member is distinct or not according to the long type bit.
|
=icount("a","c","d","e","f","a","a")
|
5 , which is same as
A2.
When
A is a table sequence
or a record sequence:
|
=demo.query("select
EID,NAME,GENDER,DEPT from EMPLOYEE") .sort(DEPT)
|
Return
a table sequence ordered by DEPT.
|
=A1.icount@o(DEPT)
|
Find
number of departments in the EMPLOYEE table.
