# A.i d()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select
=A1.id(DEPT)
=A1.id@o(DEPT)
=A1.id([DEPT,GENDER])
=["a","b","c","a"].id()
=A1.id@u([DEPT,GENDER])
=A1.id(DEPT,GENDER)
=A1.id(DEPT,GENDER;4)
=file("D:/emp10.txt").import@t()
=A9.id@h(DEPT)
=demo.query("select * from DEPT")
=A1.id(FATHER)
=A1.id@0(FATHER)
```

## 提取内容
A.i d()
说明：
Get
distinct values from a sequence.
语法：
A .id ( xi,…;n )
注意：
The function gets top n
distinct values of the expressions x i ,….
If the number of distinct values in an expression x is less than n , return
all distinct values as a sequence; the result set consists of one ordinary
sequence when there is only one expression x .
参数：
|
x i
|
An
expression; use ~ to represent x if
the latter is omitted .
|
n
|
The
number of to-be-retrieved distinct
values counting from the beginning; return all values if it is absent .
选项：
|
@o
|
Without
sorting, remove the neighboring duplicate members only .
|
@u
|
Do
not sort the result set by x ; it
doesn’t work with @o .
|
@h
|
Used
over a grouped table with each group ordered to speed up grouping .
|
@0
|
Discard
members on which expression x is computed and gets empty result .
|
@m
|
Enable parallel processing to increase
performance of complex computations on a large volume of data, with
indefinite computing order; the parameter and @o option are
mutually-exclusive .
|
@n
|
Judge whether a member is distinct or not
according to position when there is only one x i
and x i
is a natural number .
|
@b
|
Judge whether a member is distinct or not
according to the bit length in a byte in order to reduce storage usage when
there is only one x i
and x i
is a natural number .
Return value :
Sequence
示例：
|
=demo.query("select
* from EMPLOYEE")
|
=A1.id(DEPT)
|
（原文含示意图，图像资源未迁移）
Sort
members in ascending order.
|
=A1.id@o(DEPT)
|
（原文含示意图，图像资源未迁移）
No
sorting.
|
=A1.id([DEPT,GENDER])
|
（原文含示意图，图像资源未迁移）
Get
the distinct values after sorting by DEPT & GENDER .
|
=["a","b","c","a"].id()
|
Omitting
x indicates it is the sequence
members themselves that will be computed to get the distinct values. Return ["a","b","c"] as
with this case.
|
=A1.id@u([DEPT,GENDER])
|
Do
not sort the result set by x .
|
=A1.id(DEPT,GENDER)
|
Return
all distinct values as parameter n
is absent.
（原文含示意图，图像资源未迁移）
（原文含示意图，图像资源未迁移）
（原文含示意图，图像资源未迁移）
|
=A1.id(DEPT,GENDER;4)
|
Return
the first 4 distinct values; return all GENDER values as the field has less
than 4 distinct values.
（原文含示意图，图像资源未迁移）
（原文含示意图，图像资源未迁移）
（原文含示意图，图像资源未迁移）
|
=file("D:/emp10.txt").import@t()
|
For
data file emp10.txt , every 10
records are ordered by DEPT.
（原文含示意图，图像资源未迁移）
|
=A9.id@h(DEPT)
|
As
A19 is grouped and ordered by DEPT, @h option is used to speed up grouping.
（原文含示意图，图像资源未迁移）
|
=demo.query("select * from DEPT")
|
（原文含示意图，图像资源未迁移）
|
=A1.id(FATHER)
|
返回值： [null,1,2,11,12] .
|
=A1.id@0(FATHER)
|
返回值： [1,2,11,12] .
相关函数：
A. g r oup ( x i, … )
