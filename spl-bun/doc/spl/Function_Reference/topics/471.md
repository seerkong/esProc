# cs.i d()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.cursor("select *
=A1.id(#1,DEPT;5)
=demo.cursor("select *
=A3.id(DEPT;5)
=demo.cursor("select * from EMPLOYEE"
=demo.cursor("select * from EMPLOYEE" ).id@u(DEPT)
=demo.cursor("select
=A1.id(FATHER)
=A1.reset()
=A1.id@0(FATHER)
```

## 提取内容
cs.i d()
说明：
Generate a sequence consisting of values of fields in a given cursor.
语法：
cs. id ( x i ,…; n )
注意：
The function generates a
sequence of sequences, each of which is made up of values of x i field in cursor cs . Get at most n values for each x i field
return a sequence when there is only one x i
field.
参数：
|
cs
|
A cursor
|
x i
|
An expression; use comma
to separate multiple expressions .
|
n
|
An integer; return all
values when omitted .
选项：
|
@o
|
Do not sort data while removing neighboring duplicate members; require
that data is ordered by expression x .
|
@u
|
Do not sort result set by
expression x ; the option and @o are mutually exclusive .
|
@h
|
Used to segment ordered
data, which increases efficiency .
|
@0
|
Discard members on which
expression x is computed and gets empty result .
|
@n
|
Judge whether a member is
distinct or not according to position when there is only one x i
and x i is a natural number .
|
@b
|
Judge whether a member is
distinct or not according to the bit length in a byte in order to reduce
storage usage when there is only one x i and x i
is a natural number .
返回值：
Sequence
示例：
|
=demo.cursor("select *
from EMPLOYEE" )
|
=A1.id(#1,DEPT;5)
|
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select *
from EMPLOYEE" )
|
=A3.id(DEPT;5)
|
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select * from EMPLOYEE"
).sortx(DEPT).id@o(DEPT)
|
Do not sort records, but remove the neighboring duplicate records only .
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select * from EMPLOYEE" ).id@u(DEPT)
|
Do not sort result set by
DEPT.
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select
* from DEPT")
|
Below is content of DEPT table:
（原文含示意图，图像资源未迁移）
|
=A1.id(FATHER)
|
返回值： [null,1,2,11,12] .
|
=A1.reset()
|
=A1.id@0(FATHER)
|
返回值： [1,2,11,12] .
