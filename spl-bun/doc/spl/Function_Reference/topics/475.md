# if x …else …

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=2
>A1=A1+4
>A1=A1+5
```

## 提取内容
if x …else
…
说明：
The introduction of if statement.
语法：
if x … else …
注意：
if and else are in the same line. If x is true, then
execute the code between if and else ; if x is false, execute
the code after else till the end of
this line. The else… part can be
omitted.
参数：
|
x
|
A Boolean expression .
示例：
|
D
|
=2
|
A1 returns
7 .
|
if A1>5
|
>A1=A1+4
|
else
|
>A1=A1+5
|
Related functions:
if x …{ e l se { if
x } … }
if x e l s e
i f x
