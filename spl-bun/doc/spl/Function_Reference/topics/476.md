# if x else

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=3
>A1=A1+10
>A1=A1+15
```

## 提取内容
if x
else
说明：
The introduction of if statement.
语法：
if x
…
else
…
注意：
if and else must be in the same column. Execute if code block if x is true and execute else code block if x is false. The else part
can be omitted.
参数：
|
x
|
A Boolean expression .
示例：
|
=3
|
if A1>5
|
if
code block.
|
>A1=A1+10
|
else
|
else code block.
|
>A1=A1+15
相关函数：
if x …{ else { i f x } … }
if x … el s e
…
if x
