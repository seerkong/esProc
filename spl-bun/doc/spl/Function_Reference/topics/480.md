# ifind()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ifind()
=demo.cursor("select
=A1.index(test_ifind,SALARY>=11000;GENDER)
=A1.ifind("M";test_ifind)
=A1.ifind@p("M";test_ifind)
=A1.ifind@1("M";test_ifind)
```

## 提取内容
ifind()
说明：
Search for a specified key value in an in-memory table according to
the index.
语法：
T . ifind ( k,...;I )
注意：
The function searches for
key value k in in-memory table T according to index I and
return a sequence of matching records, or null if no matching records are
found.
参数：
|
T
|
An in-memory table .
|
I
|
Index name .
|
k
|
键 value .
选项：
|
@p
|
Return a sequence of sequence
numbers of matching records .
|
@1
|
Return the first matching
record .
返回值：
Sequence/null
示例：
|
=demo.cursor("select
EID,NAME,GENDER,SALARY from employee").memory()
|
Return an in-memory
table.
|
=A1.index(test_ifind,SALARY>=11000;GENDER)
|
Create test_ifind
index on GENDER field.
|
=A1.ifind("M";test_ifind)
|
（原文含示意图，图像资源未迁移）
Use test_ifind
index to search for and return records of the in-memory table whose key value
is M according to test_ifind index.
|
=A1.ifind@p("M";test_ifind)
|
（原文含示意图，图像资源未迁移）
Return a sequence of sequence
numbers of records.
|
=A1.ifind@1("M";test_ifind)
|
（原文含示意图，图像资源未迁移）
Return the first-found
record.
