# ifn()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ifn()
ifn ( x 1 ,…, x n )
=[1,2,4].ifn()
=[1,2,4].ifn(~*10)
=[null,2,3,4].ifn()
=[null,"",2,3,4].ifn()
=ifn(null,2,3,4)
=create(ID,NAME,GENDER).record([1,"Rebecca",,2,"Ashley","F",3,"Matthew","M"])
=A1.ifn(NAME)
=A1.ifn(GENDER)
```

## 提取内容
ifn()
说明：
Compute x with each member of the sequence and
return the first non-null member of the new sequence.
语法：
|
A . ifn ( x )
|
ifn ( x 1 ,…, x n )
|
Equivalent to A .ifn() , where x 1 ,…, x n are members of sequence A .
注意：
The function loops through
members of sequence A to compute
expression x and returns the first
non-null member.
参数：
|
A sequence .
|
x
|
An expression; cannot be
omitted when sequence A is a table sequence or a record
sequence .
返回值：
The first non-null member
示例：
When A is a sequence:
|
=[1,2,4].ifn()
|
1 .
|
=[1,2,4].ifn(~*10)
|
10 ; return the first member after computing the
expression.
|
=[null,2,3,4].ifn()
|
2 ; return the first non-null member.
|
=[null,"",2,3,4].ifn()
|
“” .
|
=ifn(null,2,3,4)
|
2, which is same as A3.
When A is a table sequence or a record sequence:
|
=create(ID,NAME,GENDER).record([1,"Rebecca",,2,"Ashley","F",3,"Matthew","M"])
|
（原文含示意图，图像资源未迁移）
|
=A1.ifn(NAME)
|
Get the first non-null
NAME value.
|
=A1.ifn(GENDER)
|
Get the first non-null
GENDER value.
