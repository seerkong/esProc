# ifpure()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ifpure()
ifpure( x )
=["a","w","s"]
=ifpure(A1)
=A1.i()
=ifpure(A3)
=3.new(~:ID,~*~:Num)
=ifpure(A1)
=A1.i()
=ifpure(A3)
ifr()
ifa()
ifv()
```

## 提取内容
ifpure()
说明：
Judge whether a data object
is a pure sequence or a pure table sequence.
语法：
ifpure( x )
注意：
The function judges whether
data object x is a pure sequence or a pure table sequence. Only supported by
esProc Enterprise Edition.
参数：
|
x
|
Any data object,
including constant, expression, record, sequence, table sequence and record
sequence .
返回值：
Boolean
示例：
Judge whether a data object
is pure sequence:
|
=["a","w","s"]
|
A sequence.
|
=ifpure(A1)
|
Judge whether A1 is a pure sequence or a pure table
sequence, and return false .
|
=A1.i()
|
Convert A1 into a pure sequence .
|
=ifpure(A3)
|
Return true .
Judge whether a data object
is pure table sequence:
|
=3.new(~:ID,~*~:Num)
|
A table sequence.
|
=ifpure(A1)
|
Judge whether A1 is a pure sequence or a pure table
sequence, and return false .
|
=A1.i()
|
Convert A1 into a pure table sequence .
|
=ifpure(A3)
|
Return true .
相关函数：
ifr()
ifa()
ifv()
