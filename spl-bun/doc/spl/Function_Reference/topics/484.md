# ifr()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ifr()
ifr( x )
=demo.query("select * from
=ifr(A1(1))
=ifr(A1)
=ifr(A1(1).BIRTHDAY)
```

## 提取内容
ifr()
说明：
Find whether an object is a
record.
语法：
ifr( x )
注意：
The function finds whether
the parameter x is a record.
参数：
|
x
|
Any data object, for
example a constant, an expression, a record, a record sequence, a sequence or
a table sequence .
返回值：
Boolean
示例：
|
=demo.query("select * from
EMPLOYEE")
|
=ifr(A1(1))
|
true
|
=ifr(A1)
|
false
|
=ifr(A1(1).BIRTHDAY)
|
false
相关函数：
if t ( )
i f a()
if v ()
