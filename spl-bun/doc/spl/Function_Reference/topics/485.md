# ifstring()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ifstring()
ifstring( Exp )
ifstring("abc")
ifstring(1234)
ifstring("1980-01-01")
ifstring(date("1980-01-01"))
```

## 提取内容
ifstring()
说明：
Find whether the specified
data is string type.
语法：
ifstring( Exp )
注意：
The function finds whether
the parameter Exp is of string type.
参数：
|
Exp
|
An expression of any data
type .
返回值：
Boolean
示例：
|
ifstring("abc")
|
true
|
ifstring(1234)
|
false
|
ifstring("1980-01-01")
|
true
|
ifstring(date("1980-01-01"))
|
false
相关函数：
ifn u m b e r()
