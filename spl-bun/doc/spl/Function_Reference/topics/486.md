# ift()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ift()
ift( x )
=demo.query("select * from
=ift(A1(1))
=ift(A1)
=ift([1,2,3])
ifa( )
```

## 提取内容
ift()
说明：
Find whether an object is a
table sequence.
语法：
ift( x )
注意：
The function finds whether
the parameter x is a table sequence.
参数：
|
x
|
Any object, for example a
constant, an expression, a record, a record sequence, a sequence or a table
sequence.
返回值：
Boolean
示例：
|
=demo.query("select * from
EMPLOYEE")
|
=ift(A1(1))
|
false
|
=ift(A1)
|
true
|
=ift([1,2,3])
|
false
相关函数：
if r ()
ifa( )
if v ( )
