# ifx_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ifx_close()
ifx_close (ifxconn)
="d:/frag.txt"
=ifx_conn("com.informix.jdbc.IfxDriver","jdbc:informix-sqli://192.168.0.3:9088/tpch:informixserver=tpch&user=informix&pwd=informix";A1)
=ifx_close(A2)
```

## 提取内容
ifx_close()
说明：
Close Informix database
connection.
语法：
ifx_close (ifxconn)
注意：
The InformixCli
external library function (See External Library Guide ) closes
connection to Informix database.
参数：
|
ifxconn
|
Ifxconn object .
示例：
|
="d:/frag.txt"
|
A fragmentation
information file.
|
=ifx_conn("com.informix.jdbc.IfxDriver","jdbc:informix-sqli://192.168.0.3:9088/tpch:informixserver=tpch&user=informix&pwd=informix";A1)
|
Connect to Informix
database and get the fragmentation information via a file handle.
|
=ifx_close(A2)
|
Close Informix database
connection.
