# ifx_listfrag()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ifx_listfrag()
ifx_listfrag (ifxconn, table)
="d:/frag1.txt"
=ifx_conn("com.informix.jdbc.IfxDriver","jdbc:informix-sqli:192.168.0.7:8000/tpch:informixserver=tramsserver&user=informix&pwd=informix";A1)
=ifx_takefrag(A2)
=ifx_listfrag(A2)
=
ifx_listfrag(A2,"orders")
```

## 提取内容
ifx_listfrag()
说明：
List the fragmentation information of a specified table with table
sequences.
语法：
ifx_listfrag (ifxconn, table)
注意：
The InformixCli external library
function (See External Library Guide ) lists the
fragmentation information of a specified table with table sequences on the
condition that the fragmentation information is already written into the
ifxconn object. List the fragmentation information all fragmented table being
fragmented if parameter table is
absent.
参数：
|
ifxconn
|
The ifxconn object which
holds the fragmentation information .
|
table
|
A table being fragmented .
返回值：
A table sequence
示例：
|
="d:/frag1.txt"
|
Create
and save a fragmentation information file.
|
=ifx_conn("com.informix.jdbc.IfxDriver","jdbc:informix-sqli:192.168.0.7:8000/tpch:informixserver=tramsserver&user=informix&pwd=informix";A1)
|
Connect
to Informix database.
|
=ifx_takefrag(A2)
|
Extract
the fragmentation information of all specified tables.
|
=ifx_listfrag(A2)
|
List
the fragmentation information of all specified tables.
（原文含示意图，图像资源未迁移）
|
=
ifx_listfrag(A2,"orders")
|
List
fragmentation information of the orders
table.
（原文含示意图，图像资源未迁移）
