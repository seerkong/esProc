# ifx_setfrag()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ifx_setfrag()
ifx_setfrag ( ifxconn, table, fieldName, min1, min2,.... )
="d:/frag1.txt"
=ifx_conn("com.informix.jdbc.IfxDriver","jdbc:informix-sqli:192.168.0.7:8000/tpch:informixserver=tramsserver&user=informix&pwd=informix";A1)
=ifx_takefrag(A2,"orders")
=ifx_setfrag(A2,"orders","o_orderkey",1,400000000)
=ifx_setfrag(A2,"orders","o_orderkey")
```

## 提取内容
ifx_setfrag()
说明：
Set the fragmentation
information of a specified table.
语法：
ifx_setfrag ( ifxconn, table, fieldName, min1, min2,.... )
注意：
The InformixCli
external library function (See External Library Guide ) sets
the fragmentation information of the specified table. If the parameters after
parameter fieldname are absent, then
delete the specified table’s fragmentation information stored in the IfxConn
object. The the number of the values of parameter min is the number of fragmented files the specified table has.
参数：
|
ifxconn
|
An ifxconn object .
|
table
|
A table being fragmented .
|
fieldName
|
Fragmented field .
|
min
|
The minimum value in a
fragmented file; can be omitted and can have multiple values .
返回值：
Table sequence/null
示例：
|
="d:/frag1.txt"
|
Create
and save a fragmentation information file.
|
=ifx_conn("com.informix.jdbc.IfxDriver","jdbc:informix-sqli:192.168.0.7:8000/tpch:informixserver=tramsserver&user=informix&pwd=informix";A1)
|
Connect
to Informix database.
|
=ifx_takefrag(A2,"orders")
|
Extract
fragmentation information of the orders
table.
（原文含示意图，图像资源未迁移）
|
=ifx_setfrag(A2,"orders","o_orderkey",1,400000000)
|
Set
fragmentation information of the orders
table.
（原文含示意图，图像资源未迁移）
|
=ifx_setfrag(A2,"orders","o_orderkey")
|
Delete
the fragmentation information.
（原文含示意图，图像资源未迁移）
