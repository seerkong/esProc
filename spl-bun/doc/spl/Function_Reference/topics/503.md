# P.impute()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.impute()
P.impute(cn)
P.impute@r( cn, Rec )
=T("D:
=A1.impute@I(4)
=A1.impute@rc("LotFrontage",A2(2))
```

## 提取内容
P.impute()
说明：
Impute missing values to a
table sequence/record sequence type variable during modeling.
语法：
|
P.impute(cn)
|
During
modeling, impute missing values to variable cn and return a binary sequence
containing the imputation result and imputation process records Rec.
|
P.impute@r( cn, Rec )
|
During
scoring, impute missing values to variable cn according to imputation process
records Rec.
注意：
The MathCli external
library function (See External Library Guide ).
参数：
|
P
|
table sequence/record sequence.
|
cn
|
string/number, which is the name of column (or the column number starting
from 1) – the to-be-pre-processed variable – in a table sequence or record
sequence.
|
Rec
|
sequence of imputation process records.
选项：
|
@c
|
Modify
the original data to the imputation result after execution.
|
@BNIED
|
Each
option specifies a target type, and options are mutual-exclusive
automatically judge the type when no option is used; the order of priorities
of the options is binary, numeric, integer and date.
返回值：
Sequence
示例：
|
=T("D:
//house_prices_train.csv")
|
=A1.impute@I(4)
|
（原文含示意图，图像资源未迁移）
Impute missing values to the variable of
column 4:
A2(1) Imputation result
A2(2) Imputation process records.
@I specifies the type of variable as
integer.
|
=A1.impute@rc("LotFrontage",A2(2))
|
（原文含示意图，图像资源未迁移）
Impute missing data to A1’s variable
according to A2’s imputation process records and return imputation result.
@c enables changing the original data to
the imputation result after execution.
