# T.index()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.index()
=demo.query("select EID,NAME,SALARY from EMPLOYEE where
=A1.keys(EID)
=A1.index(10)
=A1.keys(EID,NAME)
=3.new(k(~:2):id,~*~:num)
=A1.keys(id)
=A2.index@s()
=A2.index(0)
=connect("demo").query("SELECT
=connect("demo").query("SELECT
=A2.index@n()
=A1.switch(STATEID,A2)
```

## 提取内容
T.index()
说明：
Create an index table for a table sequence.
语法：
T .index( n )
注意：
The function creates an index table of length n base key. The index table will be cleared if n =0, or when the table sequence’s key is reset.
To create an index table, the records’ primary key values need to be unique,
otherwise error will be reported. base key
参数：
|
T
|
A table sequence with a key .
|
n
|
Index length. Use the default index length
when the parameter is absent .
选项：
|
@s
|
Create
a multilevel tree-structured index and ignore parameter n if the table sequence’s base key is the serial byte key.
|
@n
|
Create
an sequence-number-based index for the table sequence, and ignore parameter n
when the option works. Directly use record numbers when table sequence T
does not have a sequence number key.
The
sequence-number-based index is used to achieve foreign key numberization,
which refers to the correspondence between foreign key values of the fact
table and sequence numbers of the dimension table records. When P.join() and
P.switch() work to perform an association operation, the sequence number key
can be absent from the join expression.
|
@m
|
Use
parallel processing to create the index.
Return
value:
Table
sequence
示例：
|
=demo.query("select EID,NAME,SALARY from EMPLOYEE where
EID<4")
|
=A1.keys(EID)
|
Set
EID as A1’s key .
|
=A1.index(10)
|
Create
an index table of length 10 for the table sequence .
|
=A1.keys(EID,NAME)
|
Reset
the key for A1’s table sequence and index table is deleted.
Create
a multilevel tree-structured index:
|
=3.new(k(~:2):id,~*~:num)
|
Create
a table sequence where id field is serial byte type .
|
=A1.keys(id)
|
Set
id as A1’s key .
（原文含示意图，图像资源未迁移）
|
=A2.index@s()
|
As
A1’s base key is serial byte type, use @s option to create a multilevel
tree-structured index .
|
=A2.index(0)
|
As
parameter is 0, delete the index table.
Create
an ordinal-number-based index:
|
=connect("demo").query("SELECT
* FROM CITIES")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=connect("demo").query("SELECT
* FROM STATECAPITAL")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.index@n()
|
Create
an ordinal-number index for A2’s table sequence .
|
=A1.switch(STATEID,A2)
|
A1’s CITIES table acts as the fact table
and A3’s STATECAPITAL as the dimension table; the switch() function performs
a table join by converting values of STATEID, the CITIES’s key, to corresponding
referencing records in STATECAPITAL. The index table on the foreign key is
used during the computation, so the sequence number key is omitted from the
expression, which is equivalent to A1.switch(STATEID,A2:#) . The statement returns the following result
set:
（原文含示意图，图像资源未迁移）
function:
r . k e y ()
