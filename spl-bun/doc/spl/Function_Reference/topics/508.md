# T.index(I:h,w;C,…)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.index(I:h,w;C,…)
=demo.cursor("select
=file("SCORES_ClassTwo.ctx")
=A2.create@y(#CLASS,#STUDENTID,SUBJECT,SCORE)
=A3.append@i(A1)
=A4.memory()
=A5.index(index1:10,CLASS
=="Class one";SCORE)
=A5.icursor(;;index1)
```

## 提取内容
T.index(I:h,w;C,…)
说明：
Create
one or multiple non-primary-key-based indexes for an in-memory table.
语法：
T . index ( I:h,w;C,… )
注意：
Create
index I for records meeting condition w ,
which can be omitted , based on non-key field(s) C,… .
When
parameter h is present, create a hash index of length
h . When parameter h is 0, automatically compute the hash
table length using the default value.
Delete
index I when
parameter C is
absent, and delete all existing indexes when parameter I is absent.
The
function creates one or multiple non-primary-key-based indexes for in-memory
table T during creating a cursor.
参数：
|
T
|
An
in-memory table.
|
I
|
Index
name.
|
w
|
Filtering
condition; read the whole set when it is absent.
|
The
field(s) on which index is created.
|
h
|
Index
length.
Return
value:
In-memory
table
示例：
|
=demo.cursor("select
* from SCORES ")
|
=file("SCORES_ClassTwo.ctx")
|
Create
a composite table file.
|
=A2.create@y(#CLASS,#STUDENTID,SUBJECT,SCORE)
|
Create
the composite table’s base table, where CLASS,#STUDENTID is the composite
table’s key.
|
=A3.append@i(A1)
|
Append
data of A1’s cursor to the base table.
|
=A4.memory()
|
Generate
an in-memory table.
|
=A5.index(index1:10,CLASS
=="Class one";SCORE)
|
Create
a non-primary-key-based index named index1 for A5’s in-memory table.
|
=A5.icursor(;;index1)
|
Query
in-memory table data according to the table’s index name, and return result
as a cursor.
