# influx_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
influx_close()
influx_close (hd)
=influx_open("http://127.0.0.1:8086",
=influx_close(A1)
```

## 提取内容
influx_close()
说明：
Close InfluxDB connection.
语法：
influx_close (hd)
注意：
The InfluxdbCli external library function (See External Library Guide ) closes InfluxDB connection. It is valid only with InfluxDB 1.x.
参数：
|
hd
|
Connection object .
Return
value:
None
示例：
|
=influx_open("http://127.0.0.1:8086",
"mydb", "autogen", "admin", "admin")
|
=influx_close(A1)
