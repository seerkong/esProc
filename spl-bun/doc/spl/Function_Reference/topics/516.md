# influx2_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
influx2_close()
Influx2_close (hd)
=influx2_open("http://localhost:8086?org=esprocOrg&bucket=test1&token=ZHL...fxWg==")
=influx2_close(A1)
```

## 提取内容
influx2_close()
说明：
Close
InfluxDB2 connection.
语法：
Influx2_close (hd)
注意：
The
InfluxdbCli external library function (See External Library Guide ) closes
connection to InfluxDB2. It is valid only with InfluxDB 2.x.
参数：
|
hd
|
The
connection object.
Return
value:
None
示例：
|
=influx2_open("http://localhost:8086?org=esprocOrg&bucket=test1&token=ZHL...fxWg==")
|
=influx2_close(A1)
