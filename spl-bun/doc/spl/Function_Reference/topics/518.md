# influx2_open()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
influx2_open()
influx2_open (url)
=influx2_open("http://localhost:8086?org=esprocOrg&bucket=test1&token=ZHL...fxWg==")
=influx2_close(A1)
```

## 提取内容
influx2_open()
说明：
Connect
to InfluxDB2.
语法：
influx2_open (url)
注意：
The
InfluxdbCli external library function (See External Library Guide ) connects
to InfluxDB2. It is valid only with InfluxDB 2.x.
参数：
|
url
|
The
connection URL, containing information such as server address, port number,
organization, bucket, token for authenticating a user agent and timeout
parameters.
Return
value:
Database
connection object
示例：
|
=influx2_open("http://localhost:8086?org=esprocOrg&bucket=test1&token=ZHL...fxWg==")
|
=influx2_close(A1)
