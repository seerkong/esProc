# influx2_query()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
influx2_query()
influx2_query (url,InfluxQL)
=influx2_open("http://localhost:8086?org=esprocOrg&bucket=test1&token=ZHL...fxWg==")
=influx2_query(A1,"from(bucket:\"test1\")
=influx2_close(A1)
```

## 提取内容
influx2_query()
说明：
Query
InfluxDB 2 database.
语法：
influx2_query (url,InfluxQL)
注意：
The
InfluxdbCli external library function (See External Library Guide ) queries
an InfluxDB2 database. It is possible that the result set is made up multiple
table sequences of different structures. It is valid only with InfluxDB 2.x.
参数：
|
url
|
The
connection URL.
|
InfluxQL
|
The
Influx query statement.
Return
value:
Query
result set
示例：
|
=influx2_open("http://localhost:8086?org=esprocOrg&bucket=test1&token=ZHL...fxWg==")
|
=influx2_query(A1,"from(bucket:\"test1\")
|> range(start: 0)"
|
=influx2_close(A1)
