# A.insert()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.insert()
=["a","c","d","e","f"]
=A1.insert(0,"g")
=A1.insert(2,["g","j"])
=demo.query("select
=A4.insert@n(2,ID+10:ID,"Lily":NAME,"M":GENDER,15:AGE)
=A1.insert(,"b")
=A1.insert(,"a")
=A1.insert(-2,"h")
```

## 提取内容
A.insert()
说明：
Insert
members into a sequence.
语法：
|
A .insert( k , X )
|
Insert
the members of sequence X before position k in
A, and return A .
|
A .insert( k , x )
|
Insert
member x before position k
in A, and return A .
注意：
The
function inserts member x or members of sequence X before position k in sequence A . When parameter k is omitted, we assume the sequence A is ordered and insert
member X (or x ); if member
X (or x ) have already existed in A , the function won’t
perform the insertion. Automatically update the index, if any, and check
distinctness.
参数：
|
k
|
The
position before which one or more members are inserted, when k ==0, the member(s) will be appended in the end; insert backward
from the end when k is less than 0.
|
sequence.
|
X
|
sequence composed of the members to
be inserted.
|
x
|
member.
选项：
|
@n
|
Return
the inserted record or a record sequence of the inserted records.
返回值：
Sequence
示例：
|
=["a","c","d","e","f"]
|
=A1.insert(0,"g")
|
[a,c,d,e,f,g] , the
member is appended in the end and A1 changes.
|
=A1.insert(2,["g","j"])
|
[a,g,j,c,d,e,f,g] ,
insert the new members before the second member.
|
=demo.query("select
* from STUDENTS")
|
=A4.insert@n(2,ID+10:ID,"Lily":NAME,"M":GENDER,15:AGE)
|
（原文含示意图，图像资源未迁移）
Return the
inserted record .
|
=A1.insert(,"b")
|
[a,b,g,j,c,d,e,f,g]
assume A1 is ordered and insert a new member b .
|
=A1.insert(,"a")
|
[a,b,g,j,c,d,e,f,g]
member a exists in A1, so the same member won’t be inserted .
|
=A1.insert(-2,"h")
|
As k <0,
insert backwards from the end and return [a,b,g,j,c,d,e,h,f,g] .
相关函数：
A .dele t e()
A .modi f y()
T .inse r t ()
