# T. insert()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=create(id,name,age)
=A1.insert(0,1,"Jack",29)
=A1.insert(1,2,"Lucy",30)
=A1.insert(0)
=A1.insert(0:3)
=A1.insert@n(2:1,10,"Lily",30)
=create(ID,Name,Age)
=A7.insert(0:A1,id:ID,name:Name,age:Age)
=A1.delete(A1.select(id<2))
=A7.insert@r(5:A9)
=create(ID,Name,AGE)
=A11.insert@f(3:A7)
=A1.keys(id)
=A1.insert(,5,"Mary",28)
=A1.insert(,2,"CC",32)
=file("D:\\test.txt").import@t()
=A16.insert(0,1,"2008-8-4")
```

## 提取内容
T. insert()
说明：
Insert
one or more records into a table sequence.
语法：
|
T . insert ( k )
|
Insert
an empty record before the position k in the T. If k is 0 , then append it in the end and return the
new T .
|
T . insert ( k , x i : F i ,… )
|
I nsert a
record into T before
the position k where
the value of F i is x i and return the new T . When
parameter k is omitted, we
assume the table sequence T is
already ordered by the key and insert records ; if the key value has already existed,
the function won’t perform the insertion.
|
T . insert ( k : A , x i : F i ,… )
|
Insert
multiple records into T
before the position k where
the value of F i is x i and return the new
T. The number of the records to be inserted is determined by the length
of sequence A .
注意：
The
function inserts one or more records into the table sequence T .
参数：
|
k
|
The
position before which the member, or the record, is inserted.
|
x i
|
The
F i field value before
which the new record is to be inserted.
|
F i
|
The
name of field where x i resides; without F i , it will be the corresponding i th field.
|
T
|
table sequence.
|
sequence or an integer; if A is an integer, then it is equal to to( A )
.
选项：
|
@n
|
Return
the inserted record or a record sequence of the inserted records.
|
@r(k:A)
|
Insert
sequence A into table sequence T from the k th record according to the order of the fields.
|
@f(k:A)
|
Insert
sequence A into table sequence T from the k th record; only the common fields are inserted.
返回值：
Table
sequence
示例：
|
=create(id,name,age)
|
Construct an empty table sequence.
（原文含示意图，图像资源未迁移）
|
=A1.insert(0,1,"Jack",29)
|
Append a record whose field values are 1, Jack, and 29 .
（原文含示意图，图像资源未迁移）
|
=A1.insert(1,2,"Lucy",30)
|
Add a record before the first record whose
values are 2, Lucy, and 30 .
（原文含示意图，图像资源未迁移）
|
=A1.insert(0)
|
Append an empty record in the end:
（原文含示意图，图像资源未迁移）
|
=A1.insert(0:3)
|
Append three empty records in the end:
（原文含示意图，图像资源未迁移）
|
=A1.insert@n(2:1,10,"Lily",30)
|
Return the inserted record:
（原文含示意图，图像资源未迁移）
|
=create(ID,Name,Age)
|
=A7.insert(0:A1,id:ID,name:Name,age:Age)
|
Add the records of A1 into A7 one by one:
（原文含示意图，图像资源未迁移）
|
=A1.delete(A1.select(id<2))
|
=A7.insert@r(5:A9)
|
Insert A9 into A7 from the fifth record:
（原文含示意图，图像资源未迁移）
|
=create(ID,Name,AGE)
|
=A11.insert@f(3:A7)
|
Insert only the ID and Name fields of A7 to
A11 from the third record:
（原文含示意图，图像资源未迁移）
|
=A1.keys(id)
|
Set id as A1’s primary key,
then A1’s data is as follows:
（原文含示意图，图像资源未迁移）
|
=A1.insert(,5,"Mary",28)
|
Parameter k is omitted; A1
is already ordered by id and insert
the records .
（原文含示意图，图像资源未迁移）
|
=A1.insert(,2,"CC",32)
|
Same result as A14; since the record where the key value is 2 already
exists, the function cancels the insertion .
|
=file("D:\\test.txt").import@t()
|
test.txt defines only the column headers .
（原文含示意图，图像资源未迁移）
|
=A16.insert(0,1,"2008-8-4")
|
Insert a record:
（原文含示意图，图像资源未迁移）
functions:
T .mo d i fy()
T .dele t e()
A .inse r t ()
