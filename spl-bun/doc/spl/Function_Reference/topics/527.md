# A.inv(p)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.inv(p)
=A1.inv([2,3,1,4])
=A3.inv([2,1,1,3])
```

## 提取内容
A.inv(p)
说明：
Adjust the order of members of a sequence.
语法：
A .inv( p )
注意：
The function
adjusts the order of members of A according
to the integer sequence p , whose members are ranks of members
of sequence A , and return the new A .
If p has
duplicate members or has different number of members from A , or the
value of a member of p exceeds the maximum sequence number of A’s
member, the function will return null. It does not handle the ties.
参数：
|
p
|
An
integer sequence, whose members are ranks of members of A . So, the number of its
members is the same as that of members of A , and it is a unique n -integer sequence ( n is the length of sequence A ) .
|
sequence or a record sequence.
返回值：
Sequence
示例：
|
[b,c,a,d]
|
=A1.inv([2,3,1,4])
|
[a,b,c,d]
|
[b,a,a,c]
|
=A3.inv([2,1,1,3])
|
[a,b,c,null]
相关函数：
p . i n v ( k )
