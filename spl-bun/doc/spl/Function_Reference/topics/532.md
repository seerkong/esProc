# isdigit()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
isdigit()
isdigit ( string )
isdigit("123")
isdigit(123)
isdigit("abc")
isdigit("123ss")
```

## 提取内容
isdigit()
说明：
Check
if a string is composed of numbers.
语法：
isdigit ( string )
注意：
The
function checks if the string string is composed
of numbers. If string is an
integer, look it up in the ASCII table to see if the corresponding characters
are a number.
参数：
|
字符串
|
字符串/
numeric expression.
返回值：
Boolean
示例：
|
isdigit("123")
|
true
|
isdigit(123)
|
false
|
isdigit("abc")
|
false
|
isdigit("123ss")
|
false
相关函数：
isal p h a()
