# isect()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
isect()
=[[1,2,3,4,5],[3,7,8]].isect()
=[[1,2,3],[3,2]].isect()
=[[1,2,2,3],[2]].isect()
=demo.query("select
=demo.query("select
=[A1,A2].isect(~.(NAME))
```

## 提取内容
isect()
说明：
Compute
intersection of all sequence-type members in a sequence.
语法：
A . isect( x )
注意：
Members
of sequence A are also sequences. The
function gets intersection of members of sequence A and returns a sequence composed of all common members of the
sub-sequences.
When
parameter x is present, compute the
expression with members of sequence A
and then finds the intersection.
参数：
|
sequence whose members are also sequences.
|
x
|
An
expression that returns a sequence.
返回值：
Sequence
示例：
When
A is a sequence:
|
=[[1,2,3,4,5],[3,7,8]].isect()
|
[3]; all sub-sequences
contain member 3 .
|
=[[1,2,3],[3,2]].isect()
|
[2,3]; all sub-sequences
contain 2 and 3 .
|
=[[1,2,2,3],[2]].isect()
|
[2] ; all
sub-sequences contain member 2 .
When
A is a table sequence or a record
sequence:
|
=demo.query("select
* from STUDENTS where ID>3")
|
（原文含示意图，图像资源未迁移）
|
=demo.query("select
* from STUDENTS1")
|
（原文含示意图，图像资源未迁移）
|
=[A1,A2].isect(~.(NAME))
|
Return Lauren .
