# db.is olate()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=connect("demo")
=A1.isolate@r()
```

## 提取内容
db.is olate()
说明：
Define
transaction isolation level for the connection according to the option and
return the original level.
语法：
db .isolate ()
注意：
The function sets
transaction isolation level for the connection according to the option and
returns the original level, i.e. its corresponding character of the option.
Establish the connection via JDBC by default if no option is used.
选项：
|
@ncurs
|
Correspond
to NONE,READ_COMMITTED,READ_UNCOMMITTED,REPEATABLE_READ,SERIALIZABLE
respectively.
参数：
|
db
|
Database
connection.
返回值：
The
original level of the database connection
示例：
|
=connect("demo")
|
=A1.isolate@r()
|
Set transaction isolation level as REPEATABLE_READ and return the original level " c ".
Related functions:
conn e c t ()
