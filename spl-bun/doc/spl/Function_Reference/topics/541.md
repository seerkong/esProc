# isupper()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
isupper()
isupper( string )
isupper("ADSFDGKJ")
isupper(85)
isupper("SDsdsSDAS")
isupper("8ASDS7")
```

## 提取内容
isupper()
说明：
Check
if a string is composed of uppercase letters.
语法：
isupper( string )
注意：
The
function checks if the string string is composed
of uppercase letters. If string is an
integer, look it up in the ASCII table to see if the corresponding characters
are in uppercase.
参数：
|
字符串
|
字符串
expression/numeric expression.
返回值：
Boolean
示例：
|
isupper("ADSFDGKJ")
|
true
|
isupper(85)
|
true
|
isupper("SDsdsSDAS")
|
false
|
isupper("8ASDS7")
|
false
相关函数：
islo w e r()
