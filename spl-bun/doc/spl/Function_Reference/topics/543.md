# A.it erate()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=A1.iterate(~*2)
=A1.iterate@a(~*2)
=A1.iterate(~~*2,3)
=A1.iterate(~~*2,5,~>500)
=demo.query("select * from SALES")
=A6.derive(A6.(AMOUNT).iterate(~~*2,5,~>20000):Cumulation)
```

## 提取内容
A.it erate()
说明：
Perform
iterative loop on a record sequence and return the result of the last
calculation of a given expression.
语法：
A . iterate ( x,a,c )
注意：
The function loops through
members of record sequence A to calculate
expression x and returns
the result of the last calculation of x .
~~
represents the previous value of x .
Parameter a
is the default initial value for each calculation, whose absence indicates a
null initial value .
Exit the loop once the result of expression c
is true.
参数：
|
sequence/record sequence.
|
x
|
An
expression.
|
a
|
The
initial value.
|
c
|
Boolean expression.
选项：
|
@a
|
Return the results of calculating the
expression over every member.
Return
value:
Result
of expression x
示例：
|
[2,222,22,122,2222]
|
=A1.iterate(~*2)
|
=A1.iterate@a(~*2)
|
[4,444,44,244,4444]
|
=A1.iterate(~~*2,3)
|
=A1.iterate(~~*2,5,~>500)
|
=demo.query("select * from SALES")
|
=A6.derive(A6.(AMOUNT).iterate(~~*2,5,~>20000):Cumulation)
|
（原文含示意图，图像资源未迁移）
function:
itera t e ()
