# ch.iterate()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.iterate()
=[2,222,22,122,2222].cursor()
=channel()
=channel()
=A2.iterate(~*2)
=A3.iterate(~~*2,3)
=A1.push(A2,A3)
=A1.fetch()
=A2.result()
=A3.result()
=[100,200,800,2000].cursor()
=channel()
=A11.iterate(~~*3,10,~>250)
=A10.push(A11)
=A10.fetch()
=A11.result()
```

## 提取内容
ch.iterate()
说明：
Perform iterative loop on record sequences
in a channel and return the result of the last calculation of a given
expression.
语法：
ch . iterate( x,a,c )
注意：
The
function loops through record sequences in channel ch to calculate expression x and returns
the result of the last calculation of x .
~~
represents the previous value of x .
Parameter a
is the default initial value for each calculation, whose absence indicates a
null initial value .
Exit the loop once the result of expression c
is true.
参数：
|
ch
|
A channel.
|
x
|
An expression.
|
a
|
Initial value.
|
c
|
An expression returns true/false.
Return
value:
Result
of expression x
示例：
|
=[2,222,22,122,2222].cursor()
|
=channel()
|
=channel()
|
=A2.iterate(~*2)
|
=A3.iterate(~~*2,3)
|
=A1.push(A2,A3)
|
=A1.fetch()
|
=A2.result()
|
=A3.result()
|
=[100,200,800,2000].cursor()
|
=channel()
|
=A11.iterate(~~*3,10,~>250)
|
=A10.push(A11)
|
=A10.fetch()
|
=A11.result()
|
