# iterate ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
iterate ()
iterate ( x,a;G i ,… )
=demo.query("select top 7 * from SCORES ")
=A1.groups(CLASS:Class,STUDENTID:StudentID;iterate(~~*2): Score1)
=A1.groups(CLASS:Class,STUDENTID:StudentID;iterate(~~*2,10): Score1)
=A1.derive(iterate(~~*2,10;STUDENTID):F1)
```

## 提取内容
iterate ()
说明：
An
iterative loop for calculating an expression.
语法：
iterate ( x,a;G i ,… )
注意：
The function performs an
interative loop to compute expression x
– which contains a ~~, and returns the result. Each time when the value of
field G i changes, the value of ~~ becomes a. The parameter G i
can be omitted. When parameter a
isn’t supplied, its value is null.
参数：
|
x
|
An
expression containing ~~.
|
a
|
Initial
value.
|
G i
|
Field
name.
Return
value:
Value
of the given expression
示例：
|
=demo.query("select top 7 * from SCORES ")
|
=A1.groups(CLASS:Class,STUDENTID:StudentID;iterate(~~*2): Score1)
|
（原文含示意图，图像资源未迁移）
|
=A1.groups(CLASS:Class,STUDENTID:StudentID;iterate(~~*2,10): Score1)
|
（原文含示意图，图像资源未迁移）
|
=A1.derive(iterate(~~*2,10;STUDENTID):F1)
|
（原文含示意图，图像资源未迁移）
function:
A. it e rate()
