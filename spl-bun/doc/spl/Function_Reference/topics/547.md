# itx()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
itx()
itx ( s , d , m )
=itx(,1)
=addx(date("2025-01-01"),A1)
=itx(,2,3)
=addx(datetime("2025-01-01
=itx(600)
=addx(datetime("2025-01-01
=cmpx(subx(date("2025-12-14"),date("2025-9-12")),itx(,2,3))
```

## 提取内容
itx()
说明：
Generate
a time interval constant according to the specified
time component(s).
语法：
itx ( s , d , m )
注意：
The function performs an operation according
to the specified time component(s), which can be second, day or month and
generates a time interval constant.
Only
three time components are supported. They are itx(s), itx(,d) and itx(,d,m).
参数：
|
s
|
Second.
When this parameter is absent, retain the comma between parameter d .
|
d
|
Day.
|
m
|
Month.
Return
value:
Time
interval constant
示例：
|
=itx(,1)
|
Return
a time interval constant:
（原文含示意图，图像资源未迁移）
It
represents 1 day.
|
=addx(date("2025-01-01"),A1)
|
2025-01-02
|
=itx(,2,3)
|
Return
a time interval constant representing three months and 2 days.
|
=addx(datetime("2025-01-01
01:10:30"),A3)
|
2025-04-03
01:10:30
|
=itx(600)
|
Return a time interval
constant representing 10 minutes.
|
=addx(datetime("2025-01-01
01:10:30"),A5)
|
2025-01-01 01:20:30
|
=cmpx(subx(date("2025-12-14"),date("2025-9-12")),itx(,2,3))
|
Find whether the number
of days between 2025-12-14 and 2025-9-12 is greater than 3 months and 2 days.
The returned result being 0 means that they are equal.
