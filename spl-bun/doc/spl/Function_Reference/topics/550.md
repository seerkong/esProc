# join()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
join()
join ( A i : F i , x j ,..;…)
=demo.query("select
=demo.query("select
=join(A1:Employee,EID;A2:Familymembers,EID)
=join@f(A1:Employee,EID;A2:Familymembers,EID)
=join@1(A1:Employee,EID;A2:Familymembers,EID)
=join@m(A1:Employee,EID;A2:Familymembers,EID)
=join@p(A1:Employee;A2:Familymembers)
=join(A1:Employee;A2:Familymembers)
=join(A1:Employee1;A2:Familymembers1)
=join(A8:Employee2;A9:Familymembers2)
=join@i(A1;A2)
=join@d(A1;A2)
```

## 提取内容
join()
说明：
Join
multiple sequences together.
语法：
join ( A i : F i , x j ,..;…)
注意：
The function joins multiple
sequences of A i according
to the condition that the value of join field/expression x j is equal to x 1 , and generates a table
sequence whose fields are F i ,….
which are assigned with corresponding records of the original record sequences A i . If x j is omitted, then use the key of A i . An omitted x j won’t be
matched.
参数：
|
F i
|
Field
name of the resulting table sequence.
|
A i
|
Sequences
or record sequences to be joined.
|
x j
|
Join
field/ expression.
选项：
|
@f
|
Full
join; if no matching records are found, then use nulls to correspond.
|
@1
|
Left
join; it is the number "1" instead of the letter "l" .
|
@m
|
If
all A i are ordered against x j , then use
merge operation to compute.
|
@p
|
Perform
a join according to positions while ignoring parameter x j .
|
@i
|
Used
only to filter A 1 and
ignore parameter F i ; do
not work with @f@1 options.
|
@d
|
Used
only to filter A 1 to
retain records that cannot be found and ignore parameter F i ; do not work with @f@1 options.
返回值：
Table
sequence
示例：
|
=demo.query("select
top 3 EID,NAME from EMPLOYEE").keys(EID)
|
（原文含示意图，图像资源未迁移）
|
=demo.query("select
top 3 EID,NAME from FAMILY").keys(EID)
|
（原文含示意图，图像资源未迁移）
|
=join(A1:Employee,EID;A2:Familymembers,EID)
|
（原文含示意图，图像资源未迁移）
A normal join that discards
non-matching item s ; every field is
a ref field pointing
to the corresponding record in the original table sequence .
|
=join@f(A1:Employee,EID;A2:Familymembers,EID)
|
（原文含示意图，图像资源未迁移）
full join that uses nulls to represent non-matching records .
|
=join@1(A1:Employee,EID;A2:Familymembers,EID)
|
（原文含示意图，图像资源未迁移）
left join that uses the first table sequence as the basis and that uses nulls
to correspond when no matching records can be found .
|
=join@m(A1:Employee,EID;A2:Familymembers,EID)
|
（原文含示意图，图像资源未迁移）
If
all the join field s are in the same order,
then merge operation can be used to compute ; if they are not in the same order, then error will occur .
|
=join@p(A1:Employee;A2:Familymembers)
|
（原文含示意图，图像资源未迁移）
|
=join(A1:Employee;A2:Familymembers)
|
=join(A1:Employee1;A2:Familymembers1)
|
=join(A8:Employee2;A9:Familymembers2)
|
（原文含示意图，图像资源未迁移）
|
=join@i(A1;A2)
|
（原文含示意图，图像资源未迁移）
Filter
A1 to retain records that can be found .
|
=join@d(A1;A2)
|
（原文含示意图，图像资源未迁移）
Filter
A1 to retain records that cannot be found .
相关函数：
xjo i n()
