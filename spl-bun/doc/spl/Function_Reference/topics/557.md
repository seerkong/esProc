# joinx()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
joinx()
joinx ( cs i : F i , x j ,..;…)
=demo.cur sor("select top 10 EID,NAME,DEPT,GENDER from
=demo.cursor("select top 10 EID,NAME,RELATION,GENDER,AGE
=joinx(A1:Emp,EID;A2:Family,EID)
=A3.fetch()
=demo.query("select
=demo.query("select top 10 EID,NAME,RELATION,GENDER,AGE
=joinx(A1:Emp;A2:Family)
=A3.fetch()
=demo.cursor("select EID,NAME,DEPT,GENDER from EMPLOYEE
=demo.query("select top 10 EID,NAME,RELATION,GENDER,AGE
=joinx@f(A1:Emp,EID;A2:Family,EID)
=A3.fetch()
=demo.cursor("select EID,NAME,DEPT,GENDER from EMPLOYEE
=demo.query("select top 10 EID,NAME,RELATION,GENDER,AGE
=joinx@1(A1:Emp,EID;A2:Family,EID)
=A3.fetch()
=demo.cursor("select EID,NAME,DEPT,GENDER from EMPLOYEE
=demo.query("select top 10 EID,NAME,RELATION,GENDER,AGE
=joinx@p(A1:Emp,EID;A2:Family,EID)
=A3.fetch()
=demo.cursor("select top 10 EID,NAME,DEPT,GENDER from
=demo.query("select top 5 * from DEPARTMENT " ).sort(DEPT)
=joinx@i(A1,DEPT;A2,DEPT)
=A3.fetch()
=demo.query("select top 5 * from DEPARTMENT " ).sort(DEPT)
=joinx@d(A1,DEPT;A2,DEPT)
=A3.fetch()
join ( )
```

## 提取内容
joinx()
说明：
Join
table sequences retrieved from a series of cursors.
语法：
joinx ( cs i : F i , x j ,..;…)
注意：
The
function applies MERGE algorithm over a resulting set generated from a series
of ordered cursors cs i and returns a new cursor. If parameter x j is
omitted, join the cursors according to their primary keys; if parameter x j is
present but there isn’t any primary key, perform the join based on values of x j . The
function can be also used to join multiple multicursors where each one must
have the same number of parallel cursors or subcursor. Parameter cs i
can be a table sequence.
Supposing
multiple cursors cs i are already ordered by join
field/expression x j , the function performs
a join between them on the condition that x j ’s
value is equivalent to the value of x 1 f ield and returns a cursor
consisting of F i ,…. fields . F i ,…. are referencing fields that reference the records of the cursors cs i . Note : x j … only supports the ascending
order.
Regardless
of the number of cursors being joined, the equivalence determination is
conducted according to cursor cs 1 ’s
x 1 field. Therefore, it is
a one-to-many relationship.
This
is a delayed function.
选项：
|
@f
|
Full
join. If no matching records are found, then use nulls to correspond.
|
@1
|
Left
join. Note that it is the number “1”, instead of letter “l” .
|
@p
|
Perform
a join according to positions, while ignoring the parameter x j .
|
@i
|
Used
only to filter A1 cs 1 and ignore parameter F i ; do not work with @f@1 options.
|
@d
|
Used
only to filter cs 1 to retain records that
cannot be found and ignore parameter F i
do not work with @f@1 options.
参数：
|
cs i
|
游标s/table
sequences being joined.
|
F i
|
Field
name of the result table sequence.
|
x j
|
Join
field/expression.
返回值：
游标
示例：
|
=demo.cur sor("select top 10 EID,NAME,DEPT,GENDER from
EMPLOYEE " )
|
Return a cursor,
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select top 10 EID,NAME,RELATION,GENDER,AGE
from FAMILY " )
|
Return a cursor,
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=joinx(A1:Emp,EID;A2:Family,EID)
|
A normal join that
discards every non-matching record, during which each field value in the
result set points to a record in the original cursor.
|
=A3.fetch()
|
Fetch data from
cursor A3:
（原文含示意图，图像资源未迁移）
Join
by primary key:
|
=demo.query("select
top 10 EID,NAME,DEPT,GENDER from EMPLOYEE " ).keys(EID).cursor()
|
Return a cursor
where the table’s primary key is EID and whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=demo.query("select top 10 EID,NAME,RELATION,GENDER,AGE
from FAMILY " ).keys(EID)
|
Return a table
sequence whose key is EID and whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=joinx(A1:Emp;A2:Family)
|
Join by the
primary key.
|
=A3.fetch()
|
Fetch data from
cursor A3:
（原文含示意图，图像资源未迁移）
Use
@f option to perform full join:
|
=demo.cursor("select EID,NAME,DEPT,GENDER from EMPLOYEE
where EID>2 and EID<10" )
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=demo.query("select top 10 EID,NAME,RELATION,GENDER,AGE
from FAMILY " )
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=joinx@f(A1:Emp,EID;A2:Family,EID)
|
Use @f option to
enable a full join that records a field as null when no matching value can be
found.
|
=A3.fetch()
|
Fetch data from
cursor A3:
（原文含示意图，图像资源未迁移）
Use
@1 to perform left join:
|
=demo.cursor("select EID,NAME,DEPT,GENDER from EMPLOYEE
where EID>2 and EID<10" )
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=demo.query("select top 10 EID,NAME,RELATION,GENDER,AGE
from FAMILY " )
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=joinx@1(A1:Emp,EID;A2:Family,EID)
|
Use @1 option to
enable left join based on the first cursor and that records a value that does
not have a matching value in another cursor as null.
|
=A3.fetch()
|
Fetch data from
cursor A3:
（原文含示意图，图像资源未迁移）
Use
@p option to perform position-based join:
|
=demo.cursor("select EID,NAME,DEPT,GENDER from EMPLOYEE
where EID>2 and EID<10" )
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=demo.query("select top 10 EID,NAME,RELATION,GENDER,AGE
from FAMILY " )
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=joinx@p(A1:Emp,EID;A2:Family,EID)
|
Use @p option to perform
position-based join.
|
=A3.fetch()
|
Fetch data from
cursor A3:
（原文含示意图，图像资源未迁移）
Use
@i option to enable a filtering operation only:
|
=demo.cursor("select top 10 EID,NAME,DEPT,GENDER from
EMPLOYEE" ).sortx(DEPT)
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=demo.query("select top 5 * from DEPARTMENT " ).sort(DEPT)
|
Return a table
sequence whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=joinx@i(A1,DEPT;A2,DEPT)
|
Use @i option to
filter cursor A1, during which records that match A2 are retained.
|
=A3.fetch()
|
Fetch data from
cursor A3:
（原文含示意图，图像资源未迁移）
Use
@d option to enable a filtering operation opposite to @i:
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=demo.query("select top 5 * from DEPARTMENT " ).sort(DEPT)
|
Return a table
sequence whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=joinx@d(A1,DEPT;A2,DEPT)
|
Use @d option to
filter cursor A1, during which records that do not match A2 are retained.
|
=A3.fetch()
|
Fetch data from
cursor A3:
（原文含示意图，图像资源未迁移）
相关函数：
join ( )
