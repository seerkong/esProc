# cs.joinx ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.joinx ()
=file("PersonnelInfo.txt").cursor@t(ID,Name,City,State)
=file("StateFile.btx")
=A1.joinx(State,A2:ABBR,NAME:SName,CAPITAL:SCapital;10000)
=A3.fetch@x(100)
=file("PersonnelInfo.txt").cursor@t(ID,Name,City,State)
=file("StateFile.btx")
=A1.joinx@i(State,A2:ABBR,NAME:SName,CAPITAL:SCapital;10000)
=A3.fetch@x(100)
=file("PersonnelInfo.txt").cursor@t(ID,Name,City,State)
=file("StateFile.btx")
=A1.joinx@d(State,A2:ABBR;10000)
=A3.fetch@x(100)
=file("PersonnelInfo.txt").import@t(ID,Name,City,State)
=file("StateFile.btx")
=A1.joinx@iq(State,A2:ABBR,NAME:SName,CAPITAL:SCapital)
=file("PersonnelInfo.txt").cursor@t(ID,Name,City,State).sortx(State)
=file("StateFile.btx").cursor@b()
=A1.joinx@m(State,A2:ABBR,NAME:SName,CAPITAL:SCapital;10000)
=A3.fetch@x(100)
```

## 提取内容
cs.joinx ()
说明：
Join up a cursor and a segmentable bin file according
to the foreign key.
语法：
cs . joinx ( C:…,f:K:…,x:F,…;…;…;n )
注意：
The function matches the foreign key fields C , … in
cursor cs with field K in segmentable bin file f , which is ordered by K , to find the corresponding record in f , and makes the record’s expression x
a new field F and adds it to cs to generate a new cursor. The
value corresponding to a mismatched record will be recorded as null. The cursor
the function returns is irreversible.
选项：
|
@i
|
Discard the records whose foreign key
values can’t be matched; represent them as nulls by default
When parameters x : F are
absent, only perform the filtering on the cursor/multicursor.
|
@ d
|
Delete
records matching the foreign key and perform the filtering operation only on cursor
cs ; in this
case, parameters x:F are absent .
|
@ q
|
Speed up the matching action according to
a certain order when the cursor contains a relatively small amount of data or
it is a sequence; return a sequence when cursor/multicursor cs is a
sequence.
|
@ u
|
Speed up the matching operation by
shuffling records in the cursor.
|
@m
|
Perform
a merge join when cs
is ordered by C
and f is
ordered by K ; with this option, the bin file f can be a cursor, and when cs is a multicursor, it is the multicursor
partitioned in the same way.
参数：
|
cs
|
cursor/multicursor .
|
cs ’s foreign key .
|
f
|
segmentable bin file
|
K
|
A field of the bin file; it is treated as
row number when written as # .
|
x
|
An expression of the field of f .
|
F
|
Name of the field of expression x .
|
n
|
Number of buffer rows.
Return
value:
游标/Sequence
示例：
Normal join between a cursor and a bin
file:
|
=file("PersonnelInfo.txt").cursor@t(ID,Name,City,State)
|
Return a cursor, whose
data is as follows:
（原文含示意图，图像资源未迁移）
|
=file("StateFile.btx")
|
Return a segmented bin
file ordered by ABBR field; below is data in the file:
（原文含示意图，图像资源未迁移）
|
=A1.joinx(State,A2:ABBR,NAME:SName,CAPITAL:SCapital;10000)
|
Match cursor A1’s State
field with StateFile.btx ’s ABBR field, rename StateFile.btx ’s
NAME field and CAPTITAL field SName and SCapital, and piece them to
cursor A1 to generate a new cursor.
|
=A3.fetch@x(100)
|
Fetch the first 100
records from A3’s cursor, close the cursor when the fetching is finished, and
return the following data:
（原文含示意图，图像资源未迁移）
Use @i option to delete
records that cannot match the foreign key:
|
=file("PersonnelInfo.txt").cursor@t(ID,Name,City,State)
|
Return a cursor, whose
data is as follows:
（原文含示意图，图像资源未迁移）
|
=file("StateFile.btx")
|
Return a segmented bin
file ordered by ABBR field; below is data in the file:
（原文含示意图，图像资源未迁移）
|
=A1.joinx@i(State,A2:ABBR,NAME:SName,CAPITAL:SCapital;10000)
|
Use @i option to delete
records that cannot match cursor A1’s foreign key State.
|
=A3.fetch@x(100)
|
Fetch the first 100 records
from A3’s cursor, close the cursor when the fetching is finished, and return
the following data:
（原文含示意图，图像资源未迁移）
Use @d option:
|
=file("PersonnelInfo.txt").cursor@t(ID,Name,City,State)
|
Return a cursor, whose
data is as follows:
（原文含示意图，图像资源未迁移）
|
=file("StateFile.btx")
|
Return a segmented bin
file ordered by ABBR field; below is data in the file:
（原文含示意图，图像资源未迁移）
|
=A1.joinx@d(State,A2:ABBR;10000)
|
Use @d option to retain
records that cannot match cursor A1’s foreign key State.
|
=A3.fetch@x(100)
|
Fetch the first 100
records from A3’s cursor, close the cursor when the fetching is finished, and
return the following data:
（原文含示意图，图像资源未迁移）
Use @q option when
parameter cs is a table sequence:
|
=file("PersonnelInfo.txt").import@t(ID,Name,City,State)
|
Return a table sequence,
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=file("StateFile.btx")
|
Return a segmented bin
file ordered by ABBR field; below is data in the file:
（原文含示意图，图像资源未迁移）
|
=A1.joinx@iq(State,A2:ABBR,NAME:SName,CAPITAL:SCapital)
|
Return a table sequence
when parameter cs is a table sequence; use @q option to speed up the
computation and return the following result:
（原文含示意图，图像资源未迁移）
When parameter f is
a cursor, use @m option to perform the MERGE algorithm:
|
=file("PersonnelInfo.txt").cursor@t(ID,Name,City,State).sortx(State)
|
Return a cursor ordered
by State field, whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=file("StateFile.btx").cursor@b()
|
Return a cursor ordered
by State field, whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=A1.joinx@m(State,A2:ABBR,NAME:SName,CAPITAL:SCapital;10000)
|
As A1 is ordered by State
and A2 is ordered by ABBR, use @m option to perform MERGE algorithm.
|
=A3.fetch@x(100)
|
Fetch the first 100
records from A3’s cursor, close the cursor when the fetching is finished, and
return the following data:
（原文含示意图，图像资源未迁移）
