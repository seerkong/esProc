# json()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
json()
json(x)
=file("user.txt").read()
=json(A1)
=demo.query("select top 5 EID,NAME,SURNAME
=json(A1)
="{number:rand(10)}"
=json(A1)
=json@v(A1)
="[{ID:1,NAME:aa,GENDER:F},{GENDER:M,NAME:bb,ID:2}]"
=json(A1)
=json@t(A1)
```

## 提取内容
json()
说明：
Parse
a string, a record, a sequence or a table sequence of JSON format.
语法：
json(x)
注意：
If parameter x is a JSON
string, the function parses it into a table sequence to return; if parameter x
is a record, a sequence or a table sequence, the function parses it into a JSON
string to return.
The
format of JSON string is [{F:v, … }, … ], where v is the value of F and a string constant should be quoted.
参数：
|
x
|
JSON string/record/sequence.
选项：
|
@v
|
Treat parameter x as an expression and calculate its
result .
|
@ t
|
Arrange
the returned result set as a same-structure table sequence.
返回值：
记录/Sequence/Table
sequence/JSON string
示例：
Parse a JSON string as a table sequence and
return it:
|
=file("user.txt").read()
|
Return a JSON string as follows:
（原文含示意图，图像资源未迁移）
|
=json(A1)
|
Parse A1’s JSON string as a table sequence and return it:
（原文含示意图，图像资源未迁移）
Parse a table sequence as a JSON string and
return it:
|
=demo.query("select top 5 EID,NAME,SURNAME
from EMPLOYEE")
|
Return a table sequence:
（原文含示意图，图像资源未迁移）
|
=json(A1)
|
Parse A1’s table sequence as a JSON string
and return it:
（原文含示意图，图像资源未迁移）
When @v option works:
|
="{number:rand(10)}"
|
=json(A1)
|
Return result as follows:
（原文含示意图，图像资源未迁移）
|
=json@v(A1)
|
@v option works to treat rand(10) as an
expression and compute it; and the function returns a number within 10
randomly.
When @t option works:
|
="[{ID:1,NAME:aa,GENDER:F},{GENDER:M,NAME:bb,ID:2}]"
|
=json(A1)
|
Return result as follows:
（原文含示意图，图像资源未迁移）
|
=json@t(A1)
|
@t option works to arrange the result set as a same-structure table
sequence:
（原文含示意图，图像资源未迁移）
