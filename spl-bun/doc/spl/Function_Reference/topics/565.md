# kafka_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
kafka_close()
kafka_close (fd)
=kafka_open("D://kafka.properties","topic-test")
=kafka_close(A1)
```

## 提取内容
kafka_close()
说明：
Close
a Consumer connection.
语法：
kafka_close (fd)
注意：
The
KafkaCli external library function (See
External Library Guide ) closes a Consumer connection to release
resources.
参数：
|
f d
|
connection object.
示例：
|
=kafka_open("D://kafka.properties","topic-test")
|
Connect
to Kafka server .
|
=kafka_close(A1)
|
Close
connection to Kafka server .
