# kafka_commit()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
kafka_commit()
kafka_commit (fd)
=kafka_open("D://kafka.properties","topic-test")
=kafka_poll(A1)
=kafka_commit(A1)
=kafka_commit@a(A1)
```

## 提取内容
kafka_commit()
说明：
Commit
the current consumer offset position.
语法：
kafka_commit (fd)
注意：
By
default, the KafkaCli external library function (See External Library Guide ) commits the real-time consumer offset
position synchronously.
参数：
|
fd
|
connection object.
选项：
|
@a
|
Asynchronous
commit.
示例：
|
=kafka_open("D://kafka.properties","topic-test")
|
Connect
to Kafka server .
|
=kafka_poll(A1)
|
Get
records from topictest .
|
=kafka_commit(A1)
|
Commit
the current consumer offset .
|
=kafka_commit@a(A1)
|
Asynchronous
commit .
