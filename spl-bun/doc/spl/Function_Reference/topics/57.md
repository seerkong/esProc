# #c

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select
=[]
>A2=A2|[#A3]
```

## 提取内容
#c
说明：
The
variable generated during the process of loop.
参数：
|
c
|
The
cell where for statement is placed.
|
# c
|
The
sequence number of the loop.
示例：
|
=demo.query("select
* from EMPLOYEE")
|
=[]
|
for A1
|
Loop through A1’s table sequence, during which the
current record is skipped if DEPT column value is " Sales " , otherwise append its loop number to A2’s sequence as a value .
|
if
A3.DEPT=="Sales"
|
>A2=A2|[#A3]
|
