# r.k ey()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select * from
=A1(1).key()
=A1.keys(EID)
=A1(1).key()
=A1.keys(EID,DEPT)
=A1(1).key()
=A1(1).key(DEPT)
```

## 提取内容
r.k ey()
说明：
Get the primary key value of a record.
语法：
r . key ( F i ,… )
注意：
The function gets the
primary key value of record r
according to key F i . If F i is omitted, return the base key values. If the key isn’t set for r , return null. If it is a one-field
key, just return the single key value; and with a composite key, return a
sequence of values.
参数：
|
r
|
record.
|
F i
|
键.
Return
value:
记录’s
primary key
示例：
|
=demo.query("select * from
EMPLOYEE ")
|
=A1(1).key()
|
return null because A1 doesn’t set a key .
|
=A1.keys(EID)
|
Set
EID field as the key .
|
=A1(1).key()
|
return the primary key value of the first record .
|
=A1.keys(EID,DEPT)
|
Set
EID&DEPT fields as the key .
|
=A1(1).key()
|
[1,R&D]
the primary key value is a sequence because the key consist of two fields .
|
=A1(1).key(DEPT)
|
R&D
get the value of a specified key field .
function:
T . k e y s ( )
v . v ()
