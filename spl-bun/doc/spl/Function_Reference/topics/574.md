# T.k eys()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select EID,NAME,DEPT,SALARY ,HIREDATE from
=A1.keys(EID,DEPT)
=A1(1).key()
=A1.keys()
=A1(1).key()
=A1.keys@t(EID,DEPT,HIREDATE)
=A1.create()
=A7.insert(0,1,"Jack","HR",3000 ,date("2022-03-09") )
=A7(1).key()
```

## 提取内容
T.k eys()
说明：
Set
key for a table sequence.
语法：
T . keys ( K i , …)
注意：
The
function sets K i , … as the key of the table sequence T . When parameter K i is absent, delete T ’s key.
A T . create() operation will copy the key at the same time.
参数：
|
T
|
table sequence.
|
K i
|
The
key of table sequence T .
选项：
|
@t(K i , …,KT)
|
Set
the last parameter KT as the time key, and make all other parameters
the base keys.
Return
value:
Table sequence
示例：
|
=demo.query("select EID,NAME,DEPT,SALARY ,HIREDATE from
EMPLOYEE order by EID asc,DEPT asc")
|
Return a table sequence:
（原文含示意图，图像资源未迁移）
|
=A1.keys(EID,DEPT)
|
Set EID field and DEPT field as A1’s key:
（原文含示意图，图像资源未迁移）
In a esProc result set, a
field whose name is marked by
（原文含示意图，图像资源未迁移）
is a key field.
|
=A1(1).key()
|
Get base key values in table A1’s first
record and return [1, "R&D"] .
|
=A1.keys()
|
Remove the key as the parameter is absent.
|
=A1(1).key()
|
null.
|
=A1.keys@t(EID,DEPT,HIREDATE)
|
Set EID and DEPT as A1’s base
keys and HIREDATE as the time key:
（原文含示意图，图像资源未迁移）
|
=A1.create()
|
Copy table A1’s structure as well as its key.
|
=A7.insert(0,1,"Jack","HR",3000 ,date("2022-03-09") )
|
Add a record
to table A7:
（原文含示意图，图像资源未迁移）
|
=A7(1).key()
|
base keyGet base key values in table A7’s
first record: [1,"HR"]
functions:
r . ke y ()
v .v ( )
