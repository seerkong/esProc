# T.keys( Ki,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.keys( Ki,… )
=demo.cursor("select
=A1.memory()
=A2.keys(EID,NAME)
=A3(1).key()
=A2.keys()
=A3(1).key()
=demo.cursor("select
=A7.keys@t(EID,HIREDATE)
=A7(1).key()
```

## 提取内容
T.keys( Ki,… )
说明：
Set
key(s) for an in-memory table.
语法：
T . keys ( Ki ,… )
注意：
The
function sets Ki ,… as the key of in-memory table T .
参数：
|
Ki
|
键 name; can be one or multiple keys; delete
all keys of an in-memory table when the parameter is absent .
|
T
|
An in-memory
table .
Optios:
|
@t ( Ki , …, KT )
|
Set
the last parameter KT as the time key and all other key fields
constitute the base key.
返回值：
In-memory
table
示例：
|
=demo.cursor("select
EID,NAME,GENDER from EMPLOYEE where EID<10")
|
Return
cursor of the retrieved data .
|
=A1.memory()
|
Return
an in-memory table.
|
=A2.keys(EID,NAME)
|
Set EID and NAME as the in-memory table’s
keys :
（原文含示意图，图像资源未迁移）
In a esProc result set, a field whose name is marked
by
（原文含示意图，图像资源未迁移）
is a key field.
|
=A3(1).key()
|
Get
base key values of the in-memory table’s first record and return [1, " Rebecca"].
|
=A2.keys()
|
Delete
all keys of the in-memory table as the parameter is absent .
|
=A3(1).key()
|
Get
base key values of the in-memory table’s first record and return null .
|
=demo.cursor("select
EID,NAME,HIREDATE from employee").memory()
|
Return
an in-memory table .
|
=A7.keys@t(EID,HIREDATE)
|
Set
EID as the base key and HIREDATE as the time key in A7’s in-memory table :
（原文含示意图，图像资源未迁移）
|
=A7(1).key()
|
Get
base key values of the in-memory table’s first record and return 1.
