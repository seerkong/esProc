# lasso()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
lasso()
lasso ( X , Y, learning_rate, iterations)
lasso ( X’ , F )
=lasso(A1,A2,0.001,10000)
=lasso(A1,A3)
```

## 提取内容
lasso()
说明：
Build models and performs
predictions using the Lasso regression method.
语法：
|
lasso ( X , Y, learning_rate, iterations)
|
The function fits together matrix X and vector Y using the Lasso regression method and returns model information that
includes coefficient matrix and intercept. The model information can act as
parameter F in lasso ( X’ ,
F ) to perform a fitting computation .
|
lasso ( X’ , F )
|
The function fits
together two matrices that have same number of columns – that is, perform
predictions on another matrix X’ using model F , and returns a
vector.
注意：
MathCli
external library function (See External Library Guide ).
The function builds models and performs
predictions using the Lasso regression method.
参数：
|
X
|
matrix.
|
Y
|
vector having the same number of rows as matrix X .
|
learning_rate
|
Learning
rate that is a decimal between 0 and 1; default value is 0.01.
|
iterations
|
Number
of iterations; default is 1000.
|
X’
|
matrix that has same number of columns as matrix X .
|
F
|
The
return result of lasso (X, Y, learning_rate, iterations).
返回值：
Matrix/Vector
示例：
|
[[19,1],[25,1],[31,1],[38,1],[44,1]]
|
[19,32.3,49,73.3,97.8]
|
=lasso(A1,A2,0.001,10000)
|
Fit A1 and A2 together using Lasso
regression method and return coefficient matrix A3(1) and intercept A3(2).
（原文含示意图，图像资源未迁移）
|
=lasso(A1,A3)
|
Perform prediction on A1 using model A3
the result can be compared with actual values in A2.
（原文含示意图，图像资源未迁移）
