# lcm()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
lcm()
lcm ( x i ,… )
lcm ( A )
lcm(5,2)
lcm(4,10,8,9)
lcm(-10,2,15)
lcm(10,15,20,null)
```

## 提取内容
lcm()
说明：
Return
the least common multiple.
语法：
lcm ( x i ,… )
lcm ( A )
注意：
The
function gets the least common multiple of the numeric members of x 1 , x 2 ,… . If there is any member that is equal to or less than 0, the
function returns the false value 0.
参数：
|
sequence.
|
x i
|
An expression, which will be ignored if
its value is not a number .
返回值：
The
least common multiple
示例：
|
lcm(5,2)
|
lcm(4,10,8,9)
|
lcm(-10,2,15)
|
Return 0, which represents an error, since there
is a member whose value is less than 0
|
lcm(10,15,20,null)
|
60; ignore the non-numerical member
