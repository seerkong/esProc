# left()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
left()
left( s , n )
left("abcdefg",3)
left("abcdefg",-3)
left("abcdefg",-10)
```

## 提取内容
left()
说明：
Get
a substring starting from the leftmost side of a string.
语法：
left( s , n )
注意：
The
function gets a substring from source string s starting from the first character at the leftmost side to the n th
character . If n<0 ,
get a substring starting from the first
character at the leftmost side to the abs(n) th -to-last
character .
参数：
|
s
|
The
source string.
|
n
|
The
n th character of source
string s ; return an
empty string when the absolute value of n
is greater than the length of s .
返回值：
字符串
示例：
|
left("abcdefg",3)
|
"abc".
|
left("abcdefg",-3)
|
"abcde".
|
left("abcdefg",-10)
|
Return an empty string .
Related functions:
m i d ()
r i g h t ()
