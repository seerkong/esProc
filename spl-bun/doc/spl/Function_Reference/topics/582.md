# A.len ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.len ()
=[1,2,4]
=A1.len()
=["a","b"].len()
=[]
=A4.len()
```

## 提取内容
A.len ()
说明：
Get
the length of a sequence.
语法：
A .len()
注意：
The
function gets the length of sequence A .
参数：
|
Sequence
object.
返回值：
Integer
示例：
|
=[1,2,4]
|
=A1.len()
|
=["a","b"].len()
|
=[]
|
=A4.len()
|
