# T.len()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.len()
=demo.query("select EID,NAME,GENDER from employee where EID<
=A1.memory()
=A2.len()
```

## 提取内容
T.len()
说明：
Get
the number of records in an in-memory table.
语法：
T .len()
注意：
The
function gets the number of records in in-memory table T .
参数：
|
T
|
An
in-memory table.
Return
value:
Integer
示例：
|
=demo.query("select EID,NAME,GENDER from employee where EID<
10").cursor()
|
Return
cursor of retrieved data .
|
=A1.memory()
|
Return
an in-memory table .
|
=A2.len()
|
