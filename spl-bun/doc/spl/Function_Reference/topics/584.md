# len()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
len()
len( s,cs )
len("esproc")
len("esproc",)
len(" 集算器 ",)
len("esproc","UTF-8")
```

## 提取内容
len()
说明：
Compute
the length of a string.
语法：
len( s,cs )
注意：
The
function computes the number of bytes in string s . When parameter cs , the character
set, is absent, it gets the number of characters in the string and
automatically uses charset GB2312.
参数：
|
s
|
string.
|
cs
|
Charset
can be omitted.
返回值：
Integer
示例：
|
len("esproc")
|
len(" esproc
")
|
len("esproc",)
|
len(" 集算器 ",)
|
6; use charset GB2312 as the
charset parameter is absent .
|
len("esproc","UTF-8")
|
