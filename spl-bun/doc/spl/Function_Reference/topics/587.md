# linefit()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
linefit()
linefit( A,Y )
=linefit(A1,A2)
=linefit(A1,A4)
=linefit@1(A1,A4)
```

## 提取内容
linefit()
说明：
Perform
linear fitting using the least squares method.
语法：
linefit( A,Y )
注意：
least squares fitting technique is to get the nearest possible value of
constant sequence/ vector Y by
multiplying matrix A by vector X , that is, the nearest result of AX = Y .
The function performs a linear fitting using the least squares method to get
vector X . The function returns a
matrix when parameter Y is a matrix and a vector when Y
is a vector.
参数：
|
matrix or a vector.
|
Y
|
matrix or a vector.
选项：
|
@1
|
Return
a vector when parameter Y is a vector.
Return
value:
Sequence
示例：
|
[[1,2,3,4],[2,3,1,2],[1,1,1,-1],[1,0,-2,-6]]
|
[[2,2],[3,5,1],[8],[4]]
|
=linefit(A1,A2)
|
（原文含示意图，图像资源未迁移）
Fit A1 and A2 together
using the least squares fitting technique and return
the fitting coefficient .
|
[4,6,-2,4]
|
=linefit(A1,A4)
|
（原文含示意图，图像资源未迁移）
Fit A1 and A4 together
using the least squares fitting technique and return
the fitting coefficient .
|
=linefit@1(A1,A4)
|
Use @1 option to return a vector as A4 is a vector:
（原文含示意图，图像资源未迁移）
