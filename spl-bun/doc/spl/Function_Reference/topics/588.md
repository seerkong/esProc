# lineplan()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
lineplan()
lineplan(A,B,C)
=lineplan(A1,A2,A3)
=lineplan(A1,A2,A3)
```

## 提取内容
lineplan()
说明：
For
linear programming and calculates the minimum value in linear objective
function under linear constraints.
语法：
lineplan(A,B,C)
注意：
The function uses simplex method to solve
regular linear programming problems as well as problems where the basic
solution isn’t a feasible solution (see example: the special problem). The
problem needs to be converted to the following standard form when getting
parameters:
Target
function:
（原文含示意图，图像资源未迁移）
Linear inequality constraints:
（原文含示意图，图像资源未迁移）
The MathCli
external library function (See External Library Guide ) is for linear
programming and calculates the minimum value in linear objective function under
linear constraints.
Note the method of getting the target result.
When trying to output the minimum value and its coordinates, the former is to
multiply the first number of the first sub-sequence by -1, and the coordinates
are the first n numbers of the second sub-sequence, where n is
the number of x .
参数：
|
sequence representing coefficient matrix.
|
sequence representing a constraints matrix.
|
The
objective function matrix.
返回值：
Sequence
示例：
Here
are how to solve simple linear programming problems defined by linear
inequalities:
Regular
problems:
（原文含示意图，图像资源未迁移）
|
[[2,1],[1,1],[0,1]]
|
Input
coefficient matrix A.
|
[[10,0],[8,0],[7,0]]
|
Input
constraints matrix B.
|
[[-4,-3],[0,0],[0,0]]
|
Input
objective function matrix C.
|
=lineplan(A1,A2,A3)
|
Get
the result: the objective function’s minimum value is -26 and its coordinates
are (2.0,6.0) .
The
special problem:
（原文含示意图，图像资源未迁移）
|
[[1,1],[-1,-1]]
|
Input
coefficient matrix A.
|
[[2,0],[-1,0]]
|
Input
constraints matrix B.
|
[[1,2],[0,0]]
|
Input
objective function matrix C.
|
=lineplan(A1,A2,A3)
|
（原文含示意图，图像资源未迁移）
Get the result: the objective function’s
minimum value is 1 and its coordinates are ( 1.0,0.0 ) .
