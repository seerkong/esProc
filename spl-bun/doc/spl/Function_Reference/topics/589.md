# ln()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ln()
ln( numberExp )
ln(54)
lg ( )
```

## 提取内容
ln()
说明：
Compute the natural logarithm .
语法：
ln( numberExp )
注意：
The
function computes the natural logarithm of parameter numberExp .
参数：
|
numberExp
|
positive number.
返回值：
数值型
value
示例：
|
ln(54)
|
3.9889840465642745
相关函数：
lg ( )
