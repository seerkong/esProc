# $(db)sql;…

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
$(db)sql;…
$( db ) sql ; …
$select * from
$(demo)select * from
$select * from
$(demo)insert into
$insert into
$(demo)delete from
$delete from EMPLOYEE
$delete from EMPLOYEE
$(demo)update EMPLOYEE
=?;"testnew","M",100
$update EMPLOYEE set
$(sql)update Family
```

## 提取内容
$(db)sql;…
说明：
Execute
the specified SQL statement on the data source and return the result.
语法：
$( db ) sql ; …
注意：
On
the data source db , execute the
specified SQL statement sql and
return the execution result. db is
the data source name. If omitting ( db ) , then use the data source specified by the previous statement. If no
data source is specified in the previous statement, then use any of the
currently connected data sources.
When
data source name is not specified, the statement is regarded as a simple SQL
statement.
参数：
|
sql
|
SQL statement in the form of select
* from table , for example; the SQL statement must be one of
select/insert/delete/update statements.
|
(db)
|
Data
source name.
|
…
|
Argument
value passed to the SQL statement .
返回值：
Table
sequence
示例：
SELECT
statement:
|
$select * from
EMPLOYEE
|
No
data source is specified in the previous statement since parameter (db) is absent, and error report
appears if there isn’t a currently connected data source.
|
$(demo)select * from
EMPLOYEE where EID=?;1
|
Find
the employees whose EID is 1 from the demo data source .
|
$select * from
EMPLOYEE where EID in (?) or GENDER=?;[1,3,5,7],"M"
|
Since
parameter (db) is absent, the
program uses the demo data source defined in the previous statement to find
the employees whose EID is [1,3,5,7] and gender is M .
INSERT
statement:
|
$(demo)insert into
EMPLOYEE (EID, NAME) values(?,?);100,"test"
|
Insert
statement .
|
[51,52,53,54]
|
for A2
|
$insert into
STATECAPITAL (STATEID) values(?);A3
|
Since
parameter (db) is absent, the
program uses the demo data source defined in the previous statement; pass
members of A2’s sequence to B4’s statement as parameters by loop .
DELETE
statement:
|
$(demo)delete from
EMPLOYEE where EID =? or EID=?;100,101
|
$delete from EMPLOYEE
where EID in(?);[1,5,7,9]
|
$delete from EMPLOYEE
where NAME ='Rebecca'
|
Since
parameter (db) is absent and no
data source is specified in the previous statement, the program uses one of
the currently connected data sources .
UPDATE
statement:
|
$(demo)update EMPLOYEE
set NAME =?, GENDER=?
where EID
=?;"testnew","M",100
|
$update EMPLOYEE set
NAME ='Peter' where EID =10
|
Since
parameter (db) is absent, the
program uses the data source defined in the previous statement to modify the
NAME of the employee whose EID is 10 to Peter .
|
$(sql)update Family
set Name='Rose' where Eid=?;2
|
Update
the name of a record of Family whose Eid is 2 by connecting to a data source
named sql .
相关函数：
db. q u e r y ()
