# lock()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
lock()
lock( n,s )
=lock(1)
>output("Task "/arg1/" start...")
=file("D:/file/test/1.btx")
=create(ID,Value)
>A4.record([arg1,arg1*arg1])
>sleep(1000)
>A3.export@ab(A4)
>output("Task "/arg1/" finished.")
>lock@u(1)
=file("D:/file/test/1.btx")
=callx(A2,to(100))
=A1.import@b()
```

## 提取内容
lock()
说明：
Use
a synchronization lock.
语法：
lock( n,s )
注意：
The
function enforces limits on access to a file in a multithreading execution
environment using lock-based synchronization. With the lock, only one thread
can access and process the file at a time. Return true if the locking succeeds
and return false if it fails.
参数：
|
n
|
Lock
name.
|
s
|
Timeout
(in millisecond); waiting to be locked if the parameter is absent.
选项：
|
@u
|
Unlock
the restriction.
Return
value:
Boolean
示例：
Locking
below is the script file D:\test \lock1.splx :
|
=lock(1)
|
Use a lock named 1 .
|
>output("Task "/arg1/" start...")
|
Output the parameter and start to calculate .
|
=file("D:/file/test/1.btx")
|
Create a btx file .
|
=create(ID,Value)
|
Create a table sequence .
|
>A4.record([arg1,arg1*arg1])
|
Add records to A4’s table sequence .
|
>sleep(1000)
|
Make the system sleep for 1,000 milliseconds .
|
>A3.export@ab(A4)
|
Write A4’s table sequence into the btx file .
|
>output("Task "/arg1/" finished.")
|
Output the parameter and finish the
calculation .
|
>lock@u(1)
|
Unlock the access restriction .
File
access:
|
=file("D:/file/test/1.btx")
|
D:\test \lock1.splx
|
=callx(A2,to(100))
|
When
using callx() function to acess a
file, the max number of parallel task set in Tool -> Options should be greater than 1 in case that the
lock-based synchronization reports an error .
|
=A1.import@b()
|
