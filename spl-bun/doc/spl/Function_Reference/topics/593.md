# long()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
long()
long ( stringExp )
long ( numberExp )
long ( dateExp )
long("1234567")
long(1234567.789)
long(date("1998-01-08"))
long(now())
float()
int ( )
```

## 提取内容
long()
说明：
Convert
a string, a number or a date to a 64-bit long integer.
语法：
long ( stringExp )
long ( numberExp )
long ( dateExp )
注意：
The value of parameter stringExp must be a string that
consists of a long integer which has 64 bits or less. For a value with more
than 64 bits, the result of long( stringExp ) is
imprecise and its fractional part, if any, will be truncated.
The value of parameter numberExp must be a long integer
which has 64 bits or less. For a value with more than 64 bits, the result of long( numberExp ) is imprecise and its fractional part, if any, will be truncated.
参数：
|
stringExp
|
The
string expression you want to convert as a long integer.
|
numberExp
|
number.
|
dateExp
|
日期
type expression.
返回值：
64-bit
long integer
示例：
|
long("1234567")
|
long(1234567.789)
|
long(date("1998-01-08"))
|
long(now())
|
相关函数：
float()
int ( )
decim a l()
numb e r()
strin g ( )
