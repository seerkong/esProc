# A.m ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.m ()
=A1.m(2)
=A1.m(-2)
=A1.m([2,3])
=A1.m([-2,-3])
=A1.m@0([5,12])
=A1.m@r([5,12])
=A1.m@p(2,-2)
```

## 提取内容
A.m ()
说明：
Get
members at specified positions.
语法：
|
A .m( i )
|
n is the lengh of sequence A , - n £ i £ n and i is not equal to 0; 1<= i <= n indicates getting the i th
member; - n <= i <= -1 indicates getting the i th member from the bottom.
|
A .m( P )
|
P is a non-zero n -integer sequence.
注意：
is a sequence whose length is n , from which you get members at specified positions. The function is
generally used to get members of a sequence from backwards to forwards.
参数：
|
sequence expression.
|
i
|
An integer.
|
P
|
A non-zero n- integer sequence.
选项：
|
@r
|
Turn
back if the specified position exceeds the boundary of A , that is, if the integer i that exceeds the limit of
sequence A can be fully
divided by n , i is equal to n ; otherwise, i is
equal to the remainder of i divided by n .
|
@0
|
member represented by a sequence number that exceeds the boundary of A will be excluded from the result, or a null will be returned for
it.
|
@p
|
member represented by a negative sequence number is regarded as exceeding the
boundary.
返回值：
value or a sequence composed of the members at the specified positions in
sequence A
示例：
|
[a,b,c,d,e,f,g,h,i,j]
|
=A1.m(2)
|
b
|
=A1.m(-2)
|
i
|
=A1.m([2,3])
|
[b,c]
|
=A1.m([-2,-3])
|
[i,h]
|
=A1.m@0([5,12])
|
[e]
|
=A1.m@r([5,12])
|
[e,b]
|
=A1.m@p(2,-2)
|
[b,null]
相关函数：
A . p ( )
