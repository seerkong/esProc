# max()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
max()
max ( x 1 ,…, x n )
=[8,-6,1,3,5].max()
=["c","b",null,"e","A"].max()
=["a",1].max()
=[8,-6,1,3,5].max(~*10)
=max(8,-6,1,3,5)
=demo.query("select
=A1.max(SALARY+1000)
```

## 提取内容
max()
说明：
Find the maximum value among members of a sequence.
语法：
|
A . max ( x )
|
max ( x 1 ,…, x n )
|
Equivalent to A .max() , where x 1 ,…, x n are members of sequence A .
注意：
The
function computes expression x with members of sequence A and returns the maximum member value.
参数：
|
sequence, where members should have same data type.
|
x
|
An
expression; cannot be omitted when A
is a table sequence or a record sequence.
返回值：
The
maximum value
示例：
When
A is a sequence:
|
=[8,-6,1,3,5].max()
|
8 .
|
=["c","b",null,"e","A"].max()
|
"e" .
|
=["a",1].max()
|
Error
is reported as members of the sequence have inconsistent data types .
|
=[8,-6,1,3,5].max(~*10)
|
80 ; return the maximum member
value after expression is computed .
|
=max(8,-6,1,3,5)
|
8, which is same as A1 .
When
A is a table sequence or a record
sequence:
|
=demo.query("select
EID,NAME,SALARY from EMPLOYEE").sort(SALARY:-1)
|
Query
content of EMPLOYEE table and sort result by SALARY in descending order .
|
=A1.max(SALARY+1000)
|
Get
the highest SALARY value after salary is increased by 1000 and return 17,000 .
