# A.m axp()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=A1.maxp(~*~)
=A1.maxp@a(~*~)
=A1.maxp@z(~*0)
=A1.maxp()
=demo.query("select
=A1.maxp@a(BIRTHDAY)
=A1.maxp()
```

## 提取内容
A.m axp()
说明：
Compute
expression x over sequence A and return members of A that
makes expression x get the greatest value.
语法：
A .maxp( x )
注意：
The
function computes expression x with each member
of sequence A and returns the
member which makes the maximum value for expression x .
选项：
|
@a
|
Return
all the members that fulfill the condition; by default, return the first one .
|
@z
|
Search
the member from back to front .
参数：
|
A sequence.
|
x
|
An
expression that by default returns the last member of sequence A .
返回值：
Single
value/Sequence
示例：
When
parameter A is a sequence
|
[6,2,5,6,3,2]
|
=A1.maxp(~*~)
|
6 ; return A1’s members that
make the expression get the greatest value; by default, only the first
eligible member is returned .
|
=A1.maxp@a(~*~)
|
[6,6] ; with @a option, return all
members that make the expression get the greatest value .
|
=A1.maxp@z(~*0)
|
2 ; search backwards .
|
=A1.maxp()
|
2 ; as parameter x is
absent, return the last member of A1 .
When
parameter A is a table sequence or a record sequence
|
=demo.query("select
top 10 EID,NAME,BIRTHDAY from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.maxp@a(BIRTHDAY)
|
Return record of the youngest employee:
（原文含示意图，图像资源未迁移）
|
=A1.maxp()
|
Return the last
record of A1:
（原文含示意图，图像资源未迁移）
相关函数：
A .p m ax ( )
A .mi n p ( )
