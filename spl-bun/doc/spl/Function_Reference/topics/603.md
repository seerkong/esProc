# mcumsum()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
mcumsum()
mcumsum ( A , n )
=mcumsum(A1,1)
=mcumsum(A1, 2)
=mcumsum(A1)
=mcumsum@z(A1)
=mcumsum(A6, 1)
=mcumsum(A6, 2)
```

## 提取内容
mcumsum()
说明：
Perform cumulative sum on a
matrix or a multidimensional matrix.
语法：
mcumsum ( A , n )
注意：
In the MathCli external library function
(See External Library Guide ) ,
parameter n is the sequence number of dimension level where
summarization is performed. If n is absent, the desired dimension is the
first one whose size is not 1 in matrix A .
参数：
|
matrix or a multidimensional matrix .
|
n
|
positive integer .
选项：
|
@z
|
Perform
inverse cumulative sum .
返回值：
Sequence
示例：
|
[[11,12,13],[21,22,23],[31,32,33]]
|
（原文含示意图，图像资源未迁移）
|
=mcumsum(A1,1)
|
（原文含示意图，图像资源未迁移）
Perform cumulative sum on the first level
of A1’s matrix – that is, sum members of each column .
|
=mcumsum(A1, 2)
|
（原文含示意图，图像资源未迁移）
Perform cumulative sum on the second level
of A1’s matrix – that is, sum members of each row .
|
=mcumsum(A1)
|
（原文含示意图，图像资源未迁移）
Perform cumulative sum on the first level as parameter
n is absent.
|
=mcumsum@z(A1)
|
（原文含示意图，图像资源未迁移）
Perform inverse cumulative sum.
|
[[[111,112,113],[121,122,123],[131,132,133]],
[[211,212,213],[221,222,223],[231,232,233]]]
|
（原文含示意图，图像资源未迁移）
Define a three-dimension matrix whose two
members are 3*3 matrices . Define a three-dimension matrix whose two members are 3*3 matrices .
|
=mcumsum(A6, 1)
|
（原文含示意图，图像资源未迁移）
Calculate cumulative sum on matrix A6’s
first level, that is, perform the calculation on both of its two member
matrices in alignment .
|
=mcumsum(A6, 2)
|
（原文含示意图，图像资源未迁移）
Calculate cumulative
sum on matrix A6’s second level, that is, perform the calculation on each
column of each member matrix .
