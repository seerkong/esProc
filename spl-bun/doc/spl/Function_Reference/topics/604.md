# md5()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
md5()
Md5( s )
md5("abced")
```

## 提取内容
md5()
说明：
Get
a string’s MD5 signature.
语法：
Md5( s )
注意：
The function gets string s ’s
MD5 signature. MD5 is a widely used hash function producing a hash value.
参数：
|
s
|
string.
Return
value:
字符串
示例：
|
md5("abced")
|
（原文含示意图，图像资源未迁移）
