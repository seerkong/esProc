# cs.memory( K,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.memory( K,… )
=file("test0.ctx","169.254.121.62:8281")
=A1.open()
=A2.attach(table)
=A3.cursor(NAME,GENDER;EID<6)
=A4.memory(GENDER)
```

## 提取内容
cs.memory( K,… )
说明：
Generate
a cluster in-memory table from a cluster cursor.
语法：
cs . memory ( K ,…)
注意：
According
to cluster cursor cs , the function
generates a cluster in-memory table that distributed the same way as cs and whose key is K . If cs is a cluster
multicursor, load a series of cursors in order. Only supported by esProc
Enterprise Edition.
参数：
|
K
|
The
key; can be omitted .
|
cs
|
cluster cursor .
Return
value:
Cluster
in-memory table
示例：
|
=file("test0.ctx","169.254.121.62:8281")
|
Open
a cluster file .
|
=A1.open()
|
Open
a cluster table .
|
=A2.attach(table)
|
Retrieve
cluster table table .
|
=A3.cursor(NAME,GENDER;EID<6)
|
Get
NAME field and GENDER field where EID<6 from cluster table table and return them as a cursor .
|
=A4.memory(GENDER)
|
Generate
a cluster in-memory table whose key is GENDER from a cluster cursor .
