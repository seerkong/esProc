# memory( h,V )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
memory( h,V )
memory ( h,V )
=memory(["192.168.31.72:8281","192.168.31.72:8291"],arg1)
=A1.cursor().fetch()
```

## 提取内容
memory( h,V )
说明：
Generate
a cluster in-memory table from a local in-memory table.
语法：
memory ( h,V )
注意：
The
function generates a cluster in-memory table from local in-memory table V , which is stored in a global variable,
held in node sequence h . Only
supported by esProc Enterprise Edition.
参数：
|
h
|
node sequence .
|
V
|
An
in-memory table’s global variable .
Return
value:
Cluster
in-memory table
示例：
Suppose
the global variable arg1 is already
assigned value using env(v, data)
in the initiation script init.splx :
|
=memory(["192.168.31.72:8281","192.168.31.72:8291"],arg1)
|
Generate
a cluster in-memory table from the local in-memory table arg1 .
|
=A1.cursor().fetch()
|
（原文含示意图，图像资源未迁移）
