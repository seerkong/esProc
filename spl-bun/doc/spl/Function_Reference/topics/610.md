# T.memory()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.memory()
=demo.query("select
=A1.keys(EID)
=A1.memory()
=A1.memory@o()
=A1.insert(0)
=demo.query@v("select
=A6.memory()
```

## 提取内容
T.memory()
说明：
Convert
a table sequence to an in-memory table.
语法：
T. memory ()
注意：
The
function converts table sequence T into an in-memory table. If T
is a pure table sequence, keep it pure.
参数：
|
T
|
table sequence or a pure table sequence; if the table sequence has the key and
index, they will be inherited .
选项：
|
@o
|
Directly
use records of the original table sequence; when data of the original table
sequence changes, the data in the returned in-memory table follows to change
by default, the function copies and convert data in the original table
sequence; in this case, data in the in-memory table does not follow to change
even if data in the original table sequence changes
Return
value:
In-memory
table
示例：
|
=demo.query("select
EID,NAME,GENDER,SALARY from employee where EID <= 30")
|
=A1.keys(EID)
|
Set
EID as A1’s key .
|
=A1.memory()
|
Convert
table sequence A1 to an in-memory table, during which the key is inherited .
|
=A1.memory@o()
|
=A1.insert(0)
|
Append
an empty record in the end of the original table sequence .
Check
A2 and find that no empty record is displayed at the end; check A3 and find
that there is an empty record at the end
|
=demo.query@v("select
* from DEPT")
|
Return
a pure table sequence .
|
=A6.memory()
|
Convert
A6’s pure table sequence to an in-memory table and keep it pure .
