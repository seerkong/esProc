# A. merge()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select
=demo.query("select
=[A1,A2].merge(DEPTID,DEPTNAME)
=connect("demo").query("select
=connect("demo").query("select
=[A1,A2].merge()
=connect("demo").query("select
=connect("demo").query("select
=[A1,A2].merge@u()
=[A1,A2].merge@i()
=[A1,A2].merge@d()
=[A1,A2].merge@x()
=connect("demo").query("select
=connect("demo").query("select
=[A1,A2].merge@o()
=connect("demo").query("select
=connect("demo").query("select
=A2(7).modify(null:DEPTID)
=[A1,A2].merge()
=[A1,A2].merge@0()
```

## 提取内容
A. merge()
说明：
Merge
multiple table sequences/record sequences.
语法：
A .merge ( x i ,… )
注意：
The function merges members
of sequence A according to x i
field in order.
By default, A ( i )
is treated as ordered by [ x i ,…] ; when x i is absent and when no primary key is set
for A ( i ) or A ( i ) is unordered, use @o option to simply
concatenate members.
参数：
|
sequence made up of multiple table sequences/record sequences of the same
structure .
|
x i
|
field of A ( i ); if performing merge
by multiple fields, use the comma to separate them, for example, x 1 , x 2 ...
x i is by default the primary key of 为 A ( i ) .
选项：
|
@ u
|
Remove
the duplicates from the table sequence/record sequence generated from
unioning members of A ( i )s in certain order; records with same x i have same corresponding members of A ( i ) .
|
@ i
|
Return
a table sequence/record sequence composed of the common members of A(i) s .
|
@ d
|
Generate
a new table sequence/record sequence by removing members of A(2) &… A(n) from A(1) .
|
@o
|
Do
not assume that A ( i ) is ordered by [ x i ,…], and thus simply concatenate members instead of performing
the merge operation .
|
@0
|
Put
records with null values at the end .
|
@x
|
Remove
common members of A ( i ) and union the other members to
generate a new table sequence/sequence .
返回值：
Table
sequence/记录序列
示例：
Order-based MERGE:
|
=demo.query("select
top 6 * from DEPT order by FATHER ").sort(DEPTID,DEPTNAME)
|
Return
a table sequence ordered by DEPTID and DEPTNAME:
（原文含示意图，图像资源未迁移）
|
=demo.query("select
* from DEPT where DEPTID <8
order by FATHER").sort(DEPTID,DEPTNAME)
|
Return
a table sequence ordered by DEPTID and DEPTNAME:
（原文含示意图，图像资源未迁移）
|
=[A1,A2].merge(DEPTID,DEPTNAME)
|
Merge
A1 and A2 according to DEPTID and DEPTNAME in order:
（原文含示意图，图像资源未迁移）
MERGE by the primary key
when x i is by default the primary key of A ( i ):
|
=connect("demo").query("select
top 6 * from DEPT order by FATHER ").keys(DEPTID).sort(DEPTID)
|
Return
a table sequence using DEPTID as the primary key:
（原文含示意图，图像资源未迁移）
|
=connect("demo").query("select
* from DEPT where DEPTID <8
order by FATHER").keys(DEPTID).sort(DEPTID)
|
Return
a table sequence using DEPTID as the primary key:
（原文含示意图，图像资源未迁移）
|
=[A1,A2].merge()
|
Merge A1 and A2 according to primary key DEPTID as
parameter x i is absent:
（原文含示意图，图像资源未迁移）
Other methods of MERGE:
|
=connect("demo").query("select
top 6 * from DEPT order by FATHER ").keys(DEPTID).sort(DEPTID)
|
Return
a table sequence using DEPTID as the primary key:
（原文含示意图，图像资源未迁移）
|
=connect("demo").query("select
* from DEPT where DEPTID <8
order by FATHER").keys(DEPTID).sort(DEPTID)
|
Return
a table sequence using DEPTID as the primary key:
（原文含示意图，图像资源未迁移）
|
=[A1,A2].merge@u()
|
Merge A1 and A2 according to primary key DEPTID as
parameter x i is absent, and use @u option
to remove duplicate records having same DEPTID:
（原文含示意图，图像资源未迁移）
|
=[A1,A2].merge@i()
|
Merge A1 and A2 according to primary key DEPTID as
parameter x i is absent, and use @i option
to retain only common members of A1 and A2:
（原文含示意图，图像资源未迁移）
|
=[A1,A2].merge@d()
|
Merge A1 and A2 according to primary key DEPTID as
parameter x i is absent, and use @d option
to return a sequence of members of A2 from which members of A1 is removed:
（原文含示意图，图像资源未迁移）
|
=[A1,A2].merge@x()
|
Merge A1 and A2 according to primary key DEPTID as
parameter x i is absent, and use @x option
to return a sequence of members of both A1 and A2 from which their common
members are removed:
（原文含示意图，图像资源未迁移）
Use @o option to simply concatenate records instead of performing
the order-based MERGE:
|
=connect("demo").query("select
top 6 * from DEPT order by FATHER ")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=connect("demo").query("select
* from DEPT where DEPTID <8
order by FATHER")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=[A1,A2].merge@o()
|
记录s
of A1 and A2 are unordered; use @o option to simly concatenate them without
sorting:
（原文含示意图，图像资源未迁移）
Use @0 option to place records whose primary keys are null at the
end:
|
=connect("demo").query("select
top 6 * from DEPT order by FATHER ").keys(DEPTID).sort(DEPTID)
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=connect("demo").query("select
* from DEPT where DEPTID <8
order by FATHER").keys(DEPTID).sort(DEPTID)
|
=A2(7).modify(null:DEPTID)
|
Now
this is data of A2’s table sequence:
（原文含示意图，图像资源未迁移）
|
=[A1,A2].merge()
|
（原文含示意图，图像资源未迁移）
|
=[A1,A2].merge@0()
|
Merge A1 and A2 according to primary key DEPTID as
parameter x i is absent, and use @0 option
to place records where DEPTID is null at the end:
（原文含示意图，图像资源未迁移）
Related functions:
CS. m e r g e ()
