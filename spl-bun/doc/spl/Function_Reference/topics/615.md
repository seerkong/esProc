# CS.merge()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
CS.merge()
=connect("demo").cursor("SELECT top 10 EID,NAME,DEPT,GENDER FROM employee ")
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where GENDER='M' ")
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where DEPT='Sales' ")
=[A1,A2,A3]
=A4.merge(EID)
=A5.fetch()
=connect("demo").cursor("SELECT top 10 EID,NAME,DEPT,GENDER FROM employee ")
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where GENDER='M' ")
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where DEPT='Sales' ")
=[A1,A2,A3]
=A4.merge@u(EID)
=A5.fetch()
=connect("demo").cursor("SELECT top 10 EID,NAME,DEPT,GENDER FROM employee ")
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where GENDER='M' ")
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where DEPT='Sales' ")
=[A1,A2,A3]
=A4.merge@i(EID)
=A5.fetch()
=connect("demo").cursor("SELECT top 10 EID,NAME,DEPT,GENDER FROM employee ")
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where GENDER='M' ")
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where DEPT='Sales' ")
=[A1,A2,A3]
=A4.merge@d(EID)
=A5.fetch()
=connect("demo").cursor("SELECT top 10 EID,NAME,DEPT,GENDER FROM employee ")
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where GENDER='M' ")
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where DEPT='Sales' ")
=[A1,A2,A3]
=A4.merge@x(EID)
=A5.fetch()
```

## 提取内容
CS.merge()
说明：
Merge
data of the member cursors of a 游标序列 and return a multicursor.
语法：
CS. merge ( x i ,… )
注意：
CS is a 游标序列 ordered by [x i ,…], and from
each cursor a sequence of records can be output. The function merges the
records of these cursors by the expression x i
and returns a multicursor.
Members of the cursor
sequence must be of the same structure. If members are multicursors, they must
have the same number of parallel cursors and be segmented synchronically.
This is a delayed function.
参数：
|
CS
|
游标序列.
|
x i
|
An
expression. If performing merge by multiple fields, use comma to separate
them, for example, x 1 , x 2 .. .
选项：
|
@ u
|
Union
operation. Remove the duplicate records from the resulting cursor obtained by
unioning the cursor members of CS
in certain order. By default, the
duplicate records are included.
|
@i
|
Intersection
operation. Return a cursor composed of common members of members of CS , the 游标序列.
|
@d
|
Difference
operation. Create a new cursor by removing members of CS 2 & … CSn from CS 1.
|
@0
|
Put
records with null values at the end.
|
@x
|
Only
merge distinct records in member cursors.
返回值：
多游标
示例：
|
=connect("demo").cursor("SELECT top 10 EID,NAME,DEPT,GENDER FROM employee ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where GENDER='M' ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where DEPT='Sales' ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=[A1,A2,A3]
|
Return a sequence
of cursors.
|
=A4.merge(EID)
|
Merge records in
the cursor members of the sequence in order according to EID field.
|
=A5.fetch()
|
Fetch data from
cursor A5 (it would be better to fetch data in batches when a huge amount of
data is involved):
（原文含示意图，图像资源未迁移）
Use
@u option to perform union operation:
|
=connect("demo").cursor("SELECT top 10 EID,NAME,DEPT,GENDER FROM employee ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where GENDER='M' ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where DEPT='Sales' ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=[A1,A2,A3]
|
Return a sequence
of cursors.
|
=A4.merge@u(EID)
|
Use @u option to
merge records of member cursors in the sequence in order according to EID
field, during which dulicate members are discarded.
|
=A5.fetch()
|
Fetch data from
cursor A5:
（原文含示意图，图像资源未迁移）
Use
@i option to perform intersection operation:
|
=connect("demo").cursor("SELECT top 10 EID,NAME,DEPT,GENDER FROM employee ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where GENDER='M' ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where DEPT='Sales' ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=[A1,A2,A3]
|
Return a sequence
of cursors.
|
=A4.merge@i(EID)
|
Use @i option to
get the intersection and returna cursor containing common records of the
member cursors in the sequence.
|
=A5.fetch()
|
Fetch data from
cursor A5:
（原文含示意图，图像资源未迁移）
Use
@d option to perform difference operation:
|
=connect("demo").cursor("SELECT top 10 EID,NAME,DEPT,GENDER FROM employee ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where GENDER='M' ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where DEPT='Sales' ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=[A1,A2,A3]
|
Return a sequence
of cursors.
|
=A4.merge@d(EID)
|
Use @d option to
perform the difference operation that removes records of cursor A2 and cursor
A3 from cursor A1 and forms a new cursor.
|
=A5.fetch()
|
Fetch data from
cursor A5:
（原文含示意图，图像资源未迁移）
Use
@x option to get non-duplicate members from the 游标序列 to form a
new sequence:
|
=connect("demo").cursor("SELECT top 10 EID,NAME,DEPT,GENDER FROM employee ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where GENDER='M' ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT top 5 EID,NAME,DEPT,GENDER FROM employee where DEPT='Sales' ")
|
Return a cursor
whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=[A1,A2,A3]
|
Return a sequence
of cursors.
|
=A4.merge@x(EID)
|
Use @x option to
perform the difference operation that gets non-duplicate records from members
cursors of the sequence to form a new sequence.
|
=A5.fetch()
|
Fetch data from
cursor A5:
（原文含示意图，图像资源未迁移）
相关函数：
A.mer g e ()
