# CS.mergex()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
CS.mergex()
CS.mergex()
=file("emp_f.ctx").open().cursor()
=file("emp_m.ctx").open().cursor()
=[A1,A2].mergex()
=A3.fetch()
=file("emp_f.ctx").open().cursor@m(;;3)
=file("emp_m.ctx").open().cursor(;;A1)
=[A1,A2].mergex()
=A3.fetch()
```

## 提取内容
CS.mergex()
说明：
Merge
members of a sequence of composite table (multi)cursors.
语法：
CS.mergex()
注意：
CS is a sequence of composite table
cursors. The function merges members of CS
automatically via the dimension field (and the field before it).
Members of CS should be of same structure. The
function supports multicursors; if members are multicursors, they should have
the same number of subcursors and are segmented synchronously.
参数：
|
CS
|
sequence of composite table cursors .
Return
value:
游标
示例：
Perform
merge on a sequence of composite table cursors:
|
=file("emp_f.ctx").open().cursor()
|
Below
is content of composite table emp_f.ctx :
（原文含示意图，图像资源未迁移）
|
=file("emp_m.ctx").open().cursor()
|
Below
is content of composite table emp_m.ctx :
（原文含示意图，图像资源未迁移）
|
=[A1,A2].mergex()
|
Merge
composite tables in A1 and A2 via their dimension fields and return result as
a cursor .
|
=A3.fetch()
|
Fetch
data from A3’s cursor:
（原文含示意图，图像资源未迁移）
Perform
merge on a sequence of composite table multicursors:
|
=file("emp_f.ctx").open().cursor@m(;;3)
|
Generate
a multicursor having 3 subcursors based on composite table emp_f.ctx ,
whose content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("emp_m.ctx").open().cursor(;;A1)
|
Synchronously
divide entity table emp_m.ctx according to A1’s multicursor; below is
result content of the entity table:
（原文含示意图，图像资源未迁移）
|
=[A1,A2].mergex()
|
Merge
composite table multicursors in A1 and A2 via their dimension fields and
return result as a cursor .
|
=A3.fetch()
|
Fetch
data from A3’s cursor:
（原文含示意图，图像资源未迁移）
function:
CS. m erge()
