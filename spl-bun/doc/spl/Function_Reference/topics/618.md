# mfind()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
mfind()
mfind ( A , n )
=mfind(A1)
=mfind(A1,3)
=mfind(A1,10)
=mfind(A5,3)
```

## 提取内容
mfind()
说明：
Search for positions of non-zero members in a vector or matrix.
语法：
mfind ( A , n )
注意：
The MathCli external library function
(See External Library Guide )
searches for positions of the first n non-zero members in vector or
matrix A .
It gets the position of the
first non-zero member when parameter n is absent. If the total number of
non-zero members is fewer than n , it returns positions of all non-zero,
non-null members.
Search column by column
when A is a matrix and return sequence numbers of positions of non-zero
members in the matrix.
参数：
|
vector or a matrix .
|
n
|
positive integer .
Return
value:
Sequence
示例：
|
[0,,5,2,0,2,1,0]
|
=mfind(A1)
|
（原文含示意图，图像资源未迁移）
Return
position of the first non-zero member as parameter n is absent.
|
=mfind(A1,3)
|
（原文含示意图，图像资源未迁移）
Return
positions of the first non-zero members.
|
=mfind(A1,10)
|
（原文含示意图，图像资源未迁移）
Return
positions of all non-zero members since the number of them in matrix A1 is
fewer than n (here is 10).
|
[[1,0,2],[0,0,2],[0,4,7]]
|
=mfind(A5,3)
|
（原文含示意图，图像资源未迁移）
As
parameter A is a matrix, search for the first three non-zero members by column
and the result are “1”,“4”,”2”, whose sequence numbers of positions are
1,6,7.
