# A.mi()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.mi()
A.mi()
=A1.mi()
```

## 提取内容
A.mi()
说明：
Create indicator variable
for missing values in a sequence variable.
语法：
A.mi()
注意：
The MathCli external library function (See External
Library Guide ) creates indicator
variable for missing values in sequence variable A during modeling. If
the missing value rate is ≤ 5% or ≥ 95%, do not generate the indicator variable and return null.
参数：
|
A sequence .
Return
value:
Table sequence
示例：
|
[1,2,3,null,5]
|
=A1.mi()
|
（原文含示意图，图像资源未迁移）
