# millisecond()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
millisecond()
millisecond( datetimeExp )
millisecond(now())
year ( )
```

## 提取内容
millisecond()
说明：
Get
the millisecond from a specified datetime value.
语法：
millisecond( datetimeExp )
注意：
The
function gets the millisecond from the datetime value
datetimeExp .
参数：
|
datetimeExp
|
An
expression whose result is a date or a datetime value of standard format .
返回值：
Integer
示例：
|
millisecond(datetime("1980-02-27
12:00:02:123 ","yyyy-MM-dd
HH:mm:ss:SSS"))
|
123.
|
millisecond(now())
|
Milliseconds
of the current time .
相关函数：
year ( )
mo n t h( )
da y ()
ho u r ()
minut e ( )
seco n d()
