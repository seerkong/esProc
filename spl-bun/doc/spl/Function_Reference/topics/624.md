# min()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
min()
min ( x 1 ,…, x n )
=["c","b","e","A"].min()
=[8,-6,1,3,5].min(~*2)
=["a",1].min()
=["a",null,"b"].min()
=["a",null,"b"].min@0()
=min("c","b","e","A")
=demo.query("select
=A1.min(SALARY+1000)
=demo.query("select
=A3.min(FATHER)
```

## 提取内容
min()
说明：
find
the minimum value among members of a sequence.
语法：
|
A .min( x )
|
min ( x 1 ,…, x n )
|
Equivalent
to A .min() , where x 1 ,…, x n are members of sequence A .
注意：
The
function computes expression x with members of sequence A and by
default, returns the minimum non-null value.
参数：
|
sequence, where members should have same data type .
|
x
|
An
expression; cannot be omitted when sequence A is a table sequence or a record sequence .
选项：
|
@0
|
Do
not ignore null members; syntax min ( x 1 ,…, x n ) isn’t
supported when this option works .
返回值：
The
minimum value
示例：
When
A is a sequence:
|
=["c","b","e","A"].min()
|
"A"
|
=[8,-6,1,3,5].min(~*2)
|
-12
|
=["a",1].min()
|
Error
is reported as members of the sequence have inconsistent data types .
|
=["a",null,"b"].min()
|
"a" ; ignore
null members .
|
=["a",null,"b"].min@0()
|
null .
|
=min("c","b","e","A")
|
"A", which is same
as A1 .
When
A is a table sequence or a record sequence:
|
=demo.query("select
EID,NAME,SALARY from EMPLOYEE").sort(SALARY)
|
Query
content of EMPLOYEE table and sort result by SALARY in ascending order .
（原文含示意图，图像资源未迁移）
|
=A1.min(SALARY+1000)
|
Get
the lowest SALARY value after salary is increased by 1000 and return 4000 .
|
=demo.query("select
* from AREA")
|
Query
AREA table .
（原文含示意图，图像资源未迁移）
|
=A3.min(FATHER)
|
Get
the minimum non-null FATHER value in AREA table and return 0 .
