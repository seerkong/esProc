# A. minp()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=A1.minp(~*~)
=A1.minp@a(~*~)
=A1.minp@z(~*0)
=A1.minp()
=demo.query("select
=A1.minp@a(BIRTHDAY)
=A1.minp()
```

## 提取内容
A. minp()
说明：
Get
the member that makes the minimum value for the expression.
语法：
A .minp( x )
注意：
The
function computes expression x with each member
of sequence A and returns the
member which makes the minimum value for the expression x .
选项：
|
@a
|
Return
all the members that fulfill the condition. By default, return the first
eligible member .
|
@z
|
Search
the member from back to front .
参数：
|
A sequence.
|
x
|
An
expression that by default returns the first member of A .
返回值：
Single
value/Sequence
示例：
When
A is a sequence:
|
[2,5,6,3,2,3]
|
=A1.minp(~*~)
|
2; return members of A1 that
make the expression get the smallest value; by default, return only the first eligible member .
|
=A1.minp@a(~*~)
|
[2,2] ; as @a option is present,
return all members that make the expression get
the smallest value .
|
=A1.minp@z(~*0)
|
3 ; search from front to back .
|
=A1.minp()
|
2 ; return the first member of
A1 as parameter x is absent
When
A is a table sequence or a record sequence:
|
=demo.query("select
top 10 EID,NAME,BIRTHDAY from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.minp@a(BIRTHDAY)
|
Return record of the eldest employee.
（原文含示意图，图像资源未迁移）
|
=A1.minp()
|
Return the first
record of A1.
（原文含示意图，图像资源未迁移）
相关函数：
A .pm i n ()
A .ma x p ( )
