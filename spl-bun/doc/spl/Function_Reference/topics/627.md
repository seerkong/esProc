# minute()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
minute()
minute( datetimeExp )
minute(datetime("19800227","yyyyMMdd"))
day ( )
```

## 提取内容
minute()
说明：
Get
the minute from a datetime value.
语法：
minute( datetimeExp )
注意：
The
function gets the minute from the specified datetime value datetimeExp .
参数：
|
datetimeExp
|
An
expression whose result is a date or a datetime value of standard format .
返回值：
Integer
示例：
|
minute(datetime("19800227","yyyyMMdd"))
|
minute("1972-11-08
10:20:30")
|
minute(datetime("2006-01-15
13:20:30"))
|
相关函数：
ye a r ()
mo n t h()
day ( )
h o u r ()
seco n d ( )
millise c on d ()
