# mode()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
mode()
mode ( x 1 ,…, x n )
=[1,2,"hello",2,"hello",2].mode()
=[15,20,15,20,"hello"].mode()
=[,,,].mode()
=["A",,,,"A"].mode()
=mode(1,2,"hello",2,"hello",2)
=demo.query("select
=A1.mode(DEPT)
```

## 提取内容
mode()
说明：
Get
the most frequently appearing members from a sequence or a table sequence.
语法：
|
A .mode( x )
|
mode ( x 1 ,…, x n )
|
Equivalent to A .mode() , where x 1 ,…, x n are
members of sequence A
注意：
The
function gets the most frequently appearing ones among the non-null members in
sequence A . When there are multiple members that have same number of
appearances, the function returns one of them randomly. The function returns
null when all members are nulls.
参数：
|
sequence .
|
x
|
An
expression; cannot be omitted when sequence A is a table
sequence or a record sequence .
Return
value:
The
most frequently appearing member
示例：
When
A is a sequence:
|
=[1,2,"hello",2,"hello",2].mode()
|
2.
|
=[15,20,15,20,"hello"].mode()
|
20 ; as 15 and 20 have same
appearing frequency, one of them is returned randomly .
|
=[,,,].mode()
|
Return null because all
members of the sequence are nulls.
|
=["A",,,,"A"].mode()
|
"A".
|
=mode(1,2,"hello",2,"hello",2)
|
2, which is same as A1 .
When
A is a table sequence or a record sequence:
|
=demo.query("select
EID,GENDER,DEPT from EMPLOYEE where EID <10")
|
（原文含示意图，图像资源未迁移）
|
=A1.mode(DEPT)
|
Get the most-frequently appearing DEPT value from
table sequence A1 and return Sales .
