# r.modify( x i : F i ,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.modify( x i : F i ,… )
=[[1,"Lucy",29]].new(~(1):ID,~(2):Name,~(3):Age)
=A1(1).modify(2,"Petter")
=A2.modify(30:Age)
=A3.modify(3,33:Age)
=[[4,"Lily",23]].new(~(1):SID,~(2):SName,~(3):SAge)
=A4.modify@r(A5(1))
=A6.modify@f(([["ella"]].new(~(1):Name))(1))
```

## 提取内容
r.modify( x i : F i ,… )
说明：
Modify
the field values of a record.
语法：
r .modify( x i : F i , … )
注意：
The
function modifies the field values in record r.
参数：
|
r
|
The
record to be modified .
|
x i
|
Modification
expression .
|
F i
|
Name of the field to be modified. The i th field in r will be modified if F i isn’t supplied .
|
r'
|
The new record that will replace record r .
选项：
|
@r(r')
|
Replace record r with record r' according to the former’s order .
|
@f(r')
|
Modify
a certain field value of record r with record r' .
返回值：
记录
示例：
|
=[[1,"Lucy",29]].new(~(1):ID,~(2):Name,~(3):Age)
|
（原文含示意图，图像资源未迁移）
|
=A1(1).modify(2,"Petter")
|
Modify
the first and the second field .
（原文含示意图，图像资源未迁移）
|
=A2.modify(30:Age)
|
Modify
Age field .
（原文含示意图，图像资源未迁移）
|
=A3.modify(3,33:Age)
|
Modify
the first field and Age field .
（原文含示意图，图像资源未迁移）
|
=[[4,"Lily",23]].new(~(1):SID,~(2):SName,~(3):SAge)
|
（原文含示意图，图像资源未迁移）
|
=A4.modify@r(A5(1))
|
Fill
members of A5(1) in A4 according to A4’s order .
（原文含示意图，图像资源未迁移）
|
=A6.modify@f(([["ella"]].new(~(1):Name))(1))
|
Modify only the value of Name field in A6 .
（原文含示意图，图像资源未迁移）
相关函数：
T .m o d ify()
T .insert()
T .delete()
