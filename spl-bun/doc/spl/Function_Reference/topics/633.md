# T. modify()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select
=A1.modify(1,"Sales",5)
=A1.modify(2,6:MANAGER)
=A1.modify@n(2:1,"AAA":DEPT,76:MANAGER)
=create(DeptName,ManagerID)
=A5.modify(1:A1,DEPT:DeptName,MANAGER:ManagerID)
=A1.delete(A1.select(MANAGER>5))
=A5.modify@r(1:A7)
=create(DeptName,MANAGER)
=A9.modify@f(1:A7)
```

## 提取内容
T. modify()
说明：
Modify
field values in a table sequence.
语法：
|
T .modify( k , x i : F i ,… )
|
Modify
the k th record , which is equal to T ( k ). modify ( x i : F i ,…) .
|
T .modify( k : A , x i : F i ,… )
|
Modify the
records from the k th record to the ( k + | A | - 1) th record.
注意：
The
function modifies one or more records at the specified position(s).
参数：
|
k
|
The
position at which the record will be modified. If k exceeds the limit, then append a new record in the end .
|
x i
|
The value of F i field to be modified .
|
F i
|
Name of the field
of the record which will be modified. If F i is omitted, then modify the i th field of T .
|
T
|
table sequence .
|
sequence or an integer; If A is an integer, then it is equal to to( A ) .
选项：
|
@n
|
Return the newly-modified record or sequence .
|
@r(k:A)
|
Modify
table sequence T using sequence A from the k th record according to the order of the fields .
|
@f(k:A)
|
Modify
table sequence T using sequence A from the k th record; only the common fields are modified.
返回值：
The updated record
or sequence
示例：
|
=demo.query("select
* from DEPARTMENT")
|
（原文含示意图，图像资源未迁移）
|
=A1.modify(1,"Sales",5)
|
Modify
the first and the second fields of the first record .
（原文含示意图，图像资源未迁移）
|
=A1.modify(2,6:MANAGER)
|
Modify
the MANAGER field of the second record .
（原文含示意图，图像资源未迁移）
|
=A1.modify@n(2:1,"AAA":DEPT,76:MANAGER)
|
Return the
modified second record .
（原文含示意图，图像资源未迁移）
|
=create(DeptName,ManagerID)
|
（原文含示意图，图像资源未迁移）
|
=A5.modify(1:A1,DEPT:DeptName,MANAGER:ManagerID)
|
Append
a new record in the end because 1 exceeds the limit .
（原文含示意图，图像资源未迁移）
|
=A1.delete(A1.select(MANAGER>5))
|
=A5.modify@r(1:A7)
|
Modify
A5 using A7 from the first record .
|
=create(DeptName,MANAGER)
|
=A9.modify@f(1:A7)
|
Modify MANAGER
field only .
（原文含示意图，图像资源未迁移）
相关函数：
r .modif y ()
T .ins e r t ()
T .d e lete()
A .mod i fy()
