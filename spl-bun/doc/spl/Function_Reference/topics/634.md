# A.modify ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.modify ()
=["a","c","d","e","f"]
=A1.modify(2,"g")
=A1.modify(6,[2,4,5])
=demo.query("select
=A4.modify@n(2:3,ID+10:ID)
=A1.
modify(-2,"h")
```

## 提取内容
A.modify ()
说明：
Assign
value(s) to one or more members of a sequence according to the specified
position(s).
语法：
|
A .modify( k,x )
|
Assign
x to the k th member of sequence A .
|
A .modify( k,X )
|
Assign
members of X to members of A in order from the k th position to the ( k + | X |- 1) th
position; do the assignment backward from the end when k <0 .
注意：
The
function assigns x to the k th
member of sequence A or
members of X to the members of A from the k th position to
the number ( k + | X |- 1) th
position in order.
参数：
|
sequence .
|
k
|
member position; If k is greater than the length of sequence A , then append the member in the end .
|
x
|
member value .
|
X
|
sequence .
选项：
|
@n
|
Return
the modified record or a record sequence of modified records .
返回值：
Sequence
示例：
|
=["a","c","d","e","f"]
|
=A1.modify(2,"g")
|
[a,g,d,e,f] .
|
=A1.modify(6,[2,4,5])
|
[a,g,d,e,f,2,4,5] ; append the values at the end as the specified position exceeds
the limit .
|
=demo.query("select
* from STUDENTS")
|
=A4.modify@n(2:3,ID+10:ID)
|
（原文含示意图，图像资源未迁移）
Return
a record sequence of modified records .
|
=A1.
modify(-2,"h")
|
Assign values backward and return [a,g,d,e,f,2,4,h,5]
as parameter k <0 .
相关函数：
A .del e te()
A .ins e r t()
T . modify()
