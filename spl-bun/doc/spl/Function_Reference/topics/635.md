# mongo_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
mongo_close()
mongo_close ( fd )
=mongo_open("mongodb://localhost:27017/mydb")
=mongo_close()
```

## 提取内容
mongo_close()
说明：
Close connection to MongoDB database.
语法：
mongo_close ( fd )
注意：
The MongoCli external library function (See External Library Guide ) closes
connection to MongoDB database.
参数：
|
fd
|
MongoDB connection object.
示例：
|
=mongo_open("mongodb://localhost:27017/mydb")
|
Connect to a MongoDB .
|
=mongo_close()
|
Close the connection .
