# mongo_shell()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
mongo_shell()
mongo_shell ( fd,s )
=mongo_open("mongodb://localhost:27017/mydb" )
=mongo_shell(A1,"{'find':'emp'}")
=mongo_shell(A1,"{'find':'emp'}")
=mongo_shell(A1,"{'find':'emp',filter:{GENDER:'F'}}")
=A4.fetch()
=mongo_shell@d(A1,"{'find':'emp',projection:{NAME:1,DEPT:1,SALARY:1},sort:{SALARY:1}}")
=mongo_shell(A1,"{'count':'emp'}")
=mongo_shell(A1,"{'distinct':'emp',key:'DEPT'}")
=mongo_shell@d(A1,"{'aggregate':'emp',pipeline:
=mongo_close(A1)
```

## 提取内容
mongo_shell()
说明：
Query MongoDB database and return a table sequence.
语法：
mongo_shell ( fd,s )
注意：
The
MongoCli external library function (See
External Library Guide ) queries data in a MongoDB database and
returns the table sequence in the outermost layer.
选项：
|
@e
|
Go on with the execution when error
appears and return the error information.
|
@d
|
Return a table sequence.
|
@c
|
Return a cursor; should be used
together with @d option.
参数：
|
fd
|
MongoDB connection object .
|
s
|
A JSON string that is
equivalent to parameter str in db.runCommand(str) .
返回值：
Table sequence
示例：
|
=mongo_open("mongodb://localhost:27017/mydb" )
|
Connect to database mydb on Mongo server .
|
=mongo_shell(A1,"{'find':'emp'}")
|
Return
the outermost table sequence .
（原文含示意图，图像资源未迁移）
|
=mongo_shell(A1,"{'find':'emp'}")
|
Query all records of the emp set and return a table sequence .
（原文含示意图，图像资源未迁移）
|
=mongo_shell(A1,"{'find':'emp',filter:{GENDER:'F'}}")
|
Query records where GENDER is F and return
a cursor .
|
=A4.fetch()
|
（原文含示意图，图像资源未迁移）
|
=mongo_shell@d(A1,"{'find':'emp',projection:{NAME:1,DEPT:1,SALARY:1},sort:{SALARY:1}}")
|
Display
a table sequence consisting of NAME field, DEPT field and SALARY field and
sorted by SALARY in ascending order .
（原文含示意图，图像资源未迁移）
|
=mongo_shell(A1,"{'count':'emp'}")
|
Count records in emp :
（原文含示意图，图像资源未迁移）
|
=mongo_shell(A1,"{'distinct':'emp',key:'DEPT'}")
|
Get unique DEPT
field values from emp .
（原文含示意图，图像资源未迁移）
|
=mongo_shell@d(A1,"{'aggregate':'emp',pipeline:
[{'$group':{'_id':'$DEPT','count':{'$sum':1}}}],cursor: { }}")
|
Compute the number
of employees in each department:
（原文含示意图，图像资源未迁移）
|
=mongo_close(A1)
|
Close the database connection .
|
