# [a:b]

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=[A1:C1]
=[A1:A3]
=[A1:C3]
=[A1:C1,A2,C2:C3]
```

## 提取内容
[a:b]
说明：
Get
values from row/column/cell a to
row/column/cell b to create a
sequence.
语法：
[ a:b ]
注意：
The
function creates a sequence by getting values from row/column/cell a to row/column/cell b .
If
a and b are in the same row or column, get values from cell a to cell b to create a sequence
If
a and b are neither in the same row nor in the same column, get values
from a rectangular area whose diagonal is the line connecting cell a and cell b to create a sequence.
The
function can also be written as [a,b] ,
which means getting values of cell a
and cell b to creat a sequence; allow
multiple parameters, like [a:b,c,d:e] .
参数：
|
a
|
Cell
name.
|
b
|
Cell
name.
Return
value:
Sequence
示例：
|
a
|
b
|
c
|
%
|
&
|
*
|
=[A1:C1]
|
[1,2,3]
|
=[A1:A3]
|
[1,a,%]
|
=[A1:C3]
|
[1,2,3,a,b,c,%,&,*]
|
=[A1:C1,A2,C2:C3]
|
[1,2,3,a,c,*]
