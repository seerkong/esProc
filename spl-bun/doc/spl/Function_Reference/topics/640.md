# movefile( fn:z,path )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
movefile( fn:z,path )
movefile( fn:z,path )
=movefile("E://test.property","D://testfile.property")
=movefile("D://testfile.property","file.property")
=movefile("D://file.property")
=movefile@y("E://test1.property","D://testfile1.property")
=movefile@c("D://testfile1.property","file.property")
=movefile@cy("E://test2.property","D://
=movefile@y("D://testfile1.property")
=movefile@cp("D://testfile1.property","file.property")
```

## 提取内容
movefile( fn:z,path )
说明：
Move,
delete, or rename a file.
语法：
movefile( fn:z,path )
注意：
The
function moves file fn to a file specified by parameter path . Delete file fn if path is absent ; rename the file if only the file name is specified in the
path .
参数：
|
fn
|
File
name .
|
z
|
An
integer; it represents the zone table number when parameter fn
is a homo-name files group; or is omitted when fn isn’t a
homo-name files group .
|
path
|
The
path (with file name) along which the file is moved, or a file name .
选项：
|
@y
|
Force
an execution of the function if the target file already exists. Without the
option you can’t force an execution. Delete the target file if parameter path is absent .
|
@c
|
Copy
the file. If the target file name is the same as that of the specified file
to which it will be moved, the copy fails .
|
@p
|
path is relative to the main directory
when it is a relative path; default is relative to fn ’s parent
directory .
返回值：
Boolean
示例：
|
=movefile("E://test.property","D://testfile.property")
|
Move
the file to testfile.property file
under root directory on driver D .
|
=movefile("D://testfile.property","file.property")
|
D://testfile.property is renamed file.property .
|
=movefile("D://file.property")
|
Delete
file.property .
|
=movefile@y("E://test1.property","D://testfile1.property")
|
The
file testfile1.property already exists. Force the move and override the
original file .
|
=movefile@c("D://testfile1.property","file.property")
|
Copy
the file to directory D:// and name it file.property . Note that this
isn’t a renaming action.
|
=movefile@cy("E://test2.property","D://
file.property")
|
file.property already exists. Force the copy and override the
original file .
|
=movefile@y("D://testfile1.property")
|
Delete
testfile1.property .
|
=movefile@cp("D://testfile1.property","file.property")
|
Copy
the file to the main directory and rename it file.property .
相关函数：
f . ex i s t s ()
f . dat e ()
f . size()
