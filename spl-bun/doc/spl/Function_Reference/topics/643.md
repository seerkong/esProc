# mul()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
mul()
mul( A, X )
=mul(A1,A2)
```

## 提取内容
mul()
说明：
Calculate
the product of two matrices or vectors.
语法：
mul( A, X )
注意：
The
function calculates the product of multiplying vector/matrix A by vector/matrix X , during which vector X on
the right will be automatically transposed.
参数：
|
A/X
|
matrix or a vector
Return
value:
Sequence
示例：
|
[[3,5],[2,8]]
|
[[4,6],[2,4]]
|
=mul(A1,A2)
|
（原文含示意图，图像资源未迁移）
