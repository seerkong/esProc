# n.f(x)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
n.f(x)
=3.(~*2)
n.f ( x )
=3.sum(~*2)
```

## 提取内容
n.f(x)
说明：
Perform
a loop operation by declaring an integer loop variable.
注意：
The
function performs a loop operation with integer n as the loop variable.
|
n .( x )
|
Equivalent
to to( n ).( x ) .
|
n . f ( x )
|
Equivalent
to to( n ), f ( x ) .
参数：
|
n
|
An
integer .
|
x
|
An expression.
|
f
|
The
function name .
示例：
Ø
n .( x )
|
=3.(~*2)
|
[2,4,6]
Ø
n.f ( x )
|
=3.sum(~*2)
|
