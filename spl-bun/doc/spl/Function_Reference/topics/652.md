# f. name()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
f.name()
=file(" G:/Program
=file("E:/Program files/raqsoft/esProc/data/data.txt").name()
=file("employee1.txt").name@p()
```

## 提取内容
f. name()
说明：
Return
a file’s path relative to the main directory.
语法：
f.name()
注意：
The
function returns file f ’s path
relative to the main directory; return the full path if f isn’t in the main directory.
参数：
|
f
|
file .
Optios:
|
@p
|
Return
a full path .
返回值：
字符串
示例：
|
=file(" G:/Program
files/raqsoft/esProc/demo/ data/data.txt").name()
|
（原文含示意图，图像资源未迁移）
Return the path
which is relative to the main directory because the file is under the main
directory: G:/Program files/raqsoft/esProc/demo .
|
=file("E:/Program files/raqsoft/esProc/data/data.txt").name()
|
Return
the full path when the file isn’t in the main directory .
|
=file("employee1.txt").name@p()
|
（原文含示意图，图像资源未迁移）
Return the full path since employee1.txt is in the main directory .
