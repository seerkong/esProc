# new ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
new ()
new( x i : F i ,… )
=new(1:ID,"ZHS":name)
=new@t(1:ID,"ZHS":name)
```

## 提取内容
new ()
说明：
Generate a record according to the specified field name and
field value.
语法：
new( x i : F i ,… )
注意：
The function generates a record made up of one field whose
name is F i and value is x i .
参数：
|
x i
|
Field value .
|
F i
|
字段名 .
选项：
|
@t
|
Enable returning result as a table sequence .
返回值：
记录/Table sequence
示例：
|
=new(1:ID,"ZHS":name)
|
Generate a record:
（原文含示意图，图像资源未迁移）
|
=new@t(1:ID,"ZHS":name)
|
Use @t option to generate
a table sequence made up of one record:
（原文含示意图，图像资源未迁移）
