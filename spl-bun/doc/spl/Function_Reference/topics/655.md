# A.new( x i : F i ,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.new( x i : F i ,… )
=demo.query("select
=A1.new(EID:EmployeeID,NAME, #3:dept)
=A1.new(NAME,age(BIRTHDAY):AGE)
=A1.new@m(NAME,age(BIRTHDAY):AGE)
=file("D:\\txt_files\\data1.txt").import@t()
=A5.new@i(CLASS,STUDENTID,SUBJECT,SCORE:score)
=demo.query("select EID,NAME,DEPT,BIRTHDAY from EMPLOYEE").i()
=A1.new@o(EID,NAME, #3:dept)
=A2(1).NAME="aaa"
=create(Name,Chinese).record(["Jack",99,"Lucy",90])
=create(Name,Math).record(["Jack",89,"Lucy",96])
=A1.new(Name:Name,Chinese:Chinese,A2(#).Math:Math)
=demo.query("select
=A1.new(CLASS,STUDENTID,SUBJECT,SCORE,cum(SCORE;CLASS,STUDENTID):F1)
=A1.new@z(CLASS,STUDENTID,SUBJECT,SCORE,cum(SCORE;CLASS,STUDENTID):F1)
```

## 提取内容
A.new( x i : F i ,… )
说明：
Perform
computation on a sequence to generate a new table sequence.
语法：
A .new( x i : F i ,… )
注意：
The
function computes expression x i on each member of sequence A and generate a new table
sequence having same number of records as A and using x i as field values and F i as field names.
参数：
|
F i
|
Field
names of the result table sequence; use x i when the parameter is absent; use the original field names when x i is #i .
|
x i
|
An
expression whose results are field values; if omitted, field values will be
nulls a, and when is absent, : Fi must not be omitted .
|
sequence .
选项：
|
@m
|
Use parallel processing to speed up
computation .
|
@i
|
Won’t generate a record if the result of
expression xi is null .
|
@o
|
When
parameter A is a pure table sequence, directly reference an old column
if it is unmodified instead of generating a new column; sequence A
will also be updated when the result table sequence is updated . Only supported by
esProc Enterprise Edition.
|
@z
|
Perform
an inverse operation; only apply to non-pure sequences .
返回值：
Table
sequence
示例：
Ø
Generate from an individual table sequence
|
=demo.query("select
EID,NAME,DEPT,BIRTHDAY from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.new(EID:EmployeeID,NAME, #3:dept)
|
（原文含示意图，图像资源未迁移）
Generate
a new table sequence directly. If the field names are the same as those of A1 , F i can be omitted.
|
=A1.new(NAME,age(BIRTHDAY):AGE)
|
（原文含示意图，图像资源未迁移）
Generate the new
table sequence by computing new field values.
|
=A1.new@m(NAME,age(BIRTHDAY):AGE)
|
Use
@m option to increase performance of big data handling.
|
=file("D:\\txt_files\\data1.txt").import@t()
|
Below is the file data1.txt :
（原文含示意图，图像资源未迁移）
|
=A5.new@i(CLASS,STUDENTID,SUBJECT,SCORE:score)
|
If
the SCORE value is null, the corresponding record won’t be generated .
（原文含示意图，图像资源未迁移）
Ø
Generate from a pure table
sequence
|
=demo.query("select EID,NAME,DEPT,BIRTHDAY from EMPLOYEE").i()
|
Return
a pure table sequence .
|
=A1.new@o(EID,NAME, #3:dept)
|
With
@o option, directly reference the unmodified old columns instead of
generating new ones .
|
=A2(1).NAME="aaa"
|
Modifying
column values results in the modification of source table; below is A2’s
result after execution:
（原文含示意图，图像资源未迁移）
And A1’s result is
as follows:
（原文含示意图，图像资源未迁移）
Ø
Generate from multiple table sequences of the same order
|
=create(Name,Chinese).record(["Jack",99,"Lucy",90])
|
（原文含示意图，图像资源未迁移）
|
=create(Name,Math).record(["Jack",89,"Lucy",96])
|
（原文含示意图，图像资源未迁移）
|
=A1.new(Name:Name,Chinese:Chinese,A2(#).Math:Math)
|
（原文含示意图，图像资源未迁移）
Use
A2(#) to get the record from A2 in the same position .
Ø
Perform the inverse operation:
|
=demo.query("select
* from SCORES ")
|
Return a table
sequence:
（原文含示意图，图像资源未迁移）
|
=A1.new(CLASS,STUDENTID,SUBJECT,SCORE,cum(SCORE;CLASS,STUDENTID):F1)
|
Perform iterative operation in a loop function to get
cumulative total on SCORE values of records having same CLASS value and
STUDENTID value, use the results as values of F1 column and return a new
table sequence as follows:
（原文含示意图，图像资源未迁移）
|
=A1.new@z(CLASS,STUDENTID,SUBJECT,SCORE,cum(SCORE;CLASS,STUDENTID):F1)
|
Use @z option to
perform an inverse operation:
（原文含示意图，图像资源未迁移）
相关函数：
cs . new()
