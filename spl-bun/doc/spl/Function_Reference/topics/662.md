# ch.news()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.news()
=demo.cursor("select
=demo.query("select
=A2.group(GENDER;~:gup)
=A1.groupx(GENDER;avg(SALARY):avg)
=A4.join(GENDER,A3:GENDER,gup)
=channel(A5)
=A6.news(gup;EID,#2:Lname,GENDER,age(~.BIRTHDAY):Age,SALARY+50:
=A6.fetch()
=A5.fetch()
=A6.result()
=demo.cursor("select EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY from
=demo.query("select EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY from
=A2.group(GENDER;~:gup)
=A1.groupx(GENDER;avg(SALARY):avg)
=A4.join(GENDER,A3:GENDER,gup)
=channel(A5)
=A6.news@1(gup;EID,NAME,GENDER,age(~.BIRTHDAY):Age,Salary,avg:AvgSalary)
=A6.fetch()
=A5.fetch()
=A6.result()
```

## 提取内容
ch.news()
说明：
Generate
records according to the given condition, concatenate them into a new table
sequence and return it to the original channel.
语法：
ch. news ( X; x i : F i ,… )
注意：
The function attaches a
computation to channel ch , which will
compute expression x i
on record sequence X , use the results as values of the new field
F i to generate multiple records and form a
new table sequence, and returns the result to the original channel.
This is an attachment
computation.
参数：
|
ch
|
channel .
|
X
|
record sequence .
|
x i
|
Expression,
whose results will be field values; the sign ~ used in the parameter
references data from X instead of A . The sign # is used to represent a
field with a sequence number .
|
F i
|
Field
name in the given channel; will be automatically identified if the parameter
is omitted .
选项：
|
@1
|
Enable
a left join; when a record in the given record sequence is empty, create an
empty record for the new table sequence .
Return
value:
管道
示例：
|
=demo.cursor("select
EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY from EMPLOYEE")
|
Return
a cursor.
|
=demo.query("select
EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY from EMPLOYEE")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.group(GENDER;~:gup)
|
Group table sequence A2
by GENDER field and return the following result:
（原文含示意图，图像资源未迁移）
|
=A1.groupx(GENDER;avg(SALARY):avg)
|
Group table sequence A1
by GENDER field, compute average SALARY values in each group and return a
cursor. Below is the returned data:
（原文含示意图，图像资源未迁移）
|
=A4.join(GENDER,A3:GENDER,gup)
|
Attach a computation to
cursor A4, which will perform a foreign-key-style join with table A3, and
return the original cursor A4. Below is the returned data of A4 executing
A5’s compuation:
（原文含示意图，图像资源未迁移）
|
=channel(A5)
|
Create a channel and be
ready to push cursor A5’s data to the channel, but the push action needs to wait.
|
=A6.news(gup;EID,#2:Lname,GENDER,age(~.BIRTHDAY):Age,SALARY+50:
Salary,avg:AvgSalary)
|
Attach
a computation to channel A6, which will get values for gup table - #2 ： Lname means renaming the table’s second field Lname, and form a
table sequence consisiting of fields EID, Lname,GENDER, Age, Salary and AvgSalary,
and return the result to the original channel.
|
=A6.fetch()
|
Execute the result
fetching function in channel A6 and keep the current data in channel.
|
=A5.fetch()
|
Fetch data from cursor A5
while pushing data to channel A6 to execute the attached computation and keep
the result.
|
=A6.result()
|
Get channel A6’s result:
（原文含示意图，图像资源未迁移）
Use @1 option:
|
=demo.cursor("select EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY from
EMPLOYEE")
|
Return
a cursor.
|
=demo.query("select EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY from
EMPLOYEE where GENDER='M' ")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.group(GENDER;~:gup)
|
Group
table sequence A2 by GENDER field and return the following result:
（原文含示意图，图像资源未迁移）
|
=A1.groupx(GENDER;avg(SALARY):avg)
|
Group
table sequence A1 by GENDER field, compute average SALARY values in each
group and return a cursor. Below is the returned data:
（原文含示意图，图像资源未迁移）
|
=A4.join(GENDER,A3:GENDER,gup)
|
Attach
a computation to cursor A4, which will perform a foreign-key-style join with
table A3, and return the original cursor A4. Below is the returned data of A4
executing A5’s compuation:
（原文含示意图，图像资源未迁移）
|
=channel(A5)
|
Create
a channel and be ready to push cursor A5’s data to the channel, but the push
action needs to wait.
|
=A6.news@1(gup;EID,NAME,GENDER,age(~.BIRTHDAY):Age,Salary,avg:AvgSalary)
|
Attach
a computation to channel A6, which will get values for gup table - #2 ： Lname means renaming the table’s second field Lname, and form a
table sequence consisiting of fields EID, Lname,GENDER, Age, Salary and AvgSalary,
and return the result to the original channel.
Use
@1 option to perform a left join; create an empty record for the result table
sequence when a record in the record sequene is empty.
|
=A6.fetch()
|
Execute
the result fetching function in channel A6 and keep the current data in
channel.
|
=A5.fetch()
|
Fetch
data from cursor A5 while pushing data to channel A6 to execute the attached
computation and keep the result.
|
=A6.result()
|
Get
channel A6’s result:
（原文含示意图，图像资源未迁移）
