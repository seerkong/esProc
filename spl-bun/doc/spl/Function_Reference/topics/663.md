# cs.news()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.news()
=demo.cursor("select
=demo.query("select
=A2.group(GENDER;~:gup)
=A1.groupx(GENDER;avg(SALARY):avg)
=A4.join(GENDER,A3:GENDER,gup)
=A4.news(gup;EID,#2:Lname,GENDER,age(~.BIRTHDAY):Age,SALARY+50:
=A4.fetch()
=demo.cursor("select EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY
=demo.query("select
=A2.group(GENDER;~:gup)
=A1.groupx(GENDER;avg(SALARY):avg)
=A4.join(GENDER,A3:GENDER,gup)
=A4.news@1(gup;EID,NAME,GENDER,age(~.BIRTHDAY):Age,Salary,avg:AvgSalary)
=A4.fetch()
```

## 提取内容
cs.news()
说明：
Generate
multiple records based on a record sequence, concatenate them into a new table
sequence and return it to the original cursor.
语法：
cs. news ( X; x i : F i ,… )
注意：
The function attaches a
computation to cursor cs , which
computes expression x i on record sequence X ,
makes the results values of the new field F i , and generates multiple records to form a new table sequence, and
returns the table sequence to the original cursor cs .
This is a delayed function.
参数：
|
cs
|
cursor .
|
X
|
记录
sequence .
|
x i
|
An expression, whose values are uses as the
new field values. It is treated as null if omitted; in that case, : F i can’t be omitted. The sign # is used to
represent a field with a sequence number .
|
F i
|
Filed
name of the new cs
use the identifiers parsed from expression x i if it is omitted .
选项：
|
@1
|
Left
join, which creates an empty record when record sequence X is empty .
返回值：
游标
示例：
|
=demo.cursor("select
EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY from EMPLOYEE")
|
Return a cursor.
|
=demo.query("select
EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY from EMPLOYEE")
|
Return a table
sequence:
（原文含示意图，图像资源未迁移）
|
=A2.group(GENDER;~:gup)
|
Group table
sequence A2 by GENDER field and return result set as follows:
（原文含示意图，图像资源未迁移）
|
=A1.groupx(GENDER;avg(SALARY):avg)
|
Group cursor A1 by
GENDER field, compute average SALARY value in each group and return a cursor
below is data in the result cursor:
（原文含示意图，图像资源未迁移）
|
=A4.join(GENDER,A3:GENDER,gup)
|
Attach a
computation to cursor A4, which performs foreign key join with A3 and return
the original cursor A4; below is the data of cursor A4 where A5’s computation
is executed:
（原文含示意图，图像资源未迁移）
|
=A4.news(gup;EID,#2:Lname,GENDER,age(~.BIRTHDAY):Age,SALARY+50:
Salary,avg:AvgSalary)
|
Attach a computation to cursor A4 to compute gup
field values, renames the 2 nd field Lname using expression #2 ： Lname, form a table sequence
consisting of EID, Lname, GENDER, Age, Salary and AvgSalary to return cursor
A4, and returns the original cursor.
|
=A4.fetch()
|
Fetch data from
cursor A4 where A6’s computation is executed (it would be better to fetch
data in batches when a huge amount of data is involved):
（原文含示意图，图像资源未迁移）
When
@1 option works:
|
=demo.cursor("select EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY
from EMPLOYEE")
|
Return a cursor.
|
=demo.query("select
EID,NAME,DEPT,GENDER,BIRTHDAY,SALARY from EMPLOYEE where GENDER='M' ")
|
Return a table
sequence:
（原文含示意图，图像资源未迁移）
|
=A2.group(GENDER;~:gup)
|
Group table
sequence A2 by GENDER field and return result set as follows:
（原文含示意图，图像资源未迁移）
|
=A1.groupx(GENDER;avg(SALARY):avg)
|
Group cursor A1 by
GENDER field, compute average SALARY value in each group and return a cursor
below is data in the result curso
（原文含示意图，图像资源未迁移）
|
=A4.join(GENDER,A3:GENDER,gup)
|
Attach a
computation to cursor A4, which performs foreign key join with A3 and returns
the original cursor A4; below is data in cursor A4 where the attached
computation is executed:
（原文含示意图，图像资源未迁移）
|
=A4.news@1(gup;EID,NAME,GENDER,age(~.BIRTHDAY):Age,Salary,avg:AvgSalary)
|
Attach a computation to cursor A4 to compute gup
field values, form a table sequence consisting of EID, NAME, GENDER, Age, Salary
and AvgSalary to return cursor A4, and returns the original cursor. @1 option
enables left join to create an empty record when the corresponding record in
record sequence X is empty.
|
=A4.fetch()
|
Fetch data from
cursor A4 where A6’s computation is executed; below is the result set:
（原文含示意图，图像资源未迁移）
Related functions:
A . news ( X; x i : F i ,… )
