# T.news( A/cs,K,x:C,…;wi,... )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.news( A/cs,K,x:C,…;wi,... )
=connect("demo").cursor("SELECT
=file("scores-news.ctx")
=A2.create@y(#STUDENTID,#CLASS,SUBJECT,SCORE)
=A3.append@i(A1)
=connect("demo").query("SELECT ID,NAME,GENDER,AGE FROM
=A4.news(A5 :ID ,CLASS,STUDENTID,NAME,GENDER,SUBJECT,SCORE)
=A5.keys(ID)
=A4.news(A7,CLASS,STUDENTID,NAME,GENDER,SUBJECT,SCORE)
=A4.news@r(A 7 ,NAME,GENDER,sum(SCORE):TotalScore)
=A4.news(A7,CLASS,STUDENTID,NAME,GENDER,SUBJECT,SCORE;CLASS=="Class
=file("sco.ctx").open()
=file("stu.txt").import@t()
=A1.news(A2:Class:StudentID,NAME,SUBJECT,SCORE)
=A1.news@r(A2:Class:StudentID,NAME,sum(SCORE):TotalScore)
=demo.cursor("select STUDENTID,CLASS,SUBJECT,SCORE from
=file("scores.ctx")
=A2.create@y(#STUDENTID,#CLASS,SUBJECT,SCORE )
=A3.append@i(A1)
=demo.query("select
=A3.news(A5:ID,STUDENTID,NAME,SUBJECT,SCORE)
=create(Subject,Num).record(["PE",01,"Math",02,"History",03]).keys(Subject)
=A3.news(A5:ID,NAME,SUBJECT,SCORE;SUBJECT=A7.find(SUBJECT))
=A3.news(A5:ID,NAME,SUBJECT,SCORE;SUBJECT=A7.pfind(SUBJECT))
=A3.news(A5:ID,NAME,SUBJECT,SCORE;SUBJECT:A7)
=A3.news(A5:ID,NAME,SCORE;SUBJECT:A7)
=A3.news(A5:ID,NAME,SUBJECT,SCORE;SUBJECT:A7:null)
=create(Score,Level).record([90,"A",80,"B",70,"C",60,"D"]).keys(Score)
=A3.news(A5:ID,NAME,SUBJECT,SCORE;(SCORE=A13.find(SCORE),SUBJECT=A7.find(SUBJECT),SCORE!=null&&SUBJECT!=null))
```

## 提取内容
T.news( A/cs,K,x:C,…;wi,... )
说明：
Return
a table sequence/cursor consisting of the specified fields according to the
correspondence between the table sequence/cursor’s key and the corresponding
field in the composite table.
语法：
T .news( A/cs:K,x:C,…;wi,... )
注意：
Composite
table T is the subtable and table sequence/cursor A/cs is the
primary table. They have a many-to-one relationship. The function matches A/cs ’s
key (/dimension) field with the corresponding fields of T (begin the correspondence from the first field) and returns a
table sequence/cursor made up of x:C fields. It is required that A/cs be ordered by the key (/dimension)
and the key (/dimension) has the same order as T ’s first field.
With a
table sequence parameter, the function returns a table sequence, and with a
cursor parameter, it returns a cursor or a multicursor.
Align
the returned result set to T and set
key/dimension for it; copy records of the primary table.
参数：
|
T
|
composite table .
|
A/cs
|
table sequence/cursor/composite table cursor .
|
K
|
A / cs ’s field name
when it is specified, use it to match with T ’s first field; when it
is absent, use A / cs ’s key (/dimension) to perform the matching; when there are multiple K s,
use colon (:) to separate them .
|
x
|
Field
names or aggregate function count/sum/max/min/avg .
|
Column
alias .
|
wi
|
Filtering
condition on T ; retrieve the whole
set when this parameter is absent; separate multiple conditions by comma(s) and
their relationships are AND. Besides regular filtering expressions, you can
also use the following five types of syntax in a filtering condition, where K
is a non-key field in entity table T :
1 ． K = w
w usually uses expression T i .find( K )
or T i .pfind( K ), where T i is a
table sequence. When value of w is null or false, the corresponding record in
the entity table will be filtered away; when w is expression T i .find( K )
and the to-be-selected fields C,... contain K , T i ’ s referencing field will be assigned to K ; when w is
expression T i .pfind( K ) and the to-be-selected fields
C,... contain K , sequence numbers of K values in T i
will be assigned to K .
2 ． ( K 1 = w 1 , … K i = w i , w )
K i = w i is an
assignment expression. Generally, parameter w i can use
expression T i .find( K i ) or T i .pfind( K ),
where T i is a table sequence; when w i is
expression T i .find( K i ) and the
to-be-selected fields C,... contain K i , T i ’s
referencing field will be assigned to K i correspondingly
when w i is expression T i .pfind( K i ) and the to-be-selected fields C,...
contain K i , sequence numbers of K i values in T i will be assigned to
K i .
w is a filter expression; you can
reference K i in w .
3 ． K : T i
T i is a table sequence.
Compare K i value in the entity table with key values of T i
and discard records whose K i value does not match; when the
to-be-selected fields C,... contain K , T i ’s
referencing field will be assigned to K .
4 ． K : T i :null
Filter
away all records that satisfy K : T i .
选项：
|
@r
|
composite table .
返回值：
Table
sequence/游标
示例：
|
=connect("demo").cursor("SELECT
STUDENTID,CLASS,SUBJECT,SCORE FROM SCORES")
|
Return a cursor .
|
=file("scores-news.ctx")
|
=A2.create@y(#STUDENTID,#CLASS,SUBJECT,SCORE)
|
Create
a composite table and set STUDENTID and CLASS as its dimension .
|
=A3.append@i(A1)
|
（原文含示意图，图像资源未迁移）
|
=connect("demo").query("SELECT ID,NAME,GENDER,AGE FROM
STUDENTS")
|
Return
a table sequence .
（原文含示意图，图像资源未迁移）
|
=A4.news(A5 :ID ,CLASS,STUDENTID,NAME,GENDER,SUBJECT,SCORE)
|
A4 is the subtable and A5 is
the primary table; perform the matching according to table sequence A5’s ID
field and T’s first key field STUDENTID and return a table sequence
consisting of CLASS, STUDENTID, NAME, GENDER, SUBJECT and SCORE fields; copy
records of the primary table and return
the following result:
（原文含示意图，图像资源未迁移）
|
=A5.keys(ID)
|
Set ID as table sequence
A5’s key.
（原文含示意图，图像资源未迁移）
|
=A4.news(A7,CLASS,STUDENTID,NAME,GENDER,SUBJECT,SCORE)
|
As parameter K is
absent, perform the matching according to A7’s key and the composite table’s
first key field and return same result as A6.
|
=A4.news@r(A 7 ,NAME,GENDER,sum(SCORE):TotalScore)
|
Use @r option to summarize
data in the subtable.
（原文含示意图，图像资源未迁移）
|
=A4.news(A7,CLASS,STUDENTID,NAME,GENDER,SUBJECT,SCORE;CLASS=="Class
one",SUBJECT=="Math")
|
Perform filtering on A4’s data according to
filter condition: CLASS=="Class one" and SUBJECT=="Math" during association .
（原文含示意图，图像资源未迁移）
When
there are multiple K parameters:
|
=file("sco.ctx").open()
|
Open a composite table as
follows:
（原文含示意图，图像资源未迁移）
|
=file("stu.txt").import@t()
|
Return a table sequence:
（原文含示意图，图像资源未迁移）
|
=A1.news(A2:Class:StudentID,NAME,SUBJECT,SCORE)
|
A1 is the subtable and A2 is
the primary table; perform the matching between table sequence A2’s Class and
StudentID fields and T ’s first two fields – Class and StudentID – and
return a table sequence consisting of NAME,SUBJECT,SCORE; copy records of the
primary table and return the following result:
（原文含示意图，图像资源未迁移）
|
=A1.news@r(A2:Class:StudentID,NAME,sum(SCORE):TotalScore)
|
Use @r option to summarize
data in the subtable.
（原文含示意图，图像资源未迁移）
Use special types of
filtering conditions:
|
=demo.cursor("select STUDENTID,CLASS,SUBJECT,SCORE from
SCORES")
|
Below is content of
the cursor:
（原文含示意图，图像资源未迁移）
|
=file("scores.ctx")
|
=A2.create@y(#STUDENTID,#CLASS,SUBJECT,SCORE )
|
Create a composite
table .
|
=A3.append@i(A1)
|
Append cursor A1’s
data to the composite table’s base table .
|
=demo.query("select
ID,NAME,GENDER,AGE from
STUDENTS")
|
Return a table
sequence :
（原文含示意图，图像资源未迁移）
|
=A3.news(A5:ID,STUDENTID,NAME,SUBJECT,SCORE)
|
Match the composite
table’s key values to table sequence’s fields and retrieve corresponding
composite table fields and return a table sequence:
（原文含示意图，图像资源未迁移）
|
=create(Subject,Num).record(["PE",01,"Math",02,"History",03]).keys(Subject)
|
Generate a table
sequence using Subject as the key:
（原文含示意图，图像资源未迁移）
|
=A3.news(A5:ID,NAME,SUBJECT,SCORE;SUBJECT=A7.find(SUBJECT))
|
Use K = w
filtering mode; in this case w is T i .find( K ) and entity table records making SUBJECT=A7.find(SUBJECT) get
null or false are discarded; SUBJECT is the selected field, to which table
sequence A7’s referencing field is assigned .
（原文含示意图，图像资源未迁移）
|
=A3.news(A5:ID,NAME,SUBJECT,SCORE;SUBJECT=A7.pfind(SUBJECT))
|
Use K = w
filtering mode; in this case w is T i .pfind( K ) and entity table records making SUBJECT=A7.pfind(SUBJECT) get
null or false are discarded; SUBJECT is the selected field, to which its
sequence numbers in table sequence A7 are assigned .
（原文含示意图，图像资源未迁移）
|
=A3.news(A5:ID,NAME,SUBJECT,SCORE;SUBJECT:A7)
|
Use K : T i filtering mode; compare the
entity table’s SUBJECT values
with the table sequence’s key values and discard entity table records that
cannot match .
（原文含示意图，图像资源未迁移）
|
=A3.news(A5:ID,NAME,SCORE;SUBJECT:A7)
|
This is a case
where K isn’t selected; SUBJECT
isn’t the selected field, so only filtering is performed .
（原文含示意图，图像资源未迁移）
|
=A3.news(A5:ID,NAME,SUBJECT,SCORE;SUBJECT:A7:null)
|
Use K : T i :null filtering
mode; compare the entity table’s SUBJECT values with the table sequence’s key
values and discard entity table records that can match .
（原文含示意图，图像资源未迁移）
|
=create(Score,Level).record([90,"A",80,"B",70,"C",60,"D"]).keys(Score)
|
Return a table
sequence using Score as the key:
（原文含示意图，图像资源未迁移）
|
=A3.news(A5:ID,NAME,SUBJECT,SCORE;(SCORE=A13.find(SCORE),SUBJECT=A7.find(SUBJECT),SCORE!=null&&SUBJECT!=null))
|
Use ( K 1 = w 1 , … K i = w i , w ) filtring mode; return records that
meet all conditions .
（原文含示意图，图像资源未迁移）
注意：
The
difference between T.new() and T.news() is that the former works when T
and A/cs has a one-to-many relationship and the latter operates when T
and A/cs has a many-to-one relationship.
