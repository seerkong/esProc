# next{a}

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=[]
>A1=A1|[A2]
=demo.query("select
=create(ID,Dept)
>A8.insert(0,A9.EID:
```

## 提取内容
next{a}
说明：
Skip
the current loop and continue with the next loop.
语法：
next { a }
注意：
next is used to skip the remaining code block in the current loop and
execute the next loop as long as the cycling does not finish.
参数：
|
a
|
The
master cell of the loop code block. If the master cell a is omitted, then it is the current loop that is running .
示例：
|
D
|
=[]
|
[1,1,3,3].
|
for 5
|
In
this example, the next A2 and the break A2 are to control loop of the A2 level. The computation of A1 is the sequence [1, 1, 3, 3] .
|
for 2
|
if A2==2
|
next A2
|
if A2==4
|
break A2
|
>A1=A1|[A2]
|
=demo.query("select
* from EMPLOYEE")
|
=create(ID,Dept)
|
（原文含示意图，图像资源未迁移）
|
for A7
|
Extract
records whose DEPT values are not " Sales " from the EMPLOYEE table .
|
if A9.
DEPT=="Sales"
|
>A8.insert(0,A9.EID:
ID,A9. DEPT:Dept)
|
A8 returns result
as follows:
（原文含示意图，图像资源未迁移）
注意：
1.By
comparison, the break is to jump out of the cycling directly, while next
is to ignore the remaining code block in the current loop, and proceeds to the
next loop as long as the cycling does not finish.
2.If
a is omitted, then the current loop
will be under control. To control the loop at higher level, a is required.
