# P.nodes()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.nodes()
=file("D://emp.txt").import@t().keys(empid)
>A1.switch(mgrid,A1:empid)
=A1.nodes(mgrid,A1.select@1(name=="Diana"),5)
=A1.nodes@d(mgrid,A1.select@1(name=="Diana"),5)
=A1.nodes@p(mgrid,A1.select@1(name=="Diana"),5)
```

## 提取内容
P.nodes()
说明：
Find
records in a recursive way where the foreign key values reference a specified
record.
语法：
P . nodes ( F , r , n )
注意：
r . The function finds all records from record sequence P where foreigh key F references record r ,
the root node, in a recursive way. The resulting records are record r ’s child nodes. If these child nodes
don’t have any child nodes, that is, their foreign key values don’t reference
them, they are called the root node’s leaf nodes; otherwise, they are called
the subtrees.
If the specified record r
can’t be found, the function will return NULL. Parameter n is the maximum recursion depth, whose default is 1,000.
参数：
|
P
|
record sequence .
|
F
|
Field
name .
|
r
|
A record.
|
n
|
A number.
选项：
|
@d
|
Make
the function return leaf-level nodes .
|
@p
|
Make
the function return all child nodes and their level relative to the root node .
返回值：
Sequence
示例：
|
=file("D://emp.txt").import@t().keys(empid)
|
（原文含示意图，图像资源未迁移）
|
>A1.switch(mgrid,A1:empid)
|
=A1.nodes(mgrid,A1.select@1(name=="Diana"),5)
|
（原文含示意图，图像资源未迁移）
According to mgrid field, the foreign key
references, search for all records of Diana’s subordinates and those of the
subordinates of Diana’s subordinates .
|
=A1.nodes@d(mgrid,A1.select@1(name=="Diana"),5)
|
（原文含示意图，图像资源未迁移）
Search for the records of Diana’s
subordinates who are not a manager .
|
=A1.nodes@p(mgrid,A1.select@1(name=="Diana"),5)
|
（原文含示意图，图像资源未迁移）
Display a relationship structure with Diana
as the root, from which you see that Diana is the manager of empid = 7, who is the manager
of empid = 14 .
