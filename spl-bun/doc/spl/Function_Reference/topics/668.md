# x.nodes@r(Fi,...)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
x.nodes@r(Fi,...)
x.nodes@r(Fi,...)
=rand(10)
=A1.nodes@r()
=demo.query("select * from DEPARTMENT").keys(DEPT)
=demo.query("select
=demo.query("select
>A3.switch(DEPT,A1:DEPT)
>A3.switch(STATE,A2:NAME)
=A3.select@1(NAME=="Ashley")
=A6.nodes@r(EID,NAME)
=A6.nodes@r(DEPT,STATE)
=A6.nodes@r(DEPT)
=A3.nodes@r()
```

## 提取内容
x.nodes@r(Fi,...)
说明：
Expand
the referencing field(s) through multilevel of recursion.
语法：
x.nodes@r(Fi,...)
注意：
The function expands the
referencing fields in parameter x through multilevel of recursion.
When
parameter x is a single value, return an empty sequence
When
parameter x is a record and if the specified Fi,… fields all have
single values, return a sequence consisting of x ; otherwise return
x.Fi.nodes@r(Fi,... )|...
When
parameter x is a sequence or a record sequence, return
x.conj(~.nodes@r(Fi,...)).
When parameter Fi is absent, traverse all fields of x .
参数：
|
x
|
single value/a sequence/a record .
|
Fi
|
The
field name .
Return
value:
Sequence
示例：
When
parameter x is a single value:
|
=rand(10)
|
Return
random values within 10
|
=A1.nodes@r()
|
Return
[] .
（原文含示意图，图像资源未迁移）
When
parameter x is a single record or a sequence:
|
=demo.query("select * from DEPARTMENT").keys(DEPT)
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=demo.query("select
NAME,ABBR from STATES").keys(NAME)
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=demo.query("select
top 10 EID,NAME,DEPT,STATE from EMPLOYEE")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
>A3.switch(DEPT,A1:DEPT)
|
Convert
A3’s DEPT field into the referencing field according to the corresponding
value of A1’s key field DEPT .
|
>A3.switch(STATE,A2:NAME)
|
Convert
A3’s STATE field into the referencing field according to the corresponding
value of A2’s key field NAME; After A4 and A5 are successfully executed, A3’s
sequence becomes this:
（原文含示意图，图像资源未迁移）
|
=A3.select@1(NAME=="Ashley")
|
Get
the first record where NAME value is Ashley from A3:
（原文含示意图，图像资源未迁移）
|
=A6.nodes@r(EID,NAME)
|
Get
records where both EID field and NAME field are single values and return them
as a sequence .
（原文含示意图，图像资源未迁移）
|
=A6.nodes@r(DEPT,STATE)
|
Get
records where both DEPT field and STATE field are single values and return
them as a sequence:
（原文含示意图，图像资源未迁移）
|
=A6.nodes@r(DEPT)
|
Get
the record where DEPT field is the single value and return it as a sequence:
（原文含示意图，图像资源未迁移）
|
=A3.nodes@r()
|
As
parameter Fi is absent, traverse all fields of A3 to get records where
all fields are single values and return them as a sequence:
（原文含示意图，图像资源未迁移）
