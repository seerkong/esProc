# norminv()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
norminv()
norminv ( p , mu , sigma )
=norminv(0.25,
```

## 提取内容
norminv()
说明：
Compute the inverse normal cumulative distribution function.
语法：
norminv ( p , mu , sigma )
注意：
The MathCli external library function (See
External Library Guide ) computes the inverse normal cumulative
distribution.
参数：
|
P
|
Probability, within the interval (0,1) .
|
mu
|
The mean value .
|
sigma
|
The standard deviation, which is a positive number .
Return
value:
Double-precision number
示例：
|
=norminv(0.25,
2, 1)
|
（原文含示意图，图像资源未迁移）
