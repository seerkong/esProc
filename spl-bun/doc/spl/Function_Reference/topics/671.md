# not()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
not()
not( x )
not(6)
not(decimal(6))
```

## 提取内容
not()
说明：
On
integers, perform bitwise NOT operation to get the logical negation on each bit.
语法：
not( x )
注意：
The
function performs bitwise NOT operation to get the logical negation on each bit
of an integer. It supports a decimal without the fractional part.
参数：
|
x
|
The
numeric expression for which you want to find the logical negation on each
bit .
返回值：
Integer
示例：
|
not(6)
|
-7
|
not(decimal(6))
|
-7, which is decimal type.
