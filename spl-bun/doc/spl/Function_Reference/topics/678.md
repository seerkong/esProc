# P.numnorm()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.numnorm()
P.numnorm(cn)
P.numnorm@r(cn, Rec)
=T("D://house_prices_train.csv")
=T("D://house_prices_test.csv")
=A1.numnorm("LotArea")
=A2.numnorm@r("LotArea",A3(2))
```

## 提取内容
P.numnorm()
说明：
Normalize a table sequence/record sequence
numeric variable during modeling.
语法：
|
P.numnorm(cn)
|
During modeling, normalize numeric variable cn
and return a binary sequence containing the normalization result and normalization
process records Rec .
|
P.numnorm@r(cn, Rec)
|
During scoring, normalize numeric variable cn
according to normalization process records Rec .
注意：
The MathCli external
library function (See External Library Guide ).
The function normalizes a
table sequence/record sequence numeric variable during modeling.
参数：
|
P
|
A table sequence/record sequence .
|
cn
|
A string/number, which is the name of column (or the
column number starting from 1) – the to-be-pre-processed variable – in a
table sequence or record sequence
|
Rec
|
A sequence of normalization process records .
选项：
|
@c
|
Modify the original data to the
normalization result after execution .
返回值：
Sequence
示例：
|
=T("D://house_prices_train.csv")
|
Import modeling data .
|
=T("D://house_prices_test.csv")
|
Import scoring data .
|
=A1.numnorm("LotArea")
|
（原文含示意图，图像资源未迁移）
Normalize variable “LotArea”:
A3(1) Normalization result
A3(2) Normalization process records Rec .
|
=A2.numnorm@r("LotArea",A3(2))
|
（原文含示意图，图像资源未迁移）
Normalize variable “LotArea” on scoring data in the same rule according
to A3’s normalization process records Rec .
