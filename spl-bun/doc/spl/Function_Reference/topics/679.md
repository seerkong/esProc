# nvl()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
nvl()
nvl ( x 1 ,…, x n )
=[1,2,4].nvl()
=[null,2,3,4].nvl()
=[null,"",2,3,4].nvl()
=nvl(null,"",2,3,4)
=create(ID,Str).record([1,"",2,,3,"aaa"])
=A3.nvl(Str)
```

## 提取内容
nvl()
说明：
Get
the first non-null and non-empty-string member from a sequence.
语法：
|
A .nvl( x )
|
nvl ( x 1 ,…, x n )
|
Equivalent to A .nvl() , where x 1 ,…, x n are members of sequence A .
注意：
The
function gets the first non-null and non-empty-string member from sequence A . When parameter x is present,
first compute the expression with members of sequence A and then return the eligible member.
参数：
|
sequence .
|
x
|
An
expression; cannot be omitted when A
is a table sequence or a record sequence .
Return
value:
non-null and non-empty-string member
示例：
When
A is a sequence:
|
=[1,2,4].nvl()
|
=[null,2,3,4].nvl()
|
=[null,"",2,3,4].nvl()
|
=nvl(null,"",2,3,4)
|
When
A is a table
sequence or a record sequence:
|
=create(ID,Str).record([1,"",2,,3,"aaa"])
|
（原文含示意图，图像资源未迁移）
|
=A3.nvl(Str)
|
Return " aaa " .
