# A.o()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.o()
=["a","c","d","e","f"].i()
=A1.o()
=[1,2].i()
=[3].o()
=A3&A4
```

## 提取内容
A.o()
说明：
Convert
a sequence to an ordinary one.
语法：
A. o ( )
注意：
The
function converts sequence A , which can be an ordinary sequence or a
pure sequence to an ordinary sequence. When the computation involves an
ordinary sequence or when it is expected that the result of the computation is
not a pure sequence, sequence A will be automatically converted to an
ordinary sequence.
Only
supported by esProc Enterprise Edition.
参数：
|
sequence .
Return
value:
Sequence
示例：
|
=["a","c","d","e","f"].i()
|
Return
a pure sequence .
|
=A1.o()
|
Convert
A1’s pure sequence to an ordinary sequence .
|
=[1,2].i()
|
Return
a pure sequence .
|
=[3].o()
|
Return
a pure sequence .
|
=A3&A4
|
Return
an ordinary sequence [1,2,"3"] .
