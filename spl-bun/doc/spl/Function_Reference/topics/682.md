# P.o()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.o()
=demo.query("select
=A1.i()
=A2.o()
```

## 提取内容
P.o()
说明：
Convert
a pure table sequence to an ordinary sequence.
语法：
P . o ( )
注意：
The
function converts pure table sequence P to an ordinary sequence. Only
supported by esProc Enterprise Edition.
参数：
|
P
|
pure table sequence .
Return
value:
Table
sequence
示例：
|
=demo.query("select
EID,NAME,GENDER,DEPT,BIRTHDAY from employee")
|
Return
a table sequence where all fields are pure ones .
（原文含示意图，图像资源未迁移）
|
=A1.i()
|
Convert
A1’s table sequence to a pure one.
|
=A2.o()
|
Convert
A2’s pure table sequence to an ordinary table sequence .
