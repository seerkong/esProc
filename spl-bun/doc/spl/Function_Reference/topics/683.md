# olap_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
olap_close()
olap_close( conn )
=olap_open("http://192.168.0.178:8088/msmdpump.dll","CubeTest","Administrator","admin")
>olap_close(A1)
```

## 提取内容
olap_close()
说明：
Close
connection to a multidimensional database.
语法：
olap_close( conn )
注意：
This
Olap4jCli external library function (See
External Library Guide ) closes the connection to a multidimensional
database.
参数：
|
conn
|
Database
connection string .
示例：
|
=olap_open("http://192.168.0.178:8088/msmdpump.dll","CubeTest","Administrator","admin")
|
>olap_close(A1)
|
Close
connection to the multidimensional database.
function:
olap_ o pen()
olap _ q u ery()
