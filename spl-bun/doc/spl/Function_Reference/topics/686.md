# ones()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ones()
ones ( n 1 ,n 2 ,…)
=ones(4,3)
=ones(3)
=ones(4,3,5)
```

## 提取内容
ones()
说明：
Create a multidimensional matrix where all the elements are1.
语法：
ones ( n 1 ,n 2 ,…)
注意：
A MathCli external library function
(See External Library Guide ) where n i
defines sizes of dimensions in order. The function creates a new n * n
square matrix when there is only one parameter n .
参数：
|
n i
|
A positive integer .
Return
value:
Sequence
示例：
|
=ones(4,3)
|
（原文含示意图，图像资源未迁移）
Generate
a 4*3 matrix whose elements are all 1 .
|
=ones(3)
|
（原文含示意图，图像资源未迁移）
Generate
a matrix of size 3 whose elements are all 1 .
|
=ones(4,3,5)
|
（原文含示意图，图像资源未迁移）
Generate
a multidimensional matrix whose elements are all 1 .
