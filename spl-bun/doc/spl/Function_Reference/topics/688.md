# f.open()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
f.open()
=file("1.emp.ctx")
=A1.open()
=file("emp.ctx":[1,2])
=A3.open()
```

## 提取内容
f.open()
说明：
Open
a composite table’s base table.
语法：
f .open()
注意：
The function opens composite table f ’s
base table, or a multizone composite table when f is a homo-name files
group. Only supported by esProc Enterprise Edition.
参数：
|
f
|
composite table file object or a homo-name files group object .
Return
value:
Composite
table’s base table/multizone composite table
示例：
|
=file("1.emp.ctx")
|
=A1.open()
|
Open
base table of composite table 1.emp.ctx .
|
=file("emp.ctx":[1,2])
|
=A3.open()
|
Open
a multizone composite table .
