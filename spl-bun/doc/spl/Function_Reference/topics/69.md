# A(i)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A(i)
A ( i )
=[1,2,4]
=A1(3)
=["a","b"](1)
=file("emp.txt").cursor@w().fetch()
=A1.(~2)
=A1.([~(1),~2])
=A1.groupc(~(3);[(~1),(~2)])
A ( p )
```

## 提取内容
A(i)
说明：
Get
a member from a sequence.
语法：
A ( i )
注意：
Get
the i th
member from the sequence A .
参数：
|
sequence.
|
i
|
member’s sequence number expression, which starts from 1.
返回值：
Values
of members in a sequence
示例：
|
=[1,2,4]
|
=A1(3)
|
=["a","b"](1)
|
"a"
Get
one or more child members from member sequences of a sequence:
|
=file("emp.txt").cursor@w().fetch()
|
Return
a sequence of sequences:
（原文含示意图，图像资源未迁移）
|
=A1.(~2)
|
Get
the 2 nd member of each member sequence of A1’s sequence:
（原文含示意图，图像资源未迁移）
|
=A1.([~(1),~2])
|
Get
the 1 st and 2 nd members of each member sequence of A1’s
sequence:
（原文含示意图，图像资源未迁移）
|
=A1.groupc(~(3);[(~1),(~2)])
|
Perform
computations on A1 and return the following result:
（原文含示意图，图像资源未迁移）
Related functions:
A ( p )
