# or()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
or()
or( x i,… )
or( A )
or(3,5)
or([11,13,15])
or(decimal(6),11)
or(decimal(6),decimal(11))
```

## 提取内容
or()
说明：
Perform
bitwise OR operation on integers.
语法：
or( x i,… )
or( A )
注意：
Bitwise
OR operation on integers. The function supports a decimal without the
fractional part.
参数：
|
sequence .
|
x i
|
numeric expression based on which you perform the bitwise OR operation .
返回值：
Integer
示例：
|
or(3,5)
|
7 .
|
or([11,13,15])
|
15 .
|
or(decimal(6),11)
|
15, which is the decimal type .
|
or(decimal(6),decimal(11))
|
15, which is the decimal type .
