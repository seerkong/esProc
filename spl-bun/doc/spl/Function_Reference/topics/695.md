# oss_file()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
oss_file()
oss_file( fd,bucket,key[:cs] )
=oss_open("LTAI5tRgzSpjU73Wt","4dNAtbcjF6arKp26JGaytM","http://oss-cn-beijing.aliyuncs.com")
= oss _file(A1,
=A2.name()
=A2.import@t()
=A2.cursor@t().fetch(10)
> oss _close(A1)
```

## 提取内容
oss_file()
说明：
Get
the specified object in an OSS bucket.
语法：
oss_file( fd,bucket,key[:cs] )
注意：
OssCli
external library function (See External
Library Guide ).
The
returned file object supports file handling functions such as f.exists(),
f.name(), f.size(), f.date(), f.read(), f.import() and f.export().
参数：
|
fd
|
OSS
connection object .
|
bucket
|
Bucket
name .
|
key
|
File
name in the storage bucket .
|
cs
|
Encoding
name; use the encoding to save or open the file .
Return
value:
File
object
示例：
|
=oss_open("LTAI5tRgzSpjU73Wt","4dNAtbcjF6arKp26JGaytM","http://oss-cn-beijing.aliyuncs.com")
|
= oss _file(A1,
" unbucket101 ","emp.txt")
|
Load
emp.txt in storage bucket unbucket101.
|
=A2.name()
|
Get
the full path containing the file name.
|
=A2.import@t()
|
Return
the file data as a table sequence.
|
=A2.cursor@t().fetch(10)
|
Retrieve
data in the way of cursor.
|
> oss _close(A1)
|
Close the OSS connection.
