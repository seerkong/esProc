# A.pad()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.pad()
=A1.pad(["q",2],2)
=A1.pad("j",9)
=A1.pad@m("j",3)
=A1.pad@m("j",5)
=A1.pad@l(["q",2],11)
```

## 提取内容
A.pad()
说明：
Add
members of a sequence to another one until the latter reaches certain length.
语法：
A. pad ( x,n )
注意：
The
function adds members of sequence x
sequentially and continuously to sequence A
until the length of A reaches n . If n is less than A ’s
length, the function will return A
itself.
参数：
|
sequence .
|
x
|
single value or a sequence; with a sequence, add its members sequentially to
sequence A .
|
n
|
An
integer .
选项：
|
@l
|
Add
members before the existing members of sequence A ; by default the additions will be placed at its end .
|
@m
|
If the length of
sequence A is
the multiple of n, do not perform the adding action. If the length of A
isn’t the multiple of n, add members to A until its length is the
multiple of n; the number of additions – which are members of sequence x
– need to be added after the existing members can be calculated with the
formula – m=n-A.len()%n .
Return
value:
Sequence
示例：
|
[a,b,c,d,e,f]
|
=A1.pad(["q",2],2)
|
["a","b","c","d","e",
"f"] .
|
=A1.pad("j",9)
|
["a","b","c","d","e",
"f","j","j","j"] .
|
=A1.pad@m("j",3)
|
["a","b","c","d","e",
"f"] .
|
=A1.pad@m("j",5)
|
["a","b","c","d","e",
"f" ,"j","j","j","j" ] .
|
=A1.pad@l(["q",2],11)
|
["q",2,
"q",2, "q","a","b","c","d","e",
"f"] .
