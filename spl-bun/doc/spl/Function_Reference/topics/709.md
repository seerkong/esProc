# pca()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
pca()
pca( A,n )
pca( A,F )
=pca(A1,2)
A2(1)
A2(2)
=pca(A1,A2)
=pca@r(A1,2)
=pca(A5,A2)
```

## 提取内容
pca()
说明：
Perform PCA on a matrix and return data for dimensionality
reduction.
语法：
|
pca( A,n )
|
Perform PCA (Principal Component Analysis) on matrix A and return data for dimensionality reduction; n is the number of principal
components .
|
pca( A,F )
|
Reduce dimensionality for another matrix having same
number of columns .
注意：
External library function (See External Library Guide ) .
The function performs PCA on a matrix and return data for
dimensionality reduction.
参数：
|
A matrix .
|
n
|
Number of principal components .
|
F
|
The PCA fitting object returned by pca( A,n ) .
选项：
|
@r
|
Enable returning the result of dimensionality reduction
on matrix A , which is a matrix of n columns, directly .
返回值：
PCA fitting object/Sequence
示例：
|
[[1,2,3,4],[2,3,1,2],[1,1,1,-1],[1,0,-2,-6]]
|
=pca(A1,2)
|
（原文含示意图，图像资源未迁移）
Return
data for dimensionality reduction:
A2(1)
Averages of columns
A2(2)
Variances of principal components ordered descendingly .
A2(3) Coefficients of principal components .
|
=pca(A1,A2)
|
（原文含示意图，图像资源未迁移）
Execute dimensionality reduction on A1 .
|
=pca@r(A1,2)
|
（原文含示意图，图像资源未迁移）
Use @r option to reduce A1’s dimensionality
directly; result is the same as A3’s .
|
[[4,6,2,4],[2,3,1,2],[1,1,1,-1]]
|
=pca(A5,A2)
|
（原文含示意图，图像资源未迁移）
Execute
dimensionality reduction on another matrix having same number of columns as
the original matrix .
