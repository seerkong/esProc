# A.( x,... )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.( x,... )
=[1,2,3].(~*~)
=demo.query("select
=A2.(EID)
=A2.(age(BIRTHDAY))
=A2.()
=demo.query("select top 10 EID,NAME,GENDER from EMPLOYEE
=A6.(seq(GENDER))
=A6.@z(seq(GENDER))
=[1,2,3].(~=iterate(~~+~),~*10)
```

## 提取内容
A.( x,... )
说明：
Compute
an expression with each member of a sequence.
语法：
|
A .( x,… )
|
A .()
|
Return
A itself.
注意：
The
function computes expression x,… over each member in sequence or table sequence A and returns result of the last of expression x . Use " ~ "
to reference the current member in A .
参数：
|
sequence/a record sequence.
|
x
|
An
expression, which is generally a field name or a legal expression composed of
field names and in which " ~ " is used to reference the current record.
选项：
|
@m
|
Enable parallel processing
where the order of task computations is not fixed .
|
@z
|
Perform an inverse operation
only apply to non-pure sequences .
返回值：
Sequence
示例：
|
=[1,2,3].(~*~)
|
Return [1,4,9]; the
tilde ~ means referencing the current member .
|
=demo.query("select
EID,NAME,BIRTHDAY from EMPLOYEE where EID<10")
|
Return a table sequence .
（原文含示意图，图像资源未迁移）
|
=A2.(EID)
|
Return EID column of A2’s sequence .
（原文含示意图，图像资源未迁移）
|
=A2.(age(BIRTHDAY))
|
Calculate ages according to BIRTHDAY .
（原文含示意图，图像资源未迁移）
|
=A2.()
|
Return A2’s sequence .
（原文含示意图，图像资源未迁移）
|
=demo.query("select top 10 EID,NAME,GENDER from EMPLOYEE
")
|
（原文含示意图，图像资源未迁移）
|
=A6.(seq(GENDER))
|
Number members
having same GENDER value in A6’s sequence .
（原文含示意图，图像资源未迁移）
|
=A6.@z(seq(GENDER))
|
Use @z option to
perform an inverse operation .
（原文含示意图，图像资源未迁移）
|
=[1,2,3].(~=iterate(~~+~),~*10)
|
As there are multiple
expressions, return result of the last expression, which is [10, 30,60] .
