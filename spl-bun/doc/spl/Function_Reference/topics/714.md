# E. penum()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=["?<=70&&?>=60","?>=70 &&
=A1.penum(90)
=A1.penum@r(90)
=A1.penum@n(43)
```

## 提取内容
E. penum()
说明：
Judge
to which enum group an object belongs.
语法：
E . penum ( y )
注意：
The
functions judges to which group of sequence/record sequence E member y belongs, and
returns the sequence number(s) of the group(s), i.e. sequence number(s) of the
record(s) of E that satisfy the
grouping condition.
参数：
|
E
|
sequence/record sequence.
|
y
|
A member
allowed to be omitted.
选项：
|
@r
|
Return
the sequence numbers of all the groups satisfying the grouping condition, and
the default is to return the sequence number of the first group.
|
@n
|
If
no eligible sequence member of E is
found, return the length of E plus
1.
Function k eyword:
|
?
|
Represents
the value of y .
返回值：
Sequence/Integer
sequence
示例：
Ø
E is the sequence
|
=["?<=70&&?>=60","?>=70 &&
?<=90","?>=90"]
|
Use
y to replace ? in the grouping conditions when
computing.
|
=A1.penum(90)
|
2 ; the sequence
number of the first group satisfying the condition will be returned.
|
=A1.penum@r(90)
|
[2,3] ; return the
sequence numbers of all the groups.
|
=A1.penum@n(43)
|
4 .
相关函数：
P .enum ( )
