# permut()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
permut()
permut ( n ,k )
permut(5,4)
```

## 提取内容
permut()
说明：
Return
the number of permutations.
语法：
permut ( n ,k )
注意：
The
number of ways of rearranging the k
elements picked from a set of n
different objects.
参数：
|
n
|
An integer that is the number of the
objects .
|
k
|
An integer that is the number of the
objects in each permutation .
返回值：
The
number of permutations
示例：
|
permut(5,4)
|
