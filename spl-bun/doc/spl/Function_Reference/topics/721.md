# A.pivot( g:G,…;F,V;N i :N' i ,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.pivot( g:G,…;F,V;N i :N' i ,… )
=demo.query("select
=A1.pivot(CLASS:ClassNew,STUDENTID:StudentID;SUBJECT,SCORE;"English":"new_English","Math","PE")
=A1.pivot(CLASS,STUDENTID;SUBJECT,SCORE;"English","Math")
=A1.pivot(CLASS,STUDENTID;SUBJECT,SCORE)
=demo.query("select
=A1.pivot(CLASS:ClassNew,STUDENTID;SUBJECT,SCORE;"English":"new_English","Math","PE")
=A2.pivot@r(ClassNew:CLASS,STUDENTID;SUBJECT,SCORE;new_English:"English",Math,PE)
=A1.pivot(CLASS,STUDENTID;SUBJECT,SCORE)
=A4.pivot@r(CLASS,STUDENTID;SUBJECT,SCORE)
=demo.query("select
=A1.pivot@s(DEPT;GENDER,avg(SALARY);"F":"female","M":"male")
=A1.pivot@s(DEPT;GENDER,max(SALARY);"F":"female",:"other")
=A1.pivot@s(DEPT;GENDER,max(SALARY);:"max")
=A1.pivot@s(DEPT;GENDER,~.(NAME).concat@c())
```

## 提取内容
A.pivot( g:G,…;F,V;N i :N' i ,… )
说明：
Perform
row-to-column/column-to-row transposition for a table sequence/record sequence.
语法：
A . pivot ( g:G , … ; F , V ; N i : N' i ,…)
注意：
The function performs a
row-to-column transposition by default, or a column-to-row one. It groups table
sequence/record sequence A
according to grouping fields/expression g ,
converts values Ni in F field in each group into new columns
and name them N' i , and reassigns
values of V field to the new fields,
during which G is a field result
set and the default field name is that of g field.
参数：
|
A table
sequence/record sequence .
|
g
|
Grouping
fields/expressions.
|
G
|
field in the result set; default is g .
|
F
|
to-be-transposed field in the specified sequence.
|
V
|
field in the specified sequence.
|
N i
|
Values
of F
field and can be absent; by default, it represents all unique field values in
F .
|
N' i
|
Names
of new fields and default values are N i .
选项：
|
@r
|
Transpose
rows to columns in table sequence/record sequence
In
this case the default fields in N i
are the fields of A that
are not included in g . Perform
column-to-row transposition, where field names N i are transposed as values of F field; when parameter N' i is present, it will be
values of F
field and values of original N i field
will be assigned to new field V . In this case the default values of N i are all fields in
sequence A except for grouping
field g .
|
@s( g:G , … ; F,f(V); N i : N' i ,… )
|
Parameter
f can be an aggregate function sum – count, max, min or avg,
which supports syntax ~.f() where ~ references the current group; When N' I is
present but N i is absent, summarize V field in the N i values that hasn’t been
aggregated .
Return
value:
Table
sequence
示例：
Row-to-column
transposition:
|
=demo.query("select
* from SCORES")
|
Return a table sequence:
（原文含示意图，图像资源未迁移）
…
（原文含示意图，图像资源未迁移）
|
=A1.pivot(CLASS:ClassNew,STUDENTID:StudentID;SUBJECT,SCORE;"English":"new_English","Math","PE")
|
Perform row-to-column
transposition: group A1 by CLASS and STUDENTID, rename CLASS field and
STUDENTID field in the result set ClassNew and StudentID respectively, use
SUBJECT field values English, Math and PE as the new field names, during
which English column is renamed ENGLISH, and redistribute SCORE column values
into new columns .
（原文含示意图，图像资源未迁移）
|
=A1.pivot(CLASS,STUDENTID;SUBJECT,SCORE;"English","Math")
|
As
parameters G and N' I are
absent, use g and N i as
column names in the result set .
（原文含示意图，图像资源未迁移）
|
=A1.pivot(CLASS,STUDENTID;SUBJECT,SCORE)
|
As
N i is absent, use all
unique SUBJECT field values by default .
（原文含示意图，图像资源未迁移）
Column-to-row
transposition:
|
=demo.query("select
* from SCORES")
|
=A1.pivot(CLASS:ClassNew,STUDENTID;SUBJECT,SCORE;"English":"new_English","Math","PE")
|
Return a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.pivot@r(ClassNew:CLASS,STUDENTID;SUBJECT,SCORE;new_English:"English",Math,PE)
|
Perform
column-to-row transposition over A2: convert column names English, Math and
PE as SUBJECT field values and transform values under the original columns
ENGLISH, Math and PE into values under SCORE columns; CLASS is the new name
of ClassNew column .
（原文含示意图，图像资源未迁移）
|
=A1.pivot(CLASS,STUDENTID;SUBJECT,SCORE)
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A4.pivot@r(CLASS,STUDENTID;SUBJECT,SCORE)
|
Perform
column-to-row transposition over A4: as N i
is absent, use all field names except for CLASS and STUDENTID by
default .
（原文含示意图，图像资源未迁移）
Use
@s option to use aggregate function at transposition:
|
=demo.query("select
* from employee")
|
Return a table sequence:
（原文含示意图，图像资源未迁移）
|
=A1.pivot@s(DEPT;GENDER,avg(SALARY);"F":"female","M":"male")
|
Perform row-to-column
transposition: in each group, use average of F SALARY and M SALARY as values
of the new columns, whose new names are female and male respectively .
（原文含示意图，图像资源未迁移）
|
=A1.pivot@s(DEPT;GENDER,max(SALARY);"F":"female",:"other")
|
As
N i is absent, get the maximum value from all SALARY column
values except for F .
（原文含示意图，图像资源未迁移）
|
=A1.pivot@s(DEPT;GENDER,max(SALARY);:"max")
|
Get
the maximum value from all fields .
（原文含示意图，图像资源未迁移）
|
=A1.pivot@s(DEPT;GENDER,~.(NAME).concat@c())
|
Use
~ to reference the current group; group A1 by DEPT, and in each group join up
NAME values as a string and use it as values of F and M .
（原文含示意图，图像资源未迁移）
