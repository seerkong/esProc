# cs.pjoin ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.pjoin ()
=connect("demo").cursor("select
=connect("demo").query("select
=connect("demo").query("select
=A1.pjoin(STATEID,STATEID:ID,NAME;A2,STATEID,POPULATION;A3,STATEID,CAPITAL)
=demo.cursor("select
=demo.cursor("select EID,NAME,DEPT from
=A1.pjoin(DEPT;A2,DEPT,count(EID):Num)
=connect("demo").cursor("SELECT
=connect("demo").cursor("SELECT
=A1.pjoin(STATEID,CID,NAME,POPULATION;A2,STATEID,CAPITAL
=connect("demo").cursor("SELECT
=connect("demo").cursor("SELECT
=A1.pjoin@f(STATEID,CID,NAME,POPULATION;A2,STATEID,CAPITAL
=connect("demo").cursor("SELECT
=connect("demo").cursor("SELECT
=A1.pjoin(STATEID,CID,NAME,POPULATION;A2:null,STATEID,CAPITAL
=connect("demo").cursor("SELECT
=connect("demo").cursor("SELECT
=A1.pjoin(STATEID;A2:null,STATEID)
=file("DEPARTMENT.ctx").open().cursor()
=file("EMPLOYEE.ctx").open().cursor()
=A1.pjoin@r(DEPT;A2,DEPT,count(EID):Num)
=file("transaction.btx").cursor@b()
=file("trap.ctx")
=A2.create@ty(#UID,#Time,Change,Amount)
=A3.append@i(A1)
=A4.cursor()
=demo.cursor("select top 5 EID, NAME, DEPT from EMPLOYEE ")
=file("ep.ctx")
=A7.create@y(#EID, NAME, DEPT)
=A8.append@i(A6)
=A9.cursor()
=A10.pjoin@t(EID:date(2021,5,1),NAME,DEPT;A5,UID:Time,UID,Time,Change,Amount)
=A10.fetch()
```

## 提取内容
cs.pjoin ()
说明：
Attach the join-key-based association
with another cursor or a record sequence to a cursor and return the original
cursor.
语法：
cs. pjoin ( K :.., x : F ,…
cs i / A i : z , K i :…, x i : F i ,…
…) .
注意：
The function attaches a computation to cursor cs , which associates cursor/record sequence cs and cursor/record sequence cs i through the join
key and forms a sequence consisting of x : F ,…
and x i : F i ,… to return to the original
cursor .
cs
and cs i can be
cursors or record sequences, and both should be ordered by the join key. By
default, x : F ,… involves all fields of cs .
When relationship between cs and cs i is one-to-many, x i is an aggregate
expression.
When relationship between cs and cs i is many-to-one,
records of cs i / A i will appear
repeatedly in the result set.
Parameter z specifies the join type. It can be
absent or null. Perform an inner join when z is absent, and a left join
when z is null; and only retain records of cs that cannot find matches when z is null and both
parameters x i : F i are absent.
When both cs and cs i are composite table cursors, the latter will by
default follows the former to filter blocks. The process is this: retrieve data
from cs and then retrieve data from cs i according to the value range of the retrieved cs
data. The method filters cs i
and helps to increase computing efficiency.
This is a delayed function.
参数：
|
cs
|
cursor /record sequence.
|
K
|
cs ’s join key.
|
x
|
cs ’s expression.
|
F
|
Field
name corresponding to expression x .
|
cs i
|
cursor/composite table cursor/record sequence.
|
z
|
Join
type.
|
K i
|
Join key of cs c :/ A i .
|
x i
|
Expression
of cs c :/ A i .
|
F i
|
Field
name corresponding to expression x i .
Return
value:
游标
选项：
|
@r
|
Use this option to perform block filtering on cs 1
according to cs and increase efficiency when both cs and cs 1 , cs 2 ,...csi are table cursors and
cs 1 has a relatively small
amount of data.
|
@f
|
Enable full join while ignoring parameter z ; do not work with @r option .
|
@t
|
Use this option to make the time key the join key when
both cs and cs i are composite
table-based cursors and cs i
has the time key; do not work with @f option .
示例：
Homo-dimension table join:
|
=connect("demo").cursor("select
STATEID,NAME from STATENAME").sortx(STATEID)
|
Return a cursor ordered by STATEID; below is
the data:
（原文含示意图，图像资源未迁移）
|
=connect("demo").query("select
STATEID,POPULATION from STATEINFO").sort(STATEID)
|
Return a record sequence ordered by STATEID .
（原文含示意图，图像资源未迁移）
|
=connect("demo").query("select
STATEID,CAPITAL from STATECAPITAL").sort(STATEID)
|
Return a record sequence ordered by STATEID .
（原文含示意图，图像资源未迁移）
|
=A1.pjoin(STATEID,STATEID:ID,NAME;A2,STATEID,POPULATION;A3,STATEID,CAPITAL)
|
Attach a computation to cursor A1, which
associates cursor A1 and record sequences A2 and A3 through join key STATEID,
renames STATEID field ID and returns the original cursor A1; below is data in
cursor A1 after the attached computation is executed on the cursor A1:
（原文含示意图，图像资源未迁移）
When A and A i has a one-to-many
relationship:
|
=demo.cursor("select
top 6 DEPT,MANAGER from DEPARTMENT")
|
Return a cursor; below is the content:
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select EID,NAME,DEPT from
EMPLOYEE").sortx(DEPT)
|
Return a cursor; below is the content:
（原文含示意图，图像资源未迁移）
|
=A1.pjoin(DEPT;A2,DEPT,count(EID):Num)
|
Relationship between A1 and A2 is
one-to-many; attach a computation to cursor A1, which associates cursors A1
and A2 through DEPT, finds the number of EID values under each DEPT value and
makes the computing result a new filed v and returns the original cursor A1
below is data in cursor A1 after the attached computation is executed on the
cursor A1:
（原文含示意图，图像资源未迁移）
When A and A i has a many-to-one
relationship:
|
=connect("demo").cursor("SELECT
top 20 CID,NAME,POPULATION,STATEID FROM CITIES").sortx(STATEID)
|
Return a cursor; below is the content:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT
top 20 STATEID,CAPITAL FROM STATECAPITAL").sortx(STATEID)
|
Return a cursor; below is the content:
（原文含示意图，图像资源未迁移）
|
=A1.pjoin(STATEID,CID,NAME,POPULATION;A2,STATEID,CAPITAL
)
|
Relationship between A1 and A2 is
many-to-one; attach a computation to cursor A1, which associates them through
STATEID, during which A2’s field values appear repeatedly in the result set
and returns the original cursor A1; below is data in cursor A1 after the
attached computation is executed on the cursor A1:
（原文含示意图，图像资源未迁移）
Full join:
|
=connect("demo").cursor("SELECT
top 20 CID,NAME,POPULATION,STATEID FROM CITIES").sortx(STATEID)
|
Return a cursor; below is the content:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT
top 20 STATEID,CAPITAL FROM STATECAPITAL").sortx(STATEID)
|
Return a cursor; below is the content:
（原文含示意图，图像资源未迁移）
|
=A1.pjoin@f(STATEID,CID,NAME,POPULATION;A2,STATEID,CAPITAL
)
|
Attach a computation to cursor A1, which,
uses @f to perform full join, during which non-matching field values of
records are displayed as nulls and returns the original cursor A1; below is
data in cursor A1 after the attached computation is executed on the cursor
A1:
（原文含示意图，图像资源未迁移）
Method 1 for left join:
|
=connect("demo").cursor("SELECT
top 20 CID,NAME,POPULATION,STATEID FROM CITIES").sortx(STATEID)
|
Return a cursor; below is the content:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT
top 20 STATEID,CAPITAL FROM STATECAPITAL").sortx(STATEID)
|
Return a cursor; below is the content:
（原文含示意图，图像资源未迁移）
|
=A1.pjoin(STATEID,CID,NAME,POPULATION;A2:null,STATEID,CAPITAL
)
|
Attach a computation to cursor A1. As
parameter z is null, perform left join to list all records of A1 while
displaying non-matching field values in A2 as nulls and return the original
cursor A1; below is data in cursor A1 after the attached computation is
executed on the cursor A1:
（原文含示意图，图像资源未迁移）
Method 2 for left join:
|
=connect("demo").cursor("SELECT
top 20 CID,NAME,POPULATION,STATEID FROM CITIES").sortx(STATEID)
|
Return a cursor; below is the content:
（原文含示意图，图像资源未迁移）
|
=connect("demo").cursor("SELECT
top 20 STATEID,CAPITAL FROM STATECAPITAL").sortx(STATEID)
|
Return a cursor; below is the content:
（原文含示意图，图像资源未迁移）
|
=A1.pjoin(STATEID;A2:null,STATEID)
|
Attach a computation to cursor A1. Parameter z
is null and parameters x i : F i are
absent, so only the non-matching records of A1 are retained and return the
original cursor A1; below is data in cursor A1 after the attached computation
is executed on the cursor A1:
（原文含示意图，图像资源未迁移）
Join between composite table cursor:
|
=file("DEPARTMENT.ctx").open().cursor()
|
Return a composite table cursor; below is the
content:
（原文含示意图，图像资源未迁移）
|
=file("EMPLOYEE.ctx").open().cursor()
|
Return a composite table cursor; below is the
content:
（原文含示意图，图像资源未迁移）
|
=A1.pjoin@r(DEPT;A2,DEPT,count(EID):Num)
|
The relationship between A1 and A2 is
one-to-many; Attach a computation to associate them according to DEPT, find
the number of EID values under each DEPT and use the results to form Num
field while using @r option and return the original cursor A1; below is data
in cursor A1 after the attached computation is executed on the cursor A1:
（原文含示意图，图像资源未迁移）
When cs i has the
time key:
|
=file("transaction.btx").cursor@b()
|
Return a cursor ordered
by UID and Time .
|
=file("trap.ctx")
|
=A2.create@ty(#UID,#Time,Change,Amount)
|
Create a composite table
where UID is the base key and Time is the time key .
|
=A3.append@i(A1)
|
Append cursor A1’s records
to composite table file trap.ctx .
|
=A4.cursor()
|
Return a composite
table-based cursor, whose content is as follows:
（原文含示意图，图像资源未迁移）
… …
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select top 5 EID, NAME, DEPT from EMPLOYEE ")
|
Return a cursor .
|
=file("ep.ctx")
|
=A7.create@y(#EID, NAME, DEPT)
|
Generate a composite
table and set EID as the key .
|
=A8.append@i(A6)
|
Append cursor A6’s
records to composite table file ep.ctx .
|
=A9.cursor()
|
Return a composite
table-based cursor, whose content is as follows:
（原文含示意图，图像资源未迁移）
|
=A10.pjoin@t(EID:date(2021,5,1),NAME,DEPT;A5,UID:Time,UID,Time,Change,Amount)
|
Attach a computation to
cusor A10. Join two composite table-based cursors: A5 and A10, as composite
table trap.ctx has the time key, use @t option to return the record
where UID is equal to ID and that has the largest Time value before
2021-05-01, and return the original cursor A10:
|
=A10.fetch()
|
Fetch data from cursor
A10:
（原文含示意图，图像资源未迁移）
