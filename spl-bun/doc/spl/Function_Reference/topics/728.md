# A.p max()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=A1.pmax(~*~)
=A1.pmax@a(~*~)
=A1.pmax@z(~*~)
=A1.pmax@az(~*~)
=demo.query("select
=A6.pmax(BIRTHDAY)
```

## 提取内容
A.p max()
说明：
Get the
positions of members with the maximum value in a sequence.
语法：
A .pmax ( x )
注意：
The function computes
expression x with each member of
sequence A and returns the sequence numbers of the members
whose computations are the maximum. Can be
used to find positions of the maximum value in a sequence.
参数：
|
sequence.
|
x
|
field name or an expression, in which " ~ "
is used to reference the current record.
选项：
|
@a
|
Return
sequence numbers of all members that make expression x get the largest value. When omitted, only return the sequence
number of the first member.
|
@z
|
Perform
the search backwards from the last member; by default, perform the search
from the first member.
返回值：
Sequence
number/Sequence composed of sequence numbers
示例：
|
[2,5,4,3,2,9,4,9,3]
|
=A1.pmax(~*~)
|
=A1.pmax@a(~*~)
|
[6,8]
|
=A1.pmax@z(~*~)
|
=A1.pmax@az(~*~)
|
[8,6]
|
=demo.query("select
* from EMPLOYEE ")
|
=A6.pmax(BIRTHDAY)
|
相关函数：
A .ma x p ()
A .p m i n ( )
