# polyfit()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
polyfit()
polyfit ( X,Y,n )
=polyfit(A1,A2,2)
```

## 提取内容
polyfit()
说明：
Calculate
the coefficient between two vectors in polynomial fitting.
语法：
polyfit ( X,Y,n )
注意：
The
function calculates the coefficient between vector X and vector Y , which are
of same dimension, in a polynomial fitting with the degree of polynomial as n ,
and returns a sequence of n +1 coefficients
ordered from low order to high order.
参数：
|
X/Y
|
Vectors
of same dimension.
|
n
|
non-zero positive integer.
Return
value:
Sequence
示例：
|
[3,5,2,8]
|
[4,6,2,4]
|
=polyfit(A1,A2,2)
|
（原文含示意图，图像资源未迁移）
