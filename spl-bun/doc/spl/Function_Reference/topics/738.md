# r. prior()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=file("D://emp.txt").import@t() .keys(empid)
>A1.switch(mgrid,A1:empid)
=A1.select@1(name==" Kitty ").prior(mgrid)
=A1.select@1(name=="Kitty").prior(mgrid,A1.select@1(name=="Tiger"),5)
```

## 提取内容
r. prior()
说明 ：
Among
the records, query fields referred by the foreign key recursively.
语法：
r . prior ( F , r ' , n )
Note ：
The function recursively
queries field F referred by the
foreign key, like [ r , r . F , r . F . F ,…] in record r . The query
will not stop until the value of field F
is equal to r ' . When r is
equal to r' ,
then return [].The
default value of r ' is NULL. The recursive query will be performed through all the
records to the end. If the specified record r ' does not exist, then return NULL. n is the maximum recursion depth, its value is 1,000 by default.
Parameter ：
|
r
|
记录s.
|
F
|
Field
name.
|
r '
|
A record .
|
n
|
A Number .
Return value ：
Sequence
Example ：
|
=file("D://emp.txt").import@t() .keys(empid)
|
（原文含示意图，图像资源未迁移）
|
>A1.switch(mgrid,A1:empid)
|
=A1.select@1(name==" Kitty ").prior(mgrid)
|
Based on mgrid field referred by the foreign
key, select all records of people who are superior to Kitty .
|
=A1.select@1(name=="Kitty").prior(mgrid,A1.select@1(name=="Tiger"),5)
|
Judge if Tiger is superior to Kitty and list
all records of people superior to Barnes but inferior to Tiger .
