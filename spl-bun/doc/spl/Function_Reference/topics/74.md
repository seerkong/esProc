# acos()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
acos()
acos( number )
acos(-1)
acos(cos(pi()/2))
acos(cos(0))
atan ( )
```

## 提取内容
acos()
说明：
计算反余弦值。
语法：
acos( number )
注意：
The
function computes arc consine value of a 实数 between -1 and 1.
参数：
|
number
|
实数 between -1 and 1e.
返回值：
数值型
value
示例：
|
acos(-1)
|
3.141592653589793
|
acos(cos(pi()/2))
|
1.5707963267948966
|
acos(cos(0))
|
0.0
相关函数：
as i n ( )
atan ( )
