# A.proportion()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.proportion()
=[1,2,3,4]
=A1.proportion()
=connect("demo").query("select *
=A3.group(SELLERID;sum(AMOUNT):TolAmount)
=A4.(TolAmount).proportion()
```

## 提取内容
A.proportion()
说明：
Compute
the proportion of each member in the sum of members of a sequence.
语法：
A . proportion ( )
注意：
The
function computes the proportion of each member in the sum of members of
sequence A .
参数：
|
sequence.
Return
value:
Sequence
示例：
|
=[1,2,3,4]
|
=A1.proportion()
|
Return 【 0.1,0.2,0.3,0.4 】 .
|
=connect("demo").query("select *
from SALES")
|
=A3.group(SELLERID;sum(AMOUNT):TolAmount)
|
Calculate
total sale of each salesperson in table sequence A3 .
（原文含示意图，图像资源未迁移）
|
=A4.(TolAmount).proportion()
|
Calculate the proportion of each value of sequence A4’s TolAmount
column .
（原文含示意图，图像资源未迁移）
