# f. property()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=file("E://test.property").property("color")
=file("E://test.property").property()
```

## 提取内容
f. property()
说明：
Retrieve
property value from the property file.
语法：
f .property( p )
注意：
The
function retrieves property p from
property file f and return.
参数：
|
f
|
file object.
|
p
|
Property
name; the function returns a table sequence composed of all properties if it
is omitted.
选项：
|
@v
|
Read
a property value and parse it as what it is; by default, return the value as
a string.
返回值：
Value/Table
sequence
示例：
A regular scenario :
|
=file("E://test.property").property("color")
|
Below is content of test.property :
（原文含示意图，图像资源未迁移）
Return
“red” as the result:
|
=file("E://test.property").property()
|
（原文含示意图，图像资源未迁移）
Scenario of reading color scheme for a graph:
The color scheme file is [ installation
directory]\esProc\classes\ config\chartcolor.properties . A color
scheme format is color scheme name=[color1,color2,….]:
（原文含示意图，图像资源未迁移）
Set
colourful8 as the color scheme for the column graph:
（原文含示意图，图像资源未迁移）
注意：
To use the color schemes in plotting a graph,
make sure that the class path can be loaded onto the config directory. esProc
default design is that the config directory is already put into [installation directory]\esProc\classes .
To view the effects of color schemes on server side, users need to create a
config folder in the service’s [applicaton root directory]\WEB-INF\classes unless there’s already a config folder there and copy the color
scheme file chartcolor.properties under
it.
相关函数：
f.re a d()
f.wr i te()
