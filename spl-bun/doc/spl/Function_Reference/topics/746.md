# xs.property()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
xs.property()
=A1.property("color","blue")
=A1.property@q("size")
=A1.property@v()
=A5.property@cj("color",)
```

## 提取内容
xs.property()
说明：
Read
property values from node strings.
语法：
xs .property( n,v )
注意：
The
function retrieves value of property named n
from node string xs . Set n ’s
value as v if parameter v is present, and delete n if v
is absent. Return a table sequence made up of all properties when no parameters
exist.
参数：
|
xs
|
Node
strings, whose format is “name1=value1 name2=value2...” .
|
n
|
Node
string name.
|
v
|
Null
or a string.
选项：
|
@c
|
Separate
node strings by commas or semicolons; use spaces by default.
|
@j
|
Use
colons to separate node string name and its value; use the equal sign by
default
|
@q
|
Enclose
values by double quotation marks.
|
@v
|
Read
the string and then parse it into value; return a sequence by default.
Return
value:
single value/Sequence
示例：
|
color=red size=20 price=500
|
=A1.property("color","blue")
|
（原文含示意图，图像资源未迁移）
Change the value
of node string named “color” into “blue” .
|
=A1.property@q("size")
|
（原文含示意图，图像资源未迁移）
Enclose the value
with double quotation marks .
|
=A1.property@v()
|
Use @v option to read
the string first and then parse it into a value; as parameters are absent,
the function returns a sequence of all properties .
（原文含示意图，图像资源未迁移）
|
color:red;size:20;price:500
|
=A5.property@cj("color",)
|
Use @cj options to
retrieve property vlaues. Use semicolon to separate node strings, and use
colon to separate node string name and node string value. Since parameter n
is “color” and the value of parameter v is null, delete the node from the node
strings .
Below is the result returned:
