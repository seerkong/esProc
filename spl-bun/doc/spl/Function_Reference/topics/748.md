# A.pseg( x,y )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.pseg( x,y )
A.pseg( x,y )
=A1.pseg(12)
=A1.pseg(2222)
=A1.pseg@r(2222)
=A5.(~+~).pseg(10)
=A5.pseg(~+~,10)
=A1.pseg(1)
```

## 提取内容
A.pseg( x,y )
说明 ：
Get
the sequence number of the interval in which the parameter falls.
Syntax ：
A.pseg( x,y )
Note ：
The function gets the
sequence number of the interval in which member y falls in sequence A ,
which must be an ordered one. By default, the sequence members form intervals
that are left-closed and right-open. Parameter x is an expression which will be calculated over each member of sequence
A .
In the cases where y is not a member of A , if y is less than the smallest member of an increasing sequence A , then return 0; if y is greater than or equal to the
largest member of an increasing sequence A ,
then return the length of A .
Parameter ：
|
sequence.
|
x
|
An
expression, which is a field name or a legal expression made up of field name
and in which the sign ~ can be used to reference the current record; can be
absent.
|
y
|
member.
选项：
|
@r
|
Form
intervals that are left-open and right-closed.
Return value ：
数值型
value
Example ：
|
[2,22,122,222,2222]
|
=A1.pseg(12)
|
1.
The intervals formed by members of the sequence are [2,22), [22,122),
[122,222), [222,2222) and [2222,∞) .
|
=A1.pseg(2222)
|
=A1.pseg@r(2222)
|
[1,4,8]
|
=A5.(~+~).pseg(10)
|
=A5.pseg(~+~,10)
|
the expression is equivalent to A6’ s expression .
|
=A1.pseg(1)
|
