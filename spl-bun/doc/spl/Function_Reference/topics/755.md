# A.p sort()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=A1.psort(~)
=demo.query("select
=A3.psort(DEPT:1,BIRTHDAY:-1)
=A3.psort@m(DEPT,BIRTHDAY)
=A1.psort@z(~)
=A7.psort(~)
=A7.psort@0(~)
```

## 提取内容
A.p sort()
说明：
Get the
original positions of the sorted members of a sequence.
语法：
A .psort
( x )
A .psort( x i: d i ,... )
注意：
The
function sorts members of sequence A
according to the value of expression x
and returns their sequence numbers before they are sorted.
参数：
|
sequence.
|
x
|
The
sorting expression.
|
x i
|
If
there are multiple sorting expressions, they can be written as x i: d i ,.., a compound expression consisting of multiple expressions.
|
d i
|
Sorting
order: >0 represents ascending order, which is the default, and <0
descending order.
选项：
|
@m
|
Perform
parallel algorithm to enhance performance of handling data-intensive or
computation-intensive tasks.
|
@z
|
Sort
a sequence in descending order and return the position of a member in the original
sequence .
|
@0
|
Put
records with null values at the end.
返回值：
Sequence
示例：
|
[a,c,e,g,f,d,b]
|
=A1.psort(~)
|
[1,7,2,6,3,5,4] .
|
=demo.query("select
* from EMPLOYEE")
|
=A3.psort(DEPT:1,BIRTHDAY:-1)
|
A3
is sorted in ascending order by DEPT first, and then in descending order by BIRTHDAY .
|
=A3.psort@m(DEPT,BIRTHDAY)
|
Use
@m option to increase performance of big data handling.
|
=A1.psort@z(~)
|
[4,5,3,6,2,7,1] .
|
[a,c,e,,f,d,b]
|
=A7.psort(~)
|
[4,1,7,2,6,3,5] .
|
=A7.psort@0(~)
|
[1,7,2,6,3,5,4] .
相关函数：
A .po s ( )
A .s o r t()
A .pos ( x )
A .swap ( p , q )
A .r v s ()
