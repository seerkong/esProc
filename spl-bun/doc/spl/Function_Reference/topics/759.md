# ch.push()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.push()
=demo.cursor("select * from SALES")
=channel()
=channel()
=A2.select(ORDERID>100)
=A1.push(A2)
=A2.push(A3)
=A2.fetch()
=A3.groups(SELLERID:SellerId;sum(AMOUNT):Total)
=A1.fetch()
=A2.result()
=A3.result()
channel()
```

## 提取内容
ch.push()
说明：
Push
data in a channel into another channel.
语法：
ch. push ( ch i ,… )
注意：
The
function pushes data in channel ch
into channel ch i , and
returns channel ch itself.
参数：
|
ch
|
channel.
|
ch i
|
channel.
Return
value:
管道
示例：
|
=demo.cursor("select * from SALES")
|
Create
a channel.
|
=channel()
|
Create
a channel.
|
=channel()
|
Create
a channel.
|
=A2.select(ORDERID>100)
|
Attach
ch.select() operation to A2’s channel.
|
=A1.push(A2)
|
Push
data in A1’s cursor into A2’s channel.
|
=A2.push(A3)
|
A3
Push data in A2’s channel into A3’s channel.
|
=A2.fetch()
|
Attach
ch.fetch() function that gets the final result set to A2’s channel to fetch
and store the existing data.
|
=A3.groups(SELLERID:SellerId;sum(AMOUNT):Total)
|
Attach
ch.groups() operation to A3’s channel.
|
=A1.fetch()
|
Data
in A1’s cursor is actually pushed into the channel as the operation begins.
|
=A2.result()
|
Get
results from A2’s channel.
|
=A3.result()
|
（原文含示意图，图像资源未迁移）
Get
results from A3’s channel.
function:
cs.pu s h()
channel()
