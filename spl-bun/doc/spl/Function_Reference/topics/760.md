# cs.push()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.push()
=demo.cursor("select
=channel()
=A2.select(ORDERID>100)
=A2.fetch()
=A1.push(A2)
=A1.fetch()
=A2.result()
ch.push()
```

## 提取内容
cs.push()
说明：
Push
data in a cursor into a channel.
语法：
cs. push ( ch i ,… )
注意：
The
function pushes data in cursor cs
into channel ch i , but the
action needs to wait until the data in the cursor is fetched; then returns the
cursor itself. The function supports multicursors.
参数：
|
cs
|
游标 .
|
ch i
|
管道 .
Return
value:
游标
示例：
|
=demo.cursor("select
* from SALES")
|
Create
a cursor.
|
=channel()
|
Create
a channel.
|
=A2.select(ORDERID>100)
|
Attach
ch.select() operation to the channel.
|
=A2.fetch()
|
Keep
data in the channel.
|
=A1.push(A2)
|
Be
ready to push A1’s cursor data to the channel.
|
=A1.fetch()
|
（原文含示意图，图像资源未迁移）
Fetch
data from A1 cursor when the cursor data is really pushed into the channel
and the operations in the channel are executed.
|
=A2.result()
|
（原文含示意图，图像资源未迁移）
Fetch
the final result set from the channel.
function:
ch.push()
channe l ()
