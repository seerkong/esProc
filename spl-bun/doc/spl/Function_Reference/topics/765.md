# Qload()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
Qload()
Qload ( lfn : z,rfn : z )
=Qload("qtc01/emp.ctx":[1,2])
=Qload(,"qtc01/emp.ctx":[1,2])
=Qload("myBucket/cities.txt")
```

## 提取内容
Qload()
说明：
Upload
file(s)/homo-name files group to or download them from the remote storage.
语法：
Qload ( lfn : z,rfn : z )
注意：
Put
file(s)/homo-name files group to be uploaded to the remote storage or those to
be downloaded to the local in the specified directory.
When
QVS is service mode, the specified directory is QVM’s main directory; when QVS
uses debug mode, it is QVS’s own main directory. If no remote storage
information is configured in the QVS application configuration file, configure 【 Remote Services –> Storage Service Settings 】 in esProc IDE and connect the target cloud storage service before
using the function directly in esProc IDE.
Automatically
overwrite the namesake file(s).
参数：
|
lfn
|
local file, whose format is “ storageBucket/fileObjectName ”; means
download when omitted.
|
rfn
|
file in the storage bucket, whose format is the same as parameter lfn
means upload when omitted.
|
z
|
Specify
this parameter when parameter lfn/rfn is a homo-name files group, the
parameter is a zone table number or a sequence of zone table number.
Return
value:
Boolean
示例：
|
=Qload("qtc01/emp.ctx":[1,2])
|
Upload homo-name files group 1.emp.ctx and 2.emp.ctx
in the local directory 【 main directory 】 \qtc01 to storage bucket qt01
in the cloud storage .
|
=Qload(,"qtc01/emp.ctx":[1,2])
|
Download homo-name files group 1.emp.ctx
and 2.emp.ctx in storage bucket qtc01 to the local directory 【 main directory 】 \qtc01 .
|
=Qload("myBucket/cities.txt")
|
Upload
the local file cities.txt in 【 main
directory 】 \myBucket\ to storage bucket myBucket
