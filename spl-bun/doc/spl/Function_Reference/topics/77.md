# aes()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
aes()
aes ( input,key,iv;charset )
=charencode(A1; "GBK")
=charencode(A2; "GBK")
=aes(A3,A4)
=aes@d(A5,A4)
=charencode(A6;"GBK")
=aes@c(A1,A2,A3;"utf-8")
=aes@cd(A4,A2,A3)
=charencode(A5;"utf-8")
=charencode(A1;"unicode"
=charencode(A2;"unicode")
=aes@n(A3,A4)
=aes@nd(A5,A4)
=charencode(A6;"unicode")
```

## 提取内容
aes()
说明：
Perform AES symmetric encryption.
语法：
aes ( input,key,iv;charset )
注意：
The function uses AES symmetric encryption algorithm to encrypt/decrypt
parameter input .
key is the access key. Initiation
vector parameter iv is only needed
when CBC encryption mode is used; use ECB encryption mode by default when @c
option representing the CBC mode is absent.
Usually parameters input , key and iv are BLOB type; the
function also supports automatic string encoding.
Both key and iv must have 16 bytes. Note that they
must have 16 bytes after encoding if the to-be-encrypted value is a string.
参数：
|
input
|
Data to be encrypted/decrypted .
|
key
|
Access key .
|
iv
|
Initiation vector .
|
charset
|
字符集, which is utf-8 by default
No need to set up this parameter when both input and key are BLOB
type .
选项：
|
@d
|
Perform decryption. Values of parameters key , iv and charset should
be same for encryption and decryption .
|
@c
|
Use CBC encryption mode; use the default mode when parameter
iv is absent .
|
@n
|
Use NoPadding; in this case the number of bytes in input must be the integer multiple of
16, otherwise error will be reported .
返回值：
BLOB value
示例：
Encryption and decryption:
|
Hello World!
|
v7wz+QhTtHkYmXl/
|
=charencode(A1; "GBK")
|
Generate a BLOB value using A1’s string.
|
=charencode(A2; "GBK")
|
Generate a 16-byte BLOB value using A2’s string.
|
=aes(A3,A4)
|
Encrypt A3 using AES symmetric encryption algorithm while setting up
A4 as the access key, and return a BLOB value.
|
=aes@d(A5,A4)
|
Use @d option to decrypt A5 – the access key must be consistent with that
in A5’s encryption, and return a BLOB value.
|
=charencode(A6;"GBK")
|
Generate a string using A6’s BLOB value and return the result: Hello
World!
CBC encryption mode:
|
Hello World!
|
v7wz+QhTtHkYmXl/
|
abcdefghijkl+-$/
|
=aes@c(A1,A2,A3;"utf-8")
|
@c option enables using the CBC encryption mode; A3 is the initiation
vector and the character set is utf-8; both the encoded access key and
initiation value must have 16 bytes.
|
=aes@cd(A4,A2,A3)
|
Decrypt A4 – access key and initiation vector must be consistent with
those in A4’s encryption and use the default utf-8 as parameter charset
is absent, and return a BLOB value.
|
=charencode(A5;"utf-8")
|
Generate a string using A5’s BLOB value and return the result: Hello
World!
NoPadding:
|
Hello!!
|
v7wz+Qh
|
=charencode(A1;"unicode"
)
|
Generate a 16-byte BLOB value using A1’s string.
|
=charencode(A2;"unicode")
|
Generate a 16-byte BLOB value using A2’s string.
|
=aes@n(A3,A4)
|
@n option enables NoPadding; in this case the number of bytes in
parameter input must be the integer multiple of 16; return a BLOB
value.
|
=aes@nd(A5,A4)
|
Decrypt A5.
|
=charencode(A6;"unicode")
|
Generate a string using A6’s BLOB value and return same result as A1.
