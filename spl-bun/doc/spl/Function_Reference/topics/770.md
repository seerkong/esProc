# r.(x,...)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.(x,...)
=[[12,23]].new(~(1):col1,~(2):col2)
=A1(1).(col2+1)
=demo.query("select EID,NAME,HIREDATE,SALARY from employee")
=A3(1).(Hage=age(HIREDATE),if(Hage>10,SALARY+3000,SALARY+1000))
```

## 提取内容
r.(x,...)
说明：
Compute
expressions against a record and
return result of the last one .
语法：
r .( x,… )
注意：
The
function computes expression x,… against record r, and return result of the
last one.
参数：
|
r
|
record.
|
x,…
|
An
expression, which is generally a field name or a legal expression that is
composed of field names; use " ~ " to reference the current record.
返回值：
The computation
of x
示例：
|
=[[12,23]].new(~(1):col1,~(2):col2)
|
（原文含示意图，图像资源未迁移）
|
=A1(1).(col2+1)
|
=demo.query("select EID,NAME,HIREDATE,SALARY from employee")
|
（原文含示意图，图像资源未迁移）
|
=A3(1).(Hage=age(HIREDATE),if(Hage>10,SALARY+3000,SALARY+1000))
|
10000; return result of
computing the last expression as there are multiple expressions .
