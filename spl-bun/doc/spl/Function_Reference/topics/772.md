# r.F=x

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select
>A1(1).SALARY=A1(1).SALARY+100
```

## 提取内容
r.F=x
说明：
Assign
value to a field of a record.
语法：
r . F = x
注意：
The
function assigns x to field F of record r .
参数：
|
r
|
record.
|
F
|
Field
name.
|
x
|
An
expression, the computation of which is the field value of F .
返回值：
The
computation of x
示例：
|
=demo.query("select
EID,NAME, SALARY from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
>A1(1).SALARY=A1(1).SALARY+100
|
The
value of " SALARY " in A1(1) is changed into 7100.00 .
