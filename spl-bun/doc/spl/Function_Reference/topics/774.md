# r2dbc_exec()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r2dbc_exec()
r2dbc_exec(con,sql)
=r2dbc_open("r2dbcs:mysql://root:123456@127.0.0.1:3306/mytest")
=r2dbc_exec(A1,"create
=r2dbc_exec(A1, "drop
=r2dbc_exec(A1,"insert
```

## 提取内容
r2dbc_exec()
说明：
Execute a SQL statement
through R2DBC.
语法：
r2dbc_exec(con,sql)
注意：
The R2dbcCli external library function (See External Library Guide ) executes basic SQL statements.
参数：
|
con
|
Database
connection string.
|
sql
|
SQL statement.
示例：
|
=r2dbc_open("r2dbcs:mysql://root:123456@127.0.0.1:3306/mytest")
|
Connect
to mysql database.
|
=r2dbc_exec(A1,"create
table test (id int PRIMARY KEY,name VARCHAR(255),age INT)")
|
Create
a table.
|
=r2dbc_exec(A1, "drop
table test1")
|
Delete
a table.
|
=r2dbc_exec(A1,"insert
into test values(?,?,?)",1,"Lily",1001)
|
Insert
data to table.
