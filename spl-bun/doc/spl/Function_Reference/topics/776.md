# r2dbc_query()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r2dbc_query()
r2dbc_query ( con,sql )
=r2dbc_open("r2dbc:h2:mem:///testdb?options=DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE")
=r2dbc_query(A1,
=r2dbc_close(A1)
```

## 提取内容
r2dbc_query()
说明：
Query
a database through R2DBC.
语法：
r2dbc_query ( con,sql )
注意：
The
R2dbcCli external library function (See External Library Guide ) queries
the specified SQL statement sql in a database and returns a table
sequence consisting of the result of the SQL query.
参数：
|
con
|
Database
connection string.
|
sql
|
The
SQL statement to be queried, such as select * from table.
返回值：
Table
sequence
示例：
|
=r2dbc_open("r2dbc:h2:mem:///testdb?options=DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE")
|
Connect
to H2 database.
|
=r2dbc_query(A1,
"select * from table")
|
Query
data in the table.
|
=r2dbc_close(A1)
|
