# rand()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
rand()
rand( n )
rand()
rand(100)
=rand@s(5)
=10.(rand())
= rand@s(5)
=10.(rand())
```

## 提取内容
rand()
说明：
Get
a random number.
语法：
rand( n )
注意：
The function returns a random number between 0 and 1.
参数：
|
n
|
An
integer, without it the function returns a random float number between 0 and
1.0.
选项：
|
@s
|
Set
the seed value for generating a random number.
返回值：
数值型
type
示例：
|
rand()
|
Get a random number in the range of [0, 1.0].
|
rand()*100
|
Get a
random float number in the range of [0, 100].
|
rand(100)
|
Get a
random integer in the range of [0, 100].
The seed value for generating a random number
|
=rand@s(5)
|
=10.(rand())
|
= rand@s(5)
|
=10.(rand())
|
The
10 random numbers generated in A4 and A2 are identical.
