# rands()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
rands()
rands( s,l )
rands("abc",5)
```

## 提取内容
rands()
说明：
Get
a random string.
语法：
rands( s,l )
注意：
The
function generates a string of length l
randomly using the characters of string s .
参数：
|
s
|
A string .
|
l
|
An integer .
返回值：
string
示例：
|
rands("abc",5)
|
Generate
a string of length 5 with characters of string “ abc ” .
