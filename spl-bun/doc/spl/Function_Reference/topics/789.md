# f. read()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=file("D:/score.txt")
=A1.read()
=A1.read@n()
=file("D:/tmp2.txt")
=["Lucy",98]
=A4.write(A5)
=A4.read@nv()
=A1.read@b()
=file("atoz.txt").read()
=file("atoz.txt").read(2:7)
=file("atoz.txt").read@0()
```

## 提取内容
f. read()
说明：
Read
contents of a file and return result as strings.
语法：
f . read ( b:e )
注意：
The
function reads in the contents of file f
from the b th byte to the e th byte and
returns the substring.
参数：
|
f
|
file object, which can be a data file of txt, csv, btx and xls format .
|
b
|
byte number beginning from 0 .
|
e
|
byte number beginning from 0 .
选项：
|
@n
|
Return
the contents of the file object f as
a string sequence; each row is corresponding to a member .
|
@v
|
Return
a sequence as the corresponding data type. The combined use of this option
and @n is acceptable .
|
@0
|
Read
the file once and return the total number of bytes in it; used for test .
返回值：
字符串/numeric
value/BLOB value
示例：
|
=file("D:/score.txt")
|
=A1.read()
|
Read
the contents of file object score.txt
as a string.
（原文含示意图，图像资源未迁移）
|
=A1.read@n()
|
With
@n option, read and concatenate each line in the text file into a string and
return it as a member of the resulting sequence.
（原文含示意图，图像资源未迁移）
|
=file("D:/tmp2.txt")
|
=["Lucy",98]
|
By
default Lucy is a string and 98 is an integer.
|
=A4.write(A5)
|
=A4.read@nv()
|
With
@nv options, return a sequence consisting of rows of different data types.
（原文含示意图，图像资源未迁移）
|
=A1.read@b()
|
Read
the text file as the binary blob data.
（原文含示意图，图像资源未迁移）
|
=file("atoz.txt").read()
|
Read
in content of atoz.txt .
（原文含示意图，图像资源未迁移）
|
=file("atoz.txt").read(2:7)
|
Read
in content of atoz.txt from the 2 nd byte to the 7 th
byte.
（原文含示意图，图像资源未迁移）
|
=file("atoz.txt").read@0()
|
Use
@0 option to return the total number of byte in atoz.txt ; the result
is 26.
相关函数：
f .w r i te ( )
