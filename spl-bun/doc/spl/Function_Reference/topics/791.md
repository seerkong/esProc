# A.record()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.record()
=demo.query("select
=A1.array()
=A2.record()
```

## 提取内容
A.record()
说明：
Generate
a table sequence from a sequence of sequences.
语法：
A. record ()
注意：
The
function generates a table sequence from a sequence of sequences A by
making A ’s members the records.
参数：
|
A sequence consisting of sequences .
Return
value:
Table
sequence
示例：
|
=demo.query("select
* from SCORES")
|
=A1.array()
|
Return
a sequence of sequences.
（原文含示意图，图像资源未迁移）
|
=A2.record()
|
Generate
a table sequence from a sequence of sequences.
（原文含示意图，图像资源未迁移）
