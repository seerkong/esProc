# A.record(n)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.record(n)
=A1.record(1)
=A1.record(2)
=A1.record(3)
```

## 提取内容
A.record(n)
说明：
Generate
a table sequence containing a specified number of columns based on a sequence.
语法：
A . record ( n )
注意：
The
function generates a table sequence containing n columns based on
sequence A by using the first n members of A as the column names
and populating the remaining members to records in turn. By default, it enters
nulls to the last record if there is no available data left.
参数：
|
A sequence .
|
n
|
An integer that represents the number of columns
in the future table sequence; its value is greater than 0 and less than or
equals to A ’s length .
Return
value:
Table
sequence
示例：
|
[a,b,1,2,3,4,5,6]
|
Return
a sequence.
|
=A1.record(1)
|
Generate a
one-column table sequence :
（原文含示意图，图像资源未迁移）
|
=A1.record(2)
|
Generate a
two-column table sequence:
（原文含示意图，图像资源未迁移）
|
=A1.record(3)
|
Generate a
three-column table sequence:
（原文含示意图，图像资源未迁移）
