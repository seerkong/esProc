# r.record()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.record()
=demo.query("select
=A1(1)
=A2.record(A3)
```

## 提取内容
r.record()
|
说明 ：
Modify
field values of a record with members of a sequence.
Syntax ：
r . record ( A )
Note ：
The function uses members
of sequence A to modify the fields of
record r . If the number of A ’s members is more than that of r ’s fields, discard the surplus members
if the former is less than the latter, keep the original values of the
remaining fields.
Parameter ：
|
r
|
record .
|
A sequence .
返回值：
记录
Example ：
|
=demo.query("select
* from SCORES")
|
The original table
sequence in A1.
（原文含示意图，图像资源未迁移）
|
=A1(1)
|
[Class
four,1,English,100]
|
=A2.record(A3)
|
A1’s
table sequence after the first record is modified.
（原文含示意图，图像资源未迁移）
