# redis_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
redis_close()
redis_close ( con )
=redis _open()
=redis_close(A1)
```

## 提取内容
redis_close()
说明：
Close a Redis database connection.
语法：
redis_close ( con )
注意：
This RedisCli external library function (See External Library Guide ) disconnects from a Redis database.
参数：
|
con
|
Database
connection string .
返回值：
No return value
示例：
|
=redis _open()
|
=redis_close(A1)
