# s. regex()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=A1.regex("(\\d),([0-9]*),([a-z]),([a-z]*)")
=A2.regex@c("([a-z]),([a-z])")
=A2.regex("([a-z]),([a-z])")
=A3.regex@u("(\\u0057),(\\u0046)")
=A1.regex("([0-9])","hello")
=A1.regex@a("([0-9])","hello")
="123abc".regex("[0-9]a")
="123abc".regex@w("[0-9]a")
="9,20,hello,6.269".regex("([0-9.]+)")
="9,20,hello,6.269".regex@p("([0-9.]+)")
```

## 提取内容
s. regex()
说明：
Match
a string with the regular expression.
语法：
s .regex ( rs,rpls )
注意：
The
function matches string s with the
regular expression rs , replaces the
first matched character with string rpls , and returns the string after
replacement.
It
returns a sequence consisting of matching strings when parameter rpls is
absent, and null when the matching fails.
参数：
|
s
|
string.
|
rs
|
Regular
expression.
|
rpls
|
string.
选项：
|
@ c
|
Case insensitive.
|
@ u
|
Use Unicode to match.
|
@a
|
Replace
all matching characters.
|
@w
|
Find
whether the regular expression matches with the whole string.
|
@p
|
Parse
the result numeric string into a number.
返回值：
Sequence/字符串/数值型
value
示例：
|
4,23,a,test
|
a,D
|
W,F
|
=A1.regex("(\\d),([0-9]*),([a-z]),([a-z]*)")
|
（原文含示意图，图像资源未迁移）
|
=A2.regex@c("([a-z]),([a-z])")
|
（原文含示意图，图像资源未迁移）
With @c option, it
is case insensitive.
|
=A2.regex("([a-z]),([a-z])")
|
Return
null because they don’t match .
|
=A3.regex@u("(\\u0057),(\\u0046)")
|
（原文含示意图，图像资源未迁移）
[W,F]; Use unicode to match.
|
=A1.regex("([0-9])","hello")
|
hello ,23,a,test .
|
=A1.regex@a("([0-9])","hello")
|
hello , hellohello ,a,test .
|
="123abc".regex("[0-9]a")
|
123abc .
|
="123abc".regex@w("[0-9]a")
|
Use @w option to find
whether the whole string matches and return null .
|
="9,20,hello,6.269".regex("([0-9.]+)")
|
Return a sequence
of strings:
（原文含示意图，图像资源未迁移）
|
="9,20,hello,6.269".regex@p("([0-9.]+)")
|
Parse result numeric strings
into numeric values:
（原文含示意图，图像资源未迁移）
相关函数：
A .re g ex()
c s .re g e x()
