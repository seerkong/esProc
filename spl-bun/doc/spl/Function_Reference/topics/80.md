# P.align(A:x,y)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.align(A:x,y)
=demo.query("select * from DEPARTMENT ")
=demo.query("select * from EMPLOYEE ")
=A2.align@a(A1:DEPT,DEPT)
=A1.new(DEPT, A3(#).count():NUMBER)
=A2.align(A1:DEPT,DEPT)
=demo.query("select * from score")
=A1.sort(class)
=A1.group(class)
=["class one","class
=A1.align@a(A4,class)
=demo.query("select
=demo.query("select
=A1.align@ba(A2:EID,EID)
=demo.query("select
=demo.query("select EID,NAME,DEPT,SALARY from EMPLOYEE
=A2.align@ap(A1:DEPT,DEPT)
=demo.query("select top 15 * from SCORES")
=demo.query("select top 3 * from STUDENTS")
=A1.align@n(A2:ID,STUDENTID)
=demo.query("select
=demo.query("select
=A1.align@s(A2:ID,STUDENTID)
```

## 提取内容
P.align(A:x,y)
说明：
Align
records of a record sequence to a sequence.
语法：
P .align ( A : x , y )
注意：
The function aligns records
of record sequence P to sequence A through association between expression x and expression y .
This function is mainly used
in handling primary-and-subtable joins, where expression y is usually the foreign key field of subtable P and expression x is primary table A ’s primary key field. The computation compares value of expression
y and that of expression x and
aligns the corresponding records if they are equal.
There is generally the
one-to-many correspondence between the primary table and the subtable, that is,
a record in A is associated with
multiple records in P .
When both parameter x
and parameter y are absent, align each current record of P to the corresponding member of A .
参数：
|
P
|
record sequence, table sequence or pure table sequence, which is usually
regarded as the subtable.
|
sequence or record sequence to which records are aligned, which is usually
the primary table.
|
x
|
field or field expression according to which association is achieved; by
default, it is interpreted as ~ .
|
y
|
Alignment
field or field expression in P , which is by
default interpreted as ~ .
选项：
|
@a
|
Group
and align all records of record sequence P
according to sequence/record sequence A ,
and return each group of records as a member of the result sequence. By
default, the function returns a sequence made up of the first member of every
group.
|
@b
|
Use
binary search when A is an ordered
sequence.
|
@p
|
Return
a sequence of sequence numbers of members in P .
|
@n
|
Return
all members of P that match members of A and put non-matching members in the result set’s last group.
|
@s
|
Sort
records of P according to the order of members of A and put non-matching records in the last.
|
@v
|
Return a pure table sequence when P
is a pure table sequence; only supported by esProc Enterprise Edition.
|
@o
|
Perform merge-align between P and A when
both are ordered.
Return
value:
Sequence/记录
sequence/Pure table sequence
示例：
Align the subtable to the primary table and perform computations:
|
=demo.query("select * from DEPARTMENT ")
|
Treat
the table as the primary table .
（原文含示意图，图像资源未迁移）
|
=demo.query("select * from EMPLOYEE ")
|
Treat
the table as subtable where DEPT field is associated with A1 .
（原文含示意图，图像资源未迁移）
|
=A2.align@a(A1:DEPT,DEPT)
|
Align
EMPLOYEE table to DEPARTMENT table according to DEPT field and, as @a option
works, return all matching records .
（原文含示意图，图像资源未迁移）
|
=A1.new(DEPT, A3(#).count():NUMBER)
|
Perform association between A3 and A1 and list number
of aligned records of the subtable EMPLOYEE in each group .
（原文含示意图，图像资源未迁移）
|
=A2.align(A1:DEPT,DEPT)
|
As
no option is present, only return a sequence consisting of the first member
of every group:
（原文含示意图，图像资源未迁移）
For special sorting:
|
=demo.query("select * from score")
|
（原文含示意图，图像资源未迁移）
|
=A1.sort(class)
|
Sort
A1 by class using sort() function and display class values in the order of
four, one, three, two .
（原文含示意图，图像资源未迁移）
|
=A1.group(class)
|
Group
A1 by class using group() function and display class values in the order of
four, one, three, two; this method cannot specify the order of groups .
（原文含示意图，图像资源未迁移）
|
=["class one","class
two","class three","class four"]
|
=A1.align@a(A4,class)
|
Use
@a option to align all members, and as parameter x is absent, display A1’s
records according to the order of class field values in A4, and group the
result sequence .
（原文含示意图，图像资源未迁移）
Use @b option to enable binary search:
|
=demo.query("select
* from FAMILY").sort(EID)
|
（原文含示意图，图像资源未迁移）
|
=demo.query("select
top 11 * from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.align@ba(A2:EID,EID)
|
As
A1 is ordered by EID field, we use binary search to speed up query .
（原文含示意图，图像资源未迁移）
Use @p option to return an alignment result set consisting of
members’ sequence numbers in P :
|
=demo.query("select
* from DEPARTMENT ")
|
（原文含示意图，图像资源未迁移）
|
=demo.query("select EID,NAME,DEPT,SALARY from EMPLOYEE
")
|
（原文含示意图，图像资源未迁移）
|
=A2.align@ap(A1:DEPT,DEPT)
|
Align
A2 to A1 and return a sequence consisting of sequence numbers of members of P .
（原文含示意图，图像资源未迁移）
When @n option is present:
|
=demo.query("select top 15 * from SCORES")
|
（原文含示意图，图像资源未迁移）
|
=demo.query("select top 3 * from STUDENTS")
|
（原文含示意图，图像资源未迁移）
|
=A1.align@n(A2:ID,STUDENTID)
|
The
function works with @n option to align the primary table and the subtable and
return all members of every group; the last group stores records that cannot
match the primary table:
（原文含示意图，图像资源未迁移）
Use @s option to sort subtable records according to members of the
primary table:
|
=demo.query("select
top 9 * from SCORES").sort(SCORE)
|
（原文含示意图，图像资源未迁移）
|
=demo.query("select
top 3 * from STUDENTS")
|
（原文含示意图，图像资源未迁移）
|
=A1.align@s(A2:ID,STUDENTID)
|
As
@s option is present, sort A1 according to A2’s ID field and put non-matching
members in the end .
（原文含示意图，图像资源未迁移）
