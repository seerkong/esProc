# A. regex()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select
=A1.(~.array().concat@c())
=A2.regex("A.*")
=A2.regex("(V.*),(.*)";name,surname)
=file("D:\\a.txt").import@ts()
=A5.(#1).regex@c("(.*),(a.*)";id,name)
=file("D:\\c.txt":"UTF-8").import@ts()
=A7.(~.array().concat@c())
=A8.regex@u("(\\u9500\\u552e\\u90e8),(.*)";SalesDep,EmployeeName)
=A1.regex("V.*",NAME)
=demo.query("select EID,NAME,SALARY from
=A11.regex@p("(.*),(V.*),(.*)";id,name,salary)
```

## 提取内容
A. regex()
说明：
Match members
in a string sequence with the regular expression.
语法：
|
A .regex( rs,F i )
|
If
no extracting item is specified in rs ,
match field F i of the string with rs
based on data type. Then return the new record sequence after record sequence
A is filtered. Use the current
record to match with rs if omitting
F i .
|
A .regex( rs;F i ,… )
|
If
one or more extracting items are specified in rs , split the string members of sequence A according to them, and return the results as a table sequence
whose fields are F i .
注意：
The
function matches the members of string type in the sequence A with the regular expression rs .
The
results will be merged into a table sequence whose fields are F i for returning.
参数：
|
sequence or a record sequence whose members are strings .
|
rs
|
Regular
expressions. The extracting items are specified sub-regular expressions which
are separated from each other by separators and each is surrounded with the
parentheses. They will match the fields in sequence. For example, "(.*),(a.*)" are two
extracting items separated by a comma .
|
F i
|
Resulting
field names of string type .
选项：
|
@ c
|
Case insensitive .
|
@ u
|
Use Unicode to match .
|
@p
|
Parse
the result numeric string into a number.
返回值：
Table
sequence
示例：
|
=demo.query("select
NAME,SURNAME from EMPLOYEE")
|
=A1.(~.array().concat@c())
|
（原文含示意图，图像资源未迁移）
Convert to a
sequence of strings .
|
=A2.regex("A.*")
|
（原文含示意图，图像资源未迁移）
By
default, use current record to match with rs
if omitting F i .
|
=A2.regex("(V.*),(.*)";name,surname)
|
（原文含示意图，图像资源未迁移）
Match
members of A2 with the regular expressions and return a table sequence.
|
=file("D:\\a.txt").import@ts()
|
（原文含示意图，图像资源未迁移）
|
=A5.(#1).regex@c("(.*),(a.*)";id,name)
|
（原文含示意图，图像资源未迁移）
Match
names that start with a or A.
|
=file("D:\\c.txt":"UTF-8").import@ts()
|
=A7.(~.array().concat@c())
|
=A8.regex@u("(\\u9500\\u552e\\u90e8),(.*)";SalesDep,EmployeeName)
|
（原文含示意图，图像资源未迁移）
Use
Unicode to match the Sales Dep.
|
=A1.regex("V.*",NAME)
|
（原文含示意图，图像资源未迁移）
Without
an extracting item, match the regular expression rs with the string type F i field.
|
=demo.query("select EID,NAME,SALARY from
EMPLOYEE").(~. array().concat@c())
|
Return a sequence
of strings:
（原文含示意图，图像资源未迁移）
|
=A11.regex@p("(.*),(V.*),(.*)";id,name,salary)
|
Use @p option to parse
numeric strings in the result table sequence into numeric values:
（原文含示意图，图像资源未迁移）
相关函数：
s .reg e x ()
c s .reg e x()
