# cs .regex()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=A1.cursor()
=A2.regex("(.*),(A.*)";id,name)
=A2.fetch()
=A1.cursor()
=A2.regex@c("(.*),(A.*)";id,name)
=A2.fetch()
=A1.cursor()
=A2.regex@u("(\\u9500\\u552e\\u90e8),(.*)"; 部门 , 员工姓名 )
=A2.fetch()
=demo.cursor("select EID,NAME,SALARY from
=A1.regex@p("(.*),(V.*),(.*)";id,name,salary)
=A2.fetch()
```

## 提取内容
cs .regex()
说明：
Attach
the action of matching the regular expression to a cursor and return the
original cursor.
语法：
cs .regex( rs,F i ,… )
注意：
The
function attaches a computation to cursor cs ,
which matches the string members in cursor cs
with regular expression rs , forms a
table sequence consisting of F i
fields and returns it to the original cursor cs . It supports multicursors.
This
is a delayed function.
参数：
|
cs
|
cursor whose members are strings.
|
rs
|
Regular
expression.
|
F i
|
Resulting
field name.
选项：
|
@ c
|
Case insensitive .
|
@ u
|
Use Unicode to match .
|
@p
|
Parse numeric strings into numbers.
返回值：
游标
示例：
|
["1,Rebecca","2,ashley","3,Rachel","4,Emily","5,Ashley","6,Matthew",
"7,Alexis","8,Megan","9,Victoria","10,Ryan"]
|
=A1.cursor()
|
Return
a cursor.
|
=A2.regex("(.*),(A.*)";id,name)
|
Attach a computation to cursor A2, which will match
members starting with A after the comma and return the original cursor A2
the computation is by default case sensitive.
|
=A2.fetch()
|
Fetch data from
cursor A2 where A3’s computation is executed:
（原文含示意图，图像资源未迁移）
Use
@c option to enable a case-insensitive computation:
|
["1,Rebecca","2,ashley","3,Rachel","4,Emily","5,Ashley","6,Matthew",
"7,Alexis","8,Megan","9,Victoria","10,Ryan"]
|
=A1.cursor()
|
Return
a cursor.
|
=A2.regex@c("(.*),(A.*)";id,name)
|
Attach a computation to cursor A2, which will match
members starting with A or a after the comma and return the original cursor
A2; @c option works to enable a case-insensitive computation.
|
=A2.fetch()
|
Fetch data from
cursor A2 where A3’s computation is executed:
（原文含示意图，图像资源未迁移）
When
@u option works:
|
[" 销售部 , 李英梅 "," 人事部 , 王芳 "," 技术部 , 张峰 "," 销售部 , 孙超 "]
|
=A1.cursor()
|
Return
a cursor.
|
=A2.regex@u("(\\u9500\\u552e\\u90e8),(.*)"; 部门 , 员工姓名 )
|
Attach a computation to cursor A2, which, with @u
option, will match members starting with " 销售部 " and
return the original cursor A2.
|
=A2.fetch()
|
Fetch data from
cursor A2 where A3’s computation is executed:
（原文含示意图，图像资源未迁移）
When
@p option is present:
|
=demo.cursor("select EID,NAME,SALARY from
EMPLOYEE").(~. array().concat@c())
|
Return
a cursor, whose content is as follows:
（原文含示意图，图像资源未迁移）
|
=A1.regex@p("(.*),(V.*),(.*)";id,name,salary)
|
@p option works to
parse numeric strings in the result table sequence into numbers.
|
=A2.fetch()
|
Fetch data from
cursor A1, perform A2’s computation ,and return the following result:
（原文含示意图，图像资源未迁移）
相关函数：
s .rege x ()
A .reg e x()
