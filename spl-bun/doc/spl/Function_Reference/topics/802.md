# register()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
register()
register ( f,spl )
=connect("demo")
=A1.query("select
=register("df1","D:/emp.splx")
=df1("HR","M")
>opt="order by
>opt="order by
="select
=connect("demo").query(A3,arg2)
=register@o("df2","D:/emp2.splx")
=df2@z("F")
=df2@a("M")
=df2("M")
```

## 提取内容
register()
说明：
Register a .dfx/.splx/.spl
file as a function to be called.
语法：
register ( f,spl )
注意：
The function registers a script file spl as function f to call
it in a cellset script. Syntax of the function expression is f(x i ,...),
where x i ,... is a parameter in the spl file.
Use the comma to separate multiple parameters.
The function name is the one registered the last time.
The function cannot work in a multithreaded environment.
参数：
|
f
|
Function
name.
|
spl
|
.dfx/.splx/.spl file path, which can be an absolute
path or a relative path; a relative path is relative to the main directory .
选项：
|
@ o
|
In
a function registered with @o, o in the invocation expression fn @o is the first (string) parameter
value. If no option is specified in the invocation expression, the first
(string) parameter value passed is regarded as null.
|
@j
|
Register
the function for a specific task; by default, register it for the global use.
Return
value:
Result of executing the script file
示例：
Below is emp. splx , where arg1 and arg2 are
cellset parameters:
|
=connect("demo")
|
=A1.query("select
EID,NAME,DEPT,GENDER,SALARY from employee where DEPT=? and
GENDER=?",arg1,arg2)
|
return A2
Register emp. splx as a function and call it:
|
=register("df1","D:/emp.splx")
|
Register
emp.splx as a function named df1 .
|
=df1("HR","M")
|
Call
the registered function and pass parameter values:
（原文含示意图，图像资源未迁移）
Below is emp2. splx , where opt and arg2 are
cellset parameters:
|
if opt=="z"
|
>opt="order by
SALARY desc"
|
else if opt=="a"
|
>opt="order by
SALARY"
|
="select
EID,NAME,GENDER,SALARY from employee where GENDER=? "+opt
|
=connect("demo").query(A3,arg2)
|
Use @o option to register a function:
|
=register@o("df2","D:/emp2.splx")
|
Use
@o mtethod to register emp2.splx as a function named df2
We
can use @z option, which performs a sorting by SALARY in descending order,
and @a option, which performs a sorting by SALARY in ascending order, with
function df2 ; do not perform a sorting when both options are absent.
|
=df2@z("F")
|
Call
function df2 and use @z option to perform a sorting by SALARY in
descending order
Perform
the computation, where in emp2.splx value of the first string
parameter is z and that of parameter arg2 is F, and return result as
follows:
（原文含示意图，图像资源未迁移）
|
=df2@a("M")
|
Call
function df2 and use @a option to perform a sorting by SALARY in
ascending order
Perform
the computation, where in emp2.splx value of the first string
parameter opt is a and that of parameter arg2 is M, and return
result as follows:
（原文含示意图，图像资源未迁移）
|
=df2("M")
|
Call
function df2 and, since both parameter @z and @a are absent, sorting
will not be performed
Perform
the computation, where in emp2.splx value of the first string
parameter opt is null and that of parameter arg2 is M, and
return result as follows:
（原文含示意图，图像资源未迁移）
Below is emp. splx :
