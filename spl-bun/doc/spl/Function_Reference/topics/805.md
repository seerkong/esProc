# T. rename()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select * from EMPLOYEE")
=A1.rename(EID:ID)
=file("D:\\emp1.ctx")
=A3.open()
=A4.rename(EID:EID2)
=A5.cursor().fetch()
=A1.rename(SURNAME:Sname,NAME1:name)
cs.rename ()
=demo.cursor("select EID,NAME,GENDER from employee")
=A1.rename(EID,NAME,GENDER)
=A1.rename(EID:ID,GENDER:SEX).fetch()
```

## 提取内容
T. rename()
说明：
Rename the fields in a table sequence/(multizone)
entity table.
语法：
T .rename ( F , F' ,… )
注意：
The function renames F field F' , or renames a composite table’s attached
table and returns a table sequence with renamed T
fields .
参数：
|
F
|
field name; ignore the parameter if the field name does not exist in T .
|
F'
|
The
modified field name; when omitted, the original field names will be deleted
and replaced by _1, _2….
|
T
|
Table
sequence/entity table/multizone entity table.
返回值：
Table sequence
示例：
|
=demo.query("select * from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.rename(EID:ID)
|
（原文含示意图，图像资源未迁移）
|
=file("D:\\emp1.ctx")
|
=A3.open()
|
Open
a composite table.
（原文含示意图，图像资源未迁移）
|
=A4.rename(EID:EID2)
|
Rename
EID field EID2.
|
=A5.cursor().fetch()
|
（原文含示意图，图像资源未迁移）
|
=A1.rename(SURNAME:Sname,NAME1:name)
|
Ignore field name
NAME1 because it does not exist in A1 .
（原文含示意图，图像资源未迁移）
cs.rename ()
说明：
Rename a field of a cursor.
语法：
cs .rename( F : F' ,… )
注意：
The function renames F field
in cursor cs F' .
参数：
|
F
|
Field
name.
|
F'
|
New name of the field; If omitted, delete
the original names and name the fields _1,2... .
|
cs
|
cursor.
Return
value:
游标
示例：
|
=demo.cursor("select EID,NAME,GENDER from employee")
|
Return
a cursor.
|
=A1.rename(EID,NAME,GENDER)
|
Following
is content of the table whose fields are renamed:
（原文含示意图，图像资源未迁移）
Since parameter is omitted, delete the original
field names and name them _1, _2, _3 .
|
=A1.rename(EID:ID,GENDER:SEX).fetch()
|
Rename
EID field ID, GENDER field SEX, and return result as follows:
（原文含示意图，图像资源未迁移）
