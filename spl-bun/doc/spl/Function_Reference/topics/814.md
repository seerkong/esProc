# report_open()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
report_open()
report_open ( T )
=report_open("D:\\testT.docx")
=report_open("testT2.docx")
>report_config("config\\ raqsoftConfigReportLite .xml")
=report_open("testT3.docx")
report_insert()
report_replace()
report_write()
```

## 提取内容
report_open()
说明：
Read
a template file of docx format.
语法：
report_open ( T )
注意：
Report5Cli
external library function (See The
Programmer’s Reference).
参数：
|
T
|
The
path of docx format template file, which can be an absolute one or a relative
one. When it is the relative path, the base directory is the path specified
in esProc main directory’s < mainPath > node in the configuration file.
Return
value:
DocxChanger
object
示例：
|
=report_open("D:\\testT.docx")
|
Read
template file testT.docx .
|
=report_open("testT2.docx")
|
When
the relative path is used for reading the template file, the base directory
is the path specified in esProc main directory < mainPath > node in the
configuration file.
|
>report_config("config\\ raqsoftConfigReportLite .xml")
|
=report_open("testT3.docx")
|
After
report_config() re-loads the configuration file and when the relative path is
used for reading the template file, the base directory is the path specified
in esProc main directory < mainPath > node in the configuration file.
function:
report_insert()
report_replace()
report_write()
