# report_run()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
report_run()
report_run ( rpt , paramValue : paramName ;ds:dsName ,.... )
>report_config("config\\raqsoftConfigReport.xml")
=report_open("D:\\test.rpx")
=report_run(A2)
=report_open("D:\\test1.rpx")
=report_run(A4,"abc":"arg1",12:"arg2","2016-01-02":"arg3")
=report_run(A4,"abc":,12:,"2016-01-02":)
=report_open("D:\\stu.rpx")
=connect("demo")
=A8.query("select *
=report_run(A7;A9:"ds1")
=report_run(A7;A9:)
report_config()
```

## 提取内容
report_run()
说明：
Compute
a report/report group file.
语法：
report_run ( rpt , paramValue : paramName ;ds:dsName ,.... )
注意：
This
Report5Cli external library function (See
The Programmer’s Reference) performs computation over a report file.
参数：
|
rpt
|
report object.
|
paramValue
|
Report
parameter value; can be absent.
|
paramName
|
Parameter name
by default, write values in the order of parameters in the report .
|
ds
|
table sequence object; can be absent.
|
dsName
|
Dataset
name; by default, correspond table sequence objects and the original data
sets in the report file one by one; the colon dsName before should not be omitted.
Return
value:
Boolean
示例：
|
>report_config("config\\raqsoftConfigReport.xml")
|
=report_open("D:\\test.rpx")
|
=report_run(A2)
|
Compute
the report file test.rpx that doesn’t
have any parameters.
|
=report_open("D:\\test1.rpx")
|
（原文含示意图，图像资源未迁移）
The
report file test1.rpx has 3
parameters.
|
=report_run(A4,"abc":"arg1",12:"arg2","2016-01-02":"arg3")
|
Compute
a report file having several parameters.
|
=report_run(A4,"abc":,12:,"2016-01-02":)
|
As
the report parameter names are omitted, the parameter values are set in its
original order, with the colon retained.
|
=report_open("D:\\stu.rpx")
|
Report
stu.rpx uses SCORES table in demo database; data set name is ds1.
|
=connect("demo")
|
=A8.query("select *
from SCORES where SCORE<60")
|
Return
a table sequence.
|
=report_run(A7;A9:"ds1")
|
Take
data of table sequence A9 as the report data set ds1’s result set.
|
=report_run(A7;A9:)
|
When
parameter dsName is absent, write values in the order of data sets in
the report.
function:
report_config()
report_op e n()
