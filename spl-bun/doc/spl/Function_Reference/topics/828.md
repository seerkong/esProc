# cs.reset()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.reset()
cs.reset()
=demo.cursor("select
=A1.fetch()
=A1.fetch()
=A1.reset()
=A1.fetch()
cs.skip()
```

## 提取内容
cs.reset()
说明：
Reset
a cursor by moving it back to the beginning.
语法：
cs.reset()
注意：
The
function resets a cursor by moving it back to the beginning when it traverses cursor
data to the end.
注意：
游标s returned from certain functions are irreversible, including
db.cursor@x(), f.cursor@x(), cs.groupx(), cs.sortx(), cs.joinx() and cursor ( spl ,…).
参数：
|
cs
|
cursor.
Return
value:
游标
示例：
|
=demo.cursor("select
* from scores")
|
Return retrieved
data as a cursor .
|
=A1.fetch()
|
Fetch data from
the cursor .
（原文含示意图，图像资源未迁移）
|
=A1.fetch()
|
Return
null because A1’s cursor has come to the end of the data.
|
=A1.reset()
|
Move the cursor
back to the beginning .
|
=A1.fetch()
|
Fetch data out of
the cursor; returned result is the same as A2 .
相关函数：
cs.skip()
