# T.reset ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.reset ()
=demo.query("select
=A1.reset()
```

## 提取内容
T.reset ()
说明：
Clear
the members of a table sequence.
语法：
T .reset()
注意：
The
function clears members from table sequence T , but reserves the data structure.
参数：
|
T
|
table sequence.
返回值：
Empty
table sequence
示例：
|
=demo.query("select
* from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.reset()
|
（原文含示意图，图像资源未迁移）
