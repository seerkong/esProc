# db.rollback()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
db.rollback()
=create(ID,NAME,GENDER,AGE).record([3,
=connect@e("demo")
=A2.savepoint("s1")
>A2.execute@k(A1,"update
=A2.error()
>A2.commit()
>A2.rollback("s1")
>A2.close()
```

## 提取内容
db.rollback()
说明：
Roll
back a database transaction.
语法：
db . rollback ( spn )
注意：
The
function rolls back the transaction to the specified savepoint. Rollback all transactions
when the parameter is omitted.
参数：
|
db
|
Database connection .
|
spn
|
Savepoint name .
示例：
|
=create(ID,NAME,GENDER,AGE).record([3,
"AAA","F",14])
|
=connect@e("demo")
|
Establish
a connection and use @e option to automatically control the commit and
rollback operations.
|
=A2.savepoint("s1")
|
set
the savepoint named s1.
|
>A2.execute@k(A1,"update
STUDENTS2 set NAME=?,GENDER=?,AGE=? where ID=?",NAME,GENDER,AGE,ID)
|
Execute
the sql statement, update table STYDENTS2 according to A1’s records, and use
@k option to not to commit the transaction.
|
=A2.error()
|
Get
the error code generated from the execution of the previous SQL statement.
|
if A5==0
|
>A2.commit()
|
Commit
if there is not an error.
|
else
|
>A2.rollback("s1")
|
Roll
back if there is an error.
|
>A2.close()
|
Close
the connection.
相关函数：
db .clos e ()
db .err o r( )
db .c o m m it ( )
connec t ()
