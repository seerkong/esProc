# k. row()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.query("select
=A1.keys(ORDERID)
=2.row(A1)
=A1.select()
=A1.keys(ORDERID,SELLERID)
=[1,17].row(A1)
A.find()
```

## 提取内容
k. row()
说明：
Find the record where the key value(s)
is/are the specified one(s).
语法：
k .row(T)
注意：
The function finds the record whose key
value is k in a record sequence/a
table sequence/a sequence.
参数：
|
k
|
A key value; multiple key values will be
represented by a sequence .
|
T
|
A record sequence/a table sequence/a
sequence .
返回值：
记录
示例：
|
=demo.query("select
* from sales")
|
（原文含示意图，图像资源未迁移）
|
=A1.keys(ORDERID)
|
Set the key .
|
=2.row(A1)
|
（原文含示意图，图像资源未迁移）
Find the record whose key value is 2 .
|
=A1.select()
|
Generate a record sequence from A1’s table
sequence .
|
=A1.keys(ORDERID,SELLERID)
|
Set multiple keys .
|
=[1,17].row(A1)
|
（原文含示意图，图像资源未迁移）
Find the record where key values are [1,17] .
相关函数：
A.find()
