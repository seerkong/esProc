# k.row()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
k.row()
=demo.cursor("select EID,NAME,GENDER from employee where EID<
=A1.memory()
=A2.keys(EID)
=2.row(A2)
=A2.keys(EID,GENDER)
=[3,"F"].row(A2)
```

## 提取内容
k.row()
说明：
Find
records from an in-memory table according to the specified primary key
value(s).
语法：
k .row( T )
注意：
The
function finds records from in-memory table T
according to the specified primary key value(s) k .
参数：
|
k
|
Primary
key value(s); use a sequence to represent multiple key values.
|
T
|
An
in-memory table.
Return
value:
记录
示例：
|
=demo.cursor("select EID,NAME,GENDER from employee where EID<
10")
|
Return a cursor.
|
=A1.memory()
|
Return an in-memory table.
（原文含示意图，图像资源未迁移）
|
=A2.keys(EID)
|
Set EID as the in-memory table’s key.
|
=2.row(A2)
|
Find the record where the primary key value
is 2.
（原文含示意图，图像资源未迁移）
|
=A2.keys(EID,GENDER)
|
Set EID and NAME as the keys of the in-memory
table
|
=[3,"F"].row(A2)
|
Find the record where the primary key value
is [3,"F"] .
（原文含示意图，图像资源未迁移）
