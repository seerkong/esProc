# r .run( x i ,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=[[12,23]].new(~(1):col1,~(2):col2)
=A1(1).run(col1=col2+1)
=A1(1).run(col1=col2+1,col2=col1+col2)
```

## 提取内容
r .run( x i ,… )
说明：
Compute
an expression against a record and
return the modified record.
语法：
r .run( x i,… )
注意：
The
function computes the expression x against record r and return the modified
record. Usually, it is used to change the field values of r , for example, when x is col1=col2+1 , it will change the field value of col1 .
参数：
|
r
|
record.
|
x i
|
An
expression, which is generally a field name or a legal expression composed of
field names, and in which " ~ " references the current record.
返回值：
The modified
record
示例：
|
=[[12,23]].new(~(1):col1,~(2):col2)
|
=A1(1).run(col1=col2+1)
|
（原文含示意图，图像资源未迁移）
|
=A1(1).run(col1=col2+1,col2=col1+col2)
|
（原文含示意图，图像资源未迁移）
注意：
Difference
between r .( x )
and r .run( x ) : r .( x )
computes the value of expression x and returns it; r .run( x )
modifies r through the comp utation
of x, and
returns the modified r .
相关函数：
A .run( x 1 , x 2 ,… x i )
P. r u n(xi : F i , …)
