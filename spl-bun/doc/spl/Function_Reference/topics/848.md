# P.run(x i :F i ,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.run(x i :F i ,… )
=demo.query("select
=A1.derive(age(BIRTHDAY):Age)
=A2.run(Age+10:Age)
```

## 提取内容
P.run(x i :F i ,… )
说明：
Compute expressions against each member of a
record sequence.
语法：
P . run ( x i : F i ,…)
注意：
The function computes expression x i
for each record in record sequence/table sequence P , assigns the results to F i field and returns P that has been modified. The expression x i uses " ~ " to reference the
current member of P .
参数：
|
P
|
record sequence/table sequence.
|
x i
|
The
new value of F i field .
|
F i
|
field of A .
返回值：
Table
sequence
示例：
|
=demo.query("select
DEPT,NAME,BIRTHDAY from EMPLOYEE")
|
（原文含示意图，图像资源未迁移）
|
=A1.derive(age(BIRTHDAY):Age)
|
（原文含示意图，图像资源未迁移）
|
=A2.run(Age+10:Age)
|
（原文含示意图，图像资源未迁移）
相关函数：
r .run ( )
A .run ( x 1 , x 2 , … x i )
