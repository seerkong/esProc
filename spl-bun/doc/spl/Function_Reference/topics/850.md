# ch. run()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.cursor("select * from EMPLOYEE")
=channel()
=A2.run(SALARY+1000:SALARY)
=A2.fetch()
=A1.push(A2)
=A1.fetch()
=A2.result()
```

## 提取内容
ch. run()
说明：
Attach
a computation that assigns values to a specified field and return the original
channel.
语法：
ch. run ( x i : F i ,…)
注意：
The
function attaches a computation to channel ch ,
which will compute expression x i on
each of its records and assign the results to F i field, and returns the original channel ch .
This
is an attachment computation.
参数：
|
ch
|
channel.
|
x i
|
An
expression.
|
F i
|
Field
name in the given channel.
Return
value:
管道
示例：
|
=demo.cursor("select * from EMPLOYEE")
|
=channel()
|
Create
a channel.
|
=A2.run(SALARY+1000:SALARY)
|
Attach
a computation to A2’s channel, which will evaluate expression SALARY+1000 and
assign the results to SALARY field, and return the original channel.
|
=A2.fetch()
|
Execute
the result fetching function in channel A2 and keep the current data in
channel.
|
=A1.push(A2)
|
Be
ready to push cursor A1’s data to channel A2, but the action needs to wait.
|
=A1.fetch()
|
Fetch
data from cursor A1 while pushing data to channel A2 to execute the attached
computation and keep the result.
|
=A2.result()
|
Get channel A2’s
result:
（原文含示意图，图像资源未迁移）
