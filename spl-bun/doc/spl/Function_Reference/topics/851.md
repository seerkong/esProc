# cs. run()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=connect("demo").cursor("select
=A1.run(age(HIREDATE):HIREDATE)
=A1.run((HIREDATE * 2 *
=A1.fetch()
A.run( xi: F i, … )
```

## 提取内容
cs. run()
说明：
Attach
the action of computing expression on each record to a cursor and return the
original cursor.
语法：
cs. run ( x i : F i ,…)
注意：
The function
attaches a computation to cursor cs , which will compute expression x i over each record in cursor cs and
assign each result to F i field, and returns the modified
cursor cs . It supports multicursors.
This
is a delayed function.
参数：
|
cs
|
cursor.
|
x i
|
An
expression.
|
F i
|
field of cs .
返回值：
游标
示例：
|
=connect("demo").cursor("select
EID,NAME,SALARY,HIREDATE,0 as SUBSIDY from EMPLOYEE ")
|
Return
a cursor whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=A1.run(age(HIREDATE):HIREDATE)
|
Attach
a computation to cursor A1, which will assign results of computing
age(HIREDATE) to HIREDATE, and return the original cursor A1.
|
=A1.run((HIREDATE * 2 *
SALARY):SUBSIDY)
|
Attach
a computation to cursor A1, which will assign results of computing HIREDATE *
2 * SALARY to SUBSIDY, and return the original cursor A1.
|
=A1.fetch()
|
Fetch
data from cursor A1 where both A2 and A3’s computations are executed (it
would be better to fetch data in batches when a large amount of data is
involved):
（原文含示意图，图像资源未迁移）
相关函数：
A.run( xi: F i, … )
