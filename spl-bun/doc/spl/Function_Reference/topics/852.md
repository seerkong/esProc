# T.run ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.run ()
=create(file).record(["emp.ctx"])
=pseudo(A1)
=A2.run(SALARY+1000:SALARY)
=A3.import()
=A2.run(if(GENDER=="F",1,2):GENDER)
=A5.import()
```

## 提取内容
T.run ()
说明：
Define
value assignment operation on a pseudo table and return a new pseudo table.
语法：
T. run ( x i : F i ,…)
注意：
The
function defines a computation on pseudo table T , which will compute expression x i ,
and assign the results to F i , and returns a new pseudo table. Only supported by esProc
Enterprise Edition.
参数：
|
T
|
pseudo table.
|
x i
|
An
expression.
|
F i
|
pseudo table field.
Return
value:
Pseudo
table
示例：
|
=create(file).record(["emp.ctx"])
|
Below
is content of composite table emp.ctx :
（原文含示意图，图像资源未迁移）
|
=pseudo(A1)
|
Generate
a pseudo table object.
|
=A2.run(SALARY+1000:SALARY)
|
Define
a computation on A2’s pseudo table, which will add 1,000 to pseudo table’s
field SALARY and assign the results to the field, and return a new pseudo
table.
|
=A3.import()
|
Import
data from A3’s pseudo table while executing the computation defined in A3 on
A2’s pseudo table, and return the following table:
（原文含示意图，图像资源未迁移）
|
=A2.run(if(GENDER=="F",1,2):GENDER)
|
Define
a computation on A2’s pseudo table, which will compute its field GENDER –
return 1 if GENDER field value is F and return 2 if it isn’t – assign the
results to GENDER, and return a new pseudo table.
|
=A5.import()
|
Import
data from A5’s pseudo table while executing the computation defined in A5 on
A2’s pseudo table, and return the following table:
（原文含示意图，图像资源未迁移）
