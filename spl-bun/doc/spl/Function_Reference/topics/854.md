# A.rvs ()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.rvs ()
=[1,2,3,4].rvs()
=demo.query("select
=A2.rvs()
```

## 提取内容
A.rvs ()
说明：
Generate
a new sequence by reversing the members in a sequence.
语法：
A .rvs()
注意：
The
function generates a new sequence by reversing the members in sequence A to make the last member
the first member, and second-to-last one the second one, and so forth .
参数：
|
A sequence .
返回值：
The
reversed sequence A
示例：
|
=[1,2,3,4].rvs()
|
[4,3,2,1] .
|
=demo.query("select
* from DEPARTMENT")
|
（原文含示意图，图像资源未迁移）
|
=A2.rvs()
|
（原文含示意图，图像资源未迁移）
相关函数：
A .p s ort()
A .s o r t ( )
A .s w a p ( p , q )
