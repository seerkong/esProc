# r.alter( F i ,…; F’ i ,… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.alter( F i ,…; F’ i ,… )
r.alter( F i ,…; F’ i ,… )
=demo.query("select
=A1(1)
=A2.alter(classname;CLASS)
=A2.alter(classname,STUDENTID,gender)
=A2.alter(;CLASS)
```

## 提取内容
r.alter( F i ,…; F’ i ,… )
说明：
Alter fields of a record.
语法：
r.alter( F i ,…; F’ i ,… )
注意：
The function alters fields of record r in the
order of F i ,…. If
the to-be-altered fields are not specified, perform the modification according
to the original order of fields; if field F i does not exist
in the record, add it as a new field. F’ i is the
to-be-deleted fields.
参数：
|
r
|
record.
|
F i
|
Field
names.
|
F’ i
|
Names
of to-be-deleted fields.
返回值：
记录
示例：
|
=demo.query("select
top 9 class,studentid,subject,score from scores")
|
Return a table sequence:
（原文含示意图，图像资源未迁移）
|
=A1(1)
|
Return the 1 st record of table sequence
A1:
（原文含示意图，图像资源未迁移）
|
=A2.alter(classname;CLASS)
|
Add classname field to A2’s record while
deleting CLASS field:
（原文含示意图，图像资源未迁移）
|
=A2.alter(classname,STUDENTID,gender)
|
Add
classname field and gender field to A2’s record according to the order classname,STUDENTID,gender
put the rest of the fields CLASS,S UBJECT,SCORE after them:
（原文含示意图，图像资源未迁移）
|
=A2.alter(;CLASS)
|
Delete
CLASS field from A2’s record:
（原文含示意图，图像资源未迁移）
