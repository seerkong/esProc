# sap_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sap_close()
sap_close (client)
=sap_client@f ("C:/sap/ABAP_AS_WITH_POOL.jcoDestination")
=sap_close(A1)
```

## 提取内容
sap_close()
说明：
Exit
an SAP server connection.
语法：
sap_close (client)
注意：
This
SapCli external library function (See External Library Guide ) closes
the connection to an SAP server.
参数：
|
client
|
SAP
connection file handle.
示例：
|
=sap_client@f ("C:/sap/ABAP_AS_WITH_POOL.jcoDestination")
|
SAP
server connection.
|
=sap_close(A1)
|
Exit
the SAP server connection.
