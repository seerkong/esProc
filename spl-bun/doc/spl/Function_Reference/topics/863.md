# sap_cursor()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sap_cursor()
=sap_client@f ("C:/sap/ABAP_AS_WITH_POOL.jcoDestination")
=sap_cursor(A1, "Z_TEST1","IT_ROOM")
=A2.fetch(n)
```

## 提取内容
sap_cursor()
说明：
Return
the list of table-valued result set as a cursor.
语法：
sap _ cursor (client,functionName,tableName)
注意：
This
SapCli external library function (See External Library Guide ) returns
the list of table-valued result set as a cursor.
参数：
|
client
|
SAP
server connection handle.
|
functionName
|
The
to-be-called function.
|
tableName
|
Table
name.
Return
value:
list table cursor
示例：
|
=sap_client@f ("C:/sap/ABAP_AS_WITH_POOL.jcoDestination")
|
SAP
server connection.
|
=sap_cursor(A1, "Z_TEST1","IT_ROOM")
|
Return
the table-valued list as a cursor.
|
=A2.fetch(n)
|
Fetch
n rows of data from the
table-valued list.
