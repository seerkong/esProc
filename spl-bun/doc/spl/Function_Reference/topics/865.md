# sap_getparam()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sap_getparam()
sap_getparam (excutefunction, paramName)
=sap_client@f ("C:/sap/ABAP_AS_WITH_POOL.jcoDestination")
=sap_execute(A1, "Z_TEST1",”ROOMID”:”001”)
=sap_getparam(A2,"RETURN")
```

## 提取内容
sap_getparam()
说明：
Get
data records according to the import parameter(s).
语法：
sap_getparam (excutefunction, paramName)
注意：
With
a to-be-executed function that has parameters, this SapCli
external library function (See External Library Guide ) gets
data records according to the import parameter(s).
参数：
|
excutefunction
|
The
execute function handle.
|
paramName
|
Function
parameter name.
Return
value:
Data
records
示例：
|
=sap_client@f ("C:/sap/ABAP_AS_WITH_POOL.jcoDestination")
|
SAP
server connection.
|
=sap_execute(A1, "Z_TEST1",”ROOMID”:”001”)
|
Execute
the sap function.
|
=sap_getparam(A2,"RETURN")
|
