# sap_table()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sap_table()
sap_table (excutefunction, tableName)
=sap_client@f ("C:/sap/ABAP_AS_WITH_POOL.jcoDestination")
=sap_execute(A1, "Z_TEST1")
=sap_table(A2, "IT_ROOM")
```

## 提取内容
sap_table()
说明：
Return
a list according to the import table-valued parameter.
语法：
sap_table (excutefunction, tableName)
注意：
This
SapCli external library function (See External Library Guide ) returns
a list according to the import table-valued parameter.
参数：
|
excutefunction
|
The
execute function handle.
|
tableName
|
Table
name.
Return
value:
Table-valued
list
示例：
|
=sap_client@f ("C:/sap/ABAP_AS_WITH_POOL.jcoDestination")
|
SAP
server connection.
|
=sap_execute(A1, "Z_TEST1")
|
Execute
the sap_function that hasn’t parameters
|
=sap_table(A2, "IT_ROOM")
|
Return
a table-valued list.
