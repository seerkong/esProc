# db. savepoint()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=connect("demo")
=A1.savepoint("num1")
=A1.execute@k("delete from DEPARTMENT where DEPT='TecSupport'")
=A1.rollback("num1")
```

## 提取内容
db. savepoint()
说明：
Set
the savepoint to which you can later rollback.
语法：
db .savepoint ( spn )
注意：
The
function creates a save point named spn .
The save point name must not be omitted and repeated .
参数：
|
db
|
Database
connection.
|
spn
|
Savepoint
name.
返回值：
Boolean
示例：
|
=connect("demo")
|
=A1.savepoint("num1")
|
Set
a savepoint named " num1 " .
|
=A1.execute@k("delete from DEPARTMENT where DEPT='TecSupport'")
|
Execute
the sql statement and later do not commit the transaction.
|
=A1.rollback("num1")
|
Roll
back to " num1 " .
