# k.sbs()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
k.sbs()
k.sbs(a:b,…)
k(4,5,6,8).sbs(2)
k(4,5,6,8).sbs(1:3)
k(4,5,6,8).sbs(2:)
k(4,5,6,8).sbs(:4)
```

## 提取内容
k.sbs()
说明：
Get an integer consisting of certain bytes of a
serial byte key.
语法：
|
k.sbs(a:b,…)
|
Get
the integer made up of bytes from the a th to the b th
in serial byte key k
注意：
The function gets a certain byte a or certain bytes from a
to b to form an integer.
参数：
|
k
|
A serial byte key .
|
a
|
The a th byte of serial byte k ; begin from the first byte
when this parameter is absent .
|
b
|
The b th byte of serial byte k ; stop at the last byte when
this parameter is absent, and get the a th byte only when :b is absent .
Return
value:
Integer
示例：
|
k(4,5,6,8).sbs(2)
|
5 ; get the second byte from serial byte key k(4,5,6,8) to generate an integer .
|
k(4,5,6,8).sbs(1:3)
|
263430 ; get the bytes from 1 to 3 from serial
byte key k(4,5,6,8) to generate an integer .
|
k(4,5,6,8).sbs(2:)
|
329224 ; get bytes from the second to the end from serial byte key k(4,5,6,8)
to generate an integer .
|
k(4,5,6,8).sbs(:4)
|
67438088 ; get byte from the second to the fourth
from serial byte key k(4,5,6,8) to generate an integer .
