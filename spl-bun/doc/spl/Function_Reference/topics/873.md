# se()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
se()
A.se()
=A1.se()
```

## 提取内容
se()
说明：
Calculate the standard
error of a numeric sequence.
语法：
A.se()
注意：
MathCli external library function (See External Library Guide ). In the formula SEM=
（原文含示意图，图像资源未迁移）
, SD is the standard error.
参数：
|
A numeric sequence .
返回值：
Double-precision number
示例：
|
[0,6,5,2,0,2,1,0]
|
=A1.se()
|
（原文含示意图，图像资源未迁移）
Return A1’s standard error .
