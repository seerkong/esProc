# second()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
second()
second( datetimeExp )
second(datetime("19800227","yyyyMMdd"))
```

## 提取内容
second()
说明：
Get
the second from a specified datetime value.
语法：
second( datetimeExp )
注意：
The
function gets the second from the datetime value
datetimeExp .
参数：
|
datetimeExp
|
Expression
whose result is a datetime value.
返回值：
Integer
示例：
|
second(datetime("19800227","yyyyMMdd"))
|
second("1972-11-08
10:20:30")
|
second(datetime("2006-01-15
13:20:45"))
|
相关函数：
yea r ( )
mon t h()
da y ()
hou r ()
minu t e()
millis e c o nd()
