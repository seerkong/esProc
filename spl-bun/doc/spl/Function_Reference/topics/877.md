# A. select()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=A1.select(~>3)
=A1.select@1(~>3)
=demo.query("select EID,NAME,GENDER,DEPT,SALARY from EMPLOYEE order by EID")
=A4.select(GENDER:"F",SALARY:16000)
=A1.select (~>8)
=A1.select@0(~>8)
=A1.select(~>3)
=A1.select@z(~>3)
=A4.select@c(~>7)
=A4.select@zc(~>6)
=A4.select@r(~>10)
=demo.query("select
=A1.select@m(GENDER=="F")
=A1.sort(EID)
=A3.select@b(EID<10)
=to(3).new(~:ID,~*~:A1)
=A1.select(A1.A1==4)
=demo.query("select
=A1.i()
=A2.select(GENDER=="M")
=A2.select@v(GENDER=="F")
T.select@i()
=demo.query("select
=A1.select@i(FATHER == 12)
```

## 提取内容
A. select()
说明：
Return
members of a sequence which satisfy a condition.
语法：
|
A .select( x )
|
A .select( x 1 : y 1 , x 2 : y 2 , ...... x i : y i )
|
The
simplified syntax of "&&" for a multiple-condition query,
which is equal to A .select ( x 1 == y 1 && x 2 == y 2 && ...... x i == y i )
注意：
The
function computes expression x against each
member of the sequence A , and returns a new
sequence composed of those members which make x return true. When there are no parameters, it returns all the
members. Note that if the name of a to-be-referenced column of the sequence is
the same as a cell name, the sequence name should be attached before the column
name in the expression.
参数：
|
A sequence.
|
x
|
Boolean expression, which can be null.
|
x i : y i
|
x i is an expression, and y i is a comparing value.
选项：
|
@1
|
Return
the first member that fulfills the condition; or return null if no eligible
mebers are found.
|
@z
|
Search
the members from back to front.
|
@b
|
Use binary
search, which requires that A is an ascendingly
ordered sequence and that parameters are in the format of x:y or they are
expressions returning numeric values; the eligible members are found when
result of expressions are 0.
|
@m
|
Use parallel algorithm to handle
data-intensive or computation-intensive tasks; no definite order for the
records in the result set; it can’t be used with @1bz options.
|
@t
|
Return an empty table sequence with data
structure if the grouping and aggregate operation over the sequence returns
null.
|
@c
|
Enable
getting the eligible member(s) beginning from the first member until the
first ineligible member that makes Boolean expression x false appears.
|
@r
|
Enable
searching for the first eligible members from left to right and getting all
members after it (inclusive) until the last one.
|
@v
|
Return
result as a pure table sequence when sequence A is a pure table
sequence; return a pure sequence when this option is absent. Only supported
by esProc Enterprise Edition.
|
@0
|
Return
null when no eligble member are found; by default, return an empty sequence.
返回值：
Sequence/Table
sequence
示例：
|
[2,5,4,3,2,1,4,1,3]
|
=A1.select(~>3)
|
[5,4,4] .
|
=A1.select@1(~>3)
|
5; return the first eligible member .
|
=demo.query("select EID,NAME,GENDER,DEPT,SALARY from EMPLOYEE order by EID")
|
=A4.select(GENDER:"F",SALARY:16000)
|
Multi-condition
query .
（原文含示意图，图像资源未迁移）
|
=A1.select (~>8)
|
Return
an empty sequence.
|
=A1.select@0(~>8)
|
Return
null.
.
Specify
search direction:
|
[2,5,4,3,2,1,4,1,3]
|
=A1.select(~>3)
|
[5,4,4].
|
=A1.select@z(~>3)
|
[4,4,5]
search backwards.
|
[8,10,3,5,7,9,11,13,7]
|
=A4.select@c(~>7)
|
Return
[8,10]; search from the first member until the first ineligible member
appears.
|
=A4.select@zc(~>6)
|
Return
[7,13,11,9,7]; search backwards until the first ineligible member appears.
|
=A4.select@r(~>10)
|
Return
[11,13,7]; Search for the first eligible member from the first member in
order and get all members after it (inclusive).
High-efficiency
search and filtering:
|
=demo.query("select
EID,NAME,GENDER,DEPT,BIRTHDAY from employee")
|
=A1.select@m(GENDER=="F")
|
@m
option enables parallel computation when there is a large volume of data to
increase performance.
|
=A1.sort(EID)
|
Sort
A1 by EID in the ascending order.
|
=A3.select@b(EID<10)
|
As
A3 is an ascending sequence, here we use binary search to perform the query,
for which parameter x should be a numeric expression.
When
a column name and a cell name are same:
|
=to(3).new(~:ID,~*~:A1)
|
（原文含示意图，图像资源未迁移）
|
=A1.select(A1.A1==4)
|
As the column name and the cell name are same,
the column name should be suffixed by sequence name when it is referenced in
an expression .
（原文含示意图，图像资源未迁移）
Return
a pure table sequence:
|
=demo.query("select
EID,NAME,GENDER,DEPT,BIRTHDAY from employee").keys(EID)
|
=A1.i()
|
Convert
table sequence A1 to a pure table sequence.
|
=A2.select(GENDER=="M")
|
Return
a pure sequence.
|
=A2.select@v(GENDER=="F")
|
@v
option enables returning a pure table sequence.
相关函数：
A .pse l ect ( )
T.select@i()
说明：
Return
members of a table sequence which satisfy a condition.
语法：
T .select@i( x )
注意：
The
function computes expression x against each
member of table sequence T , and returns a new
table sequence composed of those members which make x return true .
T is a table sequence for which an
index is already created. T.select@i() will reuse the table sequence’s index and order of records in the
result set will probably be disrupted.
The
function returns all members when parameter x is absent.
参数：
|
T
|
A table sequence
for which an index is already created .
|
x
|
Filtering
expression; can be null .
Return
value:
Table
sequence
示例：
|
=demo.query("select
* from DEPT").keys@i(DEPTID)
|
Return
a table sequence whose key is DEPTID and create the hash index for it.
（原文含示意图，图像资源未迁移）
|
=A1.select@i(FATHER == 12)
|
Reuse
the index created in A1 and return the record where FATHER is 12 in A1’s
table sequence.
（原文含示意图，图像资源未迁移）
