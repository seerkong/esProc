# T.select()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.select()
=create(file).record(["D:/file/pseudo/app.ctx"])
=pseudo(A1)
=A2.select(EID>7)
=A3.import()
=A2.select()
=A5.import()
```

## 提取内容
T.select()
说明：
Define
a record filtering operation on a pseudo table and return a new pseudo table.
语法：
T .select( x )
注意：
The function defines a
computation on pseudo table T , which will calculate expression x
on each of its records and get records that make x true, and return a new pseudo table.
It returns a pseudo table retaining all records when
parameter x
is absent.
Only supported by esProc Enterprise Edition.
参数：
|
T
|
A pseudo
table .
|
x
|
Boolean expression that is a filter condition, which can be null .
Return
value:
Pseudo
table object
示例：
|
=create(file).record(["D:/file/pseudo/app.ctx"])
|
Below is data in
composite table app.ctx :
（原文含示意图，图像资源未迁移）
|
=pseudo(A1)
|
Generate
a pseudo table object .
|
=A2.select(EID>7)
|
Define a computation on A2’s pseudo table, which will get
records that satisfy EID>7, and return a new pseudo table.
|
=A3.import()
|
Import data from
A3’s pseudo table while executing the computation defined in A3 on A2’s
pseudo table, and return the following table:
（原文含示意图，图像资源未迁移）
|
=A2.select()
|
Define a computation on A2’s pseudo table, which will get
all records, and return a new pseudo table.
|
=A5.import()
|
Import data from
A5’s pseudo table while executing the computation defined in A5 on A2’s
pseudo table, and return the following table:
（原文含示意图，图像资源未迁移）
