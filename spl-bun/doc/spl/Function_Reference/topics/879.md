# ch. select()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.cursor("select * from SALES")
=channel()
=A2.select(ORDERID>100)
=A2.fetch()
=A1.push(A2)
=A1.fetch()
=A2.result()
```

## 提取内容
ch. select()
说明：
Attach
a record filtering computation to a channel and return the original channel.
语法：
ch. select ( x )
注意：
The
function attaches a computation to channel ch ,
which will calculate expression x on
each of its records and get records that make values of x true, and returns the original channel ch .
When
parameter x is absent, get all
records.
This
is an attachment function.
参数：
|
ch
|
channel .
|
x
|
Boolean
expression .
Return
value:
管道
示例：
|
=demo.cursor("select * from SALES")
|
=channel()
|
Create
a channel .
|
=A2.select(ORDERID>100)
|
Attach
a computation to A2’s channel, which will get records meeting the condition
ORDERID>100, and return the original channel .
|
=A2.fetch()
|
Fetch
and keep the current data in the channel .
|
=A1.push(A2)
|
Be
ready to push data in A1’s cursor to A2’s channel .
|
=A1.fetch()
|
Fetch data from cursor A1 while pushing data to channel A2
to execute the attached computation and keep the result .
|
=A2.result()
|
Get
result from the channel .
（原文含示意图，图像资源未迁移）
