# ch.select(x,ch’ )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.select(x,ch’ )
=demo.cursor("select EID,NAME,SALARY from EMPLOYEE " )
=channel()
=channel()
=A1.push(A2)
=A2.select(EID<5,A3)
=A2.fetch()
=A3.fetch()
=A1.skip()
=A2.result()
=A3.result()
```

## 提取内容
ch.select(x,ch’ )
说明：
Send
records in a channel that can’t meet the given condition into another channel.
语法：
ch. select ( x,ch’ )
注意：
The
function calculates expression x against
each of the records in channel ch ,
and sends those records over which the value of x is false into channel ch’ .
参数：
|
ch
|
channel .
|
x
|
Boolean expression .
|
ch’
|
channel .
Return
value:
管道
示例：
|
=demo.cursor("select EID,NAME,SALARY from EMPLOYEE " )
|
=channel()
|
Create
a channel.
|
=channel()
|
Create
a channel.
|
=A1.push(A2)
|
Push
data in A1’s cursor into A2’s channel.
|
=A2.select(EID<5,A3)
|
Push
records in A2’s channel that can’t meet the condition EID<5 into A3’s
channel.
|
=A2.fetch()
|
Attach
ch.fetch() function that gets the final result set to A2’s channel to fetch
and store the existing data in A2’s channel.
|
=A3.fetch()
|
Attach
ch.fetch() function that gets the final result set to A3’s channel to fetch
and store the existing data in A3’s channel.
|
=A1.skip()
|
It is when fetching data from A1’s cursor begins that data
in the cursor is truly pushed to the channel and computations start .
|
=A2.result()
|
（原文含示意图，图像资源未迁移）
Get result of the
computation in channel A2 .
|
=A3.result()
|
（原文含示意图，图像资源未迁移）
Get result of the
computation in channel A3 .
