# cs .select()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=demo.cursor("select * from SCORES")
=A1.select(STUDENTID>10)
=A1.fetch()
=connect("demo").cursor@v("select * from employee")
=A1.select@v(DEPT=="HR")
```

## 提取内容
cs .select()
说明：
Attach
the record filtering action to a cursor and return the original cursor.
语法：
cs. select ( x )
注意：
The
function attaches a computation to cursor cs ,
which will calculate expression x
against each of the records in cursor cs
and get records that make value of x
true, and return the original cursor cs .
When
parameter x is omitted, get all
records of cursor cs . The function
supports multicursors.
This
is a delayed function.
选项：
|
@v
|
Use
column-wise computation when parameter cs is a column-wise cursor and also
return a column-wise cursor.
参数：
|
cs
|
cursor .
|
x
|
Boolean value .
返回值：
游标
示例：
|
=demo.cursor("select * from SCORES")
|
Return
a cursor:
（原文含示意图，图像资源未迁移）
|
=A1.select(STUDENTID>10)
|
Attach
a computation to cursor A1, which will select records where STUDENTID is greater
than 10 from the cursor, and return cursor A1:
（原文含示意图，图像资源未迁移）
|
=A1.fetch()
|
Fetch
data from cursor A1 where A2’s computation is executed (it would be better
that data is fetched in batches when a large amount of data is involved):
（原文含示意图，图像资源未迁移）
When
using @v option:
|
=connect("demo").cursor@v("select * from employee")
|
Return
a column-wise cursor.
|
=A1.select@v(DEPT=="HR")
|
Perform
column-wise computation.
