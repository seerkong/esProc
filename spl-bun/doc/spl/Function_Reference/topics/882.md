# cs.select(x,ch’ )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.select(x,ch’ )
=demo.cursor("select EID,NAME,SALARY from EMPLOYEE" )
=channel()
=A1.select(EID<5,A2)
=A2.fetch()
=A1.fetch()
=A2.result()
```

## 提取内容
cs.select(x,ch’ )
说明：
Send
records in a cursor that can’t meet the given condition into a channel.
语法：
cs. select ( x,ch’ )
注意：
The
function calculates expression x
against each of the records in cursor cs ,
and sends those records over which the value of x is false into channel ch’ .
参数：
|
cs
|
cursor .
|
x
|
Boolean value .
|
ch’
|
channel .
Return
value:
管道
示例：
|
=demo.cursor("select EID,NAME,SALARY from EMPLOYEE" )
|
=channel()
|
Create
a channel.
|
=A1.select(EID<5,A2)
|
Push
cursor A1’s records not meeting condition EID<5 into A2’s channel.
|
=A2.fetch()
|
Fetch
and store the existing data in the channel.
|
=A1.fetch()
|
Fetch data from
the filtered cursor .
（原文含示意图，图像资源未迁移）
|
=A2.result()
|
Get
result from the channel.
（原文含示意图，图像资源未迁移）
