# seq()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
seq()
seq ( G i ,… )
=demo.query("select *
=A1.derive(seq(CLASS):F1)
=A1.derive(seq(CLASS,STUDENTID):F1)
```

## 提取内容
seq()
说明：
An
iterative loop for numbering records in which a certain field contains same
values.
语法：
seq ( G i ,… )
注意：
The
function performs a loop operation to number records with the same G i field
value from 1. As the value of field changes, number the records from 1 again.
参数：
|
G i
|
Field
name .
选项：
|
@n
|
The
option can increase the numbering efficiency, but can only be used when the
specified expression returns a positive integer; support using sequence
numbers .
Return
value:
游标
示例：
|
=demo.query("select *
from SCORES ")
|
=A1.derive(seq(CLASS):F1)
|
（原文含示意图，图像资源未迁移）
|
=A1.derive(seq(CLASS,STUDENTID):F1)
|
（原文含示意图，图像资源未迁移）
