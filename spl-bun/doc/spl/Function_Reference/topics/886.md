# A.sert()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.sert()
A.sert()
A.sert@r(Rec)
=T("D://house_prices_train.csv")
=A1.(LotFrontage)
=A2.max()
=A2.sert@c()
=A2.max()
```

## 提取内容
A.sert()
说明：
Remove outliers from a sequence type numeric
variable during modeling.
语法：
|
A.sert()
|
During modeling, remove outliers from numeric variable A
and return a binary sequence containing the handling result and removing
process records Rec .
|
A.sert@r(Rec)
|
During scoring, remove outliers from numeric variable A
according to removing process records Rec .
注意：
The MathCli
external library function (See External Library Guide ) removes outliers from a sequence type numeric
variable during modeling.
参数：
|
A sequence of numeric variable values .
|
Rec
|
A sequence of removing process records .
选项：
|
@c
|
Outliers are removed from the
original data after execution .
返回值：
Sequence
示例：
|
=T("D://house_prices_train.csv")
|
=A1.(LotFrontage)
|
A numeric variable .
|
=A2.max()
|
（原文含示意图，图像资源未迁移）
The maximum value in
the original data .
|
=A2.sert@c()
|
Remover outliers .
|
=A2.max()
|
（原文含示意图，图像资源未迁移）
The maximum value in
the data where outliers are removed .
