# P.sert()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.sert()
P.sert(cn)
P.sert@r(cn, Rec)
=T("D://house_prices_train.csv")
=T("D://house_prices_test.csv")
=A1.max(LotFrontage)
=A1.sert@c("LotFrontage")
=A1.max(LotFrontage)
=A2.sert@r("LotFrontage",A4(2))
```

## 提取内容
P.sert()
说明 ：
The MathCli
external library function (See External Library Guide ) removes outliers from a table sequence/record
sequence numeric variable during modeling.
语法：
|
P.sert(cn)
|
During modeling, remove outliers from numeric variable cn
and return a binary sequence containing the handling result and removing
process records Rec .
|
P.sert@r(cn, Rec)
|
During scoring, remove outliers from numeric variable cn
according to removing process records Rec .
参数：
|
P
|
A table sequence/record sequence .
|
cn
|
A string/number, which is the name of column (or the
column number starting from 1) – the to-be-pre-processed variable – in a
table sequence or record sequence .
|
Rec
|
A sequence of removing process records .
选项：
|
@c
|
Outliers are removed from the original data after
execution .
返回值：
Sequence
示例：
|
=T("D://house_prices_train.csv")
|
Import training data .
|
=T("D://house_prices_test.csv")
|
Import scoring data .
|
=A1.max(LotFrontage)
|
（原文含示意图，图像资源未迁移）
The maximum value of
the variable before outliers are removed .
|
=A1.sert@c("LotFrontage")
|
（原文含示意图，图像资源未迁移）
Remove outliers from
values of variable “LotFrantage”:
A2(1) Result of
removing outliers
A2(2) Removing
processing records Rec .
|
=A1.max(LotFrontage)
|
（原文含示意图，图像资源未迁移）
The maximum value of
the variable after outliers are removed .
|
=A2.sert@r("LotFrontage",A4(2))
|
Remove outliers from
values of corresponding variable in the scoring data according to A4’s
removing process records .
