# and()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
and()
and( x i,… )
and( A )
=and(6,11)
=and([11,13,15])
=and(decimal(6),11)
=and(decimal(6),decimal(11))
```

## 提取内容
and()
说明：
Perform
bitwise operation on integers.
语法：
and( x i,… )
and( A )
注意：
The
function performs bitwise operation on integers. It supports the decimal data
without the fractional part.
参数：
|
sequence.
|
x i
|
The
numerical expression.
返回值：
Integer
示例：
|
=and(6,11)
|
2 .
|
=and([11,13,15])
|
9 .
|
=and(decimal(6),11)
|
2 , which is the decimal type .
|
=and(decimal(6),decimal(11))
|
2 , which is the decimal type .
