# sf_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sf_close()
sf_close ( fd )
=sf_open()
=sf_close(A1)
```

## 提取内容
sf_close()
说明 ：
Close
Salesforce cloud server connected through the way of rest api.
语法：
sf_close ( fd )
注意：
The SalesforceCli external library function (See External Library Guide ) closes
Salesforce cloud server connected through rest api to release resources.
参数：
|
fd
|
restclient object .
示例：
|
=sf_open()
|
Connection.
|
=sf_close(A1)
|
Close
connection.
