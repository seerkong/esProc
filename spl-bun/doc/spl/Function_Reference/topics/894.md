# sf_wsdlclose()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sf_wsdlclose()
sf_wsdlclose ( fd )
=sf_wsdlopen("user","pwd")
=sf_wsdlclose(A1)
```

## 提取内容
sf_wsdlclose()
说明：
Close
the cloud server Salesforce server connected through soap api.
语法：
sf_wsdlclose ( fd )
注意：
The SalesforceCli external library function (See External Library Guide ) closes
the cloud server Salesforce server connected through the way of soap api to
release resources.
参数：
|
fd
|
restclient object .
示例：
|
=sf_wsdlopen("user","pwd")
|
Connection.
|
=sf_wsdlclose(A1)
|
Close
the connection.
