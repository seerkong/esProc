# sf_wsdlview()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sf_wsdlview()
sf_wsdlview( sobject )
=sf_wsdlopen("user","pwd")
=sf_wsdlquery(A1,
=sf_wsdlclose(A1)
```

## 提取内容
sf_wsdlview()
说明：
Query
data in an Sobject object and return a table sequence made up of field names
and field types.
Snytax:
sf_wsdlview( sobject )
注意：
The SalesforceCli external library function (See External Library Guide ) queries
data in an Sobject object and returns a table sequence made up of field names
(FieldName) and field types (FieldType).
参数：
|
sobject
|
The Sobject data object
to be queried .
返回值：
Table sequence
示例：
|
=sf_wsdlopen("user","pwd")
|
=sf_wsdlquery(A1,
"Account")
|
（原文含示意图，图像资源未迁移）
|
=sf_wsdlclose(A1)
|
