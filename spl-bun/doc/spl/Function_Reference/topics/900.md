# shift()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
shift()
shift( x,s )
shift(13,2)
shift(decimal(3),-2)
```

## 提取内容
shift()
说明：
The
function performs a shift operation. It supports a decimal without the
fractional part.
语法：
shift( x,s )
注意：
Shift
left if s<0; shift right if s>0.
参数：
|
x
|
The
expression based on which you perform the shift operation .
|
s
|
An
integer .
选项：
|
@s
|
When
shifting right, shift a sign into the leftmost position and shift a zero to
this position by default
返回值：
Integer
示例：
|
shift(13,2)
|
shift(decimal(3),-2)
|
12, which is the
decimal type .
