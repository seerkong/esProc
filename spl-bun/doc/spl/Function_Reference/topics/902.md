# sign()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sign()
sign( number )
sign(-10)
sign(30)
sign(0)
```

## 提取内容
sign()
说明：
Judge
whether the specified data is a positive number, a negative number or 0.
语法：
sign( number )
注意：
If
number is a positive value, return 1 ; If it is a negative value, return - 1 ; If it is 0, return 0.
参数：
|
number
|
numeric value .
返回值：
Integer
示例：
|
sign(-10)
|
-1
|
sign(30)
|
sign(0)
|
