# sin()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sin()
sin( number )
sin(pi())
sin(pi(2))
sin(pi()/2)
```

## 提取内容
sin()
说明：
Compute
the sine value.
语法：
sin( number )
注意：
The
function computes the sine value.
参数：
|
number
|
The
value of angle, which is measured in radians .
返回值：
float
type
示例：
|
sin(pi())
|
1.2246467991473532E-16 .
|
sin(pi(2))
|
-2.4492935982947064E-16 .
|
sin(pi()/2)
|
1.0 .
相关函数：
co s ( )
ta n ( )
