# cs.skip()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.skip()
=demo.cursor("select EID, NAME,STATE from EMPLOYEE order by
=A1.skip(50;STATE)
=A1.skip(20)
=A1.skip()
```

## 提取内容
cs.skip()
说明：
Get
the number of records the cursor skips.
语法：
cs . skip ( n;x )
注意：
The
function skips n records of cursor cs or till x… has changed, and returns the number of records actually skipped.
If all parameters are absent, return the number of remaining records and close
the cursor.
参数：
|
cs
|
cursor .
|
n
|
An
integer .
|
x
|
An
expression, by which cs is ordered.
With x , n will be ignored .
返回值：
Integer
示例：
|
=demo.cursor("select EID, NAME,STATE from EMPLOYEE order by
STATE")
|
Return
a data retrieval cursor, which has 500 records ordered by STATE field. Its
content is as follows:
（原文含示意图，图像资源未迁移）
|
=A1.skip(50;STATE)
|
Since
parameter x is present, ignore parameter n to skip records in
the cursor until STATE field value changes and return 4.
|
=A1.skip(20)
|
Skip
20 records.
|
=A1.skip()
|
As
parameters are absent, return the rest of the records in the cursor, close
the cursor, and return 476.
相关函数：
cs .fe t ch ( )
db .cur s o r()
