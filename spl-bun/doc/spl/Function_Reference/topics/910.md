# sleep()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sleep()
sleep( n )
=now()
=sleep(3000)
=interval@s(A1,now())
```

## 提取内容
sleep()
说明：
Make
the system sleep.
语法：
sleep( n )
注意：
The
function makes the system sleep for n
milliseconds.
参数：
|
n
|
A number, whose unit is millisecond .
示例：
|
=now()
|
=sleep(3000)
|
Suspend the system for 3000 milliseconds.
|
=interval@s(A1,now())
|
Compute
the duration from A1 to the start of execution of the script and return 3 in
seconds.
