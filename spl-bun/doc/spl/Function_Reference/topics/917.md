# ch.sortx()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.sortx()
=demo.cursor("select EID,NAME,DEPT,SALARY from EMPLOYEE order by EID
=channel()
=A2.sortx(SALARY,EID)
=A1.push(A2)
=A1.fetch()
=A2.result()
=A6.fetch()
```

## 提取内容
ch.sortx()
说明：
Sort
records in a channel.
语法：
ch . sortx ( x,… )
注意：
The
function sorts records in channel ch by expression x,… and returns the sorted records as a channel. This is a function
for directly getting a result set from the channel.
参数：
|
ch
|
channel .
|
x
|
An expression, according to which the
records in a given channel is sorted in ascending order .
Return
value:
管道
示例：
|
=demo.cursor("select EID,NAME,DEPT,SALARY from EMPLOYEE order by EID
desc")
|
Return
a cursor with desired records .
|
=channel()
|
Create
a channel .
|
=A2.sortx(SALARY,EID)
|
Sort
records in the channel by SALARY field and EID field .
|
=A1.push(A2)
|
Push
data in A1’s cursor into the channel
|
=A1.fetch()
|
Fetch
data from A1’s cursor .
|
=A2.result()
|
Return
result as a cursor .
|
=A6.fetch()
|
（原文含示意图，图像资源未迁移）
