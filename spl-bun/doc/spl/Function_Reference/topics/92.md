# T.append(cs)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.append(cs)
=create(file).record(["D:/file/Employee2.ctx"])
=pseudo(A1)
=create(Dept,AvgSalary).record(["CSD",6400.53])
=A2.append@i(A3.cursor())
>A2.close()
=[{file:"EMPLOYEE.ctx",
=pseudo@v(A1)
=create(EID,NAME,GENDER_enum).record([11,"Asew","M"])
=A3.cursor()
=A2.append(A4)
=A2.import()
>A2.close()
=create(file).record(["em.ctx"])
=pseudo(A1)
=A2.import()
=create(EID,NAME).record([0,"AAAA"])
=A2.append@y(A4.cursor())
=A2.import()
>A2.close()
=pseudo(A1).import()
```

## 提取内容
T.append(cs)
说明：
Append-write
records of a cursor to a pseudo table.
语法：
T. append ( cs )
注意：
The
function append-writes records of cursor cs to pseudo table T . If
the cursor contains pseudo fields, they will be automatically converted to real
fields at append-write.
Only
supported by esProc Enterprise Edition.
参数：
|
T
|
pseudo table.
|
cs
|
cursor.
选项：
|
@y
|
Write the appended records to
the memory instead of keeping it in the external memory .
Return
value:
Pseudo
table
示例：
Append-write
to a pseudo table:
|
=create(file).record(["D:/file/Employee2.ctx"])
|
=pseudo(A1)
|
Generate
a pseudo table object .
|
=create(Dept,AvgSalary).record(["CSD",6400.53])
|
Create
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.append@i(A3.cursor())
|
Append
A3’s cursor record to the pseudo table .
|
>A2.close()
|
Close
the pseudo table.
When
the cursor contains pseudo fields:
|
=[{file:"EMPLOYEE.ctx",
column:[{name:"GENDER",
enum:"GENDER_enum",
list:["F","M"]}]}]
|
Return
pseudo table definition record. Below is content of EMPLOYEE.CTX :
（原文含示意图，图像资源未迁移）
|
=pseudo@v(A1)
|
Generate
a pseudo table object.
|
=create(EID,NAME,GENDER_enum).record([11,"Asew","M"])
|
Generate a table sequence:
（原文含示意图，图像资源未迁移）
|
=A3.cursor()
|
Return
records of A3’s table sequence as a cursor.
|
=A2.append(A4)
|
Append
records of A4’s cursor, where GENDER_enum is a pseudo field, to A2’s pseudo
table.
|
=A2.import()
|
View records of A2’s pseudo table after the
append-write, where values of pseudo field GENDER_enum are automatically
converted to those of real field GENDER:
（原文含示意图，图像资源未迁移）
|
>A2.close()
|
Close
the pseudo table.
Use
@y option to write the appended records to the memory:
|
=create(file).record(["em.ctx"])
|
Return
the pseudo table definition record .
|
=pseudo(A1)
|
Generate
a pseudo table definition object .
|
=A2.import()
|
Read
data from the pseudo table .
（原文含示意图，图像资源未迁移）
|
=create(EID,NAME).record([0,"AAAA"])
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.append@y(A4.cursor())
|
Append
table sequence A4’s records to pseudo table A2; @y option enables converting
A4’s data to an in-memory zone table and writing it to the memory .
|
=A2.import()
|
Import
data from the pseudo table, where there are records appended in A5 .
（原文含示意图，图像资源未迁移）
|
>A2.close()
|
Close
the pseudo table .
|
=pseudo(A1).import()
|
Import
data from pseudo table A1; as A5 uses @y option and the appended data isn’t
written to the external memory, the imported pseudo table data here does not
contain A4’s data; the result is the same as A3’s:
（原文含示意图，图像资源未迁移）
