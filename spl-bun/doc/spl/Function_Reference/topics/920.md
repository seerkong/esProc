# f.sortx()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
f.sortx()
=file("PERFORMANCE.btx")
=file("PER-cp.btx")
=A1.sortx(BONUS;A2)
=A1.sortx(BONUS)
=file("f_emp.btx")
=file("m_emp.btx")
=file("emp_all.btx")
=[A1,A2].sortx@b(SALARY ,EID ;A3)
=[A1,A2].sortx@b(SALARY)
=file("f_emp.btx")
=file("m_emp.btx")
=file("emp_gb.btx")
=[A1,A2].sortx@b(;A3)
=file("emp.txt")
=file("emp-cp.csv")
=A1.sortx@ct(NAME;A2)
=A2.import@t()
```

## 提取内容
f.sortx()
说明：
Sort a data file or a
sequence of files and generate a new file.
语法：
|
f. sortx ( F i ,…; f n ,s )
|
Sort a data file according
to field F i
and generate a new file.
|
[ f i ,…]. sortx ( F i ,…; f n ,s )
|
Sort a sequence of files
according to field F i and generate a new file; perform
a simple concatenation when parameter F i is absent.
注意：
The function sorts data file
f or a sequence of files [ f i ,…] by certain field F i ,
generates a new file f n and
return non-null if the operation succeeds. Only supported by esProc Enterprise
Edition.
参数：
|
f
|
A bin/text file object.
|
[ f i ,…]
|
A sequence of bin/text
files of same structure.
|
F i
|
Name(s) of f field(s) by which the file is sorted.
|
f n
|
Bin/text file object(s)
generate a temporary file and return its cursor when this parameter is absent.
|
s
|
A custom separator. Default separator is tab. When this parameter is
absent, the comma before it can be omitted.
Ignore this parameter when f / [ f i ,…] is a bin file or a
sequence of bin files.
选项：
|
@b
|
Sort a bin file; this
option must be used when f / [ f i ,…] is a bin file or a
sequence of bin files .
|
@t
|
Work when f / [ f i ,…] is a text file to make the
first row the field names.
|
@c
|
Work when f / [ f i ,…] is a text file; when
parameter s is absent, use comma as the separator.
返回值：
Boolean/游标
示例：
Sort a bin file:
|
=file("PERFORMANCE.btx")
|
A bin file object; its
content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("PER-cp.btx")
|
Specify a bin file object .
|
=A1.sortx(BONUS;A2)
|
Sort PERFORMANCE.btx by BONUS field and generate a
new bin file PER-cp.btx , whose content is as follows:
（原文含示意图，图像资源未迁移）
|
=A1.sortx(BONUS)
|
As parameter f n is absent, generate a
temporary file in the temporary directory and return a cursor, whose content
is same as that in A3 .
Sort a sequence of bin
files:
|
=file("f_emp.btx")
|
A bin file object; its
content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("m_emp.btx")
|
A bin file object; its
content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("emp_all.btx")
|
=[A1,A2].sortx@b(SALARY ,EID ;A3)
|
Sort f_emp.btx and m _emp.btx by SALARY and EID fields and generate a new bin file emp_all.btx ,
whose content is as follows:
（原文含示意图，图像资源未迁移）
|
=[A1,A2].sortx@b(SALARY)
|
As parameter f n is absent, return a
cursor .
Perform a simple
concatenation on a sequence of bine files when parameter F i is absent:
|
=file("f_emp.btx")
|
A bin file object whose
content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("m_emp.btx")
|
A bin file object whose
content is as follows:
（原文含示意图，图像资源未迁移）
|
=file("emp_gb.btx")
|
=[A1,A2].sortx@b(;A3)
|
Concatenate f_emp.btx and m _emp.btx and generate a new bin file emp_gb.btx whose content is as follows:
（原文含示意图，图像资源未迁移）
Sort a text file:
|
=file("emp.txt")
|
Below is content of emp.txt :
（原文含示意图，图像资源未迁移）
|
=file("emp-cp.csv")
|
=A1.sortx@ct(NAME;A2)
|
Sort comma-separated emp.txt
by NAME and write it to emp-cp.csv , during which the first line is
used as field names.
|
=A2.import@t()
|
Import emp-cp.csv
and return the following result:
（原文含示意图，图像资源未迁移）
