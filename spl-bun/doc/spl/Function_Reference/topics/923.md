# spark_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
spark_close()
spark_close ( con )
=spark_open( "spark.properties"
>spark_close(A1)
```

## 提取内容
spark_close()
说明：
Close
a Spark database connection.
语法：
spark_close ( con )
注意：
This
SparkCli external library function (See External Library Guide ) closes
connection to a Spark database.
参数：
|
con
|
Database
connection string .
示例：
|
=spark_open( "spark.properties"
|
>spark_close(A1)
|
Close
connection to the spark database .
