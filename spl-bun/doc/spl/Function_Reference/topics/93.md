# argpost

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
arguments(spl)
arguments( spl )
=arg1+"
=arg2+88
=arguments()
=arguments("test.splx")
```

## 提取内容
argpost
说明：
Set
a POST parameter to be passed to the to-be-called splx file.
注意：
The
function sets a POST parameter string argpost
to be passed to the splx file.
示例：
argtest.splx
Define
a cellset parameter:
（原文含示意图，图像资源未迁移）
|
return argpost
|
The splx file returns parameter argpost
Access argtest. splx through
the URL to pass in the POST parameter. Below is the content of test.html :
<form method=post
action=" http://127.0.0.1:8503/argtest.splx ">
<input type=text
name=" aa " value=" aa111 ">
<input type=text
name=" bb " value=" bb222 ">
<input
type=submit value=submit>
</form>
Start HTTP service
to access test.html :
（原文含示意图，图像资源未迁移）
Click 【 submit 】 button to get the following result:
（原文含示意图，图像资源未迁移）
arguments(spl)
说明：
Return
a list of the specified script file’s argument names.
语法：
arguments( spl )
注意：
The
function returns a list of the specified .dfx/.splx/.spl script file’s
argument names. It returns argument names in the current script file when the
parameter is absent.
参数：
|
spl
|
Name
of a script file in the format of .dfx/.splx/.spl . The file can be
represented by an absolute path or a relative path, which is relative to the
main directory.
Return
value:
Sequence
示例：
Set
cellset arguments for test.splx :
（原文含示意图，图像资源未迁移）
Set
cellset arguments for the current script file:
（原文含示意图，图像资源未迁移）
|
=arg1+"
world!"
|
Result: Hello world!
|
=arg2+88
|
Result: 108.
|
=arguments()
|
Return
a list of the current script
file’s argument names .
（原文含示意图，图像资源未迁移）
|
=arguments("test.splx")
|
Return
a list of argument names from script file test.splx .
（原文含示意图，图像资源未迁移）
