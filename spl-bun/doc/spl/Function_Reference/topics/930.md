# s.split()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
s.split()
="1,[a,b],(2,c),'5,6'"
=A1.split@c()
=A1.split@c1()
=A1.split@cb()
="a:b:c".split(":")
=A1.split@cp()
="1,[a,b],(2,c),
="1,[a,b],(2,c),
=A1.split()
="a:;b:;c".split(":;")
="a1b2c".split@r("(\\d)")
="s,a,y\ngood,morning".split@nc()
="a:b:c:d:e".split@z(":")
="1,[a,b],<2,3>,'5,6'".split@gc()
="1,2,'3',4%5".split@y([1,3,-2,4])
```

## 提取内容
s.split()
说明：
Split a string into a sequence using the
delimiter.
语法：
s .split( d )
注意：
The function splits string s with delimiter d into a sequence and returns a sequence of strings.
参数：
|
s
|
string to be split.
|
d
|
The
delimiter; support splitting a string into multiple characters; if omitted,
split the string into a sequence of single characters.
选项：
|
@p
|
Parse
members into corresponding data types after the splitting. That is to say,
numbers shall be handled as numeric values, members enclosed by [] shall be
recognized as sequences, dates like 2001-01-01 shall be treated as date type
data, and so on. The parsing of the sign [] also involves the same handling of
a sub-sequence. Parentheses matching will be handled.
|
@1
|
Split
string into two parts at the
position of the first d found.
|
@b
|
Won’t
handle quotation marks matching and parentheses matching.
|
@t
|
Perform
trim operation to remove blank characters from both ends of each string
member and split the original string according to continuous blank characters
when d is omitted.
|
@c
|
Split
the string using the comma.
|
@r
|
Treat
parameter d as a regular
expression.
|
@n
|
Spit
a string using the carriage return \n, and split each substring according to
the other options if there are any, and return a sequence of sequences.
|
@z
|
Split
string into two parts at the
position of the last d found.
|
@g
|
Treat
angle brackets <> as parentheses; by default, they are left as is.
|
@y
|
Use
this option when parameter d is an array. In the array, each member is
regarded as representing the length of the corresponding segment of
characters, and the function splits the source string according to the
specified lengths. When a member is a negative number, skip this number of
characters.
Return
value:
Sequence
示例：
|
="1,[a,b],(2,c),'5,6'"
|
（原文含示意图，图像资源未迁移）
|
=A1.split@c()
|
（原文含示意图，图像资源未迁移）
Comma
is used to split the string .
|
=A1.split@c1()
|
（原文含示意图，图像资源未迁移）
Split
the string at the position of the first delimiter .
|
=A1.split@cb()
|
（原文含示意图，图像资源未迁移）
Use
comma as the delimiter and won’t handle quotation marks matching and
parentheses matching .
|
="a:b:c".split(":")
|
（原文含示意图，图像资源未迁移）
Use
colon as the delimiter .
|
=A1.split@cp()
|
（原文含示意图，图像资源未迁移）
Use comma as the delimiter
and parse each split member into its proper data type .
|
="1,[a,b],(2,c),
abc
,'5,6'".split@ct()
|
（原文含示意图，图像资源未迁移）
Remove
blank characters from both ends of "abc" .
|
="1,[a,b],(2,c),
abc
,'5,6'".split@t()
|
（原文含示意图，图像资源未迁移）
Split
the string according to continuous blank characters since d is omitted .
|
=A1.split()
|
（原文含示意图，图像资源未迁移）
Split
the string into a sequence of single characters since d is omitted .
|
="a:;b:;c".split(":;")
|
["a","b","c"] .
|
="a1b2c".split@r("(\\d)")
|
（原文含示意图，图像资源未迁移）
|
="s,a,y\ngood,morning".split@nc()
|
（原文含示意图，图像资源未迁移）
Split the string into
[s,a,y] and [good,moring] and then split each substring by comma .
|
="a:b:c:d:e".split@z(":")
|
Use @z option to split the string into two
segments at the position of the last delimiter:
（原文含示意图，图像资源未迁移）
|
="1,[a,b],<2,3>,'5,6'".split@gc()
|
Use @g option to treat angle brackets
<> as parentheses:
（原文含示意图，图像资源未迁移）
|
="1,2,'3',4%5".split@y([1,3,-2,4])
|
@y option enables
splitting the source array according to the specified lengths, where -2 means
skipping two characters:
（原文含示意图，图像资源未迁移）
