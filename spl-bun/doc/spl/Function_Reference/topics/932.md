# sqlparse()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sqlparse()
=A1.sqlparse()
=A1.sqlparse@s()
=A1.sqlparse@f()
=A1.sqlparse@w()
=A1.sqlparse@o()
=A1.sqlparse@a()
=A8.sqlparse@g()
=A8.sqlparse@h()
=A11.sqlparse()
=A11.sqlparse@s("deptid")
=A11.sqlparse@f("sales")
=A15.sqlparse()
=A17.sqlparse()
```

## 提取内容
sqlparse()
说明：
Split a SQL query
into a sequence.
语法：
sql . sqlparse ( part )
注意：
The function
splits away each statement/clause in a SQL query to generate and returns a
sequence. Members of the sequence are ordered like this: SELECT statement, FROM
clause (including the JOIN), WHERE clause, GROUP BY statement, HAVING clause
and ORDER BY statement. If there is a part
parameter, use it to replace the corresponding syntax unit in the original SQL
query and return a new SQL.
参数：
|
sql
|
A SQL query .
|
part
|
A SQL string for replacing a
statement/clause in the original SQL query; need to work with a certain
option .
选项：
|
@s
|
SELECT statement .
|
@f
|
FROM clause, including the JOIN .
|
@w
|
WHERE clause .
|
@g
|
GROUP BY statement .
|
@h
|
HAVING clause .
|
@o
|
ORDER BY statement .
|
@a
|
Split each
statement/clause into smaller parts; separate WHERE and HAVING clauses
according to AND .
返回值：
Sequence/SQL query
示例：
|
select emp.EID,emp.NAME,emp.BIRTHDAY,emp.SALARY,dep.DEPT,dep.MANAGER
from EMPLOYEE emp
left join DEPARTMENT dep
on emp.DEPT=dep.DEPT
where emp.EID<100 and
dep.DEPT='R&D'
order by emp.SALARY ,emp.EID
|
=A1.sqlparse()
|
（原文含示意图，图像资源未迁移）
Split each statement/clause away from a SQL
query to create a sequence; by default, the returned syntax units include SELECT statement, FROM clause, WHERE
clause, GROUP BY statement, HAVING clause and ORDER BY statement; return a
non-SQL syntax unit as null .
|
=A1.sqlparse@s()
|
（原文含示意图，图像资源未迁移）
Split
a SQL query to return its SELECT part .
|
=A1.sqlparse@f()
|
（原文含示意图，图像资源未迁移）
Split
a SQL query to return its FROM part .
|
=A1.sqlparse@w()
|
（原文含示意图，图像资源未迁移）
Split
a SQL query to return its WHERE part .
|
=A1.sqlparse@o()
|
（原文含示意图，图像资源未迁移）
Split
a SQL query to return its ORDER BY part .
|
=A1.sqlparse@a()
|
（原文含示意图，图像资源未迁移）
Re-split
a syntax part if it contains multiple syntax units .
|
SELECT STUDENTID,sum(SCORE) FROM SCORES group by studentid having sum(score)>460
|
=A8.sqlparse@g()
|
（原文含示意图，图像资源未迁移）
Split
a SQL query to return its GROUP BY part .
|
=A8.sqlparse@h()
|
（原文含示意图，图像资源未迁移）
Split
a SQL query to return its HAVING part .
|
select * from dept
|
=A11.sqlparse()
|
（原文含示意图，图像资源未迁移）
|
=A11.sqlparse@s("deptid")
|
（原文含示意图，图像资源未迁移）
Replace
the SELECT part: * with parameter part :
deptid .
|
=A11.sqlparse@f("sales")
|
（原文含示意图，图像资源未迁移）
Replace
the SELECT part: dept with parameter part :
sales .
|
select EID,NAME,GENDER from ( select * from EMPLOYEE where
salary>10000)
|
Use the SQL string of the subquery in the
FROM clause .
|
=A15.sqlparse()
|
（原文含示意图，图像资源未迁移）
|
SELECT
P.LastName, P.FirstName, O.OrderNo
FROM
Persons P
INNER
JOIN Orders O
ON
P.Id_P = O.Id_P
ORDER
BY P.LastName
|
=A17.sqlparse()
|
The FROM clause contains a JOIN .
（原文含示意图，图像资源未迁移）
