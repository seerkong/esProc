# stax_close()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
stax_close()
stax_close (staxClient)
=stax_open("127.0.0.1":9042,
= stax_close(A1)
```

## 提取内容
stax_close()
说明：
Close
connection to the Cassandra database.
语法：
stax_close (staxClient)
注意：
The
CassandraCli external library function (See
External Library Guide ) closes connection to the Cassandra database.
参数：
|
staxClient
|
Cassandra
database connection .
示例：
|
=stax_open("127.0.0.1":9042,
"mycasdb","cassandra":"cassandra")
|
Create
a Cassandra database connection .
|
= stax_close(A1)
|
Close
the Cassandra database connection .
