# stax_cursor()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
stax_cursor()
=stax_open("127.0.0.1":9042,
=stax_cursor(A1,"select
=A2.fetch()
=stax_close(A1)
```

## 提取内容
stax_cursor()
Descripiton:
Query
the Cassandra database and return result as a cursor.
语法：
stax_cursor (staxClient,
cql, [arg1], [arg2], ...)
注意：
The
CassandraCli external library function (See
External Library Guide ) queries a Cassandra database and returns the
result value as a cursor.
参数：
|
staxClient
|
The
database connection handle .
|
cql
|
CQL query statement .
|
args
|
parameter value; can be absent .
示例：
|
=stax_open("127.0.0.1":9042,
"mycasdb","cassandra":"cassandra")
|
Cassandra
database connection .
|
=stax_cursor(A1,"select
* from user")
|
Perform
the query and return result as a cursor .
|
=A2.fetch()
|
=stax_close(A1)
|
