# stax_query()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
stax_query()
=stax_open("127.0.0.1":9042,
=stax_query(A1,"select * from user where id=?",1)
=stax_close(A1)
```

## 提取内容
stax_query()
说明：
Query
a Cassandra database and return the query result as a table sequence.
语法：
stax_query (staxClient, cql, [arg1], [arg2],
...)
注意：
The
CassandraCli external library function
(See External Library Guide )
performs the query cql over a
Cassandra database and returns the query result as a table sequence. The
parameter staxClient is the database
connection.
参数：
|
staxClient
|
Database
connection handle .
|
cql
|
The
query statement .
|
args
|
Parameter
values; can be omitted .
Return
value:
Table
sequence
示例：
|
=stax_open("127.0.0.1":9042,
"mycasdb","cassandra":"cassandra")
|
Create
a Cassandra database connection .
|
=stax_query(A1,"select * from user where id=?",1)
|
Execute
query over the Cassandra database .
|
=stax_close(A1)
|
