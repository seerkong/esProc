# sum()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
sum()
sum ( x 1 ,…, x n )
=[1,2,3,4].sum()
=[2,null,3,4].sum()
=[2,
=[1,2,3,4].sum(~*~)
=sum(1,2,3,4)
=demo.query("select
=A1.sum(SALARY)
=A1.(SALARY+100).sum()
```

## 提取内容
sum()
说明：
Compute
the sum of members of a sequence.
语法：
|
A .sum( x )
|
sum ( x 1 ,…, x n )
|
Equivalent to A .sum() , where x 1 ,…, x n are members of sequence A
注意：
The
function computes expression x with members of sequence A
and returns the sum of members. Ignore non-numeric members and treat null
values as zero.
参数：
|
sequence .
|
x
|
An expression whose result should be
numeric type; can be omitted .
返回值：
数值型
value
示例：
When
A is a sequence
|
=[1,2,3,4].sum()
|
10 .
|
=[2,null,3,4].sum()
|
9; treat the null value as 0 .
|
=[2,
3,4,"2323ads"].sum()
|
9 ; ignore
non-numeric members .
|
=[1,2,3,4].sum(~*~)
|
30 ; return sum of
members of a sequence obtained after the expression is computed .
|
=sum(1,2,3,4)
|
10 , which is same
as A1 .
When
A is a table sequence or a record sequence:
|
=demo.query("select
* from EMPLOYEE")
|
=A1.sum(SALARY)
|
Compute
sum of salaries of all employees .
|
=A1.(SALARY+100).sum()
|
Compute
sum of SALARY values after salary of each employee is added 100 .
