# r.array()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
r.array()
=demo.query("select
=A1(1).array()
=A1(1).array(STUDENTID,SCORE)
```

## 提取内容
r.array()
说明：
Get
the field values from a record sequentially.
语法：
r . array ( F i ,… )
注意：
The
function gets the values of F i ,…
fields of the record r in order
and returns them as a sequence. Get
all the field values when parameter F i
is omitted.
Parameters ：
|
r
|
record.
|
Fi
|
Field
name.
Return value ：
Sequence
Example ：
|
=demo.query("select
* from SCORES")
|
Return a table
sequence:
（原文含示意图，图像资源未迁移）
|
=A1(1).array()
|
Get
the first record from A1 and return its all field values as a sequence:
（原文含示意图，图像资源未迁移）
|
=A1(1).array(STUDENTID,SCORE)
|
Get
the first record from A1 and return values of STUDENTID field and SCORE field
as a sequence:
（原文含示意图，图像资源未迁移）
