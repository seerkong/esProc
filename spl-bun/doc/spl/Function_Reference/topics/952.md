# P.switch(F i ,A i :x;… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.switch(F i ,A i :x;… )
=demo.query("select
=demo.query("select
=A2.switch(DEPT,A1:DEPT)
=demo.query("select
=demo.query("select
=A2.switch(DEPT,A1)
=connect("demo").query("SELECT * FROM CITIES")
=connect("demo").query("SELECT *
=A2.index@n()
=A1.switch(STATEID,A2)
=demo.query("select
=demo.query("select
=A2.switch@i(DEPT,A1)
=demo.query("select
=demo.query("select
=A2.switch@d(DEPT,A1)
=demo.query("select
=demo.query("select
=A2.switch(DEPT,A1)
```

## 提取内容
P.switch(F i ,A i :x;… )
说明：
Replace values of the sequence number key field with the corresponding referenced
records, or, sometimes vice versa.
语法：
P . switch ( F i , A i : x ;…)
注意：
The
function replaces values of foreign key field F i in table sequence/record
sequence P with the corresponding records in table
sequence/record sequence A i
according to the matching condition that F i value is equal to A i ’s
x field value ( F i value becomes null if it does not have a matching record), , and
returns a table sequence with switched F i values.
Take
the Attendance table as
an example. The employeeID field may store either the corresponding primary key values of employeeID or the referenced records in Employee table. So, the switch ()
function can be used to switch between the two objects.
Use
the index table of the foreign key if any and create one if unavailable.
参数：
|
P
|
A table
sequence/record sequence .
|
F i
|
The
foreign key of P .
|
A i
|
table sequence/record sequence F i is .
|
x
|
An A i table field referenced by F i field. It is by default A i ’s primary key. When this parameter is #, directly use the sequence
number to locate the corresponding record. If A i has a sequence-number-based index, symbol # can be omitted.
选项：
|
@i
|
Delete A i records that do not have a
matching F i field value.
|
@d
|
Perform the inverse operation of @i , which obtains records that have
no value corresponding to F i ; with the option do not
populate F with nulls .
|
@1
|
If the F i field
value of a specified P record doesn’t exist in A i , generate a record of the same
structure as A i in F i while setting F i as the primary key .
返回值：
Table
sequence
示例：
|
=demo.query("select
top 5 * from DEPARTMENT")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=demo.query("select
top 10 EID,NAME,DEPT,GENDER from EMPLOYEE")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.switch(DEPT,A1:DEPT)
|
Table
sequence A2’s foreign key DEPT has a correspondence relationship with table sequence
A1’s DEPT field. Replace the foreign key value with the referenced records in
A1, and enter null if no matching A1 record can be found.
（原文含示意图，图像资源未迁移）
When parameter x is
absent:
|
=demo.query("select
* from DEPARTMENT").keys(DEPT)
|
Return
a table sequence that use DEPT as the primary key.
|
=demo.query("select
top 10 EID,NAME,DEPT,GENDER from EMPLOYEE")
|
Return
a table sequence:
|
=A2.switch(DEPT,A1)
|
As
parameter x is absent, match table A2’s foreign key DEPT with table A1’s
primary key and replace the foreign key values with their referenced records
in A1:
（原文含示意图，图像资源未迁移）
Use the sequence number represented by parameter x to locate
corresponding record when table A i has a sequence
number-based index:
|
=connect("demo").query("SELECT * FROM CITIES")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=connect("demo").query("SELECT *
FROM STATECAPITAL")
|
Return
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.index@n()
|
Create
a sequence number-based index for table A2.
|
=A1.switch(STATEID,A2)
|
A1’s
CITIES table is the fact table, A3’s STATECAPITAL is the dimension table
The
switch() function performs table association by replacing values of foreign
key field STATEID in CITIES table with the referenced records in STATECAPITAL
table. The computation uses the index table created on the foreign key, and by
doing so, the sequence number key can be omitted from expression x .
Here the statement is equivalent to A1.switch(STATEID,A2:#).
The
statement returns the following result set:
（原文含示意图，图像资源未迁移）
When @i option works:
|
=demo.query("select
* from DEPARTMENT").keys(DEPT)
|
Return
a table sequence that uses DEPT as the primary key.
（原文含示意图，图像资源未迁移）
|
=demo.query("select
top 10 EID,NAME,DEPT,GENDER from EMPLOYEE")
|
Return
a table sequence.
|
=A2.switch@i(DEPT,A1)
|
With
@i option, delete the whole A2 record when it does not have a matching key
value in A1:
（原文含示意图，图像资源未迁移）
When @d option works:
|
=demo.query("select
* from DEPARTMENT").keys(DEPT)
|
Return
a table sequence that uses DEPT as the primary key.
（原文含示意图，图像资源未迁移）
|
=demo.query("select
top 10 EID,NAME,DEPT,GENDER from EMPLOYEE")
|
Return
a table sequence.
|
=A2.switch@d(DEPT,A1)
|
With
@d option, only return A2 records where DEPT field does not have a matching
value in A1’s foreign key field:
（原文含示意图，图像资源未迁移）
When @1 option works:
|
=demo.query("select
* from DEPARTMENT").keys(DEPT)
|
Return
a table sequence that uses DEPT as the primary key.
（原文含示意图，图像资源未迁移）
|
=demo.query("select
top 10 EID,NAME,DEPT,GENDER from EMPLOYEE")
|
Return
a table sequence.
|
=A2.switch(DEPT,A1)
|
With
@1 option, generate a record of same structure as table A1 in DEPT field; the
record’s primary key value is DEPT:
（原文含示意图，图像资源未迁移）
相关函数：
cs.swit c h ( )
