# T.switch( Fi, T’ :x;… )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
=create(file).record(["emp_news.ctx"])
=pseudo(A1)
=demo.query("SELECT
=A2.switch(DEPT,A3)
=A4.import()
=A2.switch@i(DEPT,A3)
=A5.import()
=A2.switch@d(DEPT,A3)
=A6.cursor().fetch()
=A2.switch@1(DEPT,A3)
=A7.cursor().fetch()
=pseudo([{file:"dmp.btx",key:"keys(DEPT)"}])
=A2.switch(DEPT,A8)
=A9.import()
```

## 提取内容
T.switch( Fi,
T’ :x;… )
说明：
Define a field value
switching operation on a pseudo table and return a new pseudo table.
语法：
T . switch ( F i , T’ : x ;…)
注意：
The function defines an
operation on pseudo table T : replace values of T ’s F i field with corresponding records of table
sequence/in-memory pseudo table T’ by matching F i field value with parameter x ,
which is T’ ’s
primary key, and returns a new pseudo table.
By default, display F i
field value as null when no record in A i matches it.
Only supported by esProc Enterprise Edition.
参数：
|
T
|
pseudo table .
|
F i
|
pseudo table field .
|
T’
|
table sequence/in-memory pseudo table .
|
x
|
The
primary key or logical key of T’ ; the parameter
can be omitted if primary key is already set for T’ .
选项：
|
@i
|
Delete the whole T’ record without a matching value
in F i .
|
@d
|
Perform the inverse operation of @i to obtain pseudo table records
that have no matching value in A i for F field .
|
@1
|
If the F field value of a pseudo
table record doesn’t exist in T’ , then
generate a record of the same structure as T’ with expression x being the primary key .
Return
value:
Pseudo
table
示例：
|
=create(file).record(["emp_news.ctx"])
|
Below
is content of composite table emp_news.ctx :
（原文含示意图，图像资源未迁移）
|
=pseudo(A1)
|
Generate a pseudo table from the
composite table.
|
=demo.query("SELECT
top 4 * FROM DEPARTMENT").keys(DEPT)
|
Return a table sequence and set
DEPT field as its primary key:
（原文含示意图，图像资源未迁移）
|
=A2.switch(DEPT,A3)
|
=A4.import()
|
Execute expression in A4 to
define a computation on A2’s pseudo table, which will convert its DEPT field
values into corresponding referencing field values in table sequence A3, and
retrun a new pseudo table.
Execute expression in B4: import
data from A4’s pseudo table while executing the computation deinfed in A4 on
A2’s pseudo table, and return the following table:
（原文含示意图，图像资源未迁移）
|
=A2.switch@i(DEPT,A3)
|
=A5.import()
|
Use @i option to delete pseudo
table records that can’t be matched; execute B5 and return the following
table:
（原文含示意图，图像资源未迁移）
|
=A2.switch@d(DEPT,A3)
|
=A6.cursor().fetch()
|
Use @d option to retain only the
non-matching records in the pseudo table; execute B6 and return the following
table:
（原文含示意图，图像资源未迁移）
|
=A2.switch@1(DEPT,A3)
|
=A7.cursor().fetch()
|
Use @1
option to generate a record having same structure as table sequence A3 when a
DEPT value of the pseudo table does not have a match in A3 and using DEPT as
the primary key; execute B7 and return the following table:
（原文含示意图，图像资源未迁移）
|
=pseudo([{file:"dmp.btx",key:"keys(DEPT)"}])
|
Use
the bin file to define an in-memory pseudo table that uses DEPT as the
primary key. Its data is as follows:
（原文含示意图，图像资源未迁移）
|
=A2.switch(DEPT,A8)
|
=A9.import()
|
Same as
B4.
