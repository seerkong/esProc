# syncfile(hs,p)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
syncfile(hs,p)
syncfile( hs,p )
=syncfile(["192.168.31.165:8281","192.168.31.39:8281"],"D:/f1")
```

## 提取内容
syncfile(hs,p)
说明：
Synchronize a
local path onto a sequence of nodes.
语法：
syncfile( hs,p )
注意：
The
function synchronizes local path p onto a node sequence of hs ,
during which the old files are replaced.
Only supported by
esProc Enterprise Edition.
参数：
|
hs
|
node sequence.
|
p
|
local path.
Return
value:
Boolean
示例：
|
=syncfile(["192.168.31.165:8281","192.168.31.39:8281"],"D:/f1")
|
Sync
the local path D:/f1 onto the node sequence
["192.168.31.165:8281","192.168.31.39:8281"] .
