# T()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T()
T ( fn : A,Fi,…;s )
=T(A1)
=T(A1,CityName)
=T(A1,#2,#3)
=T("D: / emp.xls";"emp2018")
=T("D:/users.txt";",")
=T("D:/user-a.txt":A6)
=T(A8)
=T@b(A8)
=T@c(A8)
```

## 提取内容
T()
说明：
Read in content of a file according to file
extension and return result as a table sequence.
语法：
T ( fn : A,Fi,…;s )
注意：
The function read in content of file fn according to the file extension and returns result as a
table sequence. The supported file types are txt/csv/xls/xlsx/btx/ctx.
When parameter A is present, write it to fn . The method is to first clean up the content of fn and then write data in.
参数：
|
fn
|
A data file.
|
A record sequence/cursor; can be absent.
|
Fi
|
A column header in the data file, which can
be represented by #1,#2,… (column 1, column 2, …); can be absent.
|
s
|
A separator that can be absent if the data
file is of text format; it is the sheet name , which
should not exceed 31 characters and contain special characters []:/\?*, if the file is of format of xls or xlsx.
选项：
|
@b
|
Won’t read in the column headers; will read
in them by default.
|
@c
|
Read in the file content as a cursor.
Return
value:
Table sequence
示例：
|
D:\City.txt
|
=T(A1)
|
Read all content of City.txt :
（原文含示意图，图像资源未迁移）
|
=T(A1,CityName)
|
Read in CityName column of City.txt :
（原文含示意图，图像资源未迁移）
|
=T(A1,#2,#3)
|
Read in column 2 and column 3
of City.txt :
（原文含示意图，图像资源未迁移）
|
=T("D: / emp.xls";"emp2018")
|
Read in content of sheet named emp2018
from emp.xls :
|
=T("D:/users.txt";",")
|
Read in content of comma-separated users.txt . Below is the file’s
content:
（原文含示意图，图像资源未迁移）
The table sequence returned:
（原文含示意图，图像资源未迁移）
|
=T("D:/user-a.txt":A6)
|
Delete all content from
user-a.txt and write A6’s data in it:
|
D:\node.txt
|
node.txt doesn’t have
headers. Its content is as follows:
（原文含示意图，图像资源未迁移）
|
=T(A8)
|
Read in content of node.txt
and make the first row as headers:
（原文含示意图，图像资源未迁移）
|
=T@b(A8)
|
Use @b function to assume that there aren’t headers in the file:
（原文含示意图，图像资源未迁移）
|
=T@c(A8)
|
Use @c opton to read in the file content as a cursor
（原文含示意图，图像资源未迁移）
