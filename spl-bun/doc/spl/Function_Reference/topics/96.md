# P.array()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.array()
=demo.query("select
=A1.array()
=A1.array@b()
```

## 提取内容
P.array()
说明：
Transform
each record of a table sequence/record sequence and return a sequence of
sequences.
语法：
P. array ()
注意：
The
function transforms each record of table sequence/record sequence P into
a sequence, where each member is also a sequence made up of a record’s field
values. The return value type is a sequence of sequences.
参数：
|
P
|
table sequence/record sequence.
选项：
|
@b
|
Discard
the first row.
Return
value:
Sequence of
sequences
示例：
|
=demo.query("select
* from SCORES")
|
Return
a table sequence .
|
=A1.array()
|
Return a sequence
of sequences .
（原文含示意图，图像资源未迁移）
|
=A1.array@b()
|
Discard the first
row as @b option is present:
（原文含示意图，图像资源未迁移）
