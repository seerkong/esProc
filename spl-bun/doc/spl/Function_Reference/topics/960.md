# A.tarcorskew()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
A.tarcorskew()
A.tarcorskew()
A.tarcorskew@r(Rec)
=T("D://house_prices_train.csv")
=A1.(SalePrice)
=A2.skew()
=A2.tarcorskew@c()
=A2.skew()
=A2.tarcorskew@r(A4(2))
```

## 提取内容
A.tarcorskew()
说明：
Correct skewness of a sequence of numeric
target variables.
语法：
|
A.tarcorskew()
|
During the modelling process, correct
skewness of a sequence A of numeric target variables, handle outliers,
and return the result sequence of outcomes and the sequence of correction and
handling records, represented by Rec .
|
A.tarcorskew@r(Rec)
|
Get the restored values of score values
according to Rec , the sequence of correction and handling records
after model scoring .
注意：
The MathCli external library (See External Library Guide ) corrects
skewness of a sequence of numeric target variables.
参数：
|
A sequence of numeric target variables .
|
Rec
|
The sequence of correction and handling
records .
选项：
|
@c
|
Modify the
original data to the corrected values after execution .
返回值：
Sequence
示例：
|
=T("D://house_prices_train.csv")
|
=A1.(SalePrice)
|
A sequence of numeric target variables .
|
=A2.skew()
|
（原文含示意图，图像资源未迁移）
The skewness of original data .
|
=A2.tarcorskew@c()
|
（原文含示意图，图像资源未迁移）
Return the result of skewness correction and skewness correction
records and modify the original data .
|
=A2.skew()
|
（原文含示意图，图像资源未迁移）
The corrected skewness .
|
=A2.tarcorskew@r(A4(2))
|
（原文含示意图，图像资源未迁移）
Recover the corrected data to their original values according to
skewness correction records, usually in order to restore the score values .
