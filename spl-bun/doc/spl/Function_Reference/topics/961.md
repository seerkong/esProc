# P.tarcorskew()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
P.tarcorskew()
P.tarcorskew(tn)
P.tarcorskew@r(tn, Rec)
=T("D:
=A1.(SalePrice).skew()
=A1.tarcorskew@c("SalePrice")
=A1.(SalePrice).skew()
=A1.tarcorskew@r("SalePrice",A3(2))
```

## 提取内容
P.tarcorskew()
说明：
Correct skewness of a table
sequence/record sequence numeric variable.
语法：
|
P.tarcorskew(tn)
|
During the modelling process, correct
skewness of numeric target variable tn , handle outliers, and return
the result sequence of outcomes and the sequence of correction and handling
records, represented by Rec .
|
P.tarcorskew@r(tn, Rec)
|
Restore scoring result tn according
to Rec , the sequence of correction and handling records after model
scoring .
注意：
The MathCli external library function (See External Library Guide ).
参数：
|
P
|
A table sequence/record sequence .
|
tn
|
A string/number, which is the
alias of the target variable (or the column number starting from 1) in a
table sequence or record sequence .
|
Rec
|
The sequence of correction and handling
records .
选项：
|
@c
|
Modify the
original data to the corrected values after execution .
返回值：
S equence
示例：
|
=T("D:
//house_prices_train.csv")
|
Import the modeling data .
|
=A1.(SalePrice).skew()
|
（原文含示意图，图像资源未迁移）
Calculate the skewness of the numeric targe variable .
|
=A1.tarcorskew@c("SalePrice")
|
（原文含示意图，图像资源未迁移）
A3(1) The result of skewness correction
A3(2) Rec , the sequence of skewness correction records .
|
=A1.(SalePrice).skew()
|
（原文含示意图，图像资源未迁移）
The corrected skewness .
|
=A1.tarcorskew@r("SalePrice",A3(2))
|
（原文含示意图，图像资源未迁移）
Restore the data with corrected skewness to their original values .
