# time( datetimeExp )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
time( datetimeExp )
time( datetimeExp )
time(now())
time@s(now())
time@m(now())
date( d a tetimeExp )
datetime( da t etimeExp )
```

## 提取内容
time( datetimeExp )
说明：
Get
the time part from the datetime value.
语法：
time( datetimeExp )
注意：
The
function gets the time part from the datetime value datetimeExp . The result is accurate to millisecond by default. The format must
be consistent with the time format in the configuration information. By
default, the configure information will not be displayed in millisecond.
参数：
|
datetimeExp
|
日期time
data .
选项：
|
@m
|
Accurate
to minute .
|
@s
|
Accurate
to second .
返回值：
Time
value
示例：
|
time(now())
|
16:28:26400
|
time@s(now())
|
16:28:260
|
time@m(now())
|
16:28:000
相关函数：
da t e ( )
date( d a tetimeExp )
datetime( da t etimeExp )
datetim e ()
tim e ()
