# tinv()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
tinv()
tinv ( p , nu )
=tinv(0.99, 2)
```

## 提取内容
tinv()
说明：
Compute the T inverse cumulative distribution function.
语法：
tinv ( p , nu )
注意：
MathCli external library function (See External Library Guide ) computes the T inverse cumulative
distribution.
参数：
|
P
|
Probability within the interval (0,1) An integer
|
nu
|
Degrees of freedom corresponding to probabilities An integer
Return
value:
Double-precision number
示例：
|
=tinv(0.99, 2)
|
（原文含示意图，图像资源未迁移）
