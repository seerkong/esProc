# cs.total()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
cs.total()
=demo.cursor("select
=A1.total(sum(SCORE))
=A1.reset()
=A1.total(sum(SCORE),count(SUBJECT),max(SCORE))
```

## 提取内容
cs.total()
说明：
Perform
aggregation over records in a cursor.
语法：
cs. total ( y ,…)
注意：
The
function performs aggregation over recods in a cursor. Parameter y is the aggregate function over cs . The function returns a single value
when there is only one aggregate function; or returns multiple values in order
for multiple aggregate functions.
参数：
|
cs
|
cursor.
|
y
|
Aggregate
function, only support sum/count/max/min/top/avg/iterate; Parameter Gi should not be present when the
function works with iterate( x,a;G i ,… )
function.
Return
value:
数值型
value or a sequence of numeric values
示例：
|
=demo.cursor("select
* from SCORES where CLASS = 'Class one'")
|
=A1.total(sum(SCORE))
|
（原文含示意图，图像资源未迁移）
Return
a single value .
|
=A1.reset()
|
Move the cursor
back to the beginning .
|
=A1.total(sum(SCORE),count(SUBJECT),max(SCORE))
|
（原文含示意图，图像资源未迁移）
Return
a sequence of values according to the order of the aggregate functions .
