# ch.total()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
ch.total()
=demo.cursor("select
=channel()
=channel()
=A1.push(A2,A3)
=A2.total(sum(SCORE))
=A3.total(sum(SCORE),count(SUBJECT),max(SCORE))
=A1.fetch()
=A2.result()
=A3.result()
```

## 提取内容
ch.total()
说明：
Perform
aggregation over records in a channel.
语法：
ch. total ( y ,…)
注意：
The
function performs aggregation over records in a channel. Parameter y is the aggregate function over cs . The function returns a single value
when there is only one aggregate function; or returns multiple values in order
for multiple aggregate functions. This is an attached computation.
参数：
|
ch
|
channel.
|
y
|
Aggregate
function, only support sum/count/max/min/top/avg/iterate; Parameter Gi should not be present when the
function works with iterate( x,a;G i ,… )
function.
Return
value:
数值型
value or a sequence of numeric values
示例：
|
=demo.cursor("select
* from SCORES where CLASS = 'Class one'")
|
=channel()
|
Create a channel .
|
=channel()
|
=A1.push(A2,A3)
|
Be ready to push
A1’s data to channels A2 and A3 .
|
=A2.total(sum(SCORE))
|
Perform an
aggregation over records in channel A2 .
|
=A3.total(sum(SCORE),count(SUBJECT),max(SCORE))
|
Perform multiple
aggregations over records in channel A3 .
|
=A1.fetch()
|
Fetch
data from cursor A1 when operations in the channel begin to be executed and
cursor data is pushed into the channel .
|
=A2.result()
|
（原文含示意图，图像资源未迁移）
|
=A3.result()
|
（原文含示意图，图像资源未迁移）
