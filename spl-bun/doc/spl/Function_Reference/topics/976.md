# transpose()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
transpose()
transpose (A)
=transpose(A1)
=transpose(A3)
=transpose@n(A3)
```

## 提取内容
transpose()
说明：
Transpose
a matrix.
语法：
transpose (A)
注意：
The
function transposes matrix A ; enter 0 as member values if the matrix has empty members.
参数：
|
matrix.
选项：
|
@n
|
Enter
null as member values if the matrix has empty members.
Return
value:
Sequence
示例：
|
[[1,1,1],[2,2,2],[3,3,3]]
|
=transpose(A1)
|
Calculate
positions of members of matrix A1 .
（原文含示意图，图像资源未迁移）
|
[[1],[2,2],[3,3,3]]
|
=transpose(A3)
|
Transpose
matrix A3; enter 0s to empty members .
（原文含示意图，图像资源未迁移）
|
=transpose@n(A3)
|
Use
@n option to enter nulls to empty members .
（原文含示意图，图像资源未迁移）
