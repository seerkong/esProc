# asin()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
asin()
asin( number )
asin(-1)
asin(sin(pi()/2))
asin(sin(0))
```

## 提取内容
asin()
说明：
计算反正弦值。
语法：
asin( number )
注意：
The
function computes arc sine value of a 实数 .
参数：
|
number
|
实数 between -1 and 1 .
返回值：
数值型
value
示例：
|
asin(-1)
|
-1.5707963267948966
|
asin(sin(pi()/2))
|
1.5707963267948966
|
asin(sin(0))
|
0.0
Related functions:
ac o s ()
ata n ( )
