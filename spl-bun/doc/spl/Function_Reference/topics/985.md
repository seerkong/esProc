# T.update(P:D )

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
T.update(P:D )
=file("emp.ctx")
=A1.create@y(#EID,NAME)
=connect("demo").cursor("select
=A2.append@i(A3)
=A2.attach(table3,SURNAME)
=connect("demo").cursor("select
=A5.append@i(A6)
=A2.cursor().fetch()
=A5.cursor().fetch()
=create(EID,NAME).record([1,"A",21,"B"])
=A2.update(A10)
=A2.cursor().fetch()
=create(EID,SURNAME).record([2,"aa",10,"bb"])
=A5.update@in(A13)
=A5.cursor().fetch()
=create(EID,SURNAME).record([1,"ss",6,"cc"])
=A5.update@un(A16)
=A5.cursor().fetch()
=file("empD.ctx")
=A1.create@y(#EID,NAME)
=connect("demo").cursor("select
=A2.append@i(A3)
=create(EID,NAME).record([10,"bb",11,"cc"])
=create(EID,NAME).record([1,"Rebecca"])
=A2.update@n(A5:A6)
=A2.import()
=file("empPD.ctx")
=A1.create@y(#EID,NAME)
=connect("demo").cursor("select
=A2.append@i(A3)
=create(EID,NAME).record([1,"bb",2,"cc"])
=create(EID,NAME).record([1,])
=A2.update@n(A5:A6)
=A2.import()
=file("empD.ctx")
=A1.create@y(EID,NAME)
=connect("demo").cursor("select
=A2.append@i(A3)
=create(EID,NAME).record([10,"bb"])
=create(EID,NAME).record([1,"Rebecca"])
=A2.update(A5:A6)
=A2.import()
=file("empD.ctx")
=A1.create@y(#EID,NAME)
=connect("demo").cursor("select
=A2.append@i(A3)
=create(EID,NAME).record([1,"Rebecca"])
=A2.update@y(:A5)
=A2.import()
=A2.close()
=A1.open().import@x()
=demo.cursor("select
=file("STOCKRECORDS.ctx")
=A2.create@y(#STOCKID,DATE,CLOSING)
=A3.append@i(A1)
=A2.open().cursor()@x.fetch()
=demo.cursor("select
=A2.open()
=A7.update@w(A6)
=A7.cursor()@x.fetch()
```

## 提取内容
T.update(P:D )
说明：
Update
records in an entity table.
语法：
T. update ( P:D )
注意：
The
function updates entity table T ’s
records according to table sequences/record sequences P and/or D ; P should have same structure as T .
If
a key value in P exists in entity
table T , update the corresponding T ’s record; if a key value in P doesn’t exist in T , append or insert the corresponding P ’s record in T .
If
a key value in D exists in entity
table T , delete the corresponding T ’s record; parameter D can be omitted.
When
both parameter P and parameter D are present, the function will first
handle P and then D .
Ignore
parameter D when entity table T doesn’t have dimension. In this case,
all records of P will be appended to
the end of T .
参数：
|
T
|
composite table’s entity table.
|
P
|
Data
to be updated to T , which is a
table sequence/record sequence having same structure as T ; can be omitted .
|
D
|
table sequence/record sequence
delete records from T according to its key values; can be
omitted .
选项：
|
@i
|
Only append or insert
non-matching records and ignore the matching ones.
|
@u
|
Only update the matching
records and ignore the non-matching ones.
|
@n
|
Return updated, inserted
and deleted records.
|
@w
|
When parameter P
is a cursor having same order as the original entity table, update the
latter’s fields that P has; only valid when T is stored in
column-wise format and no records should be added.
|
@y
|
Keep table
sequences/record sequences P and D in the memory and perform
the MERGE when computation begins without writing the updated result set to
the external memory.
返回值：
Entity table
示例：
Update data in an entity
table:
|
=file("emp.ctx")
|
Generate
a composite table file .
|
=A1.create@y(#EID,NAME)
|
Create
the composite table’s base table .
|
=connect("demo").cursor("select
EID,NAME from employee where EID<20")
|
Return
a cursor .
|
=A2.append@i(A3)
|
Append
data in A3’s cursor to A2’s base table .
|
=A2.attach(table3,SURNAME)
|
Add
attached table table3 that has two fields – EID, which is the key, and
SURNAME, to the base table .
|
=connect("demo").cursor("select
EID,SURNAME from employee where EID< 5")
|
Return
a cursor .
|
=A5.append@i(A6)
|
Append
data in A6’s cursor to A5’s pseudo table .
|
=A2.cursor().fetch()
|
View
data in the base table:
（原文含示意图，图像资源未迁移）
|
=A5.cursor().fetch()
|
View
data in the attached table:
（原文含示意图，图像资源未迁移）
|
=create(EID,NAME).record([1,"A",21,"B"])
|
Return
a table sequence .
|
=A2.update(A10)
|
Update
the base table using A10’s table sequence; update the corresponding base
table record when a key value of table sequence A10 exists in the base table,
and append the corresponding A10’s record to the base table when its key
value doesn’t exist in the latter .
|
=A2.cursor().fetch()
|
View
data in the base table again:
（原文含示意图，图像资源未迁移）
|
=create(EID,SURNAME).record([2,"aa",10,"bb"])
|
Return
a table sequence .
|
=A5.update@in(A13)
|
With
@i option, only handles insert and ignore records with same key values; the
cooperation with @n option makes the function return only the inserted
records .
（原文含示意图，图像资源未迁移）
|
=A5.cursor().fetch()
|
View
data in the attached table again:
（原文含示意图，图像资源未迁移）
|
=create(EID,SURNAME).record([1,"ss",6,"cc"])
|
Return
a table sequence .
|
=A5.update@un(A16)
|
With
@u option, only handles update and ignore records with different key values
the cooperation with @n option makes the function return only the updated
records .
（原文含示意图，图像资源未迁移）
|
=A5.cursor().fetch()
|
View
data in the attached table:
（原文含示意图，图像资源未迁移）
Delete
data from an entity table:
|
=file("empD.ctx")
|
=A1.create@y(#EID,NAME)
|
Create
a composite table using EID as the key
|
=connect("demo").cursor("select
top 10 EID,NAME from employee")
|
Return
a cursor whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=A2.append@i(A3)
|
Append
data of cursor A3 to entity table A2 .
|
=create(EID,NAME).record([10,"bb",11,"cc"])
|
Generate
a table sequence:
（原文含示意图，图像资源未迁移）
|
=create(EID,NAME).record([1,"Rebecca"])
|
Generate
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.update@n(A5:A6)
|
Update
entity table A2; when a table sequence A5’s key value exists in A2, update
the corresponding record in the latter; otherwise, insert the corresponding
record in A5 to A2. Delete a record in A2 when it matches an A6’s key value.
@n option works to return the updated, inserted and deleted records .
（原文含示意图，图像资源未迁移）
|
=A2.import()
|
Now
import data from entity table A2 as follows:
（原文含示意图，图像资源未迁移）
When
there are records having same key values in P and D :
|
=file("empPD.ctx")
|
=A1.create@y(#EID,NAME)
|
Create
a composite table using EID as the key.
|
=connect("demo").cursor("select
top 10 EID,NAME from employee")
|
Return
a cursor, whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=A2.append@i(A3)
|
Append
data in cursor A3 to entity table A2.
|
=create(EID,NAME).record([1,"bb",2,"cc"])
|
Generate
a table sequence:
（原文含示意图，图像资源未迁移）
|
=create(EID,NAME).record([1,])
|
Generate
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.update@n(A5:A6)
|
Update
entity table A2 - append A5’s records to A2 when their key values exist in A2
and when key values of records of A6 exist in A2, delete corresponding
records from A2, and use @n option to return updated records, inserted
records and deleted records:
（原文含示意图，图像资源未迁移）
|
=A2.import()
|
As
both parameter P and parameter D are present and the function
first handles P and then D , the record whose key value is 1
will be deleted in the end; below is the final data in entity table A2:
（原文含示意图，图像资源未迁移）
When
the entity table doesn’t have dimension:
|
=file("empD.ctx")
|
=A1.create@y(EID,NAME)
|
Create
a composite table .
|
=connect("demo").cursor("select
top 10 EID,NAME from employee")
|
Return
a cursor whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=A2.append@i(A3)
|
Append
cursor A3’s data to entity table A2 .
|
=create(EID,NAME).record([10,"bb"])
|
Generate
a table sequence:
（原文含示意图，图像资源未迁移）
|
=create(EID,NAME).record([1,"Rebecca"])
|
Generate
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.update(A5:A6)
|
As
entity table A2 doesn’t have dimension, parameter D (A6) is ignored
during the update and append all records of P (A5) to A2 .
|
=A2.import()
|
Now
import data from entity table A2 as follows:
（原文含示意图，图像资源未迁移）
When
using @y option:
|
=file("empD.ctx")
|
=A1.create@y(#EID,NAME)
|
Create
a composite table .
|
=connect("demo").cursor("select
top 10 EID,NAME from employee")
|
Return
a cursor whose data is as follows:
（原文含示意图，图像资源未迁移）
|
=A2.append@i(A3)
|
Append
cursor A3’s data to entity table A2 .
|
=create(EID,NAME).record([1,"Rebecca"])
|
Generate
a table sequence:
（原文含示意图，图像资源未迁移）
|
=A2.update@y(:A5)
|
Update
entity table A2 by deleting records that match A5’s key values; @y option
works to keep the update result set in the memory and perform MERGE when
computation really begins .
|
=A2.import()
|
Now
import data from entity table A2 as follows:
（原文含示意图，图像资源未迁移）
|
=A2.close()
|
Close
entity table A2 .
|
=A1.open().import@x()
|
Re-open
entity table A1 to retrieve data from it; the updated data in A6 has been
restored:
（原文含示意图，图像资源未迁移）
When
using @w option:
|
=demo.cursor("select
STOCKID,DATE,CLOSING from STOCKRECORDS
where STOCKID = ? and
DATE<?","000062","2015-01-20")
|
=file("STOCKRECORDS.ctx")
|
Create
a composite table file .
|
=A2.create@y(#STOCKID,DATE,CLOSING)
|
Create
the composite table’s base table .
|
=A3.append@i(A1)
|
Append
data of A1’s cursor to the composite table’s base table .
|
=A2.open().cursor()@x.fetch()
|
Fetch
data from the composite table as follows:
（原文含示意图，图像资源未迁移）
|
=demo.cursor("select
STOCKID,DATE from
STOCKRECORDS where STOCKID =
?","000792")
|
=A2.open()
|
=A7.update@w(A6)
|
Update
content of A6’s cursor to composite table STOCKRECORD.ctx , during
which @w option is used to rewrite fields A6 has. The process is this: update
A6’s STOCKID field and 日期 field into A7’s composite table and do not change
A7’s CLOSING field while keeping the original number of records in A7 without
adding any new record to it .
|
=A7.cursor()@x.fetch()
|
Fetch data from
the updated composite table file as follows:
（原文含示意图，图像资源未迁移）
