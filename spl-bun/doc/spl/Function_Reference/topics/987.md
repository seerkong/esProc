# upper()

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
upper()
upper( s )
upper("ABCdef")
upper("abcDEF")
upper@q("\'abCD\'")
=upper@q(A1)
```

## 提取内容
upper()
说明：
Convert
all characters in a string to upper case.
语法：
upper( s )
注意：
The
function converts all characters in a string to upper case.
参数：
|
s
|
Source
string to be converted to upper case.
选项：
|
@q
|
The quoted strings won’t be converted .
返回值：
字符串
示例：
|
upper("ABCdef")
|
"ABCDEF"
|
upper("abcDEF")
|
"ABCDEF"
|
upper@q("\'abCD\'")
|
'abCD'
|
"ABcde"
|
=upper@q(A1)
|
"ABcde"
functions:
lowe r ( )
