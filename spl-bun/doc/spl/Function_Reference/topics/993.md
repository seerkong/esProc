# var(x)

- 格式：为 AI 阅读提取的 Markdown

## 代码片段
```spl
var(x)
var ( x )
=demo.query("select NAME,EVENT,SCORE from GYMSCORE")
=A1.groups(EVENT;var(SCORE):SCORE_var)
=A1.groups(EVENT;var@s(SCORE):SCORE_var)
```

## 提取内容
var(x)
说明：
Calculate
the overall variance for the aggregation field value.
语法：
var ( x )
注意：
The
function calculates the overall variance for the aggregation field value x .
The function can only work as an aggregate function to be used in the groups()
function.
选项：
|
@s
|
Calculate
the sample variance of aggregation field value x by dividing it by n-1
参数：
|
x
|
Aggregation
field name, whose data type must be numeric
Return
value:
数值型
value
示例：
|
=demo.query("select NAME,EVENT,SCORE from GYMSCORE")
|
Return
a sequence:
（原文含示意图，图像资源未迁移）
|
=A1.groups(EVENT;var(SCORE):SCORE_var)
|
Group
A1’s sequence by EVENT field and calculate SCORE value’s overall variance in
each group:
（原文含示意图，图像资源未迁移）
|
=A1.groups(EVENT;var@s(SCORE):SCORE_var)
|
Use
@s option to calculate the sample variance of SCORE value in each group:
（原文含示意图，图像资源未迁移）
