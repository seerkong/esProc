# 3.5.2 使用外键

## 代码示例
```spl
=demo.query("select * from CITIES")
=demo.query("select STATEID,NAME,ABBR,CAPITAL from STATES")
=A1.derive(A2.select@1(STATEID==A1.STATEID):State)
=A3.derive(State.ABBR:SA)
=A2.derive(A1.select(STATEID==A2.STATEID):Cities)
=A4.select(like(State.NAME,"*la*"))
=demo.query("select * from CITIES")
=demo.query("select STATEID,NAME,ABBR,CAPITAL from STATES")
=A1.derive(A2.select@1(STATEID==A1.STATEID):State)
=A3.derive(State.ABBR:SA)
=A2.derive(A1.select(STATEID==A2.STATEID):Cities)
=A5.sort(-Cities.len())
=demo.query("select * from CITIES")
=demo.query("select STATEID,NAME,ABBR,CAPITAL from STATES")
=A2.derive(A1.select(STATEID==A2.STATEID):Cities)
=A1.derive(A3.select@1(STATEID==A1.STATEID):State)
=A4.select(State.Cities.len()==3)
```

## 内容说明

### 3.5.2 使用外键

在查询或展示序表数据时，外键字段与其他字段的处理方式相同。但需要注意它使用的数据类型。

例如，要从 CITIES 表中选择位于名称包含"la"的州的城市，可以在过滤表达式中直接调用外键 State 的 NAME 字段：

|   |
|---|
| =demo.query("select * from CITIES") |
| =demo.query("select STATEID,NAME,ABBR,CAPITAL from STATES") |
| =A1.derive(A2.select@1(STATEID==A1.STATEID):State) |
| =A3.derive(State.ABBR:SA) |
| =A2.derive(A1.select(STATEID==A2.STATEID):Cities) |
| =A4.select(like(State.NAME,"*la*")) |

注意这里外键字段的数据类型是记录。A6 的结果如下：

（原文含示意图，图像资源未迁移）

外键字段也可以用作排序条件，例如根据州内城市数量对州数据进行降序排序：

|   |
|---|
| =demo.query("select * from CITIES") |
| =demo.query("select STATEID,NAME,ABBR,CAPITAL from STATES") |
| =A1.derive(A2.select@1(STATEID==A1.STATEID):State) |
| =A3.derive(State.ABBR:SA) |
| =A2.derive(A1.select(STATEID==A2.STATEID):Cities) |
| =A5.sort(-Cities.len()) |

注意这里外键字段的数据类型是记录序列。A6 的排序结果如下：

（原文含示意图，图像资源未迁移）

如果序表的外键字段引用的记录也包含外键字段，它们仍然可以被引用。例如，列出所在州恰好包含三个城市的城市：

|   |
|---|
| =demo.query("select * from CITIES") |
| =demo.query("select STATEID,NAME,ABBR,CAPITAL from STATES") |
| =A2.derive(A1.select(STATEID==A2.STATEID):Cities) |
| =A1.derive(A3.select@1(STATEID==A1.STATEID):State) |
| =A4.select(State.Cities.len()==3) |

这里 A4 在向 CITIES 表添加 State 字段时，引用的是包含外键字段的 A3 的州记录，而不是原始州记录。因此 A5 直接得到所需结果：

（原文含示意图，图像资源未迁移）
