# 6.1.4 在 Linux 下使用命令行

## 内容

### 6.1.4 在 Linux 下使用命令行

我们也可以在 Linux 下使用命令行，用法与 Windows 下类似。区别在于调用的命令是 esprocx.sh。

例如，要运行不返回任何结果的网格文件 output1：

（原文含示意图，图像资源未迁移）

设置 DEBUG 日志级别以查看更详细的条目。我们可以看到输出结果与 Windows 下完全相同。

对于其他命令行执行，我们可以看到类似的输出结果。要执行返回结果的 calculate1.splx：

（原文含示意图，图像资源未迁移）

要传递参数，只需在要调用的网格文件后添加参数：

（原文含示意图，图像资源未迁移）

在 Linux 下，我们也可以使用 -c 选项执行网格脚本。与 Windows 下的执行不同，在脚本成功输入后需要按 Ctrl+D 确认开始执行。例如：

（原文含示意图，图像资源未迁移）

通过使用 -r 选项，我们可以看到 return 语句返回的结果：

（原文含示意图，图像资源未迁移）

我们也可以在 Linux 下执行简单的 SQL 查询，如下所示：

（原文含示意图，图像资源未迁移）
