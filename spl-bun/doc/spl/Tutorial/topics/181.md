# 6.8.2 使用 JDBC 企业版

## 代码片段
```spl
=dql("pseudo/test.glmd")
=A1.query("select
=A2.i()
>output(ifpure(A3))
Class.forName("com.esproc.jdbc.QDriver")
DriverManager.getConnection("jdbc:esproc:q:local://?config=D:/soft/raqsoft/esProc/q-server/webapps/qvs/WEB-INF/home/raqsoftConfig.xml")
=con.prepareCall("call testQJDBC()")
st.execute()
System.out.print("\t")
System.out.print("\n")
System.out.print(title)
System.out.print("\t")
System.out.print("\n")
System.out.print(o.toString())
System.out.println(e)
con.close()
System.out.println(e)
```

## 内容

6.8.2 使用 JDBC 企业版

使用 JDBC 企业版的方式与使用 PJDBC 相同，例如在 Java 中调用 call 语句执行 SPL 语句和调用网格文件。您可以使用企业版特有的函数来执行操作。以下面的网格文件 testQJDBC.splx 为例：

| =dql("pseudo/test.glmd") |
| =A1.query("select EID, Name, ifMarried from emps") |
| =A2.i() |
| >output(ifpure(A3)) |
| return A3 |

该网格文件使用了一些 esProc 企业版特有的函数。A1 使用 dql() 函数打开 DQL 定义文件；A2 在 DQL 中执行查询语句；A3 使用 P.i() 函数将结果转换为纯序表，其中所有字段具有相同的数据类型。为了检查转换是否成功，A4 使用 ifpure() 函数判断它是否为纯序表并输出结果。

使用以下 Java 代码调用企业版 JDBC 执行网格文件。这里我们指定配置文件为 raqsoftConfig.xml，如果未指定配置文件则使用默认文件：

```java
public void testDataServer(){
    Connection con = null
    java.sql.CallableStatement st
    try{
        // 建立连接
        Class.forName("com.esproc.jdbc.QDriver")
        con = DriverManager.getConnection("jdbc:esproc:q:local://?config=D:/soft/raqsoft/esProc/q-server/webapps/qvs/WEB-INF/home/raqsoftConfig.xml")
        // 调用存储过程；createTable1 是脚本文件名
        st = con.prepareCall("call testQJDBC()")
        // 执行存储过程
        st.execute()
        // 获取结果集
        ResultSet rs = st.getResultSet()
        // 简单处理结果集：输出字段名和数据
        ResultSetMetaData rsmd = rs.getMetaData()
        int colCount = rsmd.getColumnCount()
        for (int c = 1; c <= colCount; c++) {
            String title = rsmd.getColumnName(c)
            if (c > 1) {
                System.out.print("\t")
            } else {
                System.out.print("\n")
            }
            System.out.print(title)
        }
        while (rs.next()) {
            for (int c = 1; c <= colCount; c++) {
                if (c > 1) {
                    System.out.print("\t")
                } else {
                    System.out.print("\n")
                }
                Object o = rs.getObject(c)
                System.out.print(o.toString())
            }
        }
    } catch(Exception e){
        System.out.println(e)
    } finally{
        // 关闭连接
        if (con != null) {
            try {
                con.close()
            } catch(Exception e) {
                System.out.println(e)
            }
        }
    }
}
```

语法与调用普通 esProc JDBC 相同。这里我们使用 "call testQJDBC()" 执行 SPL 文件，结果如下：

（原文含示意图，图像资源未迁移）

请注意，此网格文件中使用的 dql()、P.i() 和 ifpure() 函数不能用于普通 esProc JDBC。阅读《DQL 工具》了解更多关于 esProc 企业版 DQL 的信息。
