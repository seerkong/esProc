# 7.4 游标的合并和连接操作

## 内容

7.4 游标的合并和连接操作

基于多个序表进行数据分析和计算时，组合它们是方便或必要的。相关函数包括 A.merge() 函数（按一定顺序组合多个序表的记录）、A.conj() 函数（将多个序表的记录合并成一个大表），以及 JOIN 函数——join、join@p 和 xjoin——通过连接序表中的数据来创建它们之间的关联，用于数据查询或计算。

同样，合并和连接操作也适用于处理大数据的游标。相关函数包括 CS.conj()、CS.merge(x) 和 joinx()。下面讨论它们的用法。
