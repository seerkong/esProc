# 8.2.2 多线程的性能优势

## 代码片段
```spl
=file("PersonnelInfo.txt")
=now()
to(4)
=A1.import@t(;A3:4)
B3.select(City=="San Diego")
=A3.conj()
=interval@ms(A2,now())
=now()
=A1.import@t(;A8:4)
>A10=A10|B8.select(City=="San
=interval@ms(A7,now())
```

## 内容

8.2.2 多线程的性能优势

多线程代码的执行看起来与循环代码的执行类似。但为什么多线程更好呢？以下两者之间的比较给出了答案：

| =file("PersonnelInfo.txt") |
| =now() |
| fork to(4) |
| =A1.import@t(;A3:4) |
| return B3.select(City=="San Diego") |
| =A3.conj() |
| =interval@ms(A2,now()) |
| =now() |
| for |
| =A1.import@t(;A8:4) |
| >A10=A10|B8.select(City=="San Diego") |
| [] |
| =interval@ms(A7,now()) |

此示例从文本文件 PersonnelInfo.txt 中查找来自圣地亚哥的员工。第 2 到第 5 行使用多线程，第 7 到第 10 行运行循环。B3 和 B8 都按段导入数据。请注意，fork to(4) 不能简化为多线程执行。我们在 A5 和 A10 中得到相同的结果：

（原文含示意图，图像资源未迁移）

B5 和 B10 计算两种方法完成计算所需的时间：

（原文含示意图，图像资源未迁移）

由于更好地利用了 CPU，多线程比循环花费的时间更少。
