# 10.1.4 应用

## 代码片段
```spl
=file("PersonnelInfo.txt")
=A1.import@t(;pPart:pAll)
=A2.select(State==pState)
=callx
=A2.conj()
```

## 内容

### 10.1.4 应用

callx指令用于在网格中将子任务分配给运行中的集群服务器。以下是网格parallel01.splx：

|
=file("PersonnelInfo.txt")
|
=A1.import@t(;pPart:pAll)
|
=A2.select(State==pState)
|
return A3

该程序从人员信息文件PersonnelInfo.txt导入一个数据段，并选择来自指定州的员工。以下是其中使用的网格参数：

（原文含示意图，图像资源未迁移）

主程序调用parallel01.splx，使用集群计算并发查找所有来自Ohio的员工：

|
[192.168.1.112:8281]
|
=callx
("D:/files/splx/parallel01.splx","OH",to(20),20;A1)
|
=A2.conj()

A1指定用于计算的并行服务器列表。A2使用callx调用这些服务器执行并行计算。执行时，A2的结果如下：

（原文含示意图，图像资源未迁移）

当作业在服务器集群上执行时，它将根据要分配给集群服务器的参数数量被拆分为多个任务。然后每个服务器将其任务分配给在其上运行的进程，这些进程将分别返回结果。A2的数据是一个包含这些结果（即序列）的记录序列。A3连接这些序列中的记录以获得最终结果：

（原文含示意图，图像资源未迁移）

通过这种形式的并行计算，主程序将复杂的计算目标或大数据计算分成多个任务，将它们分配给多个服务器分别计算，然后合并结果。我们将在集群计算章节中继续讨论集群计算。
