# 11.7.1 基本用法

## 代码片段
```spl
=demo.query("select
=A1.select(GENDER=="M")
=A1\A2
=A2.(NAME).id()
=A3.len()
=B2.(NAME).id()
=A4.len()
=A1.(SURNAME).id()
=A5.len()
=A1.(STATE).id()
=A6.len()
=to(1000).new(#:EID,
>file("pseudo/emps.btx
=A8.select(DeptID==1)
=A9.len()
=periods@x(A10,B10)
=periods@x(B10,elapse@y(B10,1))
=A11.((a=string(~,"yyMMdd"),
=B11.((a=string(~,"yyMMdd"),
rand(100)*10+200:Amount))).conj().sort(SID, OID)
=file("pseudo/emps.ctx")
=A14.create(#EID,DeptID,Gender,
>B14.append@i(A8.cursor())
=file("pseudo/1.orders.ctx")
=file("pseudo/2.orders.ctx")
=A16.create(SID, #OID,
>A17.append(A12.cursor())
=B16.create(SID, #OID,
>A18.append(A13.cursor())
>B14.close()
>A17.close()
>A18.close()
=create(file).record(["pseudo/emps.ctx"])
=pseudo(A1)
=B1.cursor().fetch@x(100)
=create(file,
=pseudo(A2)
=B2.import()
=create(file).record(["pseudo/emps.ctx"])
=pseudo(A1)
=B1.select(DeptID==2
=A2.import()
>B2.run(DeptID=0)
=B1.update(B2)
=B1.import()
=A2.import()
>B2.run(DeptID=2,
=B1.update(B2)
=B1.import()
=create(file).record(["pseudo/emps.btx"])
=pseudo(A1)
=B1.cursor().fetch@x(100)
=B1.select(like(Name,
=A3.import()
```

## 内容

### 11.7.1 基本用法

为了便于说明，我们首先生成三个组表。员工表包含员工ID、部门ID、性别、婚姻状况和姓名等字段，其中性别和婚姻状况存储在一个名为Bools的字段中。另外两个表存储订单，字段包括销售人员ID、订单编号、日期时间和金额。以下是生成组表的代码：

|
=demo.query("select
NAME, SURNAME, GENDER, STATE from employee")
|
=A1.select(GENDER=="M")
|
=A1\A2
|
=A2.(NAME).id()
|
=A3.len()
|
=B2.(NAME).id()
|
=A4.len()
|
=A1.(SURNAME).id()
|
=A5.len()
|
=A1.(STATE).id()
|
=A6.len()
|
[Sales,Technology,R&D,Financial,Admin]
|
[0,0.5,0.75,0.9,0.97,1]
|
=to(1000).new(#:EID,
B7.pseg(rand()):DeptID, if(rand()<0.5,0,1):Gender,
if(rand()>0.8,1,0):Married, if(Gender==0,A3(rand(B3)+1),
A4(rand(B4)+1))/" "/A5(rand(B5)+1):Name, bits(Gender,
Married):Bools )
|
>file("pseudo/emps.btx
").export@b(A8)
|
=A8.select(DeptID==1)
|
=A9.len()
|
2024-01-01
|
2025-01-01
|
=periods@x(A10,B10)
|
=periods@x(B10,elapse@y(B10,1))
|
=A11.((a=string(~,"yyMMdd"),
to(rand(100)+ 10).new(A9 if(rand()>0.9, (rand(B9)+1, rand(B9-20)+ 21)).EID:SID,
a/string(#, "0000"):OID, datetime(A11.~, time(rand(8)+8, rand(60),
0)):OTime, rand(100)*10+200:Amount))).conj().sort(SID, OID)
|
=B11.((a=string(~,"yyMMdd"),
to(rand(100)+ 10).new(A9 if(rand()>0.9, (rand(B9)+1, rand(B9-20)+
21)).EID:SID, a/string(#, "0000"):OID, datetime(B11.~,
time(rand(8)+8, rand(60), 0)):OTime,
rand(100)*10+200:Amount))).conj().sort(SID, OID)
|
=file("pseudo/emps.ctx")
|
=A14.create(#EID,DeptID,Gender,
Married, Name, Bools)
|
>B14.append@i(A8.cursor())
|
=file("pseudo/1.orders.ctx")
|
=file("pseudo/2.orders.ctx")
|
=A16.create(SID, #OID,
OTime, Amount)
|
>A17.append(A12.cursor())
|
=B16.create(SID, #OID,
OTime, Amount)
|
>A18.append(A13.cursor())
|
>B14.close()
|
>A17.close()
|
>A18.close()

给定的数据量较小。在Gender字段中，0表示男性，1表示女性；在Married字段中，0表示未婚，1表示已婚。A8生成的员工数据如下：

（原文含示意图，图像资源未迁移）

下面显示了A12和A13分别生成的2024年和2025年订单表数据：

（原文含示意图，图像资源未迁移）

（原文含示意图，图像资源未迁移）

这三个表存储在三个组表文件中——pseudo路径下的emps.ctx、1.orders.ctx和2.orders.ctx，以及二进制文件emps.btx。后两个组表文件构成一个多分区组表。我们将以它们为例来了解如何定义和使用虚表。现在我们备份它们，以防测试过程中数据被修改。

|
=create(file).record(["pseudo/emps.ctx"])
|
=pseudo(A1)
|
=B1.cursor().fetch@x(100)
|
=create(file,
zone).record(["pseudo/orders.ctx", [1,2]])
|
=pseudo(A2)
|
=B2.import()

虚表定义记录是具有指定结构的序表记录。其最简单的形式应包含一个file字段来指定虚表的数据源。在上面的代码中，A1生成的虚表定义记录如下：

（原文含示意图，图像资源未迁移）

最好使用组表文件（ctx）作为虚表的数据源。

基于上述记录，我们使用pseudo(pd)函数正式定义虚表。参数pd是虚表定义记录。B1生成虚表定义：

（原文含示意图，图像资源未迁移）

与组表文件类似，虚表定义使用T.cursor()函数生成游标或使用T.import()函数检索所有数据来提供数据。C1使用游标检索前100条记录：

（原文含示意图，图像资源未迁移）

虚表也可以从多分区组表文件生成。上面代码中的A2生成的虚表定义如下：

（原文含示意图，图像资源未迁移）

C2从该虚表检索数据：

（原文含示意图，图像资源未迁移）

数据按原始顺序从多分区组表文件中检索。

我们可以使用delete/update/append函数从虚表中删除、更新或添加一条或多条记录。该操作将直接导致源组表文件中的相应更改。例如：

|
=create(file).record(["pseudo/emps.ctx"])
|
=pseudo(A1)
|
=B1.select(DeptID==2
&& EID<10)
|
=A2.import()
|
>B2.run(DeptID=0)
|
=B1.update(B2)
|
=B1.import()
|
=A2.import()
|
>B2.run(DeptID=2,
EID=EID+1000)
|
=B1.update(B2)
|
=B1.import()

与使用组表类似，我们可以通过T.select()函数从虚表中选择某些记录。在上面的代码中，A2选择DeptID为2且EID小于10的员工记录，B2使用T.import()函数检索它们，如下所示：

（原文含示意图，图像资源未迁移）

A3将所选员工记录的DeptID字段值重置为0，B3使用T.update(P)函数将存储在记录序列P中的这些修改记录更新到虚表。请记住，当您使用update函数更新虚表时，必须首先为相应的组表定义维度，因为所有这些操作都是基于组表的维度执行的。修改后，A4和B4得到以下结果：

（原文含示意图，图像资源未迁移）

（原文含示意图，图像资源未迁移）

我们可以看到相应的记录已被修改。现在由于没有记录能满足A2中指定的过滤条件，B4返回的记录序列为空。

A5将B2的两条记录的DeptID值更改为原始值，并将每个EID值加1000。B5使用T.update()函数将更改更新到虚表。以下是A6的结果：

（原文含示意图，图像资源未迁移）

如果记录中的主键值被更新，包含这些记录的组表的维度将无法保持有序，虚表也无法按预期更新。

我们也可以使用与从组表生成虚表相同的方式从一个或多个二进制文件生成虚表。例如：

|
=create(file).record(["pseudo/emps.btx"])
|
=pseudo(A1)
|
=B1.cursor().fetch@x(100)
|
=B1.select(like(Name,
"*Smith"))
|
=A3.import()

A2使用游标访问B1的虚表并返回以下结果：

（原文含示意图，图像资源未迁移）

A3选择姓氏为Smith的员工记录。B3从A3导入记录并返回如下结果：

（原文含示意图，图像资源未迁移）

从二进制文件生成的虚表不能像多分区组表那样使用。
