# 1.8.1 文本数据

## 代码片段
```spl
=file("employee.txt")
=A1.import@t()
=A1.import()
```

## 内容摘要
以下是员工数据示例表：

| EID | NAME | SURNAME | GENDER | STATE | BIRTHDAY | HIREDATE | DEPT | SALARY |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Rebecca | Moore | F | California | 1984-09-28 | 2015-01-18 | R&D | ... |
| 2 | Ashley | Wilson | F | New York | 1990-05-28 | 2018-01-23 | Finance | ... |
| ... | ... | ... | ... | ... | ... | ... | ... | ... |

esProc 使用 import 函数从文件导入数据：

| =file("employee.txt") | =A1.import@t() | =A1.import() |

在 A2 中，import 函数使用 @t 选项在数据导入期间将文本文件的第一行作为序表的列名导入。

（原文含示意图，图像资源未迁移）

让我们看看不使用 @t 选项会是什么样子。以下是 A3 得到的结果序表：

（原文含示意图，图像资源未迁移）

此外，由于原始列标题的影响，程序会自动将每个字段值识别为字符串。
