# 12.3 坐标轴

## 内容

### 12.3 坐标轴

坐标系统和变换讨论了逻辑轴和物理轴之间的关系，以及在使用esProc绑制图表时如何在画布上定位坐标轴然后绘制图表元素。esProc提供多种逻辑轴用于绘制不同样式的图表。不同类型的逻辑轴需要不同的设置才能正确执行坐标变换。定义逻辑轴的主要目的是将图表元素上的逻辑坐标转换为用于绘制图表的物理坐标。无论逻辑轴是否实际绘制，图表元素的定位都不会受到影响。
