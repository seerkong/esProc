# 12.4.1 轴线属性

## 代码片段
```spl
=canvas()
=demo.query("select
=A1.plot("BackGround")
=A1.plot("EnumAxis","name":"x","axisColor":-16737895,"axisLineWeight":2,"axisArrow":256)
=A1.plot("NumericAxis","name":"y","location":2)
=A1.plot("Column","axis1":"x","data1":A2.(EVENT+","+NAME),"axis2":"y","data2":A2.(SCORE))
=A1.plot("Legend","legendText":A2.id(NAME),"x":0.81,"y":0.2,"width":
=A1.draw@p(450,250)
```

## 内容

### 12.4.1 轴线属性

在基础知识中，引用了示例来说明如何为轴线设置颜色、样式和粗细等外观属性。轴元素的外观属性还包括箭头样式。例如：

|
=canvas()
|
=demo.query("select
* from GYMSCORE order by NAME")
|
=A1.plot("BackGround")
|
=A1.plot("EnumAxis","name":"x","axisColor":-16737895,"axisLineWeight":2,"axisArrow":256)
|
=A1.plot("NumericAxis","name":"y","location":2)
|
=A1.plot("Column","axis1":"x","data1":A2.(EVENT+","+NAME),"axis2":"y","data2":A2.(SCORE))
|
=A1.plot("Legend","legendText":A2.id(NAME),"x":0.81,"y":0.2,"width":
0.18)
|
=A1.draw@p(450,250)

A2检索用于绘制图表的数据：

（原文含示意图，图像资源未迁移）

在绘图算法中，A4为x轴设置属性，包括线条颜色、线条粗细和箭头样式：

（原文含示意图，图像资源未迁移）

以下是绘图结果：

（原文含示意图，图像资源未迁移）

可以看到，通过修改图表参数，x轴的外观已经改变。
