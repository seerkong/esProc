# 12.5.2 外观属性

## 内容

### 12.5.2 外观属性

绑制散点图时，您可以设置标记样式和填充颜色、边界线属性和其他属性。例如，修改A6的图表参数来设置点图的外观属性：

（原文含示意图，图像资源未迁移）

将标记样式设置为菱形形状，将线条颜色更改为浅蓝色，将标记粗细增加到6像素，并将填充颜色更改为粉色渐变。修改后，绘图结果为：

（原文含示意图，图像资源未迁移）

可以看到，点的外观已根据设置更改。标记粗细属性设置点半径的像素。要使点的大小与其逻辑坐标匹配，请设置半径1和半径2属性，它们分别对应于数据属性设置中的逻辑轴——轴1和轴2。点的宽度和高度将根据指定的逻辑坐标值计算。

要使每个点具有不同的外观，请为某个图表参数使用序列。在A6的绑图参数中，修改点的填充颜色：

（原文含示意图，图像资源未迁移）

然后A7的绘图结果为：

（原文含示意图，图像资源未迁移）

使用序列设置属性时，如果序列长度较短，将重复使用该序列。
