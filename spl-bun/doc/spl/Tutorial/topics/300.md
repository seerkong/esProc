# 12.5.4 图表元素的共同属性

## 内容

### 12.5.4 图表元素的共同属性

最常用的图表元素有一些共同属性，如可见性和透明度，通过散点图绑制您已经对此有了一些基本了解。编辑A6的绑图参数来修改散点图的绘图算法：

（原文含示意图，图像资源未迁移）

将图表元素的透明度值更改为0.5并勾选阴影。绘图结果如下：

（原文含示意图，图像资源未迁移）

您可以看到，透明度的修改使点的颜色变淡并使它们更加透明。勾选阴影会在点的右下方创建轻微的阴影。此外，将可见设置为false将隐藏图表元素。允许文本重叠属性与图表：轴元素中提到的轴标签属性类似。在绘制一组图表元素时，如果此属性设置为false，与现有元素某部分重叠的元素将不会被绘制。
