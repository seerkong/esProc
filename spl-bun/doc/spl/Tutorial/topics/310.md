# 12.7.4 堆叠柱状图

## 代码片段
```spl
=canvas()
=demo.query("select
=A1.plot("BackGround")
=A1.plot("EnumAxis","name":"x")
=A1.plot("NumericAxis","name":"y","location":2)
=A1.plot("Column","stackType":2,"axis1":"x","data1":A2.(NAME+","+
=A1.plot("Line","stackType":2,"axis1":"x","data1":A2.(NAME+","+EVENT),
=A1.draw@p(450,200)
```

## 内容

### 12.7.4 堆叠柱状图

堆叠柱状图将同一类别的多系列数据柱子堆叠起来，而不是并排放置。以下是此类图表的绘图算法：

|
=canvas()
|
=demo.query("select
* from GYMSCORE")
|
=A1.plot("BackGround")
|
=A1.plot("EnumAxis","name":"x")
|
=A1.plot("NumericAxis","name":"y","location":2)
|
=A1.plot("Column","stackType":2,"axis1":"x","data1":A2.(NAME+","+
EVENT),"axis2":"y","data2":A2.(SCORE))
|
=A1.plot("Line","stackType":2,"axis1":"x","data1":A2.(NAME+","+EVENT),
"axis2":"y","data2":A2.(SCORE))
|
=A1.draw@p(450,200)

您还可以通过绘制线元素来制作堆叠折线图，其属性与柱元素类似。此绘图算法同时绘制柱状图和折线图。与前面的示例不同，在为柱元素和线元素设置数据属性时，它将Data1设置为A2.(NAME+","+EVENT)，使NAME成为枚举轴上的类别值，EVENT成为系列值。在绘制柱元素的A6和绘制线元素的A7中都设置了堆叠类型属性，将默认的不堆叠更改为堆叠，如下所示：

（原文含示意图，图像资源未迁移）

（原文含示意图，图像资源未迁移）

通过此设置，绘图结果如下：

（原文含示意图，图像资源未迁移）

可以看到，显示每位运动员项目成绩的柱子是堆叠的。因此，您可以在完成的图表上看到每位运动员的总成绩。

另一种堆叠图表是使用百分比堆叠的图表，可以通过设置以下属性来绘制：

（原文含示意图，图像资源未迁移）

（原文含示意图，图像资源未迁移）

绘图结果为：

（原文含示意图，图像资源未迁移）

对于按百分比堆叠柱子和线条的图表，绘制每个类别中所有系列的数据比例，每个系列中数据的总百分比为100%。这样，数据按百分比进行比较，每个类别中的总数据值失去了意义。
