# 12.8.2 外观属性

## 内容

### 12.8.2 外观属性

从上图可以看出，使用扇形元素绑制图表的结果是带有填充颜色的扇形。这意味着扇形元素的外观属性将包括填充颜色、边框样式、边框粗细和边框颜色。默认情况下，填充颜色是自动生成的，带有光照效果。现在将A6的代码修改为=A1.plot("Sector","borderWeight":1,"borderColor":-16724839,"fillColor":[["ChartColor",0,true,-52429,-13057,0],["ChartColor",0,true,-10066177,-3355393,0],["ChartColor",0,true,-13369345,-1,0]],"text":A2.(NAME),"axis1":"x","data1":A2.(NAME),"axis2":"y","data2":A2.(SCORE))。这样，外观属性将更改如下：

（原文含示意图，图像资源未迁移）

将边框线颜色设置为青绿色，粗细为1，并为扇形指定填充颜色序列。因此绘图结果为：

（原文含示意图，图像资源未迁移）

如您所见，饼图的外观已更改。填充颜色的默认光照效果已消失，渐变颜色已应用于整个圆形区域，以实现填充颜色之间的和谐。

而且，饼图的数据标签与其他样式图表中的不同，它们通过从每个扇形中心绘制的线条呈现。因此，除了数据标签文本的属性外，您还需要为这些引导线指定外观属性。通过编辑A6的代码修改扇形元素的图表属性：

（原文含示意图，图像资源未迁移）

然后绘图结果为：

（原文含示意图，图像资源未迁移）

现在让我们看看文本属性。上述绘图算法将数据标签设置为运动员的姓名。修改A6来更改扇形元素的图表属性：

（原文含示意图，图像资源未迁移）

这里修改了数据标签文本的文本字体、文本样式、文本大小和文本颜色，绘图结果为：

（原文含示意图，图像资源未迁移）

可以看到，文本属性同时应用于饼图上的文本和使用引导线的文本。
