# 12.9.5 背景元素

## 代码片段
```spl
=canvas()
=A1.plot("BackGround","imageValue":"D:\\soft\\scudata\\esProc\\logo\\logo.png")
=A1.draw@p(200,200)
```

## 内容

### 12.9.5 背景元素

除了为背景元素选择颜色外，我们还可以在其中绘制图像：

|
=canvas()
|
=A1.plot("BackGround","imageValue":"D:\\soft\\scudata\\esProc\\logo\\logo.png")
|
=A1.draw@p(200,200)

以下为背景元素设置图像值：

（原文含示意图，图像资源未迁移）

以下是最终背景：

（原文含示意图，图像资源未迁移）

有3种填充模式——左上、拉伸和平铺。
