# 12.11.1 柱形图形

## 代码片段
```spl
=canvas()
=demo.query("select
=A1.plot("GraphColumn","categories":A2.(NAME),"values":A2.(UNITPRICE))
=A1.draw@p(400,250)
=A1.plot("GraphColumn","categories":A2.(NAME),"values":A2.(UNITPRICE),"xTitle":"Fruits","xTitleFont":"Vivaldi","xTitleSize":16,"xTitleColor":-16764007,"yTitle":"Unit
=A1.plot("GraphColumn","categories":A2.(NAME),"values":A2.(UNITPRICE),"xTitle":"Fruits","xTitleFont":"Vivaldi","xTitleSize":16,"xTitleColor":-16764007,"yTitle":"Unit
=canvas()
=demo.query("select
=A1.plot("GraphColumn","categories":A2.(NAME+","+
=A1.draw@p(400,250)
```

## 内容

### 12.11.1 柱形图形

柱状图是最常用的图表类型之一。在esProc中使用柱形图形来绘制柱状图。以以下绘图算法为例：

|
=canvas()
|
=demo.query("select
* from FRUITS")
|
=A1.plot("GraphColumn","categories":A2.(NAME),"values":A2.(UNITPRICE))
|
=A1.draw@p(400,250)

您可以看到，使用现成的柱形图形，算法变得简单得多。不需要逐一绘制作为图表组成部分的图表元素，如轴、图形、图例和标题。A1创建画布，A2检索用于图表绘制的数据：

（原文含示意图，图像资源未迁移）

A3绘制柱状图：

（原文含示意图，图像资源未迁移）

您只需设置类别和值的数据属性即可绘制图表。以下是A4的绘图结果：

（原文含示意图，图像资源未迁移）

使用现成图形通过设置较少的参数很方便。但它们产生固定的绘图样式。您可以通过修改图表参数来调整绘图效果，其中一些参数（如标题文本、轴颜色和图例属性）由所有现成图形共享。

首先让我们看看水平轴和垂直轴的标题文本设置。修改A3来更改柱形图形的属性：

（原文含示意图，图像资源未迁移）

除了数据标签外，还为水平轴和垂直轴添加了标题。因此A4的绘图结果为：

（原文含示意图，图像资源未迁移）

修改柱形图形的x轴标签属性：

（原文含示意图，图像资源未迁移）

字体和颜色已更改，A4的绘图结果变为：

（原文含示意图，图像资源未迁移）

标签的字体属性与标题的属性类似，只是多了一个间隔用于按一定间隔显示标签。

柱形图形的垂直轴是数值轴，您可以为其设置标签的字体属性以及更改值范围、刻度数等。修改y轴标签的属性：

（原文含示意图，图像资源未迁移）

A4的绘图结果为：

（原文含示意图，图像资源未迁移）

在数值y轴上，您可以绘制网格线和警告线。网格线是根据刻度线绘制的参考线。警告线是根据指定的警告值绘制的。修改柱形图形属性如下：

（原文含示意图，图像资源未迁移）

以下是A4的绘图结果，您可以在上面看到网格线和警告线：

（原文含示意图，图像资源未迁移）

通过修改A3的图表参数来更改轴颜色：

（原文含示意图，图像资源未迁移）

这样轴颜色将相应更改。A4的绘图结果为：

（原文含示意图，图像资源未迁移）

此外，您可以修改边框属性来更改图形轮廓；或修改图形标题属性在图形顶部绘制标题。通过A3修改柱形图形属性：

（原文含示意图，图像资源未迁移）

您将获得以下绘图结果：

（原文含示意图，图像资源未迁移）

通过修改柱形图形的数据标记属性在每个柱子顶部添加数据标记：

（原文含示意图，图像资源未迁移）

选择显示初始数据值，数据标记将添加到每个柱子。以下是绘图结果：

（原文含示意图，图像资源未迁移）

使用现成图形，设置图例属性来绘制图例：

（原文含示意图，图像资源未迁移）

然后绘图结果为：

（原文含示意图，图像资源未迁移）

修改A3的图表参数来更改图表区域颜色、是否允许文本重叠以及设置其他属性：

（原文含示意图，图像资源未迁移）

（原文含示意图，图像资源未迁移）

修改涉及设置画布颜色（图表区域）、背景颜色（平面）和图例边框颜色（图例轮廓），以及将允许文本重叠更改为false，这意味着如果文本可能覆盖现有文本则不绘制，如上图中水平轴上的Strawberry标签。以下是A4的绘图结果：

（原文含示意图，图像资源未迁移）

这些图表属性不仅可以为柱形图形设置，还可以为所有其他现成图形设置。这里将省略对它们的属性设置。

此外，柱形图形可用于绘制具有类别和系列的柱状图，例如显示体操成绩的柱状图：

|
=canvas()
|
=demo.query("select
* from GYMSCORE")
|
=A1.plot("GraphColumn","categories":A2.(NAME+","+
EVENT),"values":A2.(SCORE))
|
=A1.draw@p(400,250)

A2检索用于图表绘制的数据：

（原文含示意图，图像资源未迁移）

A3设置柱状图的属性：

（原文含示意图，图像资源未迁移）

与坐标轴中讨论的枚举轴属性设置类似，为同时具有类别和系列的柱状图设置类别数据应包括类别值和系列值。这里NAME设置为类别，EVENT设置为系列。默认情况下，每个类别将绘制为一簇系列柱子。以下是A4的绘图结果：

（原文含示意图，图像资源未迁移）

将柱子类型设置为堆叠柱子，可以轻松将柱状图转换为二维堆叠柱状图：

（原文含示意图，图像资源未迁移）

绘图结果为：

（原文含示意图，图像资源未迁移）

转换为二维图表后，3D轴变为普通线条。通过更改柱子类型，您可以使用柱形图形绘制3D堆叠柱状图、条形图和许多其他图表。
