# 12.11.2 折线图形

## 代码片段
```spl
=canvas()
=demo.query("select
=A1.plot("GraphLine","categories":A2.(NAME+","+EVENT),
=A1.draw@p(400,250)
```

## 内容

### 12.11.2 折线图形

折线图形可用于绘制各种图表，如折线图、曲线图和雷达图。以下是显示体操成绩的折线图的绘图算法：

|
=canvas()
|
=demo.query("select
* from GYMSCORE")
|
=A1.plot("GraphLine","categories":A2.(NAME+","+EVENT),
"values":A2.(SCORE),"yStartValue":12)
|
=A1.draw@p(400,250)

A3设置折线图形的属性：

（原文含示意图，图像资源未迁移）

使用上一节中的类别数据和系列数据，A4获得以下绘图结果：

（原文含示意图，图像资源未迁移）

通过在A3中设置线条属性来设置所需的线条样式或在折线图上绘制点：

（原文含示意图，图像资源未迁移）

将绘制数据点和阴影都设置为true，并增加线条粗细。现在绘图结果为：

（原文含示意图，图像资源未迁移）

在这种情况下，不绘制点，不显示阴影效果；折线变粗了。

将原始折线图形的类型更改为雷达，以在极坐标系中绘制雷达图：

（原文含示意图，图像资源未迁移）

绘图结果为：

（原文含示意图，图像资源未迁移）

您还可以通过更改折线图形类型来绘制3D折线图或曲线拟合图。
