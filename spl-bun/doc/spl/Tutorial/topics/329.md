# 12.11.3 饼图形

## 代码片段
```spl
=canvas()
=demo.query("select
=A1.plot("GraphPie","categories":A2.(NAME),"values":
A2.(UNITPRICE))
=A1.draw@p(400,250)
```

## 内容

### 12.11.3 饼图形

esProc提供饼图形来绘制各种饼图。以下是这样的绘图算法：

|
=canvas()
|
=demo.query("select
* from FRUITS")
|
=A1.plot("GraphPie","categories":A2.(NAME),"values":
A2.(UNITPRICE))
|
=A1.draw@p(400,250)

A3绘制单系列饼图。对于多系列饼图绑制，数据属性设置与同时具有类别和系列的柱状图/折线图类似。此算法根据默认图表配置在右侧绘制图例。A4的绘图结果为：

（原文含示意图，图像资源未迁移）

修改A3将饼图类型设置为3D饼图以绘制3D饼图。并取消勾选切片饼图以禁用扇形分离效果：

（原文含示意图，图像资源未迁移）

以下是绘图结果：

（原文含示意图，图像资源未迁移）
