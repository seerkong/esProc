# 2.1.2 if(x, a, b) 函数

## 代码片段
```spl
=if(A1>=0,A1,-A1)
```

## 内容摘要
如果 x 为 true，将计算表达式 a 并返回结果；否则，将计算表达式 b 并返回结果。

有时，您可以用 if 函数替换前面提到的 if…else… 语句，使代码更简洁。

| -14 | =if(A1>=0,A1,-A1) |

A2 的结果是 A1 的绝对值：

（原文含示意图，图像资源未迁移）
