# 2.6.4 重置序表

## 代码片段
```spl
=demo.query("select
>A1.reset()
```

## 内容摘要
使用 reset 函数，您可以清空序表同时保留其数据结构：

| =demo.query("select * from STUDENTS") | >A1.reset() |

重置序表可以移除无用的记录并释放内存。

（原文含示意图，图像资源未迁移）
