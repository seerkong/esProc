# 多游标中子游标的默认数量

## 内容

Set the default number of subcursors in a multicursor. When a multicursor-related function does not specify the number of subcursors in a multicursor through a parameter, it will, by default, automatically use the value set in this option.
