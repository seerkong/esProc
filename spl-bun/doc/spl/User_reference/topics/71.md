# 缺失值(逗号分隔)

## 内容

Set missing values, which are separated by comma. When data is imported from a text file, there could be certain symbols in values of some fields that are treated as nulls. We can define missing values to represent these symbols.
