# 临时目录

## 内容

It is the directory for storing a temporary file, like a buffer file, generated during a computation. Relative to the main directory if it is specified by a relative path and there's no need to put the slash "/" before the path.
