{
  "name": "spl-bun",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "build": "bun run --filter @esproc/expression build && bun run --filter @esproc/spl-flow build && bun run --filter @esproc/core build && bun run --filter @esproc/sqlite-adapter build && bun run --filter @esproc/web-shared build && bun run --filter @esproc/composer build && bun run --filter @esproc/web-ide build && bun run --filter @esproc/web-server build && bun run --filter @esproc/test-utils build",
    "cleanup": "bun run scripts/cleanup-dev-ports.ts",
    "cleanup:frontend": "bun run scripts/cleanup-dev-ports.ts 4174 4175",
    "cleanup:backend": "bun run scripts/cleanup-dev-ports.ts 4176",
    "web-ide:dev": "bun run scripts/web-ide-dev.ts",
    "dev:frontend": "bun run cleanup:frontend && bun run --filter @esproc/web-ide dev",
    "dev:backend": "bun run cleanup:backend && bun run --filter @esproc/web-server dev",
    "dev": "bun run cleanup && bun run dev:backend & bun run dev:frontend",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "bun-types": "latest",
    "typescript": "5.4.5"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.1",
    "@univerjs/preset-sheets-core": "^0.15.1",
    "@univerjs/presets": "^0.15.1",
    "elysia": "^1.4.21"
  }
}
